// Package retiredgo lives under scripts/testdata, so the Go toolchain ignores it.
package retiredgo

type HostClient struct{}

type ChannelIdentifier string

type TeamIdentity string

var channelIdx int

type IPCNamespace string
