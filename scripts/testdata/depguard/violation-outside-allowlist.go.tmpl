// Fixture template for scripts/check-depguard-fixtures.sh (011.014-T,
// resolves 309FBF5A(ii)). This file is copied at runtime into
// internal/depguardfixture/violation.go (a real, temporary, git-ignored
// location OUTSIDE the internal/copilotprobe allowlist) to prove the
// depguard-boundary rule's DENY direction: golangci-lint must exit non-zero
// against this file. It is never committed at a compilable .go path itself
// (this file's own extension is .go.tmpl, which the Go toolchain ignores).
package depguardfixture

import (
	copilot "github.com/github/copilot-sdk/go"
)

// UseSDKOutsideAllowlist exists only so the import above is used; this
// function is never called by anything real.
func UseSDKOutsideAllowlist() *copilot.Client {
	return copilot.NewClient(nil)
}
