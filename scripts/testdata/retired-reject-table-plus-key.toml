# retired-reject-table-plus-key.toml (015.008-T, V5): a table header and
# a nested key that only becomes a forbidden sequence when their paths
# are COMPOSED together ([channel] + id). Neither segment alone matches.
[channel]
id = "value"
