# Compacted: 027-S / 030-F — Write-Path Gate Kill Switch (plan unit 3)

**Shipment:** 027-S | **Feature:** 030-F | **PR:** #71 | **Merged SHA:** 5208085379da4b945eae6d341089f0f985adc09c

## Outcome

Merged via merge-commit strategy. Both tasks (030.001-T, 030.002-T) done and
archived; covering feature 030-F transitioned active -> done at post-merge
closure via the covering-feature completion gate (all five conditions
verified); shipment 027-S closed via the P-015 verified fully-covered-root
CASCADE path (`backlogit shipment ship`), two-set gate verified clean, no
cascade anomaly.

## What shipped

- `scripts/check-write-path-precondition.sh`: added fixtures-only
  `--self-test-integrity` mode alongside the existing combined `--self-test`.
- `.github/workflows/ci.yml`: split the single CI step into an
  unconditionally-blocking integrity step and a separately
  `WRITE_PATH_GATE_ADVISORY`-toggled verdict step, mirroring the existing
  `check-retired-architecture.sh` / `RETIRED_ARCH_GATE_ADVISORY` split.
  Updated the LOCAL DIVERGENCE tracking comment.
- One in-scope naming-consistency fix applied directly (commit c5aecfc):
  dropped an embedded task-ID suffix from the new step's display name to
  match the sibling step's convention (P-021 C3).

## Quality gates

All green at reviewed/merged HEAD 341f760: `go vet`, `gofmt -l`, `go build`,
`go test ./...` (integration suite), YAML lint, `actionlint`, both
`check-write-path-precondition.sh` self-test modes, sibling
`check-retired-architecture.sh --self-test-integrity`. All 13 CI checks
green. P-009 (merge-commit-only) satisfied. P-018 Copilot-review gate:
NOT_APPLICABLE (no engagement).

## Review

Report-only review, 6 personas, zero P0/P1. Verdict: READY_WITH_FOLLOWUPS.

## P-021 deferred scope expansions (untriaged, Stage-owned)

Four stash entries captured (threadless path, provisional priority `low`,
`requires_deliberation: true`), deliberately left untriaged in this dark-mode
run per operator scope contract:

| Stash ID | Summary |
|---|---|
| 8E9F8E55 | Missing reject-*.go fixtures for os.MkdirTemp/os.Chown/os.Lchown/os.Chtimes selectors |
| B72E9715 | io.CopyN / io.CopyBuffer not in the detector's SELECTORS list |
| 8E18CCF5 | No `::notice::` mode-emission parity with check-retired-architecture.sh |
| 56B16321 | Combined `--self-test` mode no longer directly exercised by CI after the step split |

## Post-merge closure

- Post-merge closure branch: `post-merge/030-write-path-gate-kill-switch`
- Closure artifact: `docs/closure/027-S-030-F-post-merge-closure.md` (releasability: READY)
- Reconciliation reports: `.backlogit/reconcile/027-S-{pre,safe-close,post}-*.md`
- No new follow-ups beyond the four P-021 entries above.
- No `source_stash_id` / `source_deliberation_id` present on 030-F — no source
  artifact cleanup applicable.

## Session history compacted

Originals superseded: `2026-09-20-ship-027-s-pr-ready-awaiting-merge-approval.md`,
`2026-09-21-ship-027-s-halted-at-merge-approval-gate.md` (moved to
`docs/archive/memory/`).
