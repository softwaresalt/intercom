# Compacted memory — 009-S (pathsafe and config containment hardening, 010-F)

Compacted 2026-09-06 by Ship post-merge closure (P-020 mandatory
compact-context invocation). Source files (moved to `docs/archive/memory/`):
`2026-09-06-193000-ship-009-s-pre-pr-checkpoint.md`,
`2026-09-06-193500-ship-009-s-merge-approval-halt.md`.

## Outcome

Shipment 009-S shipped and closed. PR #27 merged (merge commit
`d8ca53f59871158d007acdd86188b2c0b47c2bc8`) at reviewed HEAD
`6476ab1430ba0d41d685d83ff78361c55143d624`. Backlog closed via P-015
verified fully-covered-root CASCADE (`backlogit shipment ship 009-S`);
see `.backlogit/reconcile/009-S-safe-close-20260906-214047.md` and
`docs/closure/009-S-010-F-post-merge-closure.md`.

## What changed

`internal/pathsafe`/`internal/config` hardening: `NewRoot` cause
preservation (`apperr.Wrapf`) with retained dual `errors.Is` discriminability
(010.001-T); `NewRoot` non-directory-root rejection via `os.Stat`
(010.002-T); `hasPathPrefix` filesystem-root doubled-separator fix
(010.003-T); dangling-symlink final-component accept behavior permanently
pinned as a regression test, GO-14 (010.004-T); pure zero-verdict-change
relocation of `containsDotDotSegment` to the new exported
`pathsafe.ContainsDotDotSegment`, consumed by `internal/config` rule 7
(010.005-T).

## Key decision / rejected approach

An earlier normalize-based unification design for 010.005-T was rejected
during adversarial review: it would have caused a containment regression
(accepting `..\..\etc` on Linux) and broken the deliberately-permitted
absolute `database.path` contract. Replaced with the narrower pure-move
actually shipped (zero verdict change, AC-4). Captured in deferred entry
`798002CB`'s sibling context and PR #27 commit history.

## Review and gates

5-persona local review: all READY_WITH_FOLLOWUPS, 0 P0/P1. Five P-021
deferred scope-expansion follow-ups captured (threadless, pre-PR):
`BF5DE670` (pathsafe TOCTOU/dangling-symlink-write-through/hardlink
tracking consolidation, medium), `E89E5D00` (Windows-specific security
branches untested by ubuntu-only CI, medium), `798002CB`
(`ContainsDotDotSegment` weaker-guarantee export risk, low), `8D953C4B`
(config rule 7 absolute-path containment gap vs Principle III, pre-existing,
medium), `8472E0A1` (consolidated minor P3 cluster, low). CI: 12/12 green.
P-018: NOT_APPLICABLE. P-009: merge-commit strategy used, per repo policy.

## Failed approaches / gotchas

None beyond the normalize-unification rejection above — no repeated build
failures, no 3+ attempt cycles requiring separate compound capture.

## Next steps (already executed by this closure session)

Post-merge closure completed in full: backlog safe-close (CASCADE path),
operational-closure artifact, compound-refresh evaluation (no stale
`docs/compound/` entries found referencing pathsafe/containment/dangling —
none needed refreshing), this compaction, backlogit index resync, and a
separate closure PR pending its own explicit operator approval.
