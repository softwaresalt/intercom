---
date: 2026-09-06
agent: ship
shipment: 008-S
feature: 009-F
status: closed
outcome: merged
---

# 008-S / 009-F — apperr taxonomy correction — compacted record

**Merged**: PR #24, merge commit `1334c5b14958a4755ee590d24f580b4b43701dec`,
reviewed HEAD `9acb40b8b8cb04a0199eec59b68b2b2f13d31d2a`. Closure artifact:
`docs/closure/2026-09-06-008-s-009-f-apperr-taxonomy-correction-closure.md`.

## What shipped

Retired 3 dead `Kind` taxonomy members (`KindSlack`/`KindIPC`/`KindACP`,
14→11 kinds) after verifying zero external callers and no persistence
surface on `Kind`; added `Kind.String()` (`fmt.Stringer`, byte-identical
`Error()` output, golden-tested); added a taxonomy drift guard tying
`allKinds`/sentinel counts together; added cause-preserving `Wrapf`
constructor (consumed by future `010-F`); registered `Newf`/`Wrapf` with
golangci-lint's govet printf analyzer (v2 schema,
`linters.settings.govet.settings.printf.funcs`), verified with a disposable
probe file.

All 6 units (009.001-T–009.006-T) implemented test-first per the reviewed
plan `docs/plans/2026-09-04-intercom-go-apperr-taxonomy-correction-plan.md`.

## Key decisions / learnings

* **Blast-radius-first removal discipline**: verified zero external callers
  and no persistence surface on `Kind` via `git grep` *before* deleting any
  retired const/sentinel — worth repeating for any future taxonomy trim.
* **Commit granularity deviation** (process learning, not a defect): all 6
  units were implemented before the first `git add`/commit, landing as one
  combined commit instead of per-task commits. Recovered via
  `git commit --amend` (safe only pre-push). Future sessions should commit
  per-task as each unit goes green, not batch all units before the first
  commit.
* **backlogit v1.10.1 auto-archives on `move --status done`**: individual
  task/feature `done` transitions relocate the file from
  `.backlogit/queue/` to `.backlogit/archive/` immediately (declaring
  `status: done`, not `status: archived`) — this is a *terminal relocation*,
  distinct from a true `archived_status`-provenance archive, and is exactly
  the "pre-archived by location" state the shipment-reconcile skill
  tolerates on both safe-close and cascade paths.
* **P-015 verified fully-covered-root cascade applied cleanly**: 009-F
  (root, no `parent_id`) was fully covered by its 6 manifest tasks with no
  non-manifest descendants, so `backlogit shipment ship` (cascade) was the
  machine-classified permitted close path — verified via
  `returned_ids` empty + exact `allowed_ids`/`required_ids` two-set match +
  `parent_id` preservation on all 6 tasks. Full verification report:
  `.backlogit/reconcile/008-S-safe-close-20260906-024344.md`.
* **Runtime-verification correctly skipped**: pure internal-package refactor
  with no runtime-surface (`cli`/`api`) behavior change — documented as
  not-applicable in the closure artifact rather than fabricating validator
  evidence.

## Quality gates (all green)

`gofmt`, `go vet ./...`, `go build ./...`, `go test ./...` (full repo, incl.
integration), `go test -race ./internal/apperr/...`, `golangci-lint run
./...` (0 issues) — plus CI (cross-compile ×4, lint, security, test,
pipeline-topology, ci gate) all green on PR #24.

## Review

Independent `code-review` sub-agent: **READY**, 0 P0/P1/P2/P3 findings.
P-018 Copilot gate: `NOT_APPLICABLE`.

## Follow-ups

None generated by this shipment. `010-F` (not yet started, separate future
shipment) is the planned consumer of `Wrapf`.
