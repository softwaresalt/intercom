---
date: 2026-09-05
agent: ship
session_id: ship-dark-pause-007s-boundary-20260905T180848 (resumed for 008-S)
shipment: 008-S
feature: 009-F
phase: build-complete
---

# Ship Session Checkpoint — 008-S / 009-F build complete

## Mode

Dark factory mode (DARK_MODE_ACTIVE, pre-recorded by Orchestrator). Merge
approval pre-authorized (`merge_approval_pre_authorized: true`). Admin
fallback NOT authorized. Scope strictly 008-S manifest: 009-F, 009.001-T
through 009.006-T.

## Checkpoint recovery

Resumed from `checkpoint-20260906-010918.json` (agent: ship, status: active,
session_id: ship-dark-pause-007s-boundary-20260905T180848). Operator
explicitly selected this checkpoint by filename and confirmed resume.
Single candidate, no anomalies (needs_quarantine=0, quarantined=0). Owner
validated (agent=ship). Restored context: 005-S/006-S/007-S already
complete/closed; next is 008-S. Checkpoint NOT YET resolved — will resolve
after this session's own successful resume is fully confirmed (post-merge
closure of 008-S).

## Pre-flight

* P-001: no other active shipments confirmed (`backlogit list --status
  active` empty; `backlogit shipment list` showed 008-S/009-S both queued
  before claim). 007-S closure confirmed READY/compaction done (evidence
  supplied by operator).
* Pipeline-topology gate: pre_claim PASS (BRANCH_CREATE_ELIGIBLE,
  WORKTREE_TOPOLOGY_OK) → branch `feat/008-s-apperr-taxonomy-correction-009-f`
  created from clean `main` @ f6b80522 → pre_claim re-run PASS
  (BRANCH_OK) → claimed 008-S → post_claim GLOBAL verify PASS
  (CLAIM_VERIFY_OK, sole active shipment).
* Pre-existing unrelated worktree state preserved untouched: `.gitignore`,
  `start.ps1` (modified, uncommitted), `.autoharness/gates/`,
  `.backlogit/hooks_queue.jsonl`, `.claude/`, `.github/copilot/`
  (untracked). None staged or committed by this session.
* Intake reconciliation (pre-mode, expected_status: active — resumed
  session, tasks activated by the claim cascade): all 7 manifest items
  matched, shipment record `record-consistent`. PROCEED.
* `go vet ./...`, `go build ./...` clean at session start.

## Build (all 6 tasks + covering feature)

Implemented per the reviewed plan
(`docs/plans/2026-09-04-intercom-go-apperr-taxonomy-correction-plan.md`),
test-first for every unit:

* **009.001-T (U1a) + 009.002-T (U1b)**: retired KindSlack/KindIPC/KindACP
  and their sentinels; taxonomy 14→11. Verified zero blast radius outside
  `internal/apperr` and no persistence surface on `Kind` (AC-5 blocking
  precondition satisfied) before removing consts. Landed as one commit
  pair per plan mandate.
* **009.003-T (U2)**: `Kind.String()` satisfies `fmt.Stringer`; golden test
  pins `Error()` byte-identical across all 11 kinds.
* **009.004-T (U3)**: taxonomy drift guard
  (`TestTaxonomyKindsAndSentinelsStayInSync`); proven by construction
  (temporarily desynced allKinds/allSentinels, confirmed failure, reverted
  before commit).
* **009.005-T (U5)**: `Wrapf(kind, cause, format, args...)` — formats a
  message AND preserves cause via `Unwrap()`; required by 010-F U1.
* **009.006-T (U4)**: registered `apperr.Newf` and `apperr.Wrapf` under
  `.golangci.yml`'s `linters.settings.govet.settings.printf.funcs` (v2
  schema). Proven effective with a temporary deliberate-mismatch probe
  file (both functions correctly flagged by golangci-lint), then deleted —
  never committed.

**Process note**: all six units' edits were made before the first `git add`
+ `git commit`, so they landed in a single commit rather than per-task
commits. Since the branch was still local/unpushed, the commit message was
amended (`git commit --amend`, not a history rewrite of shared/pushed
history) to accurately describe the full scope rather than leaving an
inaccurate U1a/U1b-only message. No functional issue — all content is
in-scope for 008-S/009-F; only commit granularity deviated from the
per-task convention.

## Quality gates (final, full repo)

* `gofmt -l .` → clean (2 new test files reformatted before commit)
* `go vet ./...` → clean
* `go build ./...` → clean
* `go test ./...` → all packages pass (including
  `tests/integration`, 53s)
* `go test -race ./internal/apperr/...` → pass
* `golangci-lint run ./...` → 0 issues

## Backlog state

All 6 tasks + 009-F moved to `done`; commit SHA `9acb40b8b8cb04a0199eec59b68b2b2f13d31d2a`
tracked on each task via `backlogit update --commit`.

## Next steps

1. Local review gate (Step 4.4) — self-review complete, no P0/P1 findings.
2. PR lifecycle (Step 5): push branch, open PR, P-018/§1.9 gates, await
   pre-authorized dark-mode merge.
3. Post-merge closure (Step 6): safe-close 008-S, operational-closure,
   compact-context (P-020), resolve the resumed checkpoint.
4. STOP after 008-S post-merge cleanup — do NOT touch 009-S.
