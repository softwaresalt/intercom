# Implementation Plan — Staging artifact handoff actor and push authority (B2)

- **Date**: 2026-09-18
- **Revision**: **rev 2** (post plan-review attempt 1 = FAIL; remediated)
- **Feature**: `027-F`
- **Source decision**: `docs/decisions/2026-09-18-intercom-go-staging-artifact-handoff-actor-deliberation.md` (incl. **Amendment 1**)
- **Primary objective**: Bind Orchestrator Step 1.5 item 3 to explicit actors and remove the
  direct-push-to-default-branch arm, without granting or narrowing any role's authority.
- **Requires plan hardening**: **yes** (performed; see `## Plan Hardening`)

<!-- plan-review-attempt: 2 -->

---

## 0. What changed in rev 2

Two independent adversarial reviewers returned **FAIL** on rev 1. All P1 findings are remediated:

| # | Finding | Remediation |
|---|---|---|
| S1 | F-6 "dependency cycle" does not exist (edges measured: `019.004-T` reverse set is empty; `025.001-T` deps = `[018.008-T]` only; `027-F` deps = zero) | Claim **WITHDRAWN**. Split re-grounded on sequencing/independence — decision §10.1 |
| S2 | §7 graph omitted `025-F → 026-F`, overstating value | Edge added; **no downstream unblock is claimed** — §7 |
| S3 | Carve-out incomplete: `019.004-T` **AC-4** goes green-on-arrival | Carve extended to **AC-4 + body item (4)** — §6 |
| S4 | §2.1/§10 mis-assigned AC-4 to items 1–2 | Corrected: AC-4 lives in **item 3** — §2.1, §10 |
| S5 | Option 2 rejected on a refuted premise | **Partially rejected** with evidence (B3/B4/B5 live) — decision §10.2 |
| C1 | Item 2's "unpushed local commits" route had **no sub-step** in the replacement | New sub-step **3(b)** + `STAGING_GATE_LOCAL_MAIN_AHEAD` + red harness row |
| C2 | §6 backlog mutations actor-unbound; 3 of 4 sit on Ship's P-010 MUST NOT list | §6 **bound to Stage, in-session, pre-handback**; Ship performs none |
| C3 | 3(b) narrowed Ship's authority beyond the P-010 registry (I1 false) | Clause **DELETED** — §3 |

Accepted P2/P3 remediations: PR-row attribution (decision §5), P-010-first grounding (§3 3(f)),
tokenized halts (`STAGING_GATE_NO_ROUTE`, `STAGING_GATE_OPERATOR_TIMEOUT`), bounded WAIT,
single-test-function pin (§5), Step-1.5-bounded needles (§5), guard rows for items 1–2 (§5),
`chore/stage-{shipment_id}` absence row (§5), P-005 event obligation dropped (§3 3(a)),
un-truncated policy reference (§3), honest surface count (§2, §9), honest R-4 (§7 hardening).

---

## 1. Objective

Rewrite **only item 3** of the Staging Artifact Merge Gate in
`.github/agents/_orchestrator.agent.md` (Step 1.5, lines 270–297) so that:

1. every sub-step is bound to a **named actor** exercising only authority that actor already
   holds under P-010 — granting none and **narrowing none**; and
2. the **direct push to the default branch is removed outright**, grounded in P-010, leaving the
   staging PR path as the sole *specified* route.

**Scope of effect, stated plainly (was buried in hardening in rev 1):** this task makes Step 1.5
*policy-correct and actor-bound*. It does **not** make the gate complete end-to-end — see **H3**.
After this change every path through Step 1.5 terminates in a halt until `019.004-T` lands.
**Stated without overstatement (rev 2.1):** the previously-completing arm was 3(e)'s
*attempt-direct-push-then-fall-back-to-a-PR* construct. Its direct-push head is the P-010
violation being removed; its branch-and-PR fallback tail was **legal and could complete**, and
this task removes that too — because the tail hard-codes the stale `chore/stage-{shipment_id}`
literal that `019.004-T` AC-4 exists to correct, and rebuilding it correctly requires the
handback-record branch discovery that is fenced to `019.004-T` AC-1. See **H13**.

## 2. Surface touched

| File | Region | Change |
|---|---|---|
| `.github/agents/_orchestrator.agent.md` | Step 1.5 **item 3 only** (lines 282–291) | Replaced |
| `.github/agents/_orchestrator.agent.md` | Step 1.5 items **1, 2, 4** | **UNCHANGED** |
| `tests/integration/staging_gate_actor_contract_test.go` | new | Harness (generated pre-claim) |
| `.backlogit/queue/019.004-T.md`, `027-F.md`, `027.001-T.md` | backlog | §6 — **Stage-owned, pre-handback** |

**Honest surface count**: 1 contract file + 1 harness file in the implementation PR; the backlog
records in §6 are mutated by **Stage before handback** and are not part of Ship's PR. Ship's
working surface is therefore **2 files, single skill domain (contract prose)** — the harness is
generated by `harness-architect`, not authored by the task. Well inside the 2-hour rule.

### 2.1 Why items 1, 2 and 4 are untouched

- **Items 1–2** carry the *main-scoped branch-discovery* defect (`git log origin/main..main` is
  empty whenever Stage worked on `chore/stage-{scope-slug}`). That defect is `019.004-T`
  **AC-1**. **Out of scope — fenced.**
- **Item 4** carries manifest verification and the `STAGING_GATE_FAIL` terminal. Extending it to
  verify `stage_artifact_paths` is `019.004-T` **AC-5**. **Out of scope — fenced. Preserved
  unchanged**, which also preserves the gate's fail-closed terminal.
- **Correction (rev 2)**: `019.004-T` **AC-4** does *not* live in items 1–2. Its subject, the
  literal `chore/stage-{shipment_id}`, occurs **only at lines 283 and 288 — both inside item 3**,
  the region this task rewrites. AC-4 is therefore **consumed by this task** and is carved out
  accordingly (§6). Rev 1's assignment of AC-4 to items 1–2 was a factual error.

## 3. Replacement text (pinned)

Item 3 is replaced in full by:

```markdown
3. **When staging artifacts are uncommitted or unpushed — ACTOR-BOUND, NO DEFAULT-BRANCH WRITE.**
   Every action below is bound to a named actor exercising only authority that actor already holds
   under P-010 (see `.github/policies/workflow-policies.md`). This item grants no new authority to
   any role and narrows none.

   a. **Uncommitted Stage artifacts (actor: Stage, in-session).** Committing backlog and planning
      artifacts is Stage work under P-010's `Stage MAY` grant, performed inside Stage's own
      session, on its own Stage artifact branch, before handback. The Orchestrator MUST NOT commit
      them — performing Stage work directly is precisely what P-010 forbids it. If artifacts are
      still uncommitted when this gate runs, halt with
      `STAGING_GATE_UNCOMMITTED: Stage artifacts uncommitted at handoff — re-route to Stage`.
      Do not commit, do not infer, do not proceed.

   b. **Local default branch ahead of the remote (actor: Orchestrator — detect and halt only;
      disposition: operator).** If item 2 reported unpushed commits on the local default branch,
      halt with `STAGING_GATE_LOCAL_MAIN_AHEAD: local {default_branch} is ahead of origin/{default_branch} — operator disposition required`.
      State the condition; do not attribute it. If a *pipeline agent* produced these commits that
      is a P-010 breach, but the same state arises legitimately from operator activity, and P-010
      governs only `stage`, `ship` and `orchestrator`. Do not push, do not reset, do not merge.

   c. **Unpushed Stage artifact branch (actor: OPERATOR).** Pushing the Stage artifact branch and
      opening the staging PR is reserved to the operator, per the installed Stage Role Boundary
      **PR row**: "The operator — never Stage — pushes the Stage artifact branch and opens/approves
      the resulting merge-commit staging PR; no Orchestrator authority to do so is granted or
      assumed by this release unit." The operator is the **always-available substitute actor**,
      including for a direct Stage invocation that no Orchestrator bracketed. Emit an explicit
      operator action request naming the branch, then wait:
      `STAGING_GATE_AWAITING_OPERATOR: push the Stage artifact branch and open a merge-commit
      staging PR to {default_branch}`. **The wait is bounded to a single Orchestrator cycle**: emit
      the request once, then halt with `STAGING_GATE_OPERATOR_TIMEOUT` and return control. Do not
      block indefinitely, do not re-poll inside this step, and never act in the operator's place.
      The same terminal covers a staging PR that is closed or rejected.

   d. **Merge (actor: OPERATOR).** The operator approves and merges the staging PR using a merge
      commit, preserving the merge-commit-only guarantee P-009 enforces for pipeline merges.

   e. **After merge (actor: Orchestrator).** Refresh the remote-tracking ref with
      `git fetch origin {default_branch}` and proceed to step 4. This is a read/refresh only — the
      Orchestrator performs no write to the default branch and does not check it out.

   f. **NO DIRECT DEFAULT-BRANCH PUSH (P-010; P-009 rationale).** No pipeline agent may push
      directly to the default branch, and this gate must never attempt one. The prohibition is
      P-010's and is unqualified: both the `Ship MUST NOT` and `Stage MUST NOT` lists in
      `.github/policies/workflow-policies.md` forbid committing or pushing directly to the default
      branch, Stage's Role Boundary PR row forbids Stage from pushing or opening pull requests at
      all, and the Orchestrator may perform neither agent's work. P-009 supplies the rationale: a
      direct push produces no merge commit, defeating the full-history, attribution and bisect
      guarantees the merge-commit-only rule exists to preserve, and structurally bypassing P-009's
      own Ship Step 5 gate point. The staging PR path in items (c)–(d) is the SOLE route. If it
      cannot complete for a reason item (c) does not already cover, halt with
      `STAGING_GATE_NO_ROUTE` — never fall back to a direct push.
```

## 4. Acceptance criteria

Each criterion is labelled **[E]** executable-by-harness or **[R]** reviewer-verified.

- **AC-1 [E+R]**: Item 3 binds every sub-step to a named actor. **[E]** anchor: `ACTOR-BOUND, NO
  DEFAULT-BRANCH WRITE` present in the slice, plus presence rows for `(actor: Stage`,
  `(actor: OPERATOR` and `(actor: Orchestrator`. **[R]** the semantic completeness of that binding
  (every sub-step, no subjectless verb) remains reviewer-verified — a string anchor cannot prove it.
- **AC-2 [E]**: No direct-push instruction remains in Step 1.5. Absent from the slice:
  ``Attempt a direct push to `main` first`` **and** `Branch protection handling`.
- **AC-3 [E]**: The stale `chore/stage-{shipment_id}` branch literal is absent from the Step 1.5
  slice (consumes `019.004-T` AC-4's negative half — see §6).
- **AC-4 [E]**: The uncommitted-artifact case halts with `STAGING_GATE_UNCOMMITTED`.
- **AC-5 [E]**: The local-default-branch-ahead case halts with `STAGING_GATE_LOCAL_MAIN_AHEAD`.
- **AC-6 [E]**: The operator arm emits `STAGING_GATE_AWAITING_OPERATOR` and bounds the wait with
  `STAGING_GATE_OPERATOR_TIMEOUT`.
- **AC-7 [E]**: The no-route terminal is tokenized as `STAGING_GATE_NO_ROUTE`.
- **AC-8 [E]**: Items 1, 2 and 4 are preserved. Present in the slice: `git status --short --
  .backlogit/`; `git log origin/main..main --oneline`;
  `git show origin/main:.backlogit/queue/{shipment_id}.md`; `STAGING_GATE_FAIL: shipment manifest`.
- **AC-9 [E]**: The handback-record fields are not defined here. Absent from the slice — **one row
  per needle**: `stage_artifact_paths`, `stage_base_commit`, `stage_head_commit`, `stage_outcome`,
  `stage_branch` (the fifth is required by decision §6 fence 1 and was omitted in rev 2).
- **AC-10 [R]**: The operator is named as actor for push/PR/approve/merge, cited to the
  `_stage.agent.md` **PR row**, and recorded as the **always-available substitute for direct Stage
  invocation** (carried forward from `019.004-T` AC-2 — see §6).
- **AC-11 [R]**: No role's authority is granted or narrowed. `.github/policies/workflow-policies.md`
  is **not modified by this task**, and item 3 asserts no restriction absent from the P-010 table.
- **AC-12 [R]**: The Orchestrator is directed only to halt, emit, wait and fetch — never to
  commit, push, open or merge.

## 5. Harness (P-004 red phase)

New file `tests/integration/staging_gate_actor_contract_test.go`, following the **shipped**
precedent `tests/integration/stage_branch_gate_test.go` (018.008-T, PR #58).

**Pinned structure — exactly ONE exported test function**, `TestStagingGateActorContract`, calling
one unexported helper that carries every row. Guard rows are **co-located with red rows inside
that single function**, so no test function is green-on-arrival and P-004's *per-test-function*
red requirement holds. **All assertions are bounded to the Step 1.5 slice**, delimited by
`### Step 1.5: Staging Artifact Merge Gate` and `### Step 2: Route to Ship` (measured: 2216 bytes),
so a future Amendment Log row or provenance note quoting retired text cannot false-positive.

| Row | AC | Assertion (slice-bounded) | Measured today | After |
|---|---|---|---|---|
| 1 | AC-2 | absent ``Attempt a direct push to `main` first`` | **present ×1 → RED** | green |
| 2 | AC-2 | absent `Branch protection handling` | **present ×1 → RED** | green |
| 3 | AC-3 | absent `chore/stage-{shipment_id}` | **present ×2 → RED** | green |
| 4 | AC-1 | present `ACTOR-BOUND, NO DEFAULT-BRANCH WRITE` | **absent → RED** | green |
| 5 | AC-4 | present `STAGING_GATE_UNCOMMITTED` | **absent → RED** | green |
| 6 | AC-5 | present `STAGING_GATE_LOCAL_MAIN_AHEAD` | **absent → RED** | green |
| 7 | AC-6 | present `STAGING_GATE_AWAITING_OPERATOR` | **absent → RED** | green |
| 8 | AC-6 | present `STAGING_GATE_OPERATOR_TIMEOUT` | **absent → RED** | green |
| 9 | AC-7 | present `STAGING_GATE_NO_ROUTE` | **absent → RED** | green |
| 10 | AC-9 | absent `stage_artifact_paths` | absent (fence) | absent |
| G1 | AC-8 | present `git status --short -- .backlogit/` | present ×1 (guard) | unchanged |
| G2 | AC-8 | present `git log origin/main..main --oneline` | present ×1 (guard) | unchanged |
| G3 | AC-8 | present `git show origin/main:.backlogit/queue/{shipment_id}.md` | present ×1 (guard) | unchanged |
| G4 | AC-8 | present `STAGING_GATE_FAIL: shipment manifest` | present ×1 (guard) | unchanged |

**Nine rows are genuinely red on arrival** (rows 1–9). P-004 red phase is satisfiable by
construction; the `026-F` C3 green-on-arrival failure mode does not arise. Guard rows G1–G4 pin
all three fenced regions (items 1, 2 and 4), replacing rev 1's reviewer-eyeball-only check.

## 6. Backlog mutations — **ACTOR: STAGE, in-session, on the Stage artifact branch, before handback**

Ship performs **none** of these. Three of the four sit on Ship's P-010 MUST NOT list
(`Create backlog items`; `update item planning fields (scope, acceptance criteria)`), so executing
them inside the implementation PR would be a P-010 violation by Ship.

1. **Amend `019.004-T`** — bounded to exactly these edits:
   - Mark **AC-2**, **AC-3** and **AC-4** as `CARVED OUT to 027-F`, **retaining existing AC
     numbering** (H7) so external references stay valid.
   - Drop body scope items **(2)**, **(3)** and **(4)** (item (4)'s premise — "step 3(a) names the
     branch `chore/stage-{shipment_id}`" — is falsified by this task).
   - Add a one-line pointer to `027-F`.
   - **Carry-forward guard**: `019.004-T` AC-2's clause *"records the operator as the
     always-available substitute for direct Stage invocation"* is **not** deleted with AC-2 — it is
     reproduced as `027-F` **AC-10** so the requirement is never lost from the backlog.
   - **Carry-forward guard (AC-4 positive half)**: `027-F` consumes only AC-4's *negative* half
     (deleting the `chore/stage-{shipment_id}` literal). AC-4's **positive** directive — *"the
     branch name used is the discovered `stage_branch`"* — is **retained on `019.004-T`**, folded
     into its surviving **AC-1**, so the discovery requirement is not deleted from the backlog.
     Mark AC-4 `PARTIALLY CARVED OUT (negative half) to 027-F; positive half folded into AC-1`.
   - **Green-on-arrival watch (AC-6)**: `019.004-T` AC-6 (*"with no handback record the gate still
     FAILS CLOSED"*) becomes true on every path once this task lands (H3), so its *path-coverage*
     half goes green-on-arrival. It is **not** carved — it retains genuine bite over the
     not-yet-existing handback-consumption arm. Record the narrowing as a note on `019.004-T` so a
     future planner re-derives a red assertion for it rather than assuming coverage. See **H14**.
   - **No status change, no re-plan, no shipment, no other body rewrite.** `019.004-T` stays
     `blocked`.
2. **Create `027.001-T`** under `027-F` carrying §3–§5.
3. **Move `027-F` and `027.001-T`** `blocked → queued`. *(This — not the carve-out — is what makes
   `027-F` claimable; rev 1 wrongly attributed claimability to cycle-breaking.)*
4. **Record exactly these dependency edges** (enumerated, not inferred from the §7 picture):
   - `019.004-T` **depends on** `027-F`  *(the only new cross-unit edge)*
   - `027.001-T` **depends on** `018.008-T`  *(archived done, PR #58 — satisfied on creation)*
   - **No parent↔child edge** between `027-F` and `027.001-T`: parentage is carried by `parent_id`,
     and no sibling record in this backlog encodes parentage as a dependency (`025-F` carries no
     edge to `025.001-T`).
   - **Never** record `027-F` depends on `019.004-T` — see §7 cycle safety.
5. **Create the shipment** over `[027-F, 027.001-T]` and record on it the two operator-disposition
   items from **H8** (pre-claim `harness-ready`) and **H9** (gate-enforced unclaimability), plus
   the harness-artifact actor question from **H15**. *(Enumerated here because rev 2 imposed
   shipment content in H8/H9 without listing the shipment as a mutated artifact.)*

## 7. Dependency graph (corrected)

**Arrow convention — one direction only, stated once**: `X ──> Y` reads **"X depends on Y"**
(Y must ship first). This is the direction `backlogit dep list` prints. Edges to archived-done
predecessors are shown in parentheses; the graph otherwise lists unsatisfied edges only.

```
027.001-T ──> (018.008-T, done PR #58)
019.004-T ──> 027-F          <-- NEW edge (027-F ships first)
019.004-T ──> 025.001-T      <-- pre-existing
025.001-T ──> (018.008-T, done)
025-F     ──> 026-F          <-- pre-existing; 026-F blocked on O-1
025-F     ──> 027-F          <-- pre-existing
027-F     ──> (nothing)      <-- zero forward dependencies
```

- `027-F` has **zero** forward dependencies → **no unshipped blocking predecessor** → claimable
  once staged.
- **`027-F` does NOT unblock `025-F`.** `025-F` also depends on `026-F`, which remains blocked on
  operator determination **O-1**. No downstream unblock is claimed.
- **Cycle safety**: the only new edge is `019.004-T ──> 027-F` (019.004-T depends on 027-F). H3
  establishes a *reachability* relation in the opposite sense — `027-F`'s operator arms become
  reachable once `019.004-T` lands — but reachability is **not** a dependency and **MUST NOT** be
  recorded as `027-F ──> 019.004-T`. Doing so would create a true 2-cycle where none exists today.

## 8. Invariants

- **I1** — No role's authority is granted **or narrowed**. The rev-1 Ship-narrowing clause is
  deleted; item 3 asserts nothing absent from the P-010 table.
- **I2** — Stage/Ship role boundaries unchanged; no Stage push, no Ship backlog write (§6).
- **I3** — Single implementation branch/worktree (P-016) — nothing added.
- **I4** — Merge-commit-only (P-009) preserved as rationale; the prohibition rests on P-010.
- **I5** — Stage artifacts reach `origin/main` before Ship — item 4 preserved, pinned by G3/G4.
- **I6** — Fail-closed preserved and **extended**: every terminal is tokenized
  (`STAGING_GATE_UNCOMMITTED`, `_LOCAL_MAIN_AHEAD`, `_OPERATOR_TIMEOUT`, `_NO_ROUTE`, `_FAIL`).
- **I7** — Non-circular: delivered through the PR #59/#60/#61/#62 route, proven by execution
  (decision §2.1).

## 9. Sizing

| Field | Value |
|---|---|
| `size` | `S` |
| `complexity` | `medium` |

Ship's surface: 2 files (1 contract section ~40 lines + 1 generated harness), single skill domain.
Well under 2 hours. `complexity: medium` — not `low` — because the wording must thread P-010/P-009
without widening **or narrowing** any role, the precise failure mode AC-11 exists to prevent and
the one rev 1 got wrong.

## 10. Out of scope (fenced)

Handback record definition (`025.001-T`); branch discovery via the handback record
(`019.004-T` **AC-1**) and post-merge `stage_artifact_paths` verification (`019.004-T` **AC-5**);
generalized direct-push detector, fixture corpus and CI wiring (`020-F`); **O-1**, `026-F`,
`025-F`; any edit to `.github/policies/workflow-policies.md`.

---

## Plan Hardening (rev 2)

Performed 2026-09-18 under P-006. Rev 1's hardening is superseded; findings that survived review
are retained, findings the reviewers corrected are restated.

### H0 — Red-on-arrival verified by measurement, and re-verified after remediation

All fourteen rows in §5 were measured against the installed
`.github/agents/_orchestrator.agent.md`, **bounded to the Step 1.5 slice** (2216 bytes, delimited
by `### Step 1.5: Staging Artifact Merge Gate` … `### Step 2: Route to Ship`):

- Rows 1–3 (absence): needles present ×1, ×1, ×2 → **RED**.
- Rows 4–9 (presence): all absent → **RED**.
- Row 10 (fence): `stage_artifact_paths` absent → fence holds.
- Rows G1–G4 (guards): all present ×1 → fenced regions pinned.

**Nine rows red on arrival.** P-004 is satisfiable by construction. Independently re-measured by
the policy reviewer, who confirmed all seven rev-1 needles; the four new tokens and the three new
guard rows were measured this session.

### H1 — Absence needles are default-branch-coupled (retained)

``Attempt a direct push to `main` first`` hard-codes `main`. **Resolution**: three absence rows,
two of which (`Branch protection handling`, `chore/stage-{shipment_id}`) are branch-agnostic
structural anchors of the same arm. A default-branch rename cannot defeat all three.

### H2 — Behavioural change: an illegal auto-commit becomes an explicit halt (retained)

Removing the commit arm converts a case that today silently and illegally auto-commits into a
`STAGING_GATE_UNCOMMITTED` halt. **Disclosed and accepted**: the auto-commit was itself the P-010
violation being repaired.

### H3 — MATERIAL: after this task, EVERY path through Step 1.5 halts until `019.004-T` lands

Rev 1 disclosed the path effects separately and never composed them. Stated in full:

- item 1 **dirty** → 3(a) → **halt** `STAGING_GATE_UNCOMMITTED`
- item 1 clean → item 2 **ahead** → 3(b) → **halt** `STAGING_GATE_LOCAL_MAIN_AHEAD`
- item 1 clean → item 2 empty (the normal Stage side-branch case, because `git log
  origin/main..main` is empty when Stage worked on `chore/stage-{scope-slug}`) → item 4 →
  **halt** `STAGING_GATE_FAIL`

**`027-F` removes the only arm that previously completed** — and that arm was the P-010 violation.
The gate becomes 100% non-completing until `019.004-T` restores branch discovery (its AC-1).

This does **not** invalidate `027-F`, on two grounds the reviewers independently upheld:

1. **Item 3 is reachable today** via item 1's `- If dirty: … proceed to step 3`.
2. **The defect is a standing hazard independent of reachability.** These files are
   agent-interpreted prose, not a guaranteed state machine; an agent reading Step 1.5 can act on
   the direct-push instruction regardless of control flow. Removing a policy-violating instruction
   from installed text has standalone value.

Operationally the pipeline is unaffected today: the route actually used for PRs #59–#62 is the
operator merging the staging PR **before** the gate runs, after which item 4 passes.

### H4 — AC-8 is now executable (rev 1 gap closed)

Rev 1 left items 1–2 to a reviewer-side `git diff`. Rev 2 adds guard rows **G1** and **G2**
asserting item 1's and item 2's command strings, plus **G3/G4** for item 4. All three fenced
regions are machine-pinned. The `git diff` check is retained as a secondary reviewer aid only,
with the rev-1 "lines 282–291" phrasing dropped — the post-image necessarily extends past 291 and
shifts item 4 down, so a line-range assertion was ill-defined.

### H5 — `{default_branch}` placeholder convention (retained)

Matches the file's existing convention (`{shipment_id}`, `{branch_name}`). Harness rows assert
token names, never the placeholder.

### H6 — Scope-creep risk on the `019.004-T` amendment (bounded, extended in rev 2)

Bounded to: mark **AC-2/AC-3/AC-4** carved out; drop body items **(2)(3)(4)**; add pointer; add
carry-forward of AC-2's substitute-actor clause into `027-F` AC-10; add one dependency edge.
**No status change, no re-plan, no shipment, no other body rewrite.**

### H7 — AC renumbering would break external references (retained)

Renumbering would invalidate references in
`docs/plans/2026-09-12-intercom-go-stage-persistence-enforcement-plan.md`. **Retain numbering;
mark carved-out in place.**

### H8 — DB12DA37 pre-claim harness precondition, and its actor question

A shipment claim auto-transitions descendant tasks `queued → active` before Ship Step 2 runs, and
the Stage-shipment substitution at `_ship.agent.md:362–367` drops the `harness-ready` predicate.
Ship Step 3's derived executable set *does* include `active` members, so the ready queue is **not**
vacuous — but a task could still execute without a harness.

**Mandatory precondition, disclosed and not authorized around**: `harness-architect` MUST be run
over `027.001-T` and the `harness-ready` label applied **BEFORE** the shipment is claimed. Stage
neither performs nor waives this. If it cannot be satisfied, **DO NOT CLAIM**. B1 is refuted, so a
legal in-PR harness slot exists.

**Open actor question (raised by review, not resolved here)**: P-004 records harness-architect as
`Applies To: ship (via harness-architect skill)`. An operator invoking a Ship-owned skill outside a
Ship session is a P-010-shaped question this plan surfaces but does not answer. **Recorded as an
operator-disposition item on the shipment**, not silently assumed.

### H9 — "Unclaimable until the staging PR lands" is gate-enforced, not tool-enforced

`backlogit shipment claim` has no remote-state precondition and would succeed locally at any time.
Unclaimability is enforced upstream of the claim by two independent gates:

- **Orchestrator Step 1.5 item 4** (preserved, pinned by G3/G4): `git show
  origin/main:.backlogit/queue/{shipment_id}.md` halts with `STAGING_GATE_FAIL` when the manifest
  is absent from `origin/main`.
- **Ship Step 0.5** branches from `origin/main`; a manifest absent there is unreadable to Ship.

**Disclosed precisely**: gate-enforced, not tool-enforced. Recorded on the shipment.

### H10 (new) — Regeneration risk, restated honestly

`_orchestrator.agent.md` is autoharness-generated (`Template: orchestrator.agent.md.tmpl`), and
that template is **not present in this repository**. `intercom-go` is a target workspace, so any
`install-harness` / `tune-harness` run reverts this fix. Rev 1 claimed `020-F` "exists to remove"
this risk; that is **wrong** — `020-F` builds a *detector*, which converts a silent revert into a
loud one but does not make the fix durable. **Durability requires an upstream template change
outside this workspace's reach.** Bounded and fail-closed: the §5 harness goes red on revert.

### H11 (new) — Policy text is referenced, not duplicated

Rev 1 embedded a **truncated** quotation of P-010's `Stage MAY` bullet, dropping its operative
qualifier (*"on that dedicated Stage artifact branch only — never on the default branch"*) and
creating an unasserted drift surface inside a second generated file. Rev 2 replaces it with a
cross-reference to `.github/policies/workflow-policies.md`. The one verbatim quotation retained
(the `_stage.agent.md` PR row, in 3(c)) is already asserted verbatim by the shipped
`stage_branch_gate_test.go`, so it is drift-protected.

### H12 (new) — Unrequested mechanism dropped (YAGNI)

Rev 1's 3(a) added a "record a P-010/P-005 event" obligation with no defined sink, schema or
consumer — an obligation no other Step 1.5 arm carries. **Dropped.** P-005 already binds all
agents globally; the tokenized halt alone satisfies I6.

### Hardening outcome

No finding blocks the plan. All three P1 policy findings and all four P1 scope findings are
remediated; five P2/P3 items are folded into §3–§5; four items (H2, H3, H8, H9) are disclosed as
residual, plus H10's regeneration bound. **Re-submit to plan-review (attempt 2).**

### Hardening addendum (rev 2.1) — closing plan-review attempt 2 advisories

Both personas returned **ADVISORY** on attempt 2 with **no P0 and no P1**. The substantive P2s are
closed in place below; the remainder are disclosed.

**H13 (new) — a LEGAL completing arm is removed, not only the violation.** Installed 3(e) reads
"attempt a direct push **first**; if rejected, fall back to creating a staging PR". Under branch
protection the illegal head is rejected and the **legal branch-and-PR tail completes**. Rev 2
deleted the tail as well, and §1 wrongly claimed "the only previously-completing arm was the P-010
violation". **Corrected in §1.** Justification for removing the tail: it hard-codes the stale
`chore/stage-{shipment_id}` literal (the very defect `019.004-T` AC-4 names), and rebuilding it
correctly needs handback-record branch discovery fenced to `019.004-T` AC-1. Net effect is
disclosed in H3, not concealed.

**H14 (new) — `019.004-T` AC-6 narrowing, disclosed rather than silently created.** Once every
Step 1.5 path halts (H3), AC-6's *path-coverage* half ("never proceeds on inferred state") is
true on every path — a partial green-on-arrival. AC-6 is **not** carved, because it retains real
bite over the not-yet-existing handback-consumption arm. §6.1 records the narrowing as a note on
`019.004-T` so a future planner re-derives a red assertion instead of assuming coverage. This is
the honest disposition of the one residue the AC-4 carve did not cover.

**H15 (new) — the pre-claim harness artifact has no Stage-legal home. OPERATOR-DISPOSITION,
FAIL-CLOSED.** H8 requires `harness-architect` to run and `harness-ready` to be applied **before**
the claim. But Stage `MUST NOT` write test files or run build/test tooling, so Stage can neither
author, red-confirm, nor commit `tests/integration/staging_gate_actor_contract_test.go`, and §6's
"ACTOR: STAGE" does not reach the `harness-ready` mutation. The artifact must therefore either
survive uncommitted across the handback or be produced by an actor this plan does not name.
**Stage does not resolve this and does not assume it away.** It is recorded on the shipment as a
second operator-disposition item alongside H8. **The shipment remains unclaimable until the
operator disposes of it** — consistent with H8's "If it cannot be satisfied, DO NOT CLAIM".
(`026-F` records B1 as refuted — `_ship.agent.md` provides a legal in-PR slot — which is the most
likely resolution, but adopting it is an operator call, not Stage's.)

**H16 (new) — needle authority and harness conventions pinned.** §5's single-line rows are the
**sole** needle authority; needles wrapped across lines in §3/§4 prose are illustrative only and
must be re-flowed to one line when installed (the existing `STAGING_GATE_FAIL: shipment manifest
{shipment_id} not found on origin/main` is one line; the new halt messages must match). The slice
delimiters are matched as **substrings**, not line equality — the installed heading carries a
`(NON-NEGOTIABLE)` suffix the delimiter omits. The harness uses **row-ID expected-failure markers
in every assertion**, and **no `t.Run` subtests, no skips, no build tags, no env/platform gates**,
per the shipped `stage_branch_gate_test.go` precedent — so the four guard rows can never report as
independently-passing subtests.

**Disclosed, not fixed (P3):** sub-step letters shifted when 3(b) was inserted, so deliberation
§5/§10.5 still say "3(e)" where rev 2.1 means **3(f)** — traceability note only, no behavioural
effect. Items 1/2/4 keep the literal `main` (pinned by G2–G4) while new item 3 uses
`{default_branch}`; the mixed convention is a direct consequence of the fence and resolves when
`019.004-T` rewrites items 1–2. Sub-steps 3(c)–3(e) have no entry condition until `019.004-T`
AC-1 lands (H3), so AC-10's substitute-actor clause is carried as a **backlog AC on `027-F`**
rather than relied on as installed behaviour.

### Outcome (rev 2.1)

Plan-review attempt 2: **ADVISORY / ADVISORY**, no P0, no P1. Substantive P2s closed in place;
residuals disclosed above. **Acceptable verdict — proceed to shipment formation**, with the
shipment carrying H8, H9 and H15 as operator-disposition items and remaining unclaimable until
they are disposed of and the staging PR has merged to `origin/main`.
