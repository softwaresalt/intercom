# Shipment reconcile report: 013-S (safe-close / cascade path)

- Mode: `safe-close` (Step 0 classifier verdict: `CASCADE`, P-015 verified
  fully-covered-root exception)
- Timestamp: 2026-09-08T23:15:33Z (UTC, per backlogit log)
- Merge commit: `17daffb5614f2f8ed797d7647aeadcc1dc29b1d2` (PR #40)

## Step 0 — classifier verdict

Ran `classify_shipment_close_path` (autoharness Python package,
`src/autoharness/gates/shipment_closure.py` equivalent) directly against the
013-S manifest and the live `.backlogit/` workspace:

```
ClosePathDecision(
  close_path=CASCADE,
  reason='every feature member is a verified fully-covered root; cascade close is permitted',
  qualifying_feature_ids=('014-F',)
)
```

Preconditions verified:
- `014-F` has no `parent_id` (root).
- `014-F` is fully covered: all 8 descendants (`014.001-T`…`014.008-T`) are
  manifest members; no other item in `.backlogit/queue/` or
  `.backlogit/archive/` declares `parent_id: 014-F`.
- Manifest contains nothing beyond `014-F` and its 8 children.
- No linked deliberation IDs found on `014-F` (`custom_fields` has no
  `source_deliberation_id`; description/references contain no
  `DL\d+` / `\d+(?:\.\d+)*-DL` pattern — only a plain doc-path reference).

## Pre-close declared-status snapshot (Step 0(b)/(c))

| ID | parent_id | declared status (pre-close) | location (pre-close) |
|---|---|---|---|
| 014.001-T | 014-F | done | archive |
| 014.002-T | 014-F | done | archive |
| 014.003-T | 014-F | done | archive |
| 014.004-T | 014-F | done | archive |
| 014.005-T | 014-F | done | archive |
| 014.006-T | 014-F | done | archive |
| 014.007-T | 014-F | done | archive |
| 014.008-T | 014-F | done | archive |
| 014-F | (none, root) | active | queue |
| 013-S | (n/a, shipment) | active | queue |

All 8 tasks were `pre-archived` by location (moved during the build loop's
per-task completion, not deferred to shipment close) but declared `status:
done`, never `archived` — so none were "truly archived" pre-close per the
Step 0(b) rule, and all 8 remain in `required_ids` below.

## Cascade invocation result

```
backlogit shipment ship 013-S --sha 17daffb5614f2f8ed797d7647aeadcc1dc29b1d2 \
  --message "fix(pathsafe): close Windows directory-junction containment bypass (013-S/014-F)" \
  --author "Derek Williams <42183845+softwaresalt@users.noreply.github.com>"
```

```json
{
  "shipment_id": "013-S",
  "shipment_status": "shipped",
  "archived_ids": ["014.001-T","014.002-T","014.003-T","014.004-T","014.005-T","014.006-T","014.007-T","014.008-T","013-S","014-F"],
  "returned_ids": [],
  "commit_sha": "17daffb5614f2f8ed797d7647aeadcc1dc29b1d2"
}
```

## Verification (Cascade Close Sub-Procedure steps 2–4)

- **Step 2 (`returned_ids` empty)**: PASS — `[]`.
- **Step 3 (two-set gate)**:
  - `allowed_ids` = 8 manifest tasks + qualifying feature `014-F` + shipment
    `013-S` (no linked deliberations) = 10 IDs.
  - `archived_ids` (engine result) = same 10 IDs.
  - `archived_ids - allowed_ids` = `{}` (empty) — PASS, no unexpected
    artifact archived.
  - `required_ids` = `013-S` + `014-F` (both unconditional) + all 8 tasks
    (none were truly `status: archived` pre-close, so all remain required)
    = 10 IDs.
  - `required_ids - archived_ids` = `{}` (empty) — PASS, no required
    artifact left unarchived.
- **Step 4 (`parent_id` preservation)**: re-read all 8 archived tasks
  post-close; every one still declares `parent_id: 014-F`, unchanged from
  the Step 0(b) snapshot. PASS.

## Post-mode / P-007 deleted-file guard

`git status --short -- ".backlogit/"` immediately after the cascade call
shows exactly the expected transitions and no unattributed deletions:

```
RM .backlogit/queue/013-S.md -> .backlogit/archive/013-S.md
 M .backlogit/archive/014.001-T.md ... 014.008-T.md   (status stamped: archived / archived_status updated)
 D .backlogit/queue/014-F.md
?? .backlogit/archive/014-F.md
```

`014-F` archive record: `status: archived`, `archived_status: done`.
`013-S` archive record: `status: archived`, `archived_status: shipped`.

No P-007 restore action required — no archive file appeared as a deletion
in `git status`.

## Gate decision

**CLOSED.** All verification steps pass. Safe to commit `.backlogit/`.
