# Shipment reconcile report — 014-S (safe-close)

- Mode: `safe-close` (Cascade Close Sub-Procedure, P-015 verified fully-covered-root exception)
- Date: 2026-09-08
- Shipment: 014-S / feature 015-F
- Merge commit: `d7be7e883f1d309a22037aae97ec769cb647b020` (PR #46)

## Pre-mode

- Manifest (`custom_fields.items`, 14 entries): `015-F`, `015.001-T`…`015.013-T`.
- Per-item check: all 14 manifest items found in `.backlogit/archive/`
  (classified `pre-archived` — this session archived all 13 tasks + feature
  individually via `backlogit move --status done` earlier in the same
  session, before the safe-close/cascade step ran).
- Orphan scan: 0 orphans (no queue file declares `shipment_id: 014-S` outside
  the manifest).
- Shipment-record-status classification: record `status: active` →
  `record-consistent`.
- Gate decision: **PROCEED**.

## Step 0 classification (P-015 verified fully-covered-root check)

- `015-F`: root (no `parent_id`). Fully covered: exactly 13 children
  (`015.001-T`…`015.013-T`), all present in the manifest, verified by
  scanning both `.backlogit/queue/` and `.backlogit/archive/`
  for any file with `parent_id: 015-F` or hierarchy prefix `015` — no
  further descendants found. Terminal: no manifest member declares `015-F`
  as parent besides these 13. No linked deliberation (`015-F` has no
  `custom_fields.source_deliberation_id`, and its description contains no
  `DL`-pattern token — only a `docs/decisions/*.md` path reference, which the
  engine's deliberation-ID matcher does not match).
- **Classification: CASCADE** (single qualifying root feature, `015-F`; no
  other feature members in the manifest).

Pre-close declared-status snapshot (Step 0(b)/(c), captured before the
cascade invocation):

| ID | parent_id | declared status (pre-close) |
|---|---|---|
| 015.001-T … 015.013-T | `015-F` (all 13) | `done` (all 13) |
| 015-F | (none — root) | `done` |
| 014-S | n/a | `active` |

## Cascade Close Sub-Procedure

Invocation: `backlogit shipment ship 014-S --sha d7be7e883f1d309a22037aae97ec769cb647b020 --message "feat(014-S): retired-architecture gate detection-quality hardening (#46)" --author "Derek Williams <42183845+softwaresalt@users.noreply.github.com>"`

Result:
```json
{
  "shipment_id": "014-S",
  "shipment_status": "shipped",
  "archived_ids": ["015.001-T","015.002-T","015.003-T","015.004-T","015.005-T",
    "015.006-T","015.007-T","015.008-T","015.009-T","015.010-T","015.011-T",
    "015.012-T","015.013-T","014-S","015-F"],
  "returned_ids": [],
  "commit_sha": "d7be7e883f1d309a22037aae97ec769cb647b020"
}
```

Verification:

1. `returned_ids` empty — **PASS** (matches classifier precondition).
2. Two-set gate:
   - `allowed_ids` = 13 tasks + `015-F` (qualifying feature) + `014-S`
     (shipment record) = 15 IDs. No linked deliberations to add.
   - `required_ids` = `014-S` + `015-F` (both unconditional) + all 13 tasks
     (none were truly `status: archived` pre-close — all declared `done`) =
     15 IDs.
   - `archived_ids` (from result) = the same 15 IDs.
   - `archived_ids - allowed_ids` = **empty — PASS**.
   - `required_ids - archived_ids` = **empty — PASS**.
3. `parent_id` preservation: re-read all 13 archived tasks — every one still
   declares `parent_id: 015-F`, unchanged from the pre-close snapshot —
   **PASS**.
4. Provenance:
   - `014-S`: `status: archived`, `archived_status: shipped` — **PASS**
     (matches step 8's required shipment-record provenance).
   - `015-F`: `status: archived`, `archived_status: done` — expected engine
     behavior for a qualifying feature member (the engine unconditionally
     forces the feature to `done` before archival; `archived_status: done`
     is therefore correct, not an anomaly) — **PASS**.
   - All 13 tasks: `status: archived`, `archived_status: done` — **PASS**.

**Gate decision: CLOSED.** Proceeded to post-mode.

## Post-mode

- Archive presence: `014-S`, `015-F`, and all 13 tasks confirmed present in
  `.backlogit/archive/`.
- Deleted-file guard (P-007): `git status --short -- ".backlogit/archive/"`
  reported no deletions (only modifications/renames for this closure's own
  mutations).
- Gate decision: **PROCEED**.

## Recommendation

**CLOSED.** Shipment 014-S is fully archived with verified provenance. No
cascade detected, no protected-set violation (none applicable — 015-F, the
covering feature, is itself a manifest member, so this was not a
partial-feature shipment).
