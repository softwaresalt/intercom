# Shipment Reconciliation Report — 009-S — safe-close (cascade path)

- shipment_id: 009-S
- mode: safe-close
- close_path: CASCADE (P-015 verified fully-covered-root exception)
- merge_commit_sha: d8ca53f59871158d007acdd86188b2c0b47c2bc8
- timestamp: 2026-09-06T21:40:47Z
- lock: acquired via `.github/skills/file-lock/scripts/acquire_lock.ps1` on
  `.backlogit/queue/009-S.md` before pre-mode; released after post-mode
  completed.

## Pre-Mode reconciliation (expected_status: done)

Manifest (`backlogit shipment get 009-S`): `[010-F, 010.001-T, 010.002-T,
010.003-T, 010.004-T, 010.005-T]`.

Per-item classification (queue checked first, then archive):

| id | location found | declared status | classification |
|---|---|---|---|
| 010-F | archive | done | pre-archived |
| 010.001-T | archive | done | pre-archived |
| 010.002-T | archive | done | pre-archived |
| 010.003-T | archive | done | pre-archived |
| 010.004-T | archive | done | pre-archived |
| 010.005-T | archive | done | pre-archived |

All 6 manifest items were relocated to `.backlogit/archive/` by a prior
session's direct file operations (uncommitted working-tree state carried
across the merge-approval halt) but declared `status: done`, not
`status: archived` — i.e. pre-archived by location only, not truly archived.
Orphan scan: grepped `.backlogit/queue/*.md` for `010-F`/`009-S` references
— none found outside the shipment record itself. Shipment record status:
`active` → `record-consistent`.

Recommendation: **PROCEED**.

## Step 0 — Close path classification

Manual classification against the live `.backlogit/` workspace (no Python
`classify_shipment_close_path` implementation installed in this Go
workspace; equivalent check performed directly):

- 010-F has no `parent_id` (root).
- Scanned both `.backlogit/queue/` and `.backlogit/archive/` for any record
  declaring `parent_id: 010-F` or `parent_id: 010.00X-T` — only the 5
  manifest tasks found, all direct children, no deeper descendants.
- All 5 children are manifest members (full coverage). No non-manifest
  descendants.
- No linked deliberations found on 010-F (`custom_fields` has only
  `harness_status`; no `source_deliberation_id`).
- Manifest contains nothing beyond 010-F and its 5 children.

Result: **CASCADE** — qualifying_feature_ids: `(010-F,)`.

(This determination was reinforced by the installed `backlogit` CLI itself
refusing a non-cascading `backlogit move 009-S --status shipped`: "shipment
must be shipped via ShipShipment, not a direct status update" — the
generic safe-close move path described in this skill's steps 1–10 is not
available in `backlogit` 1.10.1 for shipment records, making CASCADE the
only viable close path for this backlogit version in addition to being the
classifier-selected path.)

## Pre-close declared-status snapshot (Step 0(b)/(c), captured before cascade)

| id | parent_id | declared status (pre-close) | location (pre-close) |
|---|---|---|---|
| 010-F | (none — root) | done | archive (pre-archived by prior session) |
| 010.001-T | 010-F | done | archive |
| 010.002-T | 010-F | done | archive |
| 010.003-T | 010-F | done | archive |
| 010.004-T | 010-F | done | archive |
| 010.005-T | 010-F | done | archive |
| 009-S | (shipment) | active | queue |

## Cascade invocation

```
backlogit shipment ship 009-S --sha d8ca53f59871158d007acdd86188b2c0b47c2bc8 \
  --message "Merge pull request #27 from softwaresalt/feat/009-s-pathsafe-and-config-containment-hardening" \
  --author "Derek Williams <42183845+softwaresalt@users.noreply.github.com>"
```

Result:
```json
{
  "shipment_id": "009-S",
  "shipment_status": "shipped",
  "archived_ids": ["010.001-T","010.002-T","010.003-T","010.004-T","010.005-T","009-S","010-F"],
  "returned_ids": [],
  "commit_sha": "d8ca53f59871158d007acdd86188b2c0b47c2bc8"
}
```

## Verification (Cascade Close Sub-Procedure steps 2–4)

- **Step 2 (`returned_ids` empty check)**: `returned_ids = []` — PASS. No
  classifier/engine mismatch.
- **Step 3 (two-set gate)**:
  - `allowed_ids` = {010.001-T, 010.002-T, 010.003-T, 010.004-T, 010.005-T,
    010-F, 009-S} (5 manifest tasks + qualifying feature 010-F + shipment
    record 009-S; no linked deliberations to add)
  - `required_ids` = {009-S, 010-F} (unconditional) ∪ {all 5 tasks, none of
    which were truly `status: archived` pre-close} = same 7-member set
  - `archived_ids` (returned) = {010.001-T, 010.002-T, 010.003-T, 010.004-T,
    010.005-T, 009-S, 010-F} — exact match to both sets
  - `archived_ids - allowed_ids` = `{}` — PASS (no unexpected artifact)
  - `required_ids - archived_ids` = `{}` — PASS (nothing required left
    unarchived)
- **Step 4 (`parent_id` preservation)**: re-read all 5 archived task
  frontmatter files; all still declare `parent_id: 010-F`, unchanged from the
  pre-close snapshot — PASS.

## Post-close artifact state (re-read)

| id | status | archived_status |
|---|---|---|
| 010.001-T | archived | done |
| 010.002-T | archived | done |
| 010.003-T | archived | done |
| 010.004-T | archived | done |
| 010.005-T | archived | done |
| 010-F | archived | done |
| 009-S | archived | shipped |

## Post-Mode reconciliation

1. Archive presence: `.backlogit/archive/009-S.md` present (confirmed via
   `Test-Path`). Per-item: all 6 manifest item archive files present.
2. Deleted-file guard (P-007): `git status --short -- ".backlogit/archive/"`
   showed only additions/renames/modifications, **no deletions**, both
   immediately after the cascade and again after the commit (one benign
   post-commit metadata write on `009-S.md` — `archived_from`,
   `archived_status`, `commit` fields plus final `status: archived` —
   caught by re-`git add` + `git commit --amend --no-edit` before treating
   the tree as clean).
3. Gate decision: **PROCEED**.
4. Lock released via `.github/skills/file-lock/scripts/release_lock.ps1`.

## Gate decision (safe-close overall)

**CLOSED.** All manifest items archived, shipment record archived with
correct `archived_status: shipped` provenance, `returned_ids` empty, both
two-set gate checks empty, all `parent_id` values preserved. Backlog state
committed on `post-merge/009-s-pathsafe-and-config-containment-hardening`
at commit `a155dd8` ("chore: archive 009-S backlog artifacts").
