# Shipment Reconciliation Report — Cascade Close

- Shipment: 012-S
- Mode: safe-close (Cascade Close Sub-Procedure, P-015 verified
  fully-covered-root exception)
- Merge commit SHA: 9d8937ae2bc721cdeb15b74963acb92221843f06
- Merge PR: <https://github.com/softwaresalt/intercom/pull/36>
- Timestamp: 2026-09-08T19:48:00Z

## Step 0 — Close Path Classification

Manifest: `013-F` (root feature, no `parent_id`), `013.001-T` .. `013.006-T`
(all direct children of `013-F`, no grandchildren found).

Classification was run programmatically via
`classify_shipment_close_path(manifest_items, workspace_backlog_dir)`
(`.copilot/installed-plugins/autoharness/autoharness/src/autoharness/gates/shipment_closure.py`):

```text
ClosePathDecision(close_path=<ClosePath.CASCADE: 'cascade'>,
  reason='every feature member is a verified fully-covered root; cascade close is permitted',
  qualifying_feature_ids=('013-F',))
```

- `013-F` is a root: confirmed (`parent_id` absent from its record).
- `013-F` is fully covered: confirmed — exactly 6 children of `013-F` exist
  in the live workspace (`.backlogit/archive/013.00[1-6]-T.md`, pre-close
  location), and all 6 are manifest members. No grandchildren (subtasks of
  the 6 tasks) exist.
- `013-F` is additionally terminal: confirmed (0 descendants beyond the 6
  already-counted children).
- Manifest contains nothing beyond the qualifying root feature and its
  descendants: confirmed (`[013-F, 013.001-T, ..., 013.006-T]`, 7 items,
  no extras).
- No linked deliberation: `013-F`'s `custom_fields` contains no
  `source_deliberation_id`, and its description/references contain no
  token matching `\b(?:DL\d+|[0-9]+(?:\.[0-9]+)*-DL)\b`.

**Verdict: CASCADE** (all preconditions met).

## Step 0(b)/(c) — Pre-Close Declared-Status Snapshot

| Item | Pre-close declared status | Pre-close parent_id | Pre-close location |
|---|---|---|---|
| 013.001-T | done | 013-F | archive |
| 013.002-T | done | 013-F | archive |
| 013.003-T | done | 013-F | archive |
| 013.004-T | done | 013-F | archive |
| 013.005-T | done | 013-F | archive |
| 013.006-T | done | 013-F | archive |
| 013-F (qualifying feature) | done | (none — root) | archive |
| 012-S (shipment record) | active | n/a | queue |

None of the manifest task items or the qualifying feature were truly
`status: archived` pre-close (all declared `done`, relocated to `archive/`
during Ship's own Step 4.5 task-completion loop, which invokes
`backlogit move <id> --status done` and this backlogit version auto-relocates
`done` artifacts to `archive/` while keeping `status: done`, not
`status: archived`) — so all 8 items (6 tasks + feature + shipment record)
are `required_ids` members.

## Step 1 — Cascade Invocation

```powershell
backlogit shipment ship 012-S --sha 9d8937ae2bc721cdeb15b74963acb92221843f06 `
  --message "chore: retire the D6a gate narrowing -- broaden U-E1a to internal/** (013-F) (#36)" `
  --author "Derek Williams <42183845+softwaresalt@users.noreply.github.com>"
```

Result:

```json
{
  "shipment_id": "012-S",
  "shipment_status": "shipped",
  "archived_ids": ["013.001-T","013.002-T","013.003-T","013.004-T","013.005-T","013.006-T","012-S","013-F"],
  "returned_ids": [],
  "commit_sha": "9d8937ae2bc721cdeb15b74963acb92221843f06"
}
```

## Step 2 — `returned_ids` Check

`returned_ids` is `[]` (empty). **PASS.**

## Step 3 — Two-Set Gate

- `allowed_ids` = {013.001-T, 013.002-T, 013.003-T, 013.004-T, 013.005-T,
  013.006-T, 013-F, 012-S} (6 manifest tasks + qualifying feature + shipment
  record; no linked deliberations).
- `required_ids` = same 8-item set (shipment record + qualifying feature
  unconditionally, plus all 6 task items since none were truly archived
  pre-close).
- `archived_ids` (actual) = {013.001-T, 013.002-T, 013.003-T, 013.004-T,
  013.005-T, 013.006-T, 012-S, 013-F} — identical 8-item set.
- `archived_ids - allowed_ids` = `{}` (empty). **PASS — no unexpected
  artifact archived.**
- `required_ids - archived_ids` = `{}` (empty). **PASS — no required
  artifact left unarchived.**

## Step 4 — `parent_id` Preservation

Re-read post-close: all 6 tasks still declare `parent_id: 013-F`, unchanged
from the Step 0(b) snapshot. **PASS.**

## Step 5 — Post-Close Verified State

| Item | Post-close declared status |
|---|---|
| 013.001-T .. 013.006-T | archived |
| 013-F | status: archived, archived_status: done |
| 012-S | status: archived, archived_status: shipped |

## P-007 Archive Integrity Check

`git status --short -- ".backlogit/"` shows only a rename (012-S
queue→archive) and modifications within `.backlogit/archive/` (status field
transitions on the 6 tasks and the feature) — **no archive file appears as a
working-tree deletion**. P-007 satisfied; no restore needed.

## Gate Decision

`returned_ids` empty, `archived_ids - allowed_ids` empty, `required_ids -
archived_ids` empty, all `parent_id`s preserved, P-007 satisfied.

**Recommendation: CLOSED.** Proceed to post-mode.
