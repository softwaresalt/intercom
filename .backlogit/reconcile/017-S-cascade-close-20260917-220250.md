---
shipment_id: 017-S
mode: safe-close (Cascade Close Sub-Procedure)
merge_commit_sha: 5d69c727fc532eb61139f3e52cd3b64ab96574ac
timestamp: 2026-09-17T22:02:50-07:00
invoked_by: ship
context: S-17/S-18, closure branch post-merge/017-s-stage-artifact-branch-pr-policy-gap-correction
---

# 017-S Cascade Close Sub-Procedure Report (S-17/S-18)

## Step 0(c) classification (`classify-close-path`)

- `CLOSE_PATH_VERDICT`: `CASCADE`
- `VERDICT_REASON`: `FULLY_COVERED_ROOT`
- Qualifying feature member: `018-F` (root — no `parent_id`; fully covered at every
  depth — descendant graph ∪ `{018-F}` set-equal to the 13-member manifest, verified
  live by scanning `.backlogit/queue/` + `.backlogit/archive/` for any
  `parent_id` chain resolving to `018-F` or any of its 12 descendants outside the
  manifest: **zero found**).
- Linked-deliberation snapshot: `018-F` has no `custom_fields.source_deliberation_id`
  and no `\b(?:DL\d+|[0-9]+(?:\.[0-9]+)*-DL)\b` match in its frontmatter/description —
  linked-deliberation set is **EMPTY** for this manifest (escrow condition 8
  re-measured at invocation, matches PF-6).
- `CLASSIFICATION_BINDING`: `d8b8f9e50d78429c7d39d491640a97f2b80f38df6414a6b941dce8ed312cb145`
  (v1 binding computed over: shipment=017-S, verdict=CASCADE,
  reason=FULLY_COVERED_ROOT, skill sha256, engine=`backlogit version 1.10.1-...`,
  manifest=13 sorted IDs, deps=021-S, status=active, plus the 13-row pre-close
  declared-status snapshot below).

## Step 0(b) pre-close declared-status snapshot (captured before S-18 invocation)

| ID | artifact_type | declared status (frontmatter) | parent_id | location |
|---|---|---|---|---|
| 018-F | feature | done | — (root) | archive |
| 018.001-T | task | archived | 018-F | archive |
| 018.001.001-ST | subtask | archived | 018.001-T | archive |
| 018.001.002-ST | subtask | archived | 018.001-T | archive |
| 018.001.003-ST | subtask | archived | 018.001-T | archive |
| 018.002-T | task | archived | 018-F | archive |
| 018.002.001-ST | subtask | archived | 018.002-T | archive |
| 018.002.002-ST | subtask | archived | 018.002-T | archive |
| 018.002.003-ST | subtask | archived | 018.002-T | archive |
| 018.003-T | task | archived | 018-F | archive |
| 018.003.001-ST | subtask | archived | 018.003-T | archive |
| 018.003.002-ST | subtask | archived | 018.003-T | archive |
| 018.008-T | task | done | 018-F | archive |

Location label is descriptive only (11 members are `pre-archived` by location and
carry a genuinely-archived declared status — expected and tolerated on this path;
`018-F` and `018.008-T` reside in `archive/` while declaring `status: done`, which is
NOT truly archived per the declared-status rule).

## `backlogit_ship_shipment` result (step 1)

```json
{
  "shipment_id": "017-S",
  "shipment_status": "shipped",
  "archived_ids": ["018.008-T", "017-S", "018-F"],
  "returned_ids": [],
  "commit_sha": "5d69c727fc532eb61139f3e52cd3b64ab96574ac"
}
```

## Step 2 verification — `returned_ids` empty

`returned_ids: []` — **PASS**. No classifier/engine mismatch.

## Step 3 — two-set gate

- `allowed_ids` = 12 manifest task items + `018-F` (qualifying feature) + 0 linked
  deliberations + `017-S` (shipment record) = 14 IDs.
- `required_ids` = `017-S` (unconditional) + `018-F` (unconditional, qualifying
  feature) + `018.008-T` (not truly `archived` pre-close — declared `done`) = 3 IDs.
- `archived_ids - allowed_ids` = `{}` — **PASS** (no unexpected artifact archived).
- `required_ids - archived_ids` = `{}` — **PASS** (`archived_ids` = `{018.008-T,
  017-S, 018-F}` exactly equals `required_ids`; no required artifact left
  unarchived).
- The 11 other manifest task items were already truly `status: archived` pre-close
  and are correctly absent from `archived_ids` (tolerated, per the engine's
  `archiveItems` skip-if-already-archived behavior) — confirmed by the tracked-path
  diff below showing zero changes to any of the 11.

## Step 4 — `parent_id` preservation

Re-read every archived task's frontmatter post-close; confirmed unchanged against
the Step 0(b) snapshot above for all 13 members (spot-checked `018.001-T` →
`018-F`, `018.001.001-ST` → `018.001-T`, `018.003.002-ST` → `018.003-T`, `018.008-T`
→ `018-F`; `018-F` remains root/no `parent_id`). **PASS**.

## Cascade-scope assertion (feeds S-20.3)

`git diff --cached --name-status 24d3acf3eb8456db036da5dda8289184c1ee1ebb -- .backlogit/queue/ .backlogit/archive/`
(tracked-file diff against the S-15 `{pre_cascade_sha}` baseline):

```
R098  .backlogit/queue/017-S.md -> .backlogit/archive/017-S.md
M     .backlogit/archive/018-F.md
M     .backlogit/archive/018.008-T.md
```

Exactly the 3 `required_ids` members changed; the other 11 manifest members show
**zero** diff. `019.007-T` verified raw-byte identical to its `{pre_paths}` S-15
baseline (`git diff --exit-code` against `{pre_cascade_sha}` → exit 0). No
out-of-manifest artifact archived or deleted.

## Gate decision

`returned_ids` empty; `archived_ids - allowed_ids` empty; `required_ids -
archived_ids` empty; every `parent_id` preserved; cascade scope confined to the
manifest + shipment record; `019.007-T` untouched.

**`recommendation: CLOSED`.** Proceed to S-19/S-20 postchecks.
