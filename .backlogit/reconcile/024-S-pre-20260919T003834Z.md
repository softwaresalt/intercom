---
artifact_type: reconcile-report
shipment_id: 024-S
mode: pre
expected_status: done
timestamp: 2026-09-19T00:38:34Z
recommendation: PROCEED
---

# Shipment Reconcile — 024-S — pre-mode (expected_status: done)

## Concurrency note

Single-agent, single-branch session (post-merge closure, Ship agent). No
other agent or human operator is concurrently editing `.backlogit/`. Per
Ship's Concurrency Control section, file-lock acquisition is not required
for this invocation.

## Manifest

Loaded from `.backlogit/queue/024-S.md` `custom_fields.items`:

* `027-F` (artifact_type: feature)
* `027.001-T` (artifact_type: task)

## Per-item classification

| Item | Location found | Declared status | Expected | Classification |
|---|---|---|---|---|
| 027-F | `.backlogit/archive/027-F.md` | done | done | pre-archived |
| 027.001-T | `.backlogit/archive/027.001-T.md` | done | done | pre-archived |

Note: `027-F` was moved `active -> done` in this same session via the
Ship Step 6 covering-feature completion gate (a1) — all five conjunctive
conditions verified (sole manifest feature member, only descendant
`027.001-T` already `done`/archived, no live descendant at any depth,
descendant-graph union set-equal to manifest, containment holds, prior
live status was exactly `active`). Backlogit's `move --status done`
auto-relocated the feature file into `.backlogit/archive/` while
retaining `status: done` (not `status: archived`) — a "relocated-but-done"
record, not a truly archived one. `027.001-T` was already done/archived
from a prior Ship session (027.001-T-implementation-complete-awaiting-pr
checkpoint).

## Orphan scan

Scanned `.backlogit/queue/*.md` for `shipment_id: 024-S` in frontmatter —
zero matches. No orphans.

## Shipment-record-status classification

Shipment record `024-S` own `status`: `active` → `record-consistent`
(record `active`/`done` is always consistent per the classification table;
out of scope for the queued/blocked-only inconsistency checks).

## Gate decision

All manifest items are `matched` or `pre-archived`; zero orphans; shipment
record classification is `record-consistent`.

**recommendation: PROCEED**
