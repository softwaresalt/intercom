---
shipment_id: 024-S
mode: pre
expected_status: queued
timestamp: 2026-09-18T20:20:38Z
recommendation: PROCEED
---

# Pre-claim reconciliation report — 024-S

## Manifest items (from `custom_fields.items`)

| id | artifact_type | location | status | expected_status | classification |
|---|---|---|---|---|---|
| 027-F | feature | queue | queued | queued | matched |
| 027.001-T | task | queue | queued | queued | matched (labels include `harness-ready`) |

## Orphan scan

Scanned `.backlogit/queue/*.md` frontmatter for a `shipment_id: 024-S` declaration outside the
manifest. This backlogit installation does not populate a per-item `shipment_id` frontmatter
field (membership is tracked solely via the shipment record's `custom_fields.items`), so the
scan is vacuously zero-orphan. No orphan items found.

## Shipment-record-status classification

* Shipment record status (from `backlogit shipment get 024-S`): `queued`
* Manifest task statuses (artifact_type-filtered — 027-F excluded as a feature, not a task):
  * 027.001-T: `queued` (neither `active` nor `done`)
* Classification: **record-consistent** (record `queued`, no manifest task `active` or `done`)

## Gate decision

All manifest items are `matched`; zero orphans; shipment-record-status classification is
`record-consistent`.

**recommendation: PROCEED**
