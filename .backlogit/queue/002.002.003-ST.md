---
artifact_type: subtask
created_at: 2026-09-03T23:10:45.3644985Z
dependencies:
    - 002.002.002-ST
id: 002.002.003-ST
labels:
    - go
    - path-safety
    - security
parent_id: 002.002-T
priority: high
status: queued
title: Enforce containment in Root.Resolve
updated_at: 2026-09-03T23:11:50.6222522Z
---

Size: M | Complexity: medium (two independent axes; recorded as prose because this backend's 'subtask' template defines no structured size/complexity field).

Unit B3. Files: internal/pathsafe/pathsafe.go (extend), internal/pathsafe/contain_test.go. Add (Root).Resolve() and hasPathPrefix(). Join normalized components onto Root.path, then assert containment with an explicitly COMPONENT-AWARE comparison: exact equality, or prefix terminated by filepath.Separator. Raw strings.HasPrefix(path, root) is FORBIDDEN - it admits a sibling dir named <root>-evil (finding GO-6). Fold case ONLY when runtime.GOOS=='windows', since a config-supplied root and an EvalSymlinks-canonicalized candidate can differ in case (finding GO-7). Violations emit 'path outside workspace'. EMPTY-CANDIDATE GUARD (required): if normalize() yields ZERO components, Resolve MUST return 'path outside workspace' - without it filepath.Clean('') returns '.', the walk skips it, and filepath.Join(root,'') returns root, so the caller would be handed the workspace root as a writable target (finding CORR-2, empirically confirmed). Godoc states the postcondition: returned path is absolute, cleaned, and STRICTLY contained. DELIBERATE OMISSION: the oracle's absolute-path fast path is not ported since B2 rejects all absolute candidates; P10 must pass workspace-relative paths or revisit (finding SEC-3). ACCEPTANCE: (1) src/main.go and ./src/main.go resolve to an absolute path inside the root; (2) sibling dir <root>-evil rejected with 'path outside workspace'; (3) empty-string candidate rejected with 'path outside workspace'. Test-first.