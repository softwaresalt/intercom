---
artifact_type: task
created_at: 2026-09-19T02:31:43.9144889Z
custom_fields:
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 035.001-T
id: 035.002-T
parent_id: 035-F
priority: low
references:
    - docs/plans/2026-09-18-intercom-go-checker-test-docs-hygiene-plan.md
status: queued
title: Add function-level self-test for baseline resolution
updated_at: 2026-09-19T02:31:43.9144889Z
---

U1-T2. Add TWO self-test cases that exercise the baseline-resolution function root_gitignore_text_at DIRECTLY, rather than only through the end-to-end scenario harness (run_scenario at roughly lines 382-410), because the end-to-end path is short-circuited by the upstream SystemExit guard and therefore cannot reach the defect being fixed. A scenario-level test would be GREEN-ON-ARRIVAL and would certify nothing. The two required cases, per the governing plan (docs/plans/2026-09-18-intercom-go-checker-test-docs-hygiene-plan.md:83-97): case (a) a VALID ref whose .gitignore is ABSENT must still return an empty baseline (""), and case (b) an INVALID/nonexistent ref must raise a distinct error. Case (a) is NOT optional: without it the legitimate absent-file behaviour can be regressed into an error by U1-T1 without detection. Posture: test-first. Size: S | Complexity: medium. ACCEPTANCE CRITERIA: AC-1.3 both new cases drive root_gitignore_text_at DIRECTLY (not via run_scenario), and case (b) FAILS against the pre-change function and PASSES after; AC-1.2 case (a) returns an empty baseline ("") post-change -- the valid-ref/absent-.gitignore path stays a legitimate empty result and is NOT converted into an error; and all pre-existing self-test scenarios still pass unmodified.