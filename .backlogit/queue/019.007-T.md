---
artifact_type: task
created_at: 2026-09-11T17:23:20.2768887Z
custom_fields:
    origin_feature: 018-F
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 018.008-T
id: 019.007-T
labels:
    - policy
    - chore
    - governance
    - deferred
parent_id: 019-F
priority: high
status: queued
title: Emit Stage handback record with concrete artifact paths
updated_at: 2026-09-12T19:28:12.6104735Z
---

Parent feature 019-F (DEFERRED). Split from the original L-sized 018.009-T during the 2026-09-12 re-plan; carries ONLY items (4) and (5) of that task. Items (1)(2)(3) and (7a) moved to the reduced current release unit as 018.008-T; item (6) is now 019.008-T; item (7b) is now 019.009-T.

SCOPE - .github/agents/_stage.agent.md Step 6 (Summary) session-output contract:
(4) HANDBACK EMISSION: add one required output line emitting the STAGING HANDBACK RECORD the Orchestrator's Step 1.5 consumes: stage_branch, stage_base_commit, stage_head_commit, stage_outcome (shipment|no-shipment), stage_artifact_paths, and shipment_id when one was formed. Without it the Orchestrator's handback requirement has no producer and every pipeline run degrades. Emitting a branch name, a commit pair, and a file list transfers NO authority - it is not pushing, opening, or merging anything.
(5) PATH AND COMMIT SHAPE IN THAT SAME LINE: stage_artifact_paths is a NON-EMPTY list of CONCRETE repository-relative FILE paths - the files Stage committed BETWEEN stage_base_commit AND stage_head_commit - NEVER directory prefixes and NEVER only the tip commit's files. The line names the derivation Stage uses: git diff --name-only -z --diff-filter=d {stage_base_commit} {stage_head_commit} -- .backlogit/ docs/plans/ docs/decisions/ docs/memory/. Naming the producing command is what makes the consumer's set-equality check satisfiable BY CONSTRUCTION rather than by coincidence. Stage ECHOES stage_base_commit rather than inventing one (the Orchestrator's retained pre-invocation value is authoritative); the echo exists so a DIRECT Stage invocation, which no Orchestrator bracketed, still hands the operator a usable base.

DEPENDS ON 018.008-T: the handback names the Step 1.9 branch, which 018.008-T creates.

<!-- BEGIN:acceptance-criteria -->
- AC-1: _stage.agent.md Step 6 emits a required staging handback line carrying stage_branch, stage_base_commit, stage_head_commit, stage_outcome, stage_artifact_paths, and shipment_id when formed.
- AC-2: The contract states stage_artifact_paths is a NON-EMPTY set of concrete repository-relative FILE paths (no directory prefixes, no trailing slash) spanning base..head, not only the tip commit.
- AC-3: The contract names the exact derivation command, including --diff-filter=d and the four STAGE_ARTIFACT_ROOTS pathspecs.
- AC-4: The contract states Stage ECHOES stage_base_commit and that the Orchestrator's retained pre-invocation value is authoritative.
- AC-5: Emission is documented as authority-neutral - Stage does not push, open, or merge anything.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
SIZING: Size M (structured). Complexity medium (enum-validated prose; task type defines no complexity field in this workspace - see 018.008-T degradation note).
2-HOUR RULE: single file (.github/agents/_stage.agent.md), one step section, one output-contract line plus its shape rules. Single skill domain (contract text).
HARNESS: deferred - 019-F is NOT implementation-ready. Harness design for 019-F must first resolve the multi-task Step 4.3 deadlock (see 019-F goals). Do NOT assume the retired rev-11 manifest/witness mechanism.
TRACEABILITY: split from 018.009-T items (4)(5); adopted 018.009-T -> 019.007-T.
<!-- END:implementation-notes -->