---
artifact_type: task
created_at: 2026-09-13T01:09:29.210994Z
custom_fields:
    size: M
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
id: 022.001-T
parent_id: 022-F
priority: critical
status: queued
title: Delegate Ship close-path selection and complete covering feature
updated_at: 2026-09-13T08:11:37.6426279Z
---

Parent feature 022-F. SOLE CURRENT PREREQUISITE (A). Plan rev 3: docs/plans/2026-09-12-intercom-go-ship-feature-completion-foundation-plan.md. Governing decision: docs/decisions/2026-09-13-intercom-go-a-only-simplification-decision.md (SIMPLIFICATION_VALID). Superseded deliberation (historical): docs/decisions/2026-09-12-intercom-go-three-shipment-bootstrap-deliberation.md.

SUPERSEDES the rev-5 4-file scope. The rev-5 "ATOMICITY - ONE TASK, NOT THREE" and "sequential split FORBIDDEN (AC-17)" statements are WITHDRAWN: they were correct only for the TASK-ONLY close shape. A fully-covered-root manifest closes under the EXISTING P-015 CASCADE exception and imposes no one-live-member constraint, which dissolves T2 and re-enables splitting by surface.

EXACT FILE INVENTORY - 2 FILES: (1) .github/agents/_ship.agent.md; (2) tests/integration/ship_feature_completion_contract_test.go. New production code files: 0.

CLAUSE INVENTORY - 6 SITES (all MUST): C1 (line 756-764 reload: also reload P-015, verify merged tokens AND merge commit, add the authority-escalation carve-out with the plan section 4.2 PINNED wording); C2 (789-791: the "backlogit move <shipment_id> --status shipped" prescription is WRONG - the engine refuses it for shipments, exit 9, Probe 18); C3 (794-795); C4 (802-823: DELETE the re-derived classification prose); C5 (821-823); C6 (new generic delegation bullet). PLUS 2 additive: ADD-1 Role Boundary row (line 38), ADD-2 new Step 6.1(a1).

REV 3 - A-ONLY PARTITION. The rev-2 28-site partition A=C1-C6 (6) + B=A1-A6 (6) + C=B1-B10,B12-B17 (16) described a three-shipment chain that is SUPERSEDED for the current scope. A's OWN share is UNCHANGED at exactly 6 clause sites + 2 additive. The remaining 22 sites travel with the DEFERRED B and C plans and are NOT in current scope. A's surface, inventory, budget and closure sequence are byte-for-byte what rev 2 specified.

ROLE BOUNDARY GRANT (narrow, conjunctive, fail-closed): covering feature active -> done ONLY when all non-pre-archived executable descendants are done; no queued/active live descendant remains; parent/child topology intact; immediately before pre-mode; live status exactly active. Any condition unmet = HALT.

DELEGATION CONSTRAINT: the replacement text MUST NOT enumerate authorized verdicts. Ship invokes the close path NAMED BY the classification and no other. This is what lets future close-path work land without touching this file again.

TEST-FIRST: ONE table-driven Go test function, 13 ROWS (unchanged by rev 3). Marker "not implemented: ship feature-completion delegation contract", red 1 of 1, go vet 0, go test non-zero; then H1 green.

ACCEPTANCE: AC-1..AC-19 in plan section 11. PO-1a is DISCHARGED by Probe 22 (ARM AB measured the covering feature at ACTIVE after claim of a fully-covered-root manifest, and the active->done move accepted); PO-1b still runs at the gate. A covering feature that is neither active nor already done HALTs and returns to Stage - never widen the grant in-flight.

STEP 6.1(a1) APPLICABILITY IS THREE-WAY AND EXHAUSTIVE: (i) zero feature members in the manifest is an EXPLICIT NO-OP (record A1_NOT_APPLICABLE and proceed to a); (ii) a present feature member meeting all five conditions is moved active -> done; (iii) an unmet condition on a PRESENT feature member is a HALT. Cases (i) and (iii) must never be collapsed. REV 3 CORRECTION: 017-S is case (ii), NOT case (i) - its root 018-F has been restored to the manifest (13 members, fully-covered root), so the rev-2 claim that 017-S is the live zero-feature example is WITHDRAWN. Case (i) is RETAINED as defensive totality of the step and is still asserted by Go test row 12.

IDEMPOTENT RESUME: a feature member already done proceeds without error and without re-issuing the transition (A1_ALREADY_DONE), otherwise a partially-completed A becomes permanently unclosable. Conditions 1-4 are re-verified on every entry and no status other than active/done is ever accepted.

LIFECYCLE ORDER, read from the installed _ship.agent.md: Step 4.5 (L517) precedes Step 5 (L554) so 022.001-T reaches done BEFORE the implementation PR merges; Step 6.0 (L726) creates the post-merge closure branch from fresh main BEFORE any post-merge backlog mutation; 6.1 runs a0 -> a(pre) -> b(close) -> c(P-007) -> d(post) -> e(commit); operational-closure and P-020 land on the closure branch BEFORE the closure PR is pushed; sync follows.

PROBE 20 TRIPWIRE: the a0 --phase lifecycle gate runs WHILE 021-S IS STILL ACTIVE; no lifecycle invocation after the archive.

S2 RUNS THE FULL CHECKPOINT-RECOVERY PROTOCOL: enumerate with no status/agent filter, anomaly gate FIRST, explicit owner selection even for a single candidate, agent must be exactly ship, operator confirmation before restore, same cursor 021-S, resolve only AFTER a confirmed resume, fail closed with no fresh-start fallback.

ROLLBACK (rev 3): A-ONLY. The rev-2 mandatory C -> B -> A revert order is WITHDRAWN - B and C are deferred and land on nothing, so A may be reverted alone. ONE obligation remains: 017-S must be confirmed not active and explicitly HELD by Stage, and then re-shaped or re-planned, BEFORE any revert of A.

BUDGET ~78 min (<=2h, ~42 min margin), itemized in plan section 12. UNCHANGED by rev 3 - the A-only simplification touches no surface, no clause site and no test row. CONTINGENCY: exceeding 2h mid-execution = HALT and return to Stage. Never split, never rush contract prose.

SIZING: Size M (STRUCTURED field, persisted to custom_fields as size=M, size_source=agent, size_ruleset_version=stage-2h-rule-v1). Complexity high (enum-validated PROSE - DEGRADED). DEGRADATION RE-VERIFIED AT THIS REVISION: this workspace's .backlogit/header-def.yaml defines no complexity field on the task type; "backlogit update 022.001-T --complexity high" fails with 'artifact type "task" does not define a complexity field: backlogit: validation error' (exit 1), even though the registry advertises features.sizing: true and the CLI exposes --complexity. The WIT type definition, not the CLI surface, is the gate. See docs/compound/2026-09-04-backlogit-sizing-is-wit-gated.md. High complexity reflects contract-precision risk and the self-hosting session boundary, not volume - the volume is M and the budget is ~78 min.