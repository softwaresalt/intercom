---
artifact_type: task
created_at: 2026-09-13T01:09:29.210994Z
custom_fields:
    size: M
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
id: 022.001-T
parent_id: 022-F
priority: critical
status: queued
title: Delegate Ship close-path selection and complete covering feature
updated_at: 2026-09-13T08:11:37.6426279Z
---

Parent feature 022-F. SOLE CURRENT PREREQUISITE (A). Plan rev 3: docs/plans/2026-09-12-intercom-go-ship-feature-completion-foundation-plan.md. Governing decision: docs/decisions/2026-09-13-intercom-go-a-only-simplification-decision.md (SIMPLIFICATION_VALID). Superseded deliberation (historical): docs/decisions/2026-09-12-intercom-go-three-shipment-bootstrap-deliberation.md.

SUPERSEDES the rev-5 4-file scope. The rev-5 "ATOMICITY - ONE TASK, NOT THREE" and "sequential split FORBIDDEN (AC-17)" statements are WITHDRAWN: they were correct only for the TASK-ONLY close shape. A fully-covered-root manifest closes under the EXISTING P-015 CASCADE exception and imposes no one-live-member constraint, which dissolves T2 and re-enables splitting by surface.

EXACT FILE INVENTORY - 2 FILES: (1) .github/agents/_ship.agent.md; (2) tests/integration/ship_feature_completion_contract_test.go. New production code files: 0.

CLAUSE INVENTORY - 6 SITES (all MUST): C1 (line 756-764 reload: also reload P-015, verify merged tokens AND merge commit, add the authority-escalation carve-out with the plan section 4.2 PINNED wording); C2 (789-791: the "backlogit move <shipment_id> --status shipped" prescription is WRONG - the engine refuses it for shipments, exit 9, Probe 18); C3 (794-795); C4 (802-823: DELETE the re-derived classification prose); C5 (821-823); C6 (new generic delegation bullet). PLUS 2 additive: ADD-1 Role Boundary row (line 38), ADD-2 new Step 6.1(a1).

REV 3 - A-ONLY PARTITION. The rev-2 28-site partition A=C1-C6 (6) + B=A1-A6 (6) + C=B1-B10,B12-B17 (16) described a three-shipment chain that is SUPERSEDED for the current scope. A's OWN share is UNCHANGED at exactly 6 clause sites + 2 additive. The remaining 22 sites travel with the DEFERRED B and C plans and are NOT in current scope. A's surface, inventory, budget and closure sequence are byte-for-byte what rev 2 specified.

ROLE BOUNDARY GRANT (narrow, conjunctive, fail-closed): covering feature active -> done ONLY when all non-pre-archived executable descendants are done; no queued/active live descendant remains; parent/child topology intact; immediately before pre-mode; live status exactly active. Any condition unmet = HALT.

DELEGATION CONSTRAINT: the replacement text MUST NOT enumerate authorized verdicts. Ship invokes the close path NAMED BY the classification and no other. This is what lets future close-path work land without touching this file again.

TEST-FIRST: ONE table-driven Go test function, 13 ROWS (unchanged by rev 3). Marker "not implemented: ship feature-completion delegation contract", red 1 of 1, go vet 0, go test non-zero; then H1 green.

ACCEPTANCE: AC-1..AC-19 in plan section 11. PO-1a is DISCHARGED by Probe 22 (ARM AB measured the covering feature at ACTIVE after claim of a fully-covered-root manifest, and the active->done move accepted); PO-1b still runs at the gate. A covering feature that is neither active nor already done HALTs and returns to Stage - never widen the grant in-flight.

STEP 6.1(a1) APPLICABILITY IS THREE-WAY AND EXHAUSTIVE: (i) zero feature members in the manifest is an EXPLICIT NO-OP (record A1_NOT_APPLICABLE and proceed to a); (ii) a present feature member meeting all five conditions is moved active -> done; (iii) an unmet condition on a PRESENT feature member is a HALT. Cases (i) and (iii) must never be collapsed. REV 3 CORRECTION: 017-S is case (ii), NOT case (i) - its root 018-F has been restored to the manifest (13 members, fully-covered root), so the rev-2 claim that 017-S is the live zero-feature example is WITHDRAWN. Case (i) is RETAINED as defensive totality of the step and is still asserted by Go test row 12.

IDEMPOTENT RESUME: a feature member already done proceeds without error and without re-issuing the transition (A1_ALREADY_DONE), otherwise a partially-completed A becomes permanently unclosable. Conditions 1-4 are re-verified on every entry and no status other than active/done is ever accepted.

LIFECYCLE ORDER, read from the installed _ship.agent.md: Step 4.5 (L517) precedes Step 5 (L554) so 022.001-T reaches done BEFORE the implementation PR merges; Step 6.0 (L726) creates the post-merge closure branch from fresh main BEFORE any post-merge backlog mutation; 6.1 runs a0 -> a(pre) -> b(close) -> c(P-007) -> d(post) -> e(commit); operational-closure and P-020 land on the closure branch BEFORE the closure PR is pushed; sync follows.

PROBE 20 TRIPWIRE: the a0 --phase lifecycle gate runs WHILE 021-S IS STILL ACTIVE; no lifecycle invocation after the archive.

S2 RUNS THE FULL CHECKPOINT-RECOVERY PROTOCOL: enumerate with no status/agent filter, anomaly gate FIRST, explicit owner selection even for a single candidate, agent must be exactly ship, operator confirmation before restore, same cursor 021-S, resolve only AFTER a confirmed resume, fail closed with no fresh-start fallback.

ROLLBACK (rev 3): A-ONLY. The rev-2 mandatory C -> B -> A revert order is WITHDRAWN - B and C are deferred and land on nothing, so A may be reverted alone. ONE obligation remains: 017-S must be confirmed not active and explicitly HELD by Stage, and then re-shaped or re-planned, BEFORE any revert of A.

BUDGET ~78 min (<=2h, ~42 min margin), itemized in plan section 12. UNCHANGED by rev 3 - the A-only simplification touches no surface, no clause site and no test row. CONTINGENCY: exceeding 2h mid-execution = HALT and return to Stage. Never split, never rush contract prose.

SIZING: Size M (STRUCTURED field, persisted to custom_fields as size=M, size_source=agent, size_ruleset_version=stage-2h-rule-v1). Complexity high (enum-validated PROSE - DEGRADED). DEGRADATION RE-VERIFIED AT THIS REVISION: this workspace's .backlogit/header-def.yaml defines no complexity field on the task type; "backlogit update 022.001-T --complexity high" fails with 'artifact type "task" does not define a complexity field: backlogit: validation error' (exit 1), even though the registry advertises features.sizing: true and the CLI exposes --complexity. The WIT type definition, not the CLI surface, is the gate. See docs/compound/2026-09-04-backlogit-sizing-is-wit-gated.md. High complexity reflects contract-precision risk and the self-hosting session boundary, not volume - the volume is M and the budget is ~78 min.

=== REV 4 RECONCILIATION (2026-09-13) - THIS BLOCK SUPERSEDES THE REV-3 NUMBERS ABOVE ===

Plan is now REV 4: docs/plans/2026-09-12-intercom-go-ship-feature-completion-foundation-plan.md. Every rev-3 figure stated earlier in this description is WITHDRAWN and replaced as follows. Where this block and the text above disagree, THIS BLOCK WINS.

CLAUSE INVENTORY IS NOW 8 SITES (all MUST) + 2 additive, not 6 + 2. C1-C6 are unchanged. ADDED:
- C7 (_ship.agent.md L255-256, Step 0.5 item 6): the intake pre-mode summary says "verifies every manifest item is present in .backlogit/queue/ with the expected status" - QUEUE-ONLY and therefore false whenever a manifest member is archived.
- C8 (_ship.agent.md L776-777, Step 6.1(a) pre-archive gate): "verifies that every manifest item is present in queue with status: done" - the same defect. On 017-S's route 11 of 13 members live in .backlogit/archive/, so this sentence is literally false.
BOTH must be reconciled to the authoritative shipment-reconcile pre-mode semantics (plan section 4.4): an item is accepted when it is a QUEUE ITEM AT THE EXPECTED STATUS or an ARCHIVE-LOCATED pre-archived MEMBER ACCEPTED WITHOUT A STATUS CHECK. PROCEED requires every item matched or pre-archived, with no orphans and a record-consistent shipment record. The rev-3 instruction that L776-777 is "deliberately OUT OF A's INVENTORY" and that "a reviewer MUST NOT fail A for its absence" is WITHDRAWN. C7/C8 are both inside _ship.agent.md, so the 2-FILE SURFACE IS UNCHANGED.

STEP 6.1(a1) SELECTOR IS NOW FOUR-WAY, EXHAUSTIVE OVER MEMBER MULTIPLICITY (plan section 5.1). Compute n = count of manifest members whose artifact_type is feature - BY artifact_type, NEVER by ID suffix (subtask IDs end -ST, whose last char is also T). (i) n == 0 -> explicit no-op, record A1_NOT_APPLICABLE, proceed to 6.1(a); this is a SUCCESS. (ii) n == 1 and all five section-5 conditions hold -> active -> done on that member. (iii) n == 1 and any condition unmet -> HALT fail-closed. (iv) n > 1 -> HALT fail-closed with A1_MULTIPLE_FEATURE_MEMBERS, EVALUATE NO CONDITION AND MUTATE NO FEATURE, return to Stage. Both live manifests are n == 1 (021-S: 022-F; 017-S: 018-F), so case (iv) is defensive totality.

GO TEST IS NOW 16 ROWS, not 13, still ONE table-driven function with <=4 HELPERS (helper count unchanged; rows 14-16 add table entries, not helpers). New rows: 14 = Step 0.5 item 6's summary names the archive-located pre-archived disposition and is not queue-only; 15 = same for Step 6.1(a); 16 = a1 states the n > 1 fail-closed halt.

BUDGET IS NOW ~92 min with a ~28 min MARGIN, not ~78/~42. Delta +14: C7 +4, C8 +4, ADD-2 +3 (case iv), Go test +3 (rows 14-16). Still inside the 2-hour rule. CONTINGENCY UNCHANGED: exceeding 2h mid-execution = HALT and return to Stage.

ACCEPTANCE CRITERIA ARE NOW AC-1..AC-23, not AC-1..AC-19. AC-20 = the non-bypassable root-included 13-member cascade fixture gate; AC-21 = the exhaustive four-way a1 selector; AC-22 = C7/C8 reconciliation; AC-23 = the recalculated 8-site/~92-min/16-row envelope.

S2 CHECKPOINT RECOVERY IS THE FULL INSTALLED PROTOCOL - ELEVEN STEPS, NOT EIGHT. The rev-3 list above is INCOMPLETE and must not be used as the checklist. It omits three mandatory installed elements: (a) the restore -> prune/gate -> resume ORDERING, never restore -> resume -> prune; (b) the ENGRAM REACHABILITY GATE - agent-engram IS installed here, so the prune step is mandatory and NOT the backlogit-only no-op, and an unreachable engram substrate FAILS CLOSED to operator handoff with NO prune and NO resume (a file-based prune degradation is forbidden); (c) the NEVER-PRUNE ALLOWLIST - the active-shipment/active-task cursor (021-S), the unresolved-checkpoint pointer itself, and all recorded gate verdicts survive pruning intact. Also: a checkpoint left active by a fail-closed handoff is EXCLUDED from cleanup_checkpoints regardless of age. Authoritative sources: .github/instructions/backlogit.instructions.md "Checkpoint-Recovery / Prune-on-Restore Protocol" and _ship.agent.md's own Crash-Resumption / Startup Recovery Protocol. Execute the installed protocol as written; plan section 9.1 is a pointer and completeness checklist, not a substitute.

!!! NOT CLAIMABLE !!! Plan A's FORMAL plan-review gate ran as attempt 1 on rev 4 with all seven personas (anchor route gpt-5.6-sol) and returned decision: FAIL. The plan carries markers dispatch_mode: multi-agent / decision: FAIL in its "## Plan Review" section. Plan status remains "planned" and the plan is NOT harvest-ready. SHIP MUST NOT CLAIM 021-S until a re-review (attempt 2) returns PASS or ADVISORY-accepted. Open blockers include A-1/A-2 (the section 9.4 fixture gate placement and its P-010 Ship-authoring conflict) which require OPERATOR ADJUDICATION, plus A-3 and A-6..A-13. See the plan's "## Plan Review" and "## Plan Review - Remediation Cycle 1 disposition" sections for the full finding set.
=== REV 5 RECONCILIATION (2026-09-13) - THIS BLOCK SUPERSEDES THE REV-3 AND REV-4 BLOCKS ABOVE ===

Plan is now REV 5. Where this block and any text above disagree, THIS BLOCK WINS. Scope is UNCHANGED at 2 FILES (.github/agents/_ship.agent.md + tests/integration/ship_feature_completion_contract_test.go). CLAUSE INVENTORY UNCHANGED at 8 SITES + 2 additive.

GO TEST 16 -> 18 ROWS (one function, <=4 helpers, unchanged; stdlib assertions only, reuse package-level repoRoot from build_script_test.go). Rows 7/10/14/15 are COMPOUND (assert a pinned PRESENT phrase AND a verbatim-pinned ABSENT old literal, plan section 8.1.1). Rows 17-18 are PRESERVATION guards (C8 keeps the file-lock and orphan-scan clauses; C7 keeps the adjacent Scope note 139-F/139.001-T).

BUDGET ~92 -> ~99 min, margin ~28 -> ~21 min. ACCEPTANCE AC-1..AC-23 -> AC-1..AC-26 (adds AC-20a Ship read-only re-verification, AC-24 pre-archived exemption, AC-25 delegation trust boundary, AC-26 preservation rows).

STEP 6.1(a1) SELECTOR IS NO LONGER FOUR-WAY. It is a STRICTLY ORDERED, NON-OVERLAPPING SEQUENCE S0->S5 (plan section 5.1): S0 anomaly gate BEFORE n is computed; S1 n==0 explicit no-op; S2 n>1 HALT with no condition evaluated and no mutation; S3 n==1 and status done => idempotent no-op ONLY after conditions 1-4 revalidate, else HALT; S4 n==1 and status active => full five-condition guard then active->done; S5 n==1 any other status => HALT. No input may satisfy two steps. Legacy case (i)/(ii)/(iii)/(iv) labels map onto S1/S4/S3-S4-S5/S2.

AC-20 IS RE-SEATED. The rev-4 "non-bypassable S2-run root-included cascade fixture gate" is WITHDRAWN - it was the attempt-1 P0 (a no-override gate placed AFTER the irreversible merge) and the attempt-1 A-2 P-010 violation (it assigned plan-evidence authoring to Ship). REPLACED BY THE HYBRID: STAGE authors, executes and COMMITS Probe 25 PRE-CLAIM (plan section 9 step 0a, section 9.4); SHIP S2 performs a READ-ONLY re-verification only (section 9.4a, five checks: exact topology, PASS fields, evidence commit ancestry of the merge SHA, currently-resolved engine digest, no stale/missing record). Ship NEVER authors, modifies, re-runs or regenerates this evidence.

PROBE 25 IS DISCHARGED: docs/plans/evidence/2026-09-12-task-only-shipment-finalization/probe25-root-included-cascade-fixture.{ps1,txt}, PROBE25_RESULT=PASS, FAILED_CRITERIA=0, all eight criteria PASS, engine resolved via registered command (Get-Command) with SHA-256 compared to the committed digest - NO hardcoded path is a runtime requirement.

!!! NOT CLAIMABLE - PLAN-REVIEW ATTEMPT 2 RETURNED decision: FAIL !!! The formal plan-review gate ran as attempt 2 on rev 5 with all seven personas (anchor route gpt-5.6-sol/high; dispatch_mode: multi-agent). It closed EVERY attempt-1 P0/P1 but found 1 NEW P0 and 7 NEW P1s in rev 5's own constructs. DECISIVE P0 (B-1): section 5.1's new S0 anomaly gate HALTs on A's OWN manifest, because 022.001-T is moved to done at section 9 step 4 and the live registry routes done to archive/, so at the a1 gate it is archive-located and not on the section 5.0 eleven-ID disposition list => S0 HALTs before n is computed and 021-S can never self-close. Two personas converged on it and the committed Probe-25 transcript proves it (MEMBERS_QUEUE_LOCATED=0). FIX: re-base every location-derived check on DECLARED status, never file location. Plan status stays planned; the plan is NOT harvest-ready; 021-S must NOT be claimed; 017-S behind it stays ineligible. Remediation cycle 3 is the LAST before the Escalation Protocol applies.


=== REV 6 RECONCILIATION (2026-09-13) - THIS BLOCK SUPERSEDES THE REV-3, REV-4 AND REV-5 BLOCKS ABOVE ===

Plan is now REV 6. Where this block and ANY text above disagree, THIS BLOCK WINS. Scope UNCHANGED at 2 IMPLEMENTATION FILES (.github/agents/_ship.agent.md + tests/integration/ship_feature_completion_contract_test.go). CLAUSE INVENTORY UNCHANGED at 8 SITES + 2 additive. Planning artifacts (plan, probe25 evidence, docs/memory, docs/decisions, .backlogit records) are Stage-authored in separate commits and are NOT counted against that 2-file surface (AC-11).

DECLARED STATUS IS THE SOLE LIFECYCLE AUTHORITY - this REPLACES every location-derived rule stated above. Per the installed shipment-reconcile SKILL.md L394-399, declared status is read from the record's own frontmatter and is "never inferred from, nor substituted by, which of queue//archive/ currently holds the record". CONSEQUENCES: (1) a descendant DECLARING status: done is a COMPLETED descendant that satisfies section 5 condition 1 even when registry routing stores it under archive/; (2) PRE-ARCHIVED means DECLARED status: archived and nothing else - only such a record is subject to the section 5.0 exact 11-ID allowlist and its provenance/immutability checks. Physical-location reads survive ONLY as an exactly-one-copy containment proof (queue XOR archive). 022.001-T, done-in-archive at the a1 gate, PASSES condition 1 and raises NO S0 trigger.

THE REV-4 LOCATIONAL C7/C8 RECONCILIATION WORDING STATED ABOVE IS WITHDRAWN. C7/C8 must be reconciled in DECLARED-STATUS terms: an item either declares the expected status OR declares status: archived (pre-archived, accepted without a status check), read from the record's own frontmatter and never inferred from queue//archive/ location, preserving the PROCEED conjuncts (no orphans, record-consistent) and C8's single-writer lock.

SHIPMENT STATUS VOCABULARY CORRECTION: any statement above describing shipments B/C (022-S/023-S) as "blocked" is WITHDRAWN - blocked is NOT a valid shipment status (queued|active|shipped|abandoned). Their actual state is ARCHIVED with archived_status: queued.

VERDICT CONTRACT: _ship.agent.md MAY name the installed result tokens (CASCADE, SAFE_CLOSE, HALT) solely as an OUTPUT-VALIDATION ALLOWLIST, and MUST NOT restate any classification PREDICATE. Unknown/absent/ambiguous token => HALT; classifier not installed or not readable => HALT. a1 RUNS BEFORE pre-mode and MUST NOT require it. For 021-S and 017-S the authoritative classification is EXPECTED to return CASCADE; any other verdict is a topology discrepancy that HALTs to Stage - there is NO SAFE_CLOSE fallback on these routes.

EVIDENCE IDENTITY FIELDS (Hardening 9a): evidence_commit_sha (the LAST commit touching either evidence file), fixture_script_sha256, fixture_result_sha256, backlogit_executable_sha256. The rev-4/rev-5 "no override path" language is DELETED. Section 9.4a is now SIX read-only checks, not five.

S2 CHECKPOINT RECOVERY IS THIRTEEN STEPS (0, 1, 2, 2a, 3-11): enumerate ALL with no filter -> anomaly gate over the FULL enumeration FIRST -> THEN filter to the MATCH set (exact filename + agent: ship + session_id) -> require EXACTLY ONE MATCH. Unrelated active checkpoints are a WARNING and never strand the handoff. Order is restore -> prune/gate -> resume; resolve only after a confirmed successful resume.

BUDGET ~99 (claimed) -> 97 min ACTUAL, margin 23 min. The rev-5 table never summed to its stated total (rows added to 97). Rows now sum exactly: 8+18+4+4+12+4+4+4+29+10 = 97. ACCEPTANCE AC-1..AC-26 -> AC-1..AC-29 (adds AC-27 no-location-derived-lifecycle, AC-28 no-SAFE_CLOSE-fallback verdict contract, AC-29 S2 MATCH-set ordering). GO TEST STAYS AT 18 ROWS: compound +/- rows are now {9,10,14,15} = 4 (row 7 became ABSENT-only, row 9 joined), compound ++ = {16}, negative = {7,8}.

!!! NOT CLAIMABLE - PLAN-REVIEW ATTEMPT 3 RETURNED decision: FAIL !!! The formal plan-review gate ran as attempt 3 on rev 6 with all seven personas (anchor route gpt-5.6-sol/high; dispatch_mode: multi-agent). It CLOSED the attempt-2 P0 (B-1) unanimously, plus B-2, B-5, B-8 and the same-surface P2/P3 queue; Security Lens returned 0/0/0/0 for the second consecutive attempt and the Go Reviewer returned a clean mechanical sheet. It nonetheless returned decision: FAIL on 0 P0 and 10 P1 (E-1..E-10): an incomplete declared-status sweep (section 5.0 condition 2; section 4's inventory table), and internal contradictions introduced by rev 6's own B-3/B-6 corrections (Hardening 2 vs AC-25; section 4.1 vs AC-28; AC-25's P-015/skill disagreement check; section 9.4b's omitted parent_id restore row; section 9.4a's option 1 vs option 3). MAX RE-ENTRY CYCLES ARE EXHAUSTED - the Escalation Protocol (P-013.6) applies and Stage does NOT self-remediate these findings. Plan status stays planned; the plan is NOT harvest-ready; 021-S must NOT be claimed; NO shipment is claimed; 017-S behind it stays ineligible.