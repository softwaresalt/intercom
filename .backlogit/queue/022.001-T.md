---
artifact_type: task
created_at: 2026-09-13T01:09:29.210994Z
custom_fields:
    size: M
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
id: 022.001-T
parent_id: 022-F
priority: critical
status: queued
title: Delegate Ship close-path selection and add feature completion
updated_at: 2026-09-13T05:20:32.7190024Z
---

Parent feature 022-F. BOOTSTRAP SHIPMENT A (FOUNDATION). Plan: docs/plans/2026-09-12-intercom-go-ship-feature-completion-foundation-plan.md. Deliberation: docs/decisions/2026-09-12-intercom-go-three-shipment-bootstrap-deliberation.md.
SUPERSEDES the rev-5 4-file scope. The rev-5 "ATOMICITY - ONE TASK, NOT THREE" and "sequential split FORBIDDEN (AC-17)" statements are WITHDRAWN: they were correct only for the TASK-ONLY close shape. A fully-covered-root manifest closes under the EXISTING P-015 CASCADE exception and imposes no one-live-member constraint, which dissolves T2 and re-enables splitting by surface.
EXACT FILE INVENTORY - 2 FILES: (1) .github/agents/_ship.agent.md; (2) tests/integration/ship_feature_completion_contract_test.go. New production code files: 0.
CLAUSE INVENTORY - 6 SITES (all MUST), A's exact share of the closed 28-site inventory: C1 (line 756-764 reload: also reload P-015, verify merged tokens AND merge commit, add the authority-escalation carve-out with the plan section 4.2 PINNED wording); C2 (789-791: the "backlogit move <shipment_id> --status shipped" prescription is WRONG - the engine refuses it for shipments, exit 9, Probe 18); C3 (794-795); C4 (802-823: DELETE the re-derived classification prose); C5 (821-823); C6 (new generic delegation bullet). PLUS 2 additive: A-1 Role Boundary row (line 38), A-2 new Step 6.1(a1).
PARTITION: A=C1-C6 (6) + B=A1-A6 (6) + C=B1-B10,B12-B17 (16) = 28 EXACTLY. No duplicated obligation, no missing site.
ROLE BOUNDARY GRANT (narrow, conjunctive, fail-closed): covering feature active -> done ONLY when all non-pre-archived executable descendants are done; no queued/active live descendant remains; parent/child topology intact; immediately before pre-mode; live status exactly active. Any condition unmet = HALT.
DELEGATION CONSTRAINT: the replacement text MUST NOT enumerate authorized verdicts. Ship invokes the close path NAMED BY the classification and no other. This is what lets B and C land without touching this file again.
TEST-FIRST: ONE table-driven Go test function, 13 ROWS (rev 2: 4 negative widening guards + 1 coherence guard + ordering rows + NEW rows 12-13 for a1 applicability and idempotent resume). Marker "not implemented: ship feature-completion delegation contract", red 1 of 1, go vet 0, go test non-zero; then H1 green.
ACCEPTANCE: AC-1..AC-19 in plan section 11 (rev 2). PO-1a is DISCHARGED by Probe 22 (ARM AB measured the covering feature at ACTIVE after claim of a fully-covered-root manifest, and the active->done move accepted); PO-1b still runs at the gate. A covering feature that is neither active nor already done HALTs and returns to Stage - never widen the grant in-flight.
REV 2 ADDITIONS. (a) ADDITIVE LABEL RENAME: rev 1's A-1/A-2 are now ADD-1/ADD-2, to stop them colliding with plan B's clause-site labels A1-A8 in joint review. Traceability: A-1 = ADD-1, A-2 = ADD-2; content unchanged. (b) STEP 6.1(a1) APPLICABILITY IS THREE-WAY AND EXHAUSTIVE: zero feature members in the manifest is an EXPLICIT NO-OP (record A1_NOT_APPLICABLE and proceed to a) - this is the case 017-S hits; an unmet condition on a PRESENT feature member is a HALT; the two must never be collapsed. (c) IDEMPOTENT RESUME: a feature member already done proceeds without error and without re-issuing the transition (A1_ALREADY_DONE), otherwise a partially-completed A becomes permanently unclosable. Conditions 1-4 are re-verified on every entry and no status other than active/done is ever accepted. (d) LIFECYCLE ORDER, read from the installed _ship.agent.md: Step 4.5 (L517) precedes Step 5 (L554) so 022.001-T reaches done BEFORE the implementation PR merges; Step 6.0 (L726) creates the post-merge closure branch from fresh main BEFORE any post-merge backlog mutation; 6.1 runs a0 -> a(pre) -> b(close) -> c(P-007) -> d(post) -> e(commit); operational-closure and P-020 land on the closure branch BEFORE the closure PR is pushed; sync follows. (e) PROBE 20 TRIPWIRE: the a0 --phase lifecycle gate runs WHILE 021-S IS STILL ACTIVE; no lifecycle invocation after the archive. (f) S2 RUNS THE FULL CHECKPOINT-RECOVERY PROTOCOL: enumerate with no status/agent filter, anomaly gate FIRST, explicit owner selection even for a single candidate, agent must be exactly ship, operator confirmation before restore, same cursor 021-S, resolve only AFTER a confirmed resume, fail closed with no fresh-start fallback.
BUDGET ~71 min (<=2h, ~49 min margin), itemized in plan section 12 at the umbrella rev-6 rates. CONTINGENCY: exceeding 2h mid-execution = HALT and return to Stage. Never split, never rush contract prose.
SIZING: Size M (STRUCTURED field, persisted to custom_fields). Complexity high (enum-validated PROSE - DEGRADED: this workspace's .backlogit/header-def.yaml defines no complexity field on the task type; 'backlogit update --complexity high' fails with 'artifact type task does not define a complexity field'. See docs/compound/2026-09-04-backlogit-sizing-is-wit-gated.md). High complexity reflects contract-precision risk and the self-hosting session boundary, not volume.