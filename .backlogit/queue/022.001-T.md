---
artifact_type: task
created_at: 2026-09-13T01:09:29.210994Z
custom_fields:
    size: M
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
id: 022.001-T
parent_id: 022-F
priority: critical
status: queued
title: Delegate Ship close-path selection and complete covering feature
updated_at: 2026-09-16T19:26:57.1266935Z
---


Parent feature 022-F.

GOVERNING PLAN: docs/plans/2026-09-13-intercom-go-ship-feature-completion-decided-plan.md, revision 13. That plan is the single source of truth. Where this record and the plan differ, the plan governs. GOVERNING DECISION: docs/decisions/2026-09-13-intercom-go-a-only-simplification-decision.md (SIMPLIFICATION_VALID).

EXACT FILE INVENTORY - 4 FILES (OPTION A, operator-selected 2026-09-13):
1. .github/agents/_ship.agent.md
2. .github/policies/workflow-policies.md - single-bullet P-010 correction only
3. .github/skills/shipment-reconcile/SKILL.md - four sites (CS11-CS14); CS11-CS12 and CS14 additive, CS13 additive plus the operator-adopted D-1 narrowing
4. tests/integration/ship_feature_completion_contract_test.go - new
New production code files: 0. New module dependencies: 0.

CLAUSE INVENTORY - 16 ANCHOR-ADDRESSED SITES plus 2 additive. The plan's clause inventory section is authoritative for the anchor text, the pinned replacement wording and the mapping to test rows. Sites are addressed by unique anchor string, not by line ordinal; ordinals in the plan are an explicitly non-normative dated observation. ELEVEN sites are in the agent file (CS1-CS9, CS15 and CS16), one is the P-010 grant bullet in the policy file (CS10), and four (CS11-CS14) are in the installed shipment-reconcile skill. The two additive items are the Role Boundary grant and the new parent-completion step. EVERY site has pinned replacement wording in the plan - at revision 11 six of them did not, which is why several test needles were unsatisfiable.

TEST: one table-driven Go test, THIRTY-THREE rows, stdlib only, asserting over the three instruction-surface files. EVERY clause site has at least one pinned needle THAT RESOLVES INSIDE ITS OWN BLOCK - no site is credited by a needle belonging to another site. Both polarities are asserted: positive rows require the corrected wording to be present, negative rows require the withdrawn wording and any release-instance identifier to be absent. Every PRESENT needle is a VERBATIM SUBSTRING of the plan's pinned replacement wording for its own site, recorded in the plan's provenance column, so every transition row is satisfiable by applying the pinned wording alone.

ORDERING - TEST-FIRST, NON-NEGOTIABLE: the test lands and is RED before any of the three instruction-surface files is edited, then GREEN after. The red phase must be confirmed by harness-architect before this task is claimed. Within the implementation, the fourth file (CS11-CS14, the producer) lands BEFORE the clauses that consume it (CS3-CS6 and CS16), so the agent file never names a mode the installed skill does not expose. The narrowed sink (CS13) and its migrated caller (CS16, CS5) land in the SAME change and the same PR, so no merged state ever exposes a refusing sink to an unmigrated caller.

EXECUTION SPLIT - S1a / S1b / S2. This task is executed in three bounded stretches, each inside the 2-hour rule: S1a (~73 min) writes the THIRTY-THREE-row test and applies CS11-CS14 including the D-1 refusal contract, ending with H0 red on every agent-file and policy-file row; S1b (~80 min) applies ADD-1, ADD-2, CS1-CS10, CS15 and CS16 in the plan's dependency order to reach H1 green; S2 (~50 min) performs verification and closure in a fresh session. The S1a/S1b boundary is verifiable from go test output. THIS IS AN EXECUTION SPLIT, NOT A TASK SPLIT: no second backlog task is created, because any second task under 022-F would become a depth-1 live descendant, force a third member into the 021-S manifest, live-mismatch PA-021-CASCADE condition 3 and void that operator approval as a re-scoped action.

ROLE BOUNDARY GRANT (narrow, conjunctive, fail-closed): covering feature active to done ONLY when every non-pre-archived executable descendant across the FULL live parent graph at EVERY DEPTH is done; no queued or active live descendant remains at any depth; that descendant set unioned with the feature is set-equal to the shipment manifest; parent and child topology is intact; the transition occurs immediately before pre-mode; and the feature's live status is exactly active. The grant is additionally SCOPED to the manifest of the shipment this session has claimed and whose live status is exactly active. ANY condition unmet is a HALT with no mutation. The identical five conditions are rendered INLINE in the P-010 policy entry so the policy is self-contained.

DELEGATION (OPTION A): Ship's locally re-derived classification prose is DELETED and replaced with a pointer to the authoritative installed machinery. The verdict is obtained from a NEW read-only public mode of the installed skill, mode classify-close-path, which runs the existing Step 0 classification and STOPS before the mutating dispatch, returning CLOSE_PATH_VERDICT (CASCADE, SAFE_CLOSE or BLOCK) with VERDICT_REASON, VERDICT_EVIDENCE and a CLASSIFICATION_BINDING over the canonical manifest and dependency snapshot. ALL FOUR consumer sites CS3-CS6 name that boundary; Ship calls it BEFORE any close call, halts on any non-CASCADE result without mutating anything, carries the binding into the close call, and MUST NOT invoke mode safe-close without one. mode safe-close then revalidates the supplied binding against a freshly recomputed snapshot and refuses on drift, which closes the TOCTOU window; the binding serialization is fully specified in the plan so two implementations cannot disagree, and the residual recompute-to-mutate window is disclosed rather than denied. Three of the four skill sites are ADDITIVE. The single non-additive change is D-1, ADOPTED BY THE OPERATOR 2026-09-14: mode safe-close invoked without a valid binding REFUSES with RECONCILE_FAIL_CASCADE_UNBOUND (missing), _CLASSIFICATION_INVALID (malformed), _CLASSIFICATION_DRIFT (stale) or _CLASSIFICATION_REFUSED (bound BLOCK), all evaluated BEFORE the Step 0 dispatch and therefore before any mutation; a matching binding branches on its verdict, CASCADE to the cascade sub-procedure and SAFE_CLOSE to the unchanged steps 1-10. The legacy unbound fall-through is REMOVED, not preserved; there is no deprecation window, no grace period and no success-shaped fallback. All four public mode names, inputs, internal steps and the protected set are unchanged. No Ship runtime code is added.

INTAKE ORDERING (CS15): the installed Step 0.5 currently runs the intake reconciliation check at item 6, AFTER the shipment claim at item 4. CS15 relocates the intake bullet above the claim bullet, and a test row asserts the resulting index ordering. Plan prose alone could not establish this.

SIZE M, COMPLEXITY high. Complexity is recorded as prose because this workspace's header definition exposes no complexity field; do not attempt to set it as a structured field.

READINESS: STAGE-READY UNDER OPERATOR RESIDUAL ACCEPTANCE - NOT a review gate PASS, and NOT YET CLAIMABLE. Last gate that RAN is true-lineage ATTEMPT 8 over plan revision 13, run 2026-09-14; result FAIL (P0 = 0, P1 = 26 raw / 14 deduplicated). See docs/reviews/2026-09-13-true-lineage-attempt-8-verdict.md. That FAIL STANDS and is NOT relabelled. On 2026-09-14 the operator FROZE revision 13 and ACCEPTED the remaining documentation and gating-evidence findings as NON-BLOCKING RESIDUALS (plan section 13.7). Attempt 9 is NOT authorized and was NOT run. N-1..N-14 remain OPEN and NONE is asserted fixed. Claimable only after PR #54 merges to main and the staging artifacts are verified on origin/main. Status queued.

BINDING ACCEPTANCE CRITERIA - plan section 12.1 IV-1..IV-5, FROZEN, and GOVERNING over any conflicting AC-, clause site or pinned wording in the plan: (IV-1) classify the close path before any mutation; (IV-2) bind the classification to the manifest and dependency snapshot, and reject drift; (IV-3) require the binding on every mutating close path, including the direct backlogit_ship_shipment engine invocation, or replace that invocation with the guarded path; (IV-4) fail closed on missing, invalid, ambiguous, mixed, stale or non-matching classifications; (IV-5) the TDD harness proves refusal paths perform zero mutation and valid CASCADE/SAFE_CLOSE paths preserve intended behaviour. ANY FAILING IMPLEMENTATION INVARIANT IS BLOCKING REGARDLESS OF THE ACCEPTED PLAN-EVIDENCE RESIDUALS. Residuals adjudicated (plan section 13.7): 8 implementation-guard criteria, 7 accepted non-blocking evidence mismatches, ZERO unresolved blockers.

TEST-FIRST IS NON-NEGOTIABLE AND SURVIVES THE RESIDUAL ACCEPTANCE. The harness encoding IV-1..IV-5 lands and is RED before any of the three instruction-surface files is edited, and is GREEN before closure. A green-on-arrival harness is itself a failure of IV-5. The harness asserts BEHAVIOUR, and MUST NOT be derived from the plan's soft-wrapped section 4.2 literals (residual N-6). Each assertion must resolve inside its own subject, with no assertion credited by another's output (residual N-5). Row counts and clause-site counts are evidence residuals, not the acceptance surface; IV-1..IV-5 are.

AUDIT: superseded scope, clause counts, plan identities and plan-review history live in Git history and PR #54, not in this record. See plan section 13.2. Root-cause analysis of the seven-attempt loop: docs/compound/workflow-issues/cross-artifact-contract-closure-requires-every-surface-2026-09-13.md.

DEFERRED-ITEM DELIBERATION DECISIONS - STAGE 2026-09-16, POST-MERGE. PR #54 MERGED to main 2026-09-16 at merge commit f7112e0693804ac75cd4511d2ee3eaeac15583e4; all staging artifacts are VERIFIED ON origin/main. The claim gate condition "claimable only after PR #54 merges to main and the staging artifacts are verified on origin/main" is SATISFIED. Two high-priority deferred-scope-expansion stash entries captured from PR #54 Copilot review under P-021 C2 were deliberated at claim time under P-021 C6 and are now CLOSED; both are recorded as first-class deliberation artifacts linked to their originating stash entries (stash E47FFCE7 and stash CE28BFA3). The deliberation artifact IDs are recorded in the 021-S shipment record rather than here, DELIBERATELY: plan section 13.6 probe T-11 scans THIS FILE and 022-F for the engine's linked-deliberation matcher, and writing those IDs here would perturb a probe that PA-021-CASCADE condition (6) depends on. Neither decision creates a new backlog task, a new manifest member, a plan revision 14, or a new review history entry. Attempt 8 FAIL STANDS and is NOT relabelled. Revision 13 remains FROZEN.

DECISION D-2 (stash E47FFCE7) - IV-5 IS DISCHARGED ONLY BY FIXTURE-BACKED BEHAVIORAL TESTS. The RED harness MUST materialise a disposable fixture backlog workspace, INVOKE the real classify-close-path, safe-close and cascade flows against it, and assert over a captured pre-state/post-state comparison of the whole fixture tree. Static substring/text-needle assertions over the three instruction-surface files MAY remain, but ONLY as SUPPLEMENTAL prompt-contract coverage evidence reported separately; they CANNOT satisfy IV-5 and MUST NOT be credited toward IV-1 through IV-5. This supersedes the TEST paragraph above as the ACCEPTANCE SURFACE; the THIRTY-THREE rows, the clause-site counts and the S1a/S1b/S2 timings remain EVIDENCE RESIDUALS and are not recounted. Required behavioral rows, all RED before any instruction-surface edit: B1 for EACH refusal token RECONCILE_FAIL_CASCADE_UNBOUND, RECONCILE_FAIL_CLASSIFICATION_INVALID, RECONCILE_FAIL_CLASSIFICATION_DRIFT and RECONCILE_FAIL_CLASSIFICATION_REFUSED, assert the token is returned AND the post-state fixture tree is byte-identical to the pre-state snapshot; B2 bound CASCADE over a fully-covered-root fixture archives exactly the manifest subtree and nothing outside it; B3 bound SAFE_CLOSE over a genuine partial-feature fixture performs exactly the intended single-artifact delta and remains REACHABLE (P-015 R5, probe T-15); B4 no mutation is observable on any path until after a verdict is produced (IV-1, depends on D-3 below); B5 mutating the fixture manifest or dependency set between classify and close yields a DRIFT refusal with zero mutation (IV-2); B6 every mutating close path is guarded, INCLUDING the direct backlogit_ship_shipment engine invocation, or that invocation is replaced by the guarded path (IV-3). HARNESS HYGIENE: needles MUST NOT be derived from the plan's soft-wrapped section 4.2 literals (residual N-6); every assertion MUST resolve inside its own subject with no assertion credited by another's output (residual N-5); a green-on-arrival harness is itself a failure of IV-5.

DECISION D-3 (stash CE28BFA3) - CLASSIFY-CLOSE-PATH IS TRULY READ-ONLY. mode classify-close-path MUST NOT create, write, append to, rename or delete ANY file, and MUST NOT mutate any backlog record, status, lock or index entry, at ANY point BEFORE it returns its verdict. This EXPLICITLY includes .backlogit/reconcile/{shipment_id}-classify-close-path-{timestamp}.md. CLOSE_PATH_VERDICT, VERDICT_REASON, VERDICT_EVIDENCE, CLASSIFICATION_BINDING and CLASSIFIED_AT are returned as MACHINE-CONSUMABLE OUTPUT of the invocation result; the returned value, not any file, is the authoritative carrier, and the CLASSIFICATION_BINDING that mode safe-close revalidates is taken from the returned value. Persistence of a classification report is OPTIONAL, is NEVER a precondition of a valid verdict, occurs only AFTER the verdict is returned and OUTSIDE the zero-mutation/refusal assertion window, and is DISABLED ENTIRELY under read-only invocation; a persistence failure MUST NOT change, invalidate or block the verdict. No file-lock is acquired, consistent with the existing detect-mixed-role read-only precedent. Under plan section 12.1, IV-1 and IV-5 GOVERN, so plan section 3.4.3 (B)'s clause "writes only its own additive report, exactly as mode detect-mixed-role already does" and section 3.4.3 (C)'s clause "and records at .backlogit/reconcile/..." are SUPERSEDED FOR THE PRE-VERDICT WINDOW with NO edit to the frozen plan and NO revision 14. Everything else in section 3.4.3 is UNCHANGED: the (B1) condition-to-verdict mapping table, the (D) binding serialisation, the token set, and (B)'s no-archive / no-status-transition / no-record-mutation / no-file-lock guarantees, which this decision REINFORCES rather than relaxes. Required rows: Z1 snapshot the ENTIRE fixture tree including .backlogit/ and .backlogit/reconcile/, invoke classify-close-path for CASCADE, SAFE_CLOSE and EVERY BLOCK reason (MANIFEST_MEMBER_NOT_DESCENDANT, SNAPSHOT_AMBIGUOUS, SNAPSHOT_MISSING, ENUMERATION_INCOMPLETE, MIXED_QUALIFICATION, CLASSIFIER_ERROR), re-snapshot, and assert the path set and content hashes are IDENTICAL; Z2 assert all five fields are present in the invocation result and that safe-close revalidates a binding taken from that returned value with NO report file present on disk; Z3 assert that an absent reconcile directory changes no verdict and raises no error; Z4 if optional persistence is implemented at all, assert it is strictly post-verdict, suppressed under read-only invocation, and that a forced persistence failure leaves the verdict unchanged and non-blocking; Z5 the refusal-path zero-mutation assertion is a WHOLE-TREE equality assertion with NO exemption list and NO path-shaped carve-out. The simplest conforming implementation OMITS optional persistence entirely and satisfies Z1 through Z5 trivially.

COMPATIBILITY WITH IV-1 THROUGH IV-5 AND OPTION A - CONFIRMED. D-2 strengthens IV-5 by replacing an assertion surface that could not observe mutation with one that can. D-3 strengthens IV-1 by removing the only known pre-verdict write, and makes IV-5's zero-mutation assertion falsifiable without an exemption. IV-2, IV-3 and IV-4 are untouched in substance and are given explicit behavioral rows (B5, B6, B1). The Option-A architecture is UNCHANGED: still 4 files, 0 new production code files, 0 new module dependencies, the same four public mode names, the same additive CS11-CS12/CS14 sites, and the same single operator-adopted D-1 narrowing at CS13. Only the ACCEPTANCE SURFACE of the fourth file (the test) and the pre-verdict write behaviour of CS12 are affected. PA-021-CASCADE is NOT re-scoped: the manifest stays exactly [022-F, 022.001-T], condition (1) is STRENGTHENED because the step-14a read-only call now provably mutates nothing, and conditions (2) through (6) are unchanged.