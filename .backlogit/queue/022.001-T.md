---
artifact_type: task
created_at: 2026-09-13T01:09:29.210994Z
custom_fields:
    size: M
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
id: 022.001-T
parent_id: 022-F
priority: critical
status: queued
title: Delegate Ship close-path selection and complete covering feature
updated_at: 2026-09-13T23:17:23.9999845Z
---

Parent feature 022-F.

GOVERNING PLAN: docs/plans/2026-09-13-intercom-go-ship-feature-completion-decided-plan.md, revision 11. That plan is the single source of truth. Where this record and the plan differ, the plan governs. GOVERNING DECISION: docs/decisions/2026-09-13-intercom-go-a-only-simplification-decision.md (SIMPLIFICATION_VALID).

EXACT FILE INVENTORY - 4 FILES (OPTION A, operator-selected 2026-09-13):
1. .github/agents/_ship.agent.md
2. .github/policies/workflow-policies.md - single-bullet P-010 correction only
3. .github/skills/shipment-reconcile/SKILL.md - four ADDITIVE sites only (CS11-CS14)
4. tests/integration/ship_feature_completion_contract_test.go - new
New production code files: 0. New module dependencies: 0.

CLAUSE INVENTORY - 14 ANCHOR-ADDRESSED SITES plus 2 additive. The plan's clause inventory section is authoritative for the anchor text, the pinned replacement wording and the mapping to test rows. Sites are addressed by unique anchor string, not by line ordinal; ordinals in the plan are an explicitly non-normative dated observation. Nine sites are in the agent file, one is the P-010 grant bullet in the policy file, and four (CS11-CS14) are additive sites in the installed shipment-reconcile skill. The two additive items are the Role Boundary grant and the new parent-completion step.

TEST: one table-driven Go test, 24 rows, stdlib only, asserting over the three instruction-surface files. EVERY clause site has at least one pinned needle. Both polarities are asserted: positive rows require the corrected wording to be present, negative rows require the withdrawn wording and any release-instance identifier to be absent. The plan pins all 24 rows with their pre-change counts, and a per-site table in the plan proves every clause site is independently falsifiable by a needle no other site can satisfy.

ORDERING - TEST-FIRST, NON-NEGOTIABLE: the test lands and is RED before any of the three instruction-surface files is edited, then GREEN after. The red phase must be confirmed by harness-architect before this task is claimed.

ROLE BOUNDARY GRANT (narrow, conjunctive, fail-closed): covering feature active to done ONLY when every non-pre-archived executable descendant across the FULL live parent graph at EVERY DEPTH is done; no queued or active live descendant remains at any depth; that descendant set unioned with the feature is set-equal to the shipment manifest; parent and child topology is intact; the transition occurs immediately before pre-mode; and the feature's live status is exactly active. ANY condition unmet is a HALT with no mutation.

DELEGATION (OPTION A): Ship's locally re-derived classification prose is DELETED and replaced with a pointer to the authoritative installed machinery. The verdict is obtained from a NEW read-only public mode of the installed skill, mode classify-close-path, which runs the existing Step 0 classification and STOPS before the mutating dispatch, returning CLOSE_PATH_VERDICT (CASCADE, SAFE_CLOSE or BLOCK) with VERDICT_REASON, VERDICT_EVIDENCE and a CLASSIFICATION_BINDING over the canonical manifest and dependency snapshot. Ship calls it BEFORE any close call and halts on any non-CASCADE result without mutating anything. mode safe-close then revalidates the supplied binding against a freshly recomputed snapshot and refuses on drift, which closes the TOCTOU window. The four sites are ADDITIVE: all four pre-existing public modes and the unbound legacy fall-through are preserved verbatim and asserted by preservation rows. No Ship runtime code is added.

SIZE M, COMPLEXITY high. Complexity is recorded as prose because this workspace's header definition exposes no complexity field; do not attempt to set it as a structured field.

READINESS: true-lineage attempt 7 over plan revision 11. Gate attempt 7 returned FAIL (P0 = 0, P1 = 27). See docs/reviews/2026-09-13-true-lineage-attempt-7-verdict.md. Not claimable until that verdict returns P0 = 0 and P1 = 0. Status queued.

AUDIT: superseded scope, clause counts, plan identities and plan-review history live in Git history and PR #54, not in this record. See plan section 13.2.
