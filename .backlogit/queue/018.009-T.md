---
artifact_type: task
created_at: 2026-09-11T17:23:20.2768887Z
custom_fields:
    size: L
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 018.006-T
id: 018.009-T
labels:
    - sub-epic-b
    - governance
    - chore
parent_id: 018-F
priority: high
status: queued
title: Correct Stage Role Boundary rows and emit staging handback
updated_at: 2026-09-12T02:45:00.0000000Z
---

Parent feature 018-F. Sub-epic B, plan section 6.3 of docs/plans/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-plan.md. Correct the OPERATIVE authorization surface in .github/agents/_stage.agent.md, which role-enforcement treats as the authoritative permission set at mutation time. (1) The Role Boundary Git row Allowed cell currently reads 'Commit backlog/planning artifacts on default or admin branch'. Narrow it to the dedicated Stage/admin artifact branch only. (2) In the SAME Allowed cell, ADD an explicit grant to create and check out that dedicated artifact branch (chore/stage-{scope-slug}; a shipment ID MAY be used as the slug when one is already known, but is NEVER required), mirroring the P-010 grant added by 018.008-T - without it, role enforcement fails closed on the very branch creation the corrected policy requires. (3) Add an actor note on the PR row recording that the Orchestrator (pipeline invocation) or the operator (direct invocation) - never Stage - pushes the branch and opens/merges the staging PR, so the corrected Git row does not imply a forbidden Stage action. The PR row is the ONE row exempted from the otherwise-unchanged requirement in AC-B3.3; its prohibition is unweakened.
(4) STEP 6 HANDBACK EMISSION: add to Stage's STEP 6 (Summary) session-output contract one required line emitting the STAGING HANDBACK RECORD the Orchestrator's Step 1 consumes (018.007-T, AC-B1.5): stage_branch, stage_base_commit, stage_head_commit, stage_outcome (shipment|no-shipment), stage_artifact_paths, and shipment_id when one was formed. Without it the Orchestrator's handback requirement has no producer and every pipeline run falls into the STAGING_HANDBACK_DEGRADED path. It transfers NO authority - emitting a branch name, a commit pair, and a file list is not pushing, opening, or merging anything.
(5) PATH AND COMMIT SHAPE IN THAT SAME LINE: stage_artifact_paths is a NON-EMPTY list of CONCRETE repository-relative FILE paths - the files Stage committed BETWEEN stage_base_commit AND stage_head_commit, NEVER directory prefixes and NEVER only the tip commit's files - and the line names the derivation Stage uses to produce them: git diff --name-only -z --diff-filter=d {stage_base_commit} {stage_head_commit} -- .backlogit/ docs/plans/ docs/decisions/ docs/memory/. Naming the producing command here is what makes the consumer's SET-EQUALITY check (018.007-T AC-B1.10) satisfiable by construction rather than by coincidence: two surfaces computing the same set from the same commit PAIR with the same command cannot disagree. Stage ECHOES stage_base_commit rather than inventing one - the Orchestrator's retained pre-invocation value is authoritative (AC-B1.5) - and the echo exists so a DIRECT Stage invocation, which no Orchestrator bracketed, still hands the operator a usable base.
(6) MAKE THE no-shipment TERMINAL OUTCOME REACHABLE. As installed, two clauses make stage_outcome: no-shipment unreachable, which leaves the Orchestrator's entire second verification arm as dead prescription: Step 5.5 declares shipment assembly 'MANDATORY - not optional' whenever the registry advertises features.shipments: true and calls ending without a shipment_id a P-005 violation; and Step 6's pre-summary verification gate says that if no shipment_id exists, HALT and go back to Step 5.5 - so the summary, where the AC-B3.5 handback line lives, is never reached. Make the NARROWEST change that fixes this: (a) scope Step 5.5's mandatory rule to a harvest that PRODUCED ITEMS, preserving its existing guardrail sentence VERBATIM ('do not assemble a shipment if the harvest step produced no items or produced items with unresolved P-003 violations; halt and report before creating an empty shipment'), which is exactly the discriminator between a VALID EMPTY harvest (terminates as no-shipment) and unresolved P-003 violations (still HALTS); (b) require shipment_id in Step 6's pre-summary gate ONLY when stage_outcome is shipment, and for no-shipment require instead the COMPLETE handback record plus an explicit terminal statement that there is nothing to route to Ship; (c) the no-shipment summary MUST NOT direct the operator to Ship - with no shipment the correct handoff is NONE, and the Orchestrator's Step 1.5 no-shipment arm is the terminal gate.
FAILURES STAY FAILURES - LOAD-BEARING: no-shipment is a SUCCESS terminal for a run that had nothing to ship. It is NEVER a re-label for failure. A P-003 lineage violation, a harvest that errored, and a harvest that produced items but could not form the required shipment ALL CONTINUE TO HALT exactly as they do today and MUST NOT be recorded as no-shipment. Without that constraint this edit would convert the pipeline's loudest failure mode into a silent success - a strictly worse defect than the unreachable arm it fixes.
(7) ADD THE TWO OPERATIVE STEPS THE PERMISSION ALWAYS PRESUPPOSED. Items (1)-(6) establish what Stage MAY do and what Stage MUST REPORT; neither is an instruction to DO it. The installed _stage.agent.md contains NO branch operation and NO commit operation at any step - its Required Steps run triage, grouping, learnings, deliberation, planning, review, harvest, shipment assembly, stash archival, summary - so an executor satisfying AC-B3.1..AC-B3.6 to the letter can finish the session ON THE DEFAULT BRANCH, having written every artifact there, and then emit a handback naming a branch that was never created and commits that were never made. Add both steps, with the replacement text fully specified in plan section 6.3.1:
(7a) STEP 1.9 - STAGE ARTIFACT BRANCH GATE (NON-NEGOTIABLE), a NEW step section between the existing Step 1.8 (Learnings Retrieval) and Step 2 (Deliberation). That FIXED position is the first point at which a stable scope slug is derivable for both intake shapes - Step 1 classification having settled the feature-shaped case and Step 1.5 grouping selection the task-shaped one. DEFERRAL RULE - NOTHING TRACKED WRITES BEFORE THIS GATE (rev 10): this gate precedes EVERY tracked Stage artifact mutation of the session, WITHOUT EXCEPTION, and any such mutation that would otherwise occur at an earlier step is DEFERRED UNTIL AFTER it. The rule is CATEGORICAL; the three classes below are named because the installed contract actually schedules them earlier, and they are ILLUSTRATIVE OF THE RULE rather than a closed list a new earlier mutation could escape. (i) The WRITE HALF of the MANDATORY LATE-IDENTIFIER RECONCILIATION at Step 1: the analysis - searching Ship's residual-risk records for a late review-thread ID or PR number - is READ-ONLY and completes in Step 1, while the IN-PLACE UPDATE of the deferred stash entry that records the recovered identifiers is a tracked write and MOVES HERE. (ii) The ARCHIVAL of a duplicate stash entry found by the UNCONDITIONAL duplicate-detection scan at Step 1: detection is read-only and completes in Step 1; the stash archive call MOVES. (iii) EVERY tracked docs/memory/ CHECKPOINT whose triggering milestone precedes this gate - specifically the STASH-CLASSIFICATION checkpoint (Step 1) and the CONTEXTUAL-GROUPING / OPERATOR-SELECTION checkpoint (Step 1.5), plus any other checkpoint a session would write before reaching this step - all written AFTER the gate succeeds, against the same session state they would have recorded earlier. READ-ONLY WORK STILL RUNS FIRST AND MUST: Step 1 classification and Step 1.5 grouping analysis and operator selection are reads plus a decision, and they are what make the scope slug derivable, so they necessarily precede the gate; what is deferred is the WRITE, never the analysis or the decision. DISPOSABLE GITIGNORED OPERATIONS ARE NOT TRACKED ARTIFACTS AND ARE NOT DEFERRED: index synchronization (backlogit sync / backlogit_sync_index -> .backlogit/backlogit.db*), structured backlogit checkpoints (.backlogit/checkpoints/), hook-event polling and acknowledgement (.backlogit/hooks_queue.jsonl), and .backlogit/runtime/ state are all GITIGNORED in this repository - they never appear in git status, cannot be committed, and therefore cannot reach the default branch. They are OUTSIDE this rule and must NOT be claimed by it; conflating them with tracked artifacts would overstate the gate's reach and would wrongly forbid the Step 0.1 index sync every later query depends on. Deferring the tracked writes is sound because the gate is at most two steps later, nothing downstream consumes any of them before Step 2, and each is a Stage artifact write of exactly the kind this gate exists to place on the correct branch; an artifact written before this step is a P-005 step-order violation as well as a P-010 one. Do NOT let the gate fire early against a provisional slug instead - that contradicts the Step Sequence Contract's own execute-in-order semantics and leaves the real position unassertable. The step (i) derives {scope-slug} as the lower-kebab-case reduction of the covering feature title (task-shaped intake) or the feature/epic/chore title (feature-shaped intake), recorded once and never renamed, with an already-known shipment ID permitted as the slug but NEVER waited for; (ii) records stage_base_commit - ECHOED from the Orchestrator in a pipeline invocation, CAPTURED from git rev-parse HEAD BEFORE branch creation in a direct invocation, and never re-captured; (iii) resolves the branch by VERIFYING and USING an Orchestrator-supplied $STAGE_BRANCH (git rev-parse --verify, then git checkout when HEAD does not already name it; Stage does NOT create a second branch) or by CREATING/CHECKING OUT chore/stage-{scope-slug} from the current commit when none was supplied; (iv) asserts git rev-parse --abbrev-ref HEAD equals the resolved branch and is NOT the default branch, halting with STAGE_BRANCH_GATE_FAIL and recording P-010 otherwise, with no Stage mutation proceeding past a failed assertion; (v) keeps P-016 SINGLE-WORKTREE by SWITCHING the one existing worktree - no git worktree add, no second checkout, the time-boxed spike/research exception unrelated and unchanged; (vi) is IDEMPOTENT on re-entry - a resumed session already on the branch verifies and proceeds.
(7b) STEP 5.7 - STAGE ARTIFACT COMMIT (NON-NEGOTIABLE), a NEW step section between the existing Step 5.6 (Archive Consumed Stash Entries) and Step 6 (Summary). It runs after EVERY Stage mutation of the session - backlog items, the shipment manifest, the plan/deliberation/memory records, and consumed-stash archival - and BEFORE Step 6 emits the handback. It (i) asserts HEAD equals the Step 1.9 branch and is NOT the default branch, halting with STAGE_COMMIT_GATE_FAIL rather than committing anyway; (ii) DETECTS OUT-OF-ROOT CHANGES BEFORE STAGING ANYTHING (rev 10) - a root-scoped git add CANNOT discharge this obligation, because it IGNORES paths outside its pathspec rather than reporting them, so the rev-9 halt had no mechanism that could observe what it was about. Run an explicit full-tree inspection FIRST: git status --porcelain=v1 -z --untracked-files=all. --untracked-files=all is LOAD-BEARING (the default 'normal' mode collapses an untracked subtree to a DIRECTORY entry such as '?? sub/', which no per-file containment test can evaluate; 'all' recurses to '?? sub/deep.md') and -z is LOAD-BEARING (without it, paths with spaces are quoted and unusual bytes are backslash-escaped, so line splitting is unsafe). PARSE THE NUL-DELIMITED RECORDS, NEVER LINES: split on NUL, drop the trailing empty element; each record is XY + one space + PATH where XY is the two-column status; when EITHER status column is R (rename) or C (copy), the NEXT record is the ORIG_PATH and carries NO XY prefix - consume it as part of the same entry. BOTH ENDPOINTS of a rename or copy are affected paths and BOTH are checked. Verified output shape: ' D keep.txt<NUL>R  new name.txt<NUL>old name.txt<NUL>?? sub/deep.md<NUL>?? untracked.md<NUL>'. HALT FAIL-CLOSED on any affected path - including a rename's origin - that does not lie under the fixed STAGE_ARTIFACT_ROOTS, with STAGE_COMMIT_GATE_FAIL: change outside Stage artifact roots: {path}, recording a P-010 signal and surfacing the full offending list to the operator. Stage MUST NOT widen the commit to include it, and MUST NOT checkout, restore, stash, revert, or delete it: the change may be a human operator's unrelated in-flight work, and discarding it would be both a destructive act without approval (Constitution VII) and a data-loss risk. PRE-EXISTING UNRELATED DIRT IS A HALT, NOT A CLEANUP - Stage stops and hands the decision to the operator, leaving the working tree exactly as it found it. Gitignored paths never appear in this output and are out of scope by construction; (iii) ONLY AFTER that inspection passes, stages ONLY the allowed roots, git add -- .backlogit/ docs/plans/ docs/decisions/ docs/memory/, the same fixed STAGE_ARTIFACT_ROOTS constant the Orchestrator's Step 1.5 uses - the pathspec is a BOUND, not a detector, since detection already happened; (iv) commits in CONVENTIONAL COMMITS form (chore(stage): ... or docs(plan): ...) naming the staged scope, a NO-OP when nothing is staged; (v) sets stage_head_commit := git rev-parse HEAD taken AFTER the commit, so it names the commit that actually carries the artifacts; (vi) derives stage_artifact_paths with the AGGREGATE two-tree derivation over the base PRESERVED from Step 1.9; (vii) STOPS AT THE COMMIT - Stage MUST NOT push and MUST NOT create, update, or merge a pull request; the Orchestrator (pipeline) or the operator (direct) owns every remote operation on this branch.
(7c) REGISTER BOTH STEPS AND EXTEND THE STEP 6 GATE. A step section the Step Sequence Contract does not list is advisory in a file whose own preamble makes that checklist what a session MUST execute. Add '[ ] Step 1.9 - Stage artifact branch gate (pre-mutation)' between the checklist's Step 1.8 and Step 2 entries and '[ ] Step 5.7 - Stage artifact commit (pre-handback)' between its Step 5.6 and Step 6 entries; and add two checks to Step 6's Pre-Summary Verification Gate - Step 1.9 completed and HEAD still the Stage artifact branch, Step 5.7 completed and stage_head_commit recorded from its result - a summary presented without both being the same P-005 class the gate already enforces for shipment assembly.
ORDERING IS THE CONTRACT, NOT PRESENCE: a branch gate placed AFTER harvest leaves every artifact written on the default branch, and a commit step placed AFTER the summary emits a stage_head_commit that does not yet exist. Both ordinal positions are load-bearing and are asserted structurally by checklist-index and heading-position comparison, never by text search (plan section 6.3.2). REV 10 EXTENDS THAT INTO THE COMMIT SECTION: the out-of-root DETECTION clause must sit BEFORE the staging clause, asserted by the same index comparison applied WITHIN the section - a detection clause placed after git add documents an inspection that runs too late to prevent the commit it exists to gate, and presence alone would satisfy a section that stages first and looks afterwards.
SCOPE: items (4), (5), (6) and (7) are the ONLY edits outside the Role Boundary table; no other section of _stage.agent.md is modified.
HARNESS (P-002/P-004): harness function TestDirectPushGate_StageAgentSurfaceClean in tests/integration/directpush_gate_test.go, produced by harness-architect at Ship Step 2 per plan section 5.0. harness_cmd: go test ./tests/integration -run '^TestDirectPushGate_StageAgentSurfaceClean$' -gatetask=B3
ACCEPTANCE CRITERIA (PLAIN-TEXT RENDERING - the authoritative text is the acceptance-criteria section below; if the two disagree, that section wins and this rendering is the defect):
AC-B3.1 The Git row Allowed cell no longer authorizes committing on the default branch.
AC-B3.2 The Git row Allowed cell grants creation and check-out of the dedicated Stage/admin artifact branch, matching P-010, and names it chore/stage-{scope-slug} with a shipment ID permitted but NOT required as the slug (rev 9), so the cell does not authorize a branch whose name is underivable when Step 1.9 needs it.
AC-B3.3 The Git row Forbidden cell is unchanged, and every other Role Boundary row is unchanged EXCEPT the PR row, which is explicitly exempted to carry the required actor note. This criterion governs the Role Boundary table; the Step 6 edit required by AC-B3.5, the Step 5.5 / Step 6 edits required by AC-B3.6, and the Step 1.9 / Step 5.7 / Step Sequence Contract / Step 6-gate edits required by AC-B3.7 lie outside it.
AC-B3.4 The file agrees with the corrected P-010 text - no surviving contradiction in either direction.
AC-B3.5 Step 6's session-output contract emits the staging handback record - stage_branch, stage_base_commit, stage_head_commit, stage_outcome, stage_artifact_paths, and shipment_id when formed - every field the Orchestrator's Step 1 consumes under AC-B1.5, with none required there and absent here; and it fixes the SHAPE of stage_artifact_paths (a non-empty list of concrete repository-relative FILE paths changed between stage_base_commit and stage_head_commit, never directory prefixes and never only the tip commit's files) and names the aggregate derivation, so the emitted set satisfies the consumer's set-equality check (AC-B1.10) by construction. REV 9: those values are PRODUCED, not asserted - branch and base by Step 1.9, head and paths by Step 5.7 (AC-B3.7). REV 10: stage_outcome is ALWAYS EMITTED EXPLICITLY (never omitted for the consumer to infer) and forms a CONSISTENT PAIR with shipment_id - 'shipment' with a non-empty shipment_id, 'no-shipment' with none - which is the producing half of the consumer's fail-closed pair validation (018.007-T AC-B1.12); an agreement check needs two parties, so a producer free to omit the outcome would restore the derivation and with it the silent reclassification of a reported shipment run into a successful no-shipment one.
AC-B3.6 The no-shipment terminal outcome is REACHABLE: Step 5.5's mandatory shipment assembly is scoped to a harvest that produced items, a reviewed valid EMPTY harvest terminates with stage_outcome: no-shipment instead of halting, Step 5.5's empty-harvest / unresolved-P-003 guardrail is preserved VERBATIM, Step 6's pre-summary gate requires shipment_id ONLY when stage_outcome is shipment, and the no-shipment summary emits the complete handback plus an explicit terminal statement without routing to Ship. P-003 failures, harvest failures, and a missing required shipment after a non-empty harvest continue to HALT and are never recorded as no-shipment.
AC-B3.7 BRANCH-BEFORE-MUTATION AND COMMIT-BEFORE-HANDBACK ARE OPERATIVE AND ORDERED (rev 9). _stage.agent.md carries TWO operative steps, each registered in the Step Sequence Contract checklist and each backed by its own step section: (a) a PRE-MUTATION BRANCH GATE whose checklist entry and section both sit AFTER the learnings-retrieval step and BEFORE the deliberation step - a FIXED position, carrying a CATEGORICAL deferral rule that places EVERY tracked Stage artifact mutation of the session after the gate (rev 10) and enumerates at least its three classes - the WRITE HALF of the mandatory late-identifier reconciliation, the UNCONDITIONAL duplicate-entry archival, and EVERY tracked docs/memory/ checkpoint whose triggering milestone precedes the gate, naming the stash-classification and contextual-grouping/operator-selection checkpoints explicitly - while the read-only classification and grouping analysis that derive the scope slug still run first and gitignored disposable operations (index sync, backlogit checkpoints, hook queue, runtime state) are expressly OUTSIDE the rule rather than claimed by it, so no tracked artifact write precedes the gate - deriving a {scope-slug} without requiring a shipment_id, recording stage_base_commit (echoed from the Orchestrator in a pipeline invocation, captured from HEAD BEFORE branch creation in a direct invocation), VERIFYING AND USING an Orchestrator-supplied branch or CREATING AND CHECKING OUT chore/stage-{scope-slug} when none was supplied, REFUSING to write any artifact while HEAD is the default branch, and switching the single existing worktree rather than adding one (P-016); and (b) a POST-MUTATION ARTIFACT COMMIT whose checklist entry and section both sit AFTER the consumed-stash-archival step and BEFORE the summary step, asserting HEAD is the Stage artifact branch and not the default branch, DETECTING OUT-OF-ROOT CHANGES BEFORE STAGING ANYTHING (rev 10) via an explicit NUL-safe full-tree inspection (git status --porcelain=v1 -z --untracked-files=all) whose records are parsed NUL-wise rather than by line, whose rename/copy entries contribute BOTH endpoints, and which halts fail-closed with a P-010 signal on any path outside STAGE_ARTIFACT_ROOTS while leaving that pre-existing change UNTOUCHED (no checkout, restore, stash, revert, or delete), then staging only the four STAGE_ARTIFACT_ROOTS, committing in Conventional Commits form, setting stage_head_commit from the RESULTING HEAD, and stating that Stage neither pushes nor creates, updates, or merges a pull request. Step 6's pre-summary verification gate requires both steps complete before any summary is presented. ORDERING IS ASSERTED STRUCTURALLY, by checklist-index and heading-position comparison rather than by text search, and - rev 10 - the commit section's DETECTION clause is asserted to precede its STAGING clause by the same index comparison applied within the section. AC-B3.5, AC-B3.6 and AC-B3.7 are the ONLY changes outside the Role Boundary table.

<!-- BEGIN:implementation-notes -->
DEGRADATION NOTE: this workspace's backlogit type schema defines 'size' but NO 'complexity' field on any artifact type (verified via backlogit metadata types: task fields = priority, size, status), despite backlog-registry.yaml declaring features.sizing: true. Complexity is therefore recorded below as enum-validated prose per the Stage two-axis sizing contract. Size is stored structurally (size_source=agent, size_ruleset_version=2h-rule-v1).

Size: L | Complexity: medium

Rationale: NINE edit sites in ONE file, single domain (contract prose): the Git row Allowed cell (narrowed, extended with the branch-creation grant, and re-named to the scope-slug form), the adjacent PR-row actor note, the Step 6 session-output line, the Step 5.5 mandatory-assembly scoping, the Step 6 pre-summary gate (twice - the no-shipment scoping at rev 8 and the two step-completion checks at rev 9), the NEW Step 1.9 branch-gate section, the NEW Step 5.7 artifact-commit section, and the Step Sequence Contract checklist. S->M at rev 8 and M->L at rev 9 on VOLUME ONLY - more edit sites in the same file - never on uncertainty: every replacement text is fully specified in plan sections 6.3 and 6.3.1, so no design work remains at execution time. Still inside the 2-hour rule: one file, one domain, no code, no build. The load-bearing details are that the empty-harvest terminal must NOT swallow P-003 or harvest failures, and that the two new steps are ORDERED, not merely present.

REV-6 RECONCILIATION (PR #54 thread PRRT_kwDOTPuhps6hqWDe): this task gained the PRODUCING half of the staging handback contract whose consuming half is 018.007-T AC-B1.5. The same grant-in-one-surface-only argument that already forces AC-B3.2 applies in the opposite direction: role enforcement reads THIS file at mutation time, and the Orchestrator consumes THIS file's declared session output, so the branch Stage created must be declared here rather than only in the Orchestrator's expectations. AC-B3.5 added; AC-B3.3 clarified to scope itself to the Role Boundary table.

REV-7 RECONCILIATION (PR #54 thread PRRT_kwDOTPuhps6hrC0c): this task gained the PRODUCING half of the concrete-path contract whose consuming half is 018.007-T AC-B1.10. The rev-6 handback named the field but not its shape, so a producer emitting directory prefixes was contract-compliant - and that is exactly what the Orchestrator then verified with a git show loop that exits 0 on a tree. Fixing only the consumer would make the producer's legal output permanently fail the gate; fixing only the producer would leave the consumer unable to reject a bad one. Both halves were corrected in the same change.

REV-8 RECONCILIATION (PR #54 adversarial review, P1 blockers 1 and 2): (a) the emitted record gains stage_base_commit and the AGGREGATE two-tree derivation, matching the consumer's rev-8 form (018.007-T AC-B1.10). A producer that reports only its tip commit's files would fail the consumer's set-equality check on every multi-commit Stage session, so producer and consumer move together, as they did at rev 7. (b) NEW AC-B3.6: the review established that stage_outcome: no-shipment was UNREACHABLE against the installed _stage.agent.md - Step 5.5 declares shipment assembly mandatory and Step 6's pre-summary gate halts without a shipment_id - so 018.007-T's entire second verification arm, the 018-F DoD bullet that depends on it, and the 018.011-T coupling criterion that checks it were all gated on an outcome no producer could emit. A verification arm keyed on an unreachable outcome is not a passing check; it is an unexecuted one, and it passes every agreement test that compares only field names. The fix is deliberately narrow: two clauses scoped, Step 5.5's guardrail sentence preserved VERBATIM as the valid-empty vs failed discriminator, and an explicit statement that P-003 and harvest failures still halt. SIZE S->M on volume only. Plan section 6.3 carries identical text from the same change, so the plan<->task same-contract invariant holds. NO new task, file, fixture, harness function, or dependency edge; shipment 017-S membership and order unchanged.

HARNESS BINDING (plan section 5.0): this task does NOT carry a harness-ready label. harness-architect applies that label at Ship Step 2 after producing the Go harness and confirming go vet ./... exits 0 and go test ./... is red. Stage does not forge P-004's postcondition.

REV-9 RECONCILIATION (PR #54 thread PRRT_kwDOTPuhps6hrbAk, comment 3994429008, on this file line 22): the review established that revisions 1-8 gave Stage the PERMISSION to branch and commit (AC-B3.1/AC-B3.2, and the P-010 grant on 018.008-T) and the OBLIGATION to REPORT what it did (AC-B3.5), but NEVER AN OPERATIVE STEP THAT PERFORMS EITHER. The installed _stage.agent.md has no branch operation and no commit operation at any step, so implementing AC-B3.1..AC-B3.6 exactly as written can still end a session on the default branch with every artifact written there - the fdff9e4 shape this release unit exists to close - while emitting a handback naming a branch that was never created and commits that were never made. This is the PRODUCER/CONSUMER FAILURE CLASS IN ITS FOURTH INSTANCE: rev 1 obliged Stage to merge a PR it could not create; rev 6 obliged the Orchestrator to find a branch nothing announced; rev 8 keyed a verification on an outcome nothing could emit; rev 9 finds a REPORTED VALUE WITH NO PRODUCING ACTION. Deliberation section 12.2's discipline - for every value a contract consumes, name the surface that emits it and confirm that surface is PERMITTED to - was followed for permission and emission and not for EXECUTION; deliberation section 13 extends it to: confirm the producing surface is also INSTRUCTED, and that the instruction is ORDERED relative to the state it describes. Item (7) and NEW AC-B3.7 close it with two operative steps in the file this task already owns, plan section 6.3.2 specifies their MECHANICAL detection inside the EXISTING TestDirectPushGate_StageAgentSurfaceClean function (checklist-index and heading-position comparison, never text search - the AC-C1.6 self-match hazard one file over), and risks R17/R18 record the render-revert exposure and the bounded 'gate fires too late' residual. SECOND-ORDER CORRECTION: the rev-1..rev-8 branch name chore/stage-{shipment_id} was UNDERIVABLE once the branch must exist before the first artifact write, because Step 5.5 creates the shipment as the session's LAST mutation - so the old wording pushed an executor toward deferring the branch past the very writes it protects. The form is now chore/stage-{scope-slug} with a shipment ID permitted but not required (AC-B3.2 here, AC-B2.2 on 018.008-T); chore/stage-pipeline-policy-gap is a conforming example. SIZE M->L on volume only. Plan section 6.3 carries identical text from the same change, so the plan<->task same-contract invariant holds. NO new task, file, fixture, harness function, shipment, or dependency edge; shipment 017-S membership and order unchanged.

REV-10 RECONCILIATION (PR #54 second adversarial review, threads PRRT_kwDOTPuhps6hsPRq comment 3994747663 and PRRT_kwDOTPuhps6hsPR5 comment 3994747682, both P1; and thread PRRT_kwDOTPuhps6hsPSJ comment 3994747702, P1): two rev-9 clauses were written as OBLIGATIONS WITHOUT A MECHANISM THAT COULD DISCHARGE THEM - a different defect class from the producer/consumer one, and worth naming separately. (a) THE DEFERRAL RULE WAS AN ENUMERATION MISTAKEN FOR A RULE. It named exactly two mutations, while the installed Stage contract also schedules, before Step 1.9's ordinal, a STASH-CLASSIFICATION checkpoint, a CONTEXTUAL-GROUPING / OPERATOR-SELECTION checkpoint, and the WRITE HALF of the mandatory late-identifier reconciliation - each tracked, each therefore still legal on the default branch under the rev-9 wording, which is precisely the fdff9e4 shape the gate exists to prevent. Item (7a) is rewritten so the rule is CATEGORICAL over every tracked Stage artifact mutation, with the three classes enumerated as ILLUSTRATION rather than as the closed set, the read-only classification and grouping analysis explicitly still running first (they derive the slug), and GITIGNORED DISPOSABLES expressly EXCLUDED rather than overclaimed - .backlogit/backlogit.db*, .backlogit/checkpoints/, .backlogit/hooks_queue.jsonl, and .backlogit/runtime/ are gitignored in this repository, never appear in git status, and cannot reach the default branch, so claiming them would overstate the gate's reach and wrongly forbid the Step 0.1 index sync. (b) THE OUT-OF-ROOT HALT HAD NO DETECTOR. Item (7b) told Stage to halt on a change outside STAGE_ARTIFACT_ROOTS while prescribing only a root-scoped git add, which IGNORES such a change rather than reporting it - the halt could never fire. A concrete, implementable inspection now runs BEFORE staging, with the NUL-record parse algorithm, both rename/copy endpoints, and an explicit fail-closed halt that LEAVES PRE-EXISTING UNRELATED DIRT UNTOUCHED (no checkout, restore, stash, revert, or delete - Constitution VII). AC-B3.7 absorbs both, plan section 6.3.2 gains the matching mandatory-clause assertions plus an INTRA-SECTION ordering check (detection before staging), and R19/R20 record the residuals. Also, from thread PRRT_kwDOTPuhps6hsPRZ comment 3994747640: AC-B3.5 gains the PRODUCING half of the outcome/shipment pair contract whose consuming half is 018.007-T AC-B1.12. And from the adversarial harness finding: harness_cmd gains the -gatetask=B3 selector introduced by plan section 5.0.1. SIZE AND COMPLEXITY UNCHANGED - rev 10 reworks clauses inside the two step sections rev 9 already specified and adds no edit site. Plan sections 6.3, 6.3.1 and 6.3.2 carry identical text from the same change, so the plan<->task same-contract invariant holds. NO new task, file, fixture, harness function, shipment, or dependency edge; shipment 017-S membership and order unchanged.
<!-- END:implementation-notes -->

<!-- BEGIN:acceptance-criteria -->
- **AC-B3.1** The Git row Allowed cell no longer authorizes committing on the default branch; it is narrowed to the dedicated Stage/admin artifact branch.
- **AC-B3.2** The Git row Allowed cell ALSO grants creation and check-out of the dedicated Stage/admin artifact branch, matching the P-010 grant added by 018.008-T. Without this, role enforcement — which treats this table as the authoritative permission set at mutation time — would fail closed on the branch creation the same policy requires. **Rev 9**: the branch is named `chore/stage-{scope-slug}`, with a shipment ID permitted but **not required** as the slug, so the cell does not authorize a branch whose name is underivable at the moment Step 1.9 needs it.
- **AC-B3.3** The Git row Forbidden cell is unchanged, and every other Role Boundary row is unchanged EXCEPT the PR row, which is explicitly exempted here: it gains the required actor note that the Orchestrator (pipeline) or the operator (direct invocation) — never Stage — pushes the branch and opens/merges the staging PR. The note records actor attribution only; the PR row's prohibition is unweakened. This criterion governs the **Role Boundary table**; the Step 6 edit required by AC-B3.5, the Step 5.5 / Step 6 edits required by AC-B3.6, and the Step 1.9 / Step 5.7 / Step Sequence Contract / Step 6-gate edits required by AC-B3.7 lie outside it.
- **AC-B3.4** The file agrees with the corrected P-010 text — no surviving contradiction between agent contract and policy, in either direction (no grant in one that the other forbids).
- **AC-B3.5** **Staging handback emitted (rev 6).** Step 6's session-output contract requires `stage_branch`, `stage_base_commit` (rev 8), `stage_head_commit`, `stage_outcome`, `stage_artifact_paths`, and `shipment_id` (when formed) — every field the Orchestrator's Step 1 consumes under AC-B1.5, with none required there and absent here. **Rev 7/8**: the contract also fixes the **shape** of `stage_artifact_paths` — a non-empty list of concrete repository-relative **file** paths changed between `stage_base_commit` and `stage_head_commit`, never directory prefixes and never only the tip commit's files — and names the derivation (`git diff --name-only -z --diff-filter=d {stage_base_commit} {stage_head_commit} --` over the four Stage artifact roots), so the emitted set satisfies the consumer's set-equality check (AC-B1.10) by construction. **Rev 9**: the emitted values are **produced**, not asserted — `stage_branch` and `stage_base_commit` come from the Step 1.9 branch gate and `stage_head_commit` and `stage_artifact_paths` from the Step 5.7 artifact commit (AC-B3.7). **Rev 10**: `stage_outcome` is **always emitted explicitly** — never omitted for the consumer to infer — and forms a **consistent pair** with `shipment_id` (`shipment` with a non-empty `shipment_id`, `no-shipment` with none), which is the producing half of the consumer's fail-closed pair validation (018.007-T AC-B1.12). An agreement check needs two parties: a producer free to omit the outcome would restore the consumer-side derivation, and with it the silent reclassification of a reported `shipment` run into a successful `no-shipment` one.
- **AC-B3.6** **The `no-shipment` terminal outcome is reachable (rev 8).** Step 5.5's mandatory shipment assembly is scoped to a harvest that **produced items**; a reviewed, valid **empty** harvest terminates with `stage_outcome: no-shipment` instead of halting, and Step 5.5's existing empty-harvest / unresolved-P-003 guardrail is preserved **verbatim**. Step 6's pre-summary verification gate requires `shipment_id` **only when `stage_outcome` is `shipment`**; for `no-shipment` it requires the complete handback record (AC-B3.5) plus an explicit terminal statement, and the summary does **not** route the operator to Ship. **P-003 failures, harvest failures, and a missing required shipment after a non-empty harvest continue to HALT and are never recorded as `no-shipment`** — the outcome is a success terminal, never a failure re-label.
- **AC-B3.7** **Branch-before-mutation and commit-before-handback are operative and ordered (rev 9).** `_stage.agent.md` carries **two operative steps**, each registered in the Step Sequence Contract checklist and each backed by its own step section (plan §6.3.1): (a) a **pre-mutation branch gate** whose checklist entry and section both sit **after** the learnings-retrieval step and **before** the deliberation step — a **fixed** position, carrying a **categorical** deferral rule that places **every** tracked Stage artifact mutation of the session after the gate (rev 10), enumerating at least its three classes: the **write half** of the mandatory late-identifier reconciliation, the **unconditional duplicate-entry archival**, and **every tracked `docs/memory/` checkpoint whose triggering milestone precedes the gate**, naming the stash-classification and contextual-grouping/operator-selection checkpoints explicitly. The read-only classification and grouping analysis that derive the scope slug still run first, and gitignored disposable operations (index sync, backlogit checkpoints, hook queue, runtime state) are expressly **outside** the rule rather than claimed by it — which derives a `{scope-slug}` without requiring a `shipment_id`, records `stage_base_commit` (echoed from the Orchestrator in a pipeline invocation, captured from `HEAD` **before** branch creation in a direct invocation), **verifies and uses** an Orchestrator-supplied branch or **creates and checks out** `chore/stage-{scope-slug}` when none was supplied, **refuses to write any artifact while `HEAD` is the default branch**, and switches the single existing worktree rather than adding one (P-016); and (b) a **post-mutation artifact commit** whose checklist entry and section both sit **after** the consumed-stash-archival step and **before** the summary step, which asserts `HEAD` is the Stage artifact branch and not the default branch, **detects out-of-root changes before staging anything** (rev 10) via an explicit NUL-safe full-tree inspection (`git status --porcelain=v1 -z --untracked-files=all`) whose records are parsed NUL-wise rather than by line, whose rename/copy entries contribute **both** endpoints, and which halts fail-closed with a P-010 signal on any path outside `STAGE_ARTIFACT_ROOTS` while leaving that pre-existing change **untouched** (no checkout, restore, stash, revert, or delete), then stages only the four `STAGE_ARTIFACT_ROOTS`, commits in Conventional Commits form, sets `stage_head_commit` from the **resulting** `HEAD`, and states that Stage neither pushes nor creates, updates, or merges a pull request. Step 6's pre-summary verification gate requires both steps complete before any summary is presented. **Ordering is asserted structurally**, by checklist-index and heading-position comparison rather than by text search, and — rev 10 — the commit section's **detection** clause is asserted to precede its **staging** clause by the same index comparison applied within the section (plan §6.3.2). AC-B3.5, AC-B3.6 and AC-B3.7 are the **only** changes outside the Role Boundary table; no other section of the file is modified.
<!-- END:acceptance-criteria -->
