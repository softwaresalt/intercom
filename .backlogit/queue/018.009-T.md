---
artifact_type: task
created_at: 2026-09-11T17:23:20.2768887Z
custom_fields:
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 018.006-T
id: 018.009-T
labels:
    - sub-epic-b
    - governance
    - chore
parent_id: 018-F
priority: high
status: queued
title: Correct Stage Role Boundary rows and emit staging handback
updated_at: 2026-09-11T17:49:23.1740224Z
---

Parent feature 018-F. Sub-epic B, plan section 6.3 of docs/plans/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-plan.md. Correct the OPERATIVE authorization surface in .github/agents/_stage.agent.md, which role-enforcement treats as the authoritative permission set at mutation time. (1) The Role Boundary Git row Allowed cell currently reads 'Commit backlog/planning artifacts on default or admin branch'. Narrow it to the dedicated Stage/admin artifact branch only. (2) In the SAME Allowed cell, ADD an explicit grant to create and check out that dedicated artifact branch (chore/stage-{shipment_id}, or chore/stage-{chore-slug} when no shipment is formed), mirroring the P-010 grant added by 018.008-T - without it, role enforcement fails closed on the very branch creation the corrected policy requires. (3) Add an actor note on the PR row recording that the Orchestrator (pipeline invocation) or the operator (direct invocation) - never Stage - pushes the branch and opens/merges the staging PR, so the corrected Git row does not imply a forbidden Stage action. The PR row is the ONE row exempted from the otherwise-unchanged requirement in AC-B3.3; its prohibition is unweakened. (4) REV 6 - add to Stage's STEP 6 (Summary) session-output contract one required line emitting the STAGING HANDBACK RECORD the Orchestrator's Step 1 now consumes (018.007-T, AC-B1.5): stage_branch, stage_head_commit, stage_outcome (shipment|no-shipment), stage_artifact_paths, and shipment_id when one was formed. This is the ONLY edit outside the Role Boundary table; without it the Orchestrator's handback requirement has no producer and every pipeline run falls into the STAGING_HANDBACK_DEGRADED path. It transfers NO authority - emitting a branch name is not pushing, opening, or merging anything.
HARNESS (P-002/P-004): harness function TestDirectPushGate_StageAgentSurfaceClean in tests/integration/directpush_gate_test.go, produced by harness-architect at Ship Step 2 per plan section 5.0. harness_cmd: go test ./tests/integration -run TestDirectPushGate_StageAgentSurfaceClean
ACCEPTANCE CRITERIA:
AC-B3.1 The Git row Allowed cell no longer authorizes committing on the default branch.
AC-B3.2 The Git row Allowed cell grants creation and check-out of the dedicated Stage/admin artifact branch, matching P-010.
AC-B3.3 The Git row Forbidden cell is unchanged, and every other Role Boundary row is unchanged EXCEPT the PR row, which is explicitly exempted to carry the required actor note. This criterion governs the Role Boundary table; the Step 6 edit required by AC-B3.5 lies outside it.
AC-B3.4 The file agrees with the corrected P-010 text - no surviving contradiction in either direction.
AC-B3.5 Step 6's session-output contract emits the staging handback record - every field the Orchestrator's Step 1 consumes under AC-B1.5, with none required there and absent here. Only change outside the Role Boundary table.

<!-- BEGIN:implementation-notes -->
DEGRADATION NOTE: this workspace's backlogit type schema defines 'size' but NO 'complexity' field on any artifact type (verified via backlogit metadata types: task fields = priority, size, status), despite backlog-registry.yaml declaring features.sizing: true. Complexity is therefore recorded below as enum-validated prose per the Stage two-axis sizing contract. Size is stored structurally (size_source=agent, size_ruleset_version=2h-rule-v1).

Size: S | Complexity: low

Rationale: One table cell (narrowed and extended with the branch-creation grant), one adjacent PR-row actor note, and one Step 6 session-output line. Three small edits in one file, fully specified in plan section 6.3; XS->S on volume only, uncertainty unchanged.

REV-6 RECONCILIATION (PR #54 thread PRRT_kwDOTPuhps6hqWDe): this task gains the PRODUCING half of the staging handback contract whose consuming half is 018.007-T AC-B1.5. The same grant-in-one-surface-only argument that already forces AC-B3.2 applies in the opposite direction: role enforcement reads THIS file at mutation time, and the Orchestrator consumes THIS file's declared session output, so the branch Stage created must be declared here rather than only in the Orchestrator's expectations. AC-B3.5 added; AC-B3.3 clarified to scope itself to the Role Boundary table. Plan section 6.3 was extended with the same text in the same change, preserving the plan<->task same-contract invariant.

HARNESS BINDING (rev 4, plan section 5.0): this task does NOT carry a harness-ready label. harness-architect applies that label at Ship Step 2 after producing the Go harness and confirming go vet ./... exits 0 and go test ./... is red. Stage does not forge P-004's postcondition.
<!-- END:implementation-notes -->

<!-- BEGIN:acceptance-criteria -->
- **AC-B3.1** The Git row Allowed cell no longer authorizes committing on the default branch; it is narrowed to the dedicated Stage/admin artifact branch.
- **AC-B3.2** The Git row Allowed cell ALSO grants creation and check-out of the dedicated Stage/admin artifact branch (`chore/stage-{shipment_id}`, or `chore/stage-{chore-slug}` when no shipment is formed), matching the P-010 grant added by 018.008-T. Without this, role enforcement — which treats this table as the authoritative permission set at mutation time — would fail closed on the branch creation the same policy requires.
- **AC-B3.3** The Git row Forbidden cell is unchanged, and every other Role Boundary row is unchanged EXCEPT the PR row, which is explicitly exempted here: it gains the required actor note that the Orchestrator (pipeline) or the operator (direct invocation) — never Stage — pushes the branch and opens/merges the staging PR. The note records actor attribution only; the PR row's prohibition is unweakened. This criterion governs the **Role Boundary table**; the Step 6 edit required by AC-B3.5 lies outside it.
- **AC-B3.4** The file agrees with the corrected P-010 text — no surviving contradiction between agent contract and policy, in either direction (no grant in one that the other forbids).
- **AC-B3.5** **Staging handback emitted (rev 6).** Step 6's session-output contract requires `stage_branch`, `stage_head_commit`, `stage_outcome`, `stage_artifact_paths`, and `shipment_id` (when formed) — every field the Orchestrator's Step 1 consumes under AC-B1.5, with none required there and absent here. This is the only change outside the Role Boundary table; no other section of the file is modified.
<!-- END:acceptance-criteria -->