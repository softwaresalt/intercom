---
artifact_type: task
created_at: 2026-09-13T05:17:14.2772933Z
custom_fields:
    size: M
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
id: 024.001-T
parent_id: 024-F
priority: critical
status: blocked
title: Implement TASK_ONLY_FINALIZE classifier guards and recovery
updated_at: 2026-09-13T08:03:12.3708373Z
---

DEFERRED GENERALIZED PLATFORM WORK - NOT EXECUTABLE, NO LIVE SHIPMENT. Governing decision: docs/decisions/2026-09-13-intercom-go-a-only-simplification-decision.md (SIMPLIFICATION_VALID). This task is status: blocked under blocked parent 024-F. Shipment 023-S is ARCHIVED and MUST NOT be routed or claimed. The statement that "the first live use of TASK_ONLY_FINALIZE is 017-S" is WITHDRAWN - 017-S closes by the EXISTING CASCADE exception and never uses it. All shipment-specific execution, closure, two-session activation, dependency and rollback instructions below are HISTORICAL and are NOT current instructions. Re-entry requires a fired trigger and a fresh, separately reviewed shipment.

Parent feature 024-F. BOOTSTRAP SHIPMENT C (SKILL IMPLEMENTATION). Plan rev 2: docs/plans/2026-09-12-intercom-go-taskonly-finalize-skill-implementation-plan.md. Deliberation: docs/decisions/2026-09-12-intercom-go-three-shipment-bootstrap-deliberation.md.
REV 2 - MANIFEST RE-SHAPED, CLOSE PATH CHANGED. 023-S is now the FULLY-COVERED ROOT [024.001-T, 024-F] and C closes by the EXISTING CASCADE exception, not by TASK_ONLY_FINALIZE. Forced by measurement: Probe 22 showed a task-only close leaves the out-of-manifest covering feature ACTIVE; Probe 23 showed no barrier is constructible (backlogit refuses a blocks edge whose prerequisite is a feature: "both endpoints must be shipments"); Probe 24 showed that with the feature INSIDE the manifest the cascade archives it (archived_ids includes the feature), leaving zero other active top-level units and 017-S eligible with no barrier and no Stage disposition. The first live use of TASK_ONLY_FINALIZE is 017-S.
TWO-SESSION CLOSURE (PRIMARY, NON-NEGOTIABLE). C's merge ACTIVATES the capability token, so the merging session MUST NOT use it. S1: implement, gates, Step 4.5 task->done, Step 5 PR + merge, Merge Confirmation Gate, mandatory pre-self-close reload, then WRITE CHECKPOINT AND END. S2 (fresh): full owner-selected operator-confirmed checkpoint recovery (enumerate with no status/agent filter, anomaly gate FIRST, explicit selection even for a single candidate, agent must be exactly ship, operator confirmation before restore, same cursor 023-S, resolve only AFTER confirmed resume, fail closed with no fresh-start fallback), re-verify current main + merge SHA ancestry + merged P-015/skill/Ship tokens, THEN classify and finalize. AC-14 is NOT satisfied if one session performs both.
EXACT FILE INVENTORY - 2 FILES: (1) .github/skills/shipment-reconcile/SKILL.md; (2) tests/integration/taskonly_finalize_contract_test.go. A SEPARATE test file is used so task isolation stays unambiguous and the H0 red count is trivially 1 of 1. New production code files: 0. No clause-inventory.md is produced (rev 2, AC-12) - plan section 3 is the single system of record.
CLAUSE INVENTORY - 16 SITES (9 MUST + 7 CONSISTENCY) + 2 ADDITIVE - C's exact share of the closed 28-site inventory. MUST: B1 (line 3), B3 (14-15), B5 (30-32), B8 (356-358), B9 (408-415 Step 0(c) classifier body), B10 (505-512 SELECTOR binary -> TERNARY), B13 (698-709 no-substitution rule must bind the THIRD verdict), B15 (1015-1016 safety invariants), B17 (1074-1075 - TWO adjacent reference lines: 1074 P-015 "cascade prohibition", and 1075 the stale src/autoharness/gates/shipment_closure.py reference which must be scoped; rev 2 corrects rev 1's single-line anchor). CONSISTENCY: B2 (10-11), B4 (28), B6 (232-233), B7 (324), B12 (618-620), B14 (998), B16 (1022). ADDITIVE: B11 (591-605 safe-close step 8 fail-closed cross-reference; step 8's own text otherwise UNCHANGED); B18 (NEW TASK_ONLY_FINALIZE section).
B18 CONTAINS: capability token in frontmatter; classifier (after CASCADE, before SAFE_CLOSE; any failure/ambiguity/query error = not classified = fall through); G0-G13 conjunctive fail-closed pre-call guards; P1-P10 post-call guards evaluated on the RAW POST-CALL STATE BEFORE ANY ARCHIVE RESTORATION; the two-path failure split; bounded recovery (enumerated-set-only restore, approval before any destructive step, byte-prefix append-only preservation, out-of-bounds paths REPORTED NOT MOVED, sync-only rehydration - Probe 19); digest binding; self-host ordering INCLUDING the token-activation rule (a session MUST NOT select a verdict whose authorizing token it merged). B18 CITES T1-T5 from P-015 and does NOT restate them.
ENGINE IDENTITY IS BINDING: backlogit 1.10.1-0.20260823032255-b07729386a31+dirty, SHA-256 1E106F5FD1E2D82E4F632AFEE40FD70B95DC7416886C3EBF266361F406959A98. +dirty is not a unique build identity so the DIGEST binds. Mismatch = HALT + full probe refresh. Invocation is CLI-ONLY.
TEST-FIRST: ONE table-driven Go test function, 13 ROWS (rev 2) proving TEXT, PARITY and TOKENS - row 2 EXACT TOKEN PARITY against P-015 (discharges CO-1), rows 10-12 negative widening guards, and NEW row 13 asserting the token-activation self-host ordering rule. Marker "not implemented: task-only finalize contract", red 1 of 1, go vet 0, go test non-zero; then H1 green.
DURABLE RUNTIME PROBES: 14, 15, 18, 19, 20 retained and reused. PROBE 16 IS WITHDRAWN AS EVIDENCE (superseded by Probe 19, which is the sole authority for bounded recovery; 16 is retained for history only). Rev-4.1 %TEMP% probes 5/5b/10/12/13 remain WITHDRAWN. NEW rev-2 probes 22 (parent status at claim), 23 (feature-dependency barrier not constructible), 24 (manifest re-shape remedy). PROBE 20 TRIPWIRE: the a0 --phase lifecycle gate runs WHILE THE SHIPMENT IS STILL ACTIVE; no lifecycle invocation may occur after the archive. PROBE 21 (activation) is RELOCATED to closure/runtime evidence and budgeted under SHIP CLOSURE, not task coding; its post half runs against the exact 017-S 12-member fixture.
LIFECYCLE ORDER (installed _ship.agent.md, not inferred): Step 4.5 (L517) precedes Step 5 (L554), so 024.001-T reaches done BEFORE the implementation PR merges. Step 6.0 (L726) creates the post-merge closure branch from fresh main BEFORE any post-merge backlog mutation, because 6.1(e) commits .backlogit/. Order within 6.1 is a0 -> a(pre) -> b(close) -> c(P-007) -> d(post) -> e(commit). operational-closure and P-020 land on the closure branch BEFORE the closure PR is pushed; sync (item 9) follows.
ACCEPTANCE: AC-1..AC-23 in plan section 11. AC-23: both Probe 21 halves must remove their scratch workspace and re-run backlogit sync with ZERO duplicate-source-id warnings BEFORE any live backlog read, mutation, or closure sync (leftover scratch is rehydrated by sync and collapses into the live index). NO OVERFLOW VALVE (rev 2 - REMOVED ENTIRELY). EVERY MUST and CONSISTENCY site is implemented; any overrun is HALT AND RETURN TO STAGE, never a partial Done, never a deferral.
BUDGET ~105 min, ~15 min margin - still the TIGHTEST of the three and still thinner than B18's own estimating variance, with no valve to hide behind. Rev-2 delta: B17 +1.5, B18 +1, Go test +1, committed inventory re-scan -5.
ROLLBACK: after activation, revert in C -> B -> A order ONLY. Reverting B while C is installed leaves the skill implementing a verdict P-015 does not authorize; reverting A while B/C are installed restores the stale binary restatement and the children-only drift. DOWNSTREAM SHIPMENTS MUST BE HELD FIRST: confirm no downstream shipment is active, explicitly hold 017-S (Stage action, recorded, BEFORE the revert), and re-plan before re-routing.
SIZING: Size M (STRUCTURED field). Complexity high (enum-validated PROSE - DEGRADED: this workspace's .backlogit/header-def.yaml defines no complexity field on the task type. See docs/compound/2026-09-04-backlogit-sizing-is-wit-gated.md).