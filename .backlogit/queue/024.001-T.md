---
artifact_type: task
created_at: 2026-09-13T05:17:14.2772933Z
custom_fields:
    size: M
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
id: 024.001-T
parent_id: 024-F
priority: critical
status: queued
title: Implement TASK_ONLY_FINALIZE classifier guards and recovery
updated_at: 2026-09-13T05:19:26.0570475Z
---

Parent feature 024-F. BOOTSTRAP SHIPMENT C (SKILL IMPLEMENTATION). Plan: docs/plans/2026-09-12-intercom-go-taskonly-finalize-skill-implementation-plan.md. Deliberation: docs/decisions/2026-09-12-intercom-go-three-shipment-bootstrap-deliberation.md.
EXACT FILE INVENTORY - 2 FILES: (1) .github/skills/shipment-reconcile/SKILL.md; (2) tests/integration/taskonly_finalize_contract_test.go. A SEPARATE test file is used (not an extension of an existing integration file) so task isolation stays unambiguous and the H0 red count is trivially 1 of 1. New production code files: 0.
CLAUSE INVENTORY - 16 SITES (9 MUST + 7 CONSISTENCY) + 2 ADDITIVE - C's exact share of the closed 28-site inventory. MUST: B1 (line 3 frontmatter sole-exception phrasing), B3 (14-15), B5 (30-32), B8 (356-358), B9 (408-415 Step 0(c) classifier body), B10 (505-512 Step 0 SELECTOR, binary -> TERNARY), B13 (698-709 no-substitution rule must bind the THIRD verdict), B15 (1015-1016 safety invariants), B17 (1074 references line). CONSISTENCY: B2 (10-11), B4 (28), B6 (232-233), B7 (324), B12 (618-620), B14 (998), B16 (1022). ADDITIVE: B11 (591-605 safe-close step 8 fail-closed cross-reference; step 8's own text otherwise UNCHANGED); B18 (NEW TASK_ONLY_FINALIZE section).
B17 CLASSIFICATION: rev 6 classes B17 CONSISTENCY in its table but budgets it as MUST-mechanical in section 10.1. This task follows section 10.1 (MUST) - the stricter reading.
B18 CONTAINS: capability token in frontmatter; classifier (evaluated after CASCADE, before SAFE_CLOSE; any failure/ambiguity/query error = not classified = fall through); G0-G13 conjunctive fail-closed pre-call guards; P1-P10 post-call guards evaluated on the RAW POST-CALL STATE BEFORE ANY ARCHIVE RESTORATION (P-007 ordering, umbrella section 8.2); the two-path failure split; bounded recovery (enumerated-set-only restore, approval before any destructive step, byte-prefix append-only preservation, out-of-bounds paths REPORTED NOT MOVED, sync-only rehydration - Probe 19); digest binding; self-host ordering. B18 CITES T1-T5 from P-015 and does NOT restate them as its own authority.
ENGINE IDENTITY IS BINDING, NOT ADVISORY: backlogit 1.10.1-0.20260823032255-b07729386a31+dirty, SHA-256 1E106F5FD1E2D82E4F632AFEE40FD70B95DC7416886C3EBF266361F406959A98. The +dirty version string is NOT a unique build identity, so the DIGEST binds. Mismatch = HALT + full probe refresh; the rev-4.1 advisory fallback is WITHDRAWN. Invocation is CLI-ONLY; no MCP equivalence is claimed.
TEST-FIRST: ONE table-driven Go test function, 12 rows proving TEXT, PARITY and TOKENS - including row 2 EXACT TOKEN PARITY against P-015 (discharges CO-1 from C's side) and 3 negative widening guards. It does NOT prove runtime behavior; that is the durable probe set. Marker "not implemented: task-only finalize contract", red 1 of 1, go vet 0, go test non-zero; then H1 green.
DURABLE RUNTIME PROBES (committed, reproducible): Probes 14, 15, 16, 18, 19, 20 retained and reused from the umbrella evidence directory; rev-4.1 %TEMP%-and-deleted probes 5/5b/10/12/13 remain WITHDRAWN. NEW Probe 21 (activation): pre-merge, token absent = classifier does NOT select TASK_ONLY_FINALIZE; the post half is discharged FREE by C's own closure. Plus the section 6.1-E clause-inventory re-scan committed to the evidence directory.
ACCEPTANCE: AC-1..AC-18 in plan section 11. AC-12: a site found by the re-scan and absent from the inventory is ADDED NEVER SKIPPED; a new MUST site INVALIDATES the budget and returns the unit to Stage.
BUDGET ~106.5 min - the TIGHTEST of the three, ~13.5 min margin, smaller than B18's own estimating variance. EXECUTION ORDER IS MANDATORY: the 7 CONSISTENCY edits run LAST, after B18 and after the Go test. OVERFLOW VALVE (plan section 12.1): at 100 min, defer ONLY the 5-clause deferrable subset B2/B4/B6/B14/B16 (~7.5 min, all branch-local and still literally true). B7 and B12 are NOT DEFERRABLE - they assert sufficiency and exclusivity, which the amendment falsifies. The valve CANNOT rescue a B18 or Go-test overrun; that case is HALT and return to Stage. Deferring any MUST site is NOT authorized.
ROLLBACK: after activation, revert in C -> B -> A order ONLY. Reverting B while C is installed leaves the skill implementing a verdict P-015 does not authorize; reverting A while B/C are installed restores the stale binary restatement and the children-only drift.
SIZING: Size M (STRUCTURED field). Complexity high (enum-validated PROSE - DEGRADED: this workspace's .backlogit/header-def.yaml defines no complexity field on the task type; 'backlogit update --complexity high' fails validation. See docs/compound/2026-09-04-backlogit-sizing-is-wit-gated.md). High complexity reflects that C is the ONLY shipment of the three that changes runtime behavior.