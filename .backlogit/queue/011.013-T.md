---
artifact_type: task
created_at: 2026-09-06T23:59:21.1664778Z
custom_fields:
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
id: 011.013-T
parent_id: 011-F
priority: medium
status: queued
title: 'E: Pin the installer toolchain, not just the installed closure'
updated_at: 2026-09-07T00:01:15.1791945Z
---

Resolves AFE0E95C. .github/constraints/autoharness-lock.txt (145 lines) pins autoharness and its transitive closure, but pip/setuptools/wheel and the Python patch version are ambient on the runner. Prior learning docs/compound/2026-09-05-pip-index-proxy-staleness-vs-real-ci.md is load-bearing. Approach: configuration. Width: CI config. AC1 installer versions hash-pinned from the PUBLIC PyPI JSON API, never a local index/proxy (the recorded prior failure was a proxy reporting a stale version and silently downgrading CI). AC2 install uses --require-hashes WITH --only-binary=:all: to foreclose the unhashed-sdist PEP 517 fallback. AC3 BOOTSTRAP ORDERING - the pip/setuptools/wheel install itself runs with --require-hashes --only-binary=:all: --no-deps from its own lock file and executes BEFORE the autoharness install, otherwise the hardened installer is not the one performing the hardened install. AC4 PYTHON PIN COUPLING RECORDED - autoharness-lock.txt was resolved for cp312/manylinux_2_17_x86_64, so the setup-python patch pin must stay within 3.12 or every hash becomes unsatisfiable; the coupling is noted next to both pins. AC5 the trust root is stated explicitly: pinning pip using the runner's preinstalled pip is irreducibly trust-on-first-use. AC6 verified via a workflow_dispatch or draft-PR run ON THE IMPLEMENTATION BRANCH before the pin is relied upon (no parallel branch - P-016). AC7 the existing autoharness install continues to succeed.

<!-- BEGIN:description -->
Resolves AFE0E95C. .github/constraints/autoharness-lock.txt (145 lines) pins autoharness and its transitive closure, but pip/setuptools/wheel and the Python patch version are ambient on the runner. Prior learning docs/compound/2026-09-05-pip-index-proxy-staleness-vs-real-ci.md is load-bearing. Approach: configuration. Width: CI config. AC1 installer versions hash-pinned from the PUBLIC PyPI JSON API, never a local index/proxy (the recorded prior failure was a proxy reporting a stale version and silently downgrading CI). AC2 install uses --require-hashes WITH --only-binary=:all: to foreclose the unhashed-sdist PEP 517 fallback. AC3 BOOTSTRAP ORDERING - the pip/setuptools/wheel install itself runs with --require-hashes --only-binary=:all: --no-deps from its own lock file and executes BEFORE the autoharness install, otherwise the hardened installer is not the one performing the hardened install. AC4 PYTHON PIN COUPLING RECORDED - autoharness-lock.txt was resolved for cp312/manylinux_2_17_x86_64, so the setup-python patch pin must stay within 3.12 or every hash becomes unsatisfiable; the coupling is noted next to both pins. AC5 the trust root is stated explicitly: pinning pip using the runner's preinstalled pip is irreducibly trust-on-first-use. AC6 verified via a workflow_dispatch or draft-PR run ON THE IMPLEMENTATION BRANCH before the pin is relied upon (no parallel branch - P-016). AC7 the existing autoharness install continues to succeed.

SIZING (two independent axes): Size: M | Complexity: medium. NOTE: 'size' is stored as a structured field; 'complexity' is recorded as prose because this workspace's WIT does not define a complexity field for artifact type 'task' (backlogit validation error) - a documented, flagged degradation, not an omission.
<!-- END:description -->