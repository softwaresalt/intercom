---
artifact_type: task
created_at: 2026-09-11T17:23:18.6085075Z
custom_fields:
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
id: 018.008-T
labels:
    - policy
    - chore
    - governance
    - reduced-scope
links:
    - target_id: 019.007-T
      link_type: related_to
parent_id: 018-F
priority: high
status: queued
title: Gate Stage artifact mutation behind a dedicated branch
updated_at: 2026-09-12T19:34:49.3744932Z
---

Parent feature 018-F. SINGLE executable task of the reduced release unit (2026-09-12 re-plan). Closes the PROHIBITION half of the Stage default-branch policy gap: Stage must not commit on the default branch, and a dedicated Stage artifact branch must exist before any tracked artifact write. Absorbs the current-scope items of the retired sub-epic-B split - the P-010 correction formerly scoped here, plus items (1)(2)(3) and (7a) of 018.009-T (now 019.007-T, whose remaining items are deferred to 019-F).

SURFACE 1 - .github/policies/workflow-policies.md (P-010): replace the Stage MAY bullet phrase "on the default branch or a dedicated chore/admin branch" with a dedicated-branch-only grant; add an ACTOR-NAMED Stage MUST NOT bullet forbidding direct commit or push to the default branch, mirroring the existing Ship MUST NOT bullet "Commit or push directly to main" so the two columns become symmetric; add an explicit Stage MAY grant to create and check out chore/stage-{scope-slug}; add the clarifier that Step 1.5 staging orchestration is an Orchestrator-owned gate and that a Stage artifact branch/PR carries no source, test, or config change and is therefore not an implementation branch under P-016 nor a release unit under P-001. Append Amendment Log entry 1.25.0. LOCATOR NOTE: identify the Ship mirror bullet by its QUOTED TEXT, never by line ordinal.

SURFACE 2 - .github/agents/_stage.agent.md Role Boundary: narrow the Git row Allowed cell from "Commit backlog/planning artifacts on default or admin branch" to the dedicated Stage artifact branch only; in the SAME cell add the create/checkout grant for chore/stage-{scope-slug} (a shipment ID MAY be used as the slug when already known but is NEVER required, because the branch must exist before Stage's first artifact write while Step 5.5 creates the shipment as the session's LAST mutation); add an actor note on the PR row recording that the Orchestrator (pipeline invocation) or the operator (direct invocation) - never Stage - pushes the branch and opens/merges the staging PR. The PR row prohibition itself is UNWEAKENED.

SURFACE 3 - .github/agents/_stage.agent.md Step 1.9 (NEW SECTION): insert "Step 1.9: Stage Artifact Branch Gate (NON-NEGOTIABLE)" strictly between Step 1.8 (Learnings Retrieval) and Step 2 (Deliberation). That fixed position is the first point at which a stable scope slug is derivable for both intake shapes - Step 1 classification settles the feature-shaped case, Step 1.5 grouping selection the task-shaped one. CATEGORICAL DEFERRAL RULE: this gate precedes EVERY tracked Stage artifact mutation of the session WITHOUT EXCEPTION, and any such mutation otherwise scheduled earlier is DEFERRED until after it. The rule is categorical; the following are ILLUSTRATIVE, not a closed list - (i) the WRITE half of the Step 1 late-identifier reconciliation (its search/analysis half is read-only and stays at Step 1), and (ii) the ARCHIVAL half of the Step 1 unconditional duplicate-detection scan (its detection half is read-only and stays at Step 1).

WHY SURFACE 3 IS LOAD-BEARING: Surfaces 1-2 establish what Stage MAY do; neither is an instruction to DO it. The installed _stage.agent.md contains NO branch operation and NO commit operation at any step, so an executor satisfying only Surfaces 1-2 can finish the entire session on the default branch. Surface 3 is what makes the grant operative.

OUT OF SCOPE - DEFERRED TO 019-F: handback record emission, stage_artifact_paths derivation, no-shipment terminal reachability, the Stage artifact commit step, out-of-root path safety, the Orchestrator Step 1.5 branch-discovery and post-merge verification arms, the generalized direct-push detector, the fixture corpus and harness-state manifest, CI wiring, and the MP-series mutation proofs. This task carries NONE of that machinery.

<!-- BEGIN:acceptance-criteria -->
- AC-1: P-010's Stage MAY bullet grants artifact commit ONLY on a dedicated chore/stage-* branch; no default-branch commit grant remains anywhere in P-010.
- AC-2: P-010 carries an actor-named Stage MUST NOT bullet forbidding direct commit or push to the default branch, and an explicit Stage MAY grant to create and check out chore/stage-{scope-slug}.
- AC-3: P-010 carries the Step 1.5 / P-016 / P-001 clarifying sentence, and the Amendment Log contains a 1.25.0 entry describing this change.
- AC-4: _stage.agent.md Role Boundary Git row Allowed cell names ONLY the dedicated Stage artifact branch and includes the create/checkout grant; the Git row Forbidden cell is unchanged; the PR row carries the actor note and its prohibition is unweakened.
- AC-5: _stage.agent.md contains a "Step 1.9: Stage Artifact Branch Gate (NON-NEGOTIABLE)" section positioned strictly after Step 1.8 and strictly before Step 2, stating the categorical deferral rule and naming both illustrative deferred write halves.
- AC-6: TestStageBranchGate_ContractCorrected passes; go vet ./... exits 0; go test ./... is fully green with no skipped, pending, or conditionally-activated assertions anywhere in the suite.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
SIZING (two independent axes): Size M (structured field). Complexity medium (enum-validated PROSE).
DEGRADATION NOTE - VERIFIED 2026-09-12, NOT ASSUMED: backlogit 1.10.1 exposes a --complexity flag and the backlog registry declares a complexity param, but this workspace's .backlogit/header-def.yaml defines NO complexity field on the task artifact type, so "backlogit update <id> --complexity medium" FAILS with: artifact type "task" does not define a complexity field. The 018-F SIZING NOTE is therefore CORRECT and stands. Adding the field would mutate .backlogit/header-def.yaml, a tooling CONFIG file outside Stage's role boundary, so it is NOT done here. REGISTRY/SCHEMA MISMATCH is recorded as a finding for the operator; it is a reporting defect only and does not affect execution.
2-HOUR RULE: 3 contract/test files (.github/policies/workflow-policies.md, .github/agents/_stage.agent.md, tests/integration/stage_branch_gate_test.go plus its panic stub); edits are pre-specified textual contract corrections; one test scenario group. Single skill domain (policy/contract text) - no code, schema, or CI domain mixed in.
HARNESS (P-002/P-004): harness-architect at Ship Step 2 generates tests/integration/stage_branch_gate_test.go containing the SINGLE function TestStageBranchGate_ContractCorrected, which calls a production stub whose body is panic("not implemented: stage branch gate contract"). Red phase = that one function failing on that one marker. Posture: characterization-first over the contract corpus (read the installed markdown, assert the corrected properties).
harness_cmd: go test ./tests/integration/ -run TestStageBranchGate_ContractCorrected
SINGLE-TASK LIFECYCLE - LOAD-BEARING, DO NOT WIDEN: feature 018-F has EXACTLY ONE queued task, so Ship Step 2 generates EXACTLY ONE red function, this task's completion turns it green, and Ship Step 4.3's full "go test ./..." is green immediately afterwards. This is the ONLY configuration that satisfies P-004 (every test function red before implementation) and Ship Step 4.3 (full suite green after EVERY task) simultaneously. Therefore NO activation manifest, terminal witness, selector flag, surface predicate, or MP-series check is required or permitted here. Adding a SECOND queued task under 018-F re-creates the Step 4.3 deadlock that blocked revs 10-15 and is FORBIDDEN in this release unit.
TRACEABILITY: supersedes the current-scope half of 018.009-T (adopted to 019.007-T). The deferred half is tracked under 019-F.
<!-- END:implementation-notes -->