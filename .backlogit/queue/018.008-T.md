---
artifact_type: task
created_at: 2026-09-11T17:23:18.6085075Z
custom_fields:
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 018.006-T
id: 018.008-T
labels:
    - sub-epic-b
    - policy
    - chore
parent_id: 018-F
priority: high
status: queued
title: Correct P-010 Stage branch/PR grants and log 1.25.0
updated_at: 2026-09-12T01:15:00.0000000Z
---

Parent feature 018-F. Sub-epic B, plan section 6.2 of docs/plans/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-plan.md. Correct P-010 in .github/policies/workflow-policies.md. Replace the Stage MAY bullet's 'on the default branch or a dedicated chore/admin branch' with a dedicated-branch-only grant; add an ACTOR-NAMED Stage MUST NOT bullet forbidding direct commit or push to the default branch, MIRRORING the existing Ship MUST NOT bullet 'Commit or push directly to `main`' so the two columns become symmetric, and resolving the rev-1 self-deadlock (Stage commits on the branch; the ORCHESTRATOR - or the operator, in a direct Stage invocation - pushes the branch and opens and merges the PR, since Stage must not create, push, or merge PRs); add an explicit Stage MAY grant to create and check out the dedicated artifact branch (chore/stage-{shipment_id}, or chore/stage-{chore-slug} when no shipment is formed, per P-011's existing slug convention); add the clarifying sentence that Step 1.5 staging orchestration is an Orchestrator-owned gate rather than delegated Ship work, AND that a Stage artifact branch/PR carries no source, test, or config change and is therefore not an implementation branch under P-016 nor a release unit under P-001. Append Amendment Log entry 1.25.0.
LOCATOR NOTE: the Ship mirror bullet is identified by its QUOTED TEXT ('Commit or push directly to `main`' under the Ship MUST NOT lead-in), never by line ordinal - line numbers in this file shift with every amendment, and this task appends one.
HARNESS (P-002/P-004): harness function TestDirectPushGate_PolicySurfaceClean in tests/integration/directpush_gate_test.go, produced by harness-architect at Ship Step 2 per plan section 5.0. harness_cmd: go test ./tests/integration -run TestDirectPushGate_PolicySurfaceClean
ACCEPTANCE CRITERIA (PLAIN-TEXT RENDERING - the authoritative text is the acceptance-criteria section below, which is plan section 6.2 VERBATIM; if the two disagree, that section wins and this rendering is the defect):
AC-B2.1 No P-010 text authorizes Stage to commit or push on the default branch - no default-branch allowance remains anywhere in P-010's Stage block.
AC-B2.2 The rule holds explicitly even when NO shipment is formed (chore/stage-{chore-slug} form).
AC-B2.3 P-010's Stage and Ship columns are SYMMETRIC on direct default-branch writes: the Stage block carries a prohibition mirroring the existing Ship MUST NOT bullet 'Commit or push directly to `main`'.
AC-B2.4 No actor is both PRESCRIBED and PROHIBITED on the same operation (no self-deadlock): every obligation the new bullet creates names an authorized actor, with pushing the branch and opening/merging the PR attributed to the Orchestrator (pipeline invocation) or the operator (direct Stage invocation) - never to Stage, whose standing PR prohibition is unweakened.
AC-B2.5 The P-016/P-001 non-implementation clarification is present in the POLICY TEXT, not only in the plan: a Stage artifact branch/PR carries no source, test, or config change and is therefore neither an implementation branch under P-016 nor a release unit under P-001.
AC-B2.6 The Amendment Log gains EXACTLY ONE additive row, version 1.25.0, following the existing format; 1.24.0 and every earlier row are unmodified.

<!-- BEGIN:implementation-notes -->
DEGRADATION NOTE: this workspace's backlogit type schema defines 'size' but NO 'complexity' field on any artifact type (verified via backlogit metadata types: task fields = priority, size, status), despite backlog-registry.yaml declaring features.sizing: true. Complexity is therefore recorded below as enum-validated prose per the Stage two-axis sizing contract. Size is stored structurally (size_source=agent, size_ruleset_version=2h-rule-v1).

Size: S | Complexity: medium

Rationale: Policy wording must avoid reinstating the rev-1 self-deadlock; actor attribution (Orchestrator or operator pushes and opens/merges the PR) is the load-bearing detail. Size UNCHANGED at rev 8 - the reconciliation added no edit site, only the two criteria the other surface already required.

REV-6 NOTE (no AC change): plan section 6.2's no-shipment route table names the Orchestrator as the actor for pushing, opening, merging, and verifying the staging branch, but not how that actor LEARNS the branch name. That handoff is specified in 018.007-T (Orchestrator Step 1 staging handback record) and produced in 018.009-T (Stage Step 6 emission). P-010's text is UNCHANGED by rev 6 - the handback is a mechanism, not a permission - so this task's scope and acceptance criteria are unchanged by it.

REV-8 RECONCILIATION (PR #54 adversarial review, P1 blocker 4): plan section 6.2 and this task carried TWO DIVERGENT FIVE-CRITERION LISTS while both claimed to be one contract. The plan asserted Stage/Ship SYMMETRY and omitted the P-016/P-001 clarification; this task asserted the CLARIFICATION and omitted symmetry; and the shared IDs AC-B2.3, AC-B2.4 and AC-B2.5 were bound to DIFFERENT criteria on each surface - so an executor satisfying one list would have failed the other, and an ID-level parity check comparing counts alone would have reported agreement. The union is SIX criteria. Both surfaces now carry AC-B2.1..AC-B2.6 verbatim, in the same order, with the same IDs; exact parity re-run. The task DESCRIPTION additionally gains the mirror-bullet instruction (AC-B2.3's implementation) and the actor attribution for the operator case (AC-B2.4), both of which the plan already specified but this description did not. No new edit site, no scope change, size unchanged.

HARNESS BINDING (plan section 5.0): this task does NOT carry a harness-ready label. harness-architect applies that label at Ship Step 2 after producing the Go harness and confirming go vet ./... exits 0 and go test ./... is red. Stage does not forge P-004's postcondition.
<!-- END:implementation-notes -->

<!-- BEGIN:acceptance-criteria -->
- **AC-B2.1** No P-010 text authorizes Stage to commit or push on the default branch — no default-branch allowance remains anywhere in P-010's Stage block.
- **AC-B2.2** The rule holds explicitly **even when no shipment is formed** (`chore/stage-{chore-slug}` form).
- **AC-B2.3** P-010's **Stage and Ship columns are symmetric** on direct default-branch writes: the Stage block carries a prohibition mirroring the existing **Ship MUST NOT** bullet "Commit or push directly to `main`".
- **AC-B2.4** **No actor is both prescribed and prohibited** on the same operation (no self-deadlock): every obligation the new bullet creates names an authorized actor, with pushing the branch and opening/merging the PR attributed to the **Orchestrator** (pipeline invocation) or the **operator** (direct Stage invocation) — never to Stage, whose standing PR prohibition is unweakened.
- **AC-B2.5** The **P-016/P-001 non-implementation clarification** is present in the **policy text**, not only in this plan: a Stage artifact branch/PR carries no source, test, or config change and is therefore neither an implementation branch under P-016 nor a release unit under P-001.
- **AC-B2.6** The Amendment Log gains **exactly one additive row, version 1.25.0**, following the existing format; 1.24.0 and every earlier row are unmodified.
<!-- END:acceptance-criteria -->
