---
artifact_type: task
created_at: 2026-09-09T01:25:15.092169Z
custom_fields:
    size: S
    size_ruleset_version: autoharness-2h-v1
    size_source: agent
dependencies:
    - 015.001-T
id: 015.011-T
labels:
    - 015-D
    - stage-014-s
parent_id: 015-F
priority: high
references:
    - docs/plans/2026-09-08-intercom-go-retired-arch-gate-detection-quality-plan.md
status: queued
title: Add repo selection-guard self-test assertions
updated_at: 2026-09-09T01:26:11.5172201Z
---

Add selection-guard assertions in run_repo_selection_self_test (one seam, one domain). cmd/ (AG-5, D4): should_scan_repo_path(cmd/x/y_test.go) is True and should_scan_repo_path(cmd/x/testdata/z.go) is True, plus at least 1 tracked cmd/**/*_test.go in the real select_repo_paths(); failure message names AG-5/D4 and states the assertion guards a deliberate coverage decision. AC-6 pathspec pin (AG-1): assert the literal git ls-files pathspec and the should_scan_repo_path prefix set by READING scripts/check-retired-architecture.sh FROM DISK, then extracting the select_repo_paths and should_scan_repo_path FUNCTION REGIONS and matching the literal only inside those regions - a whole-file containment check is vacuous because the asserted literal appears in the very file being read, and a module-constant self-comparison is self-referential (the heredoc is fed on stdin so __file__ and inspect.getsource() are unavailable). Region extraction MUST anchor on the FIRST occurrence of each def <name>( because the anchors recur later inside the assertion itself; extraction failure MUST fail closed. Covers 8D603E29 + AG-1.

<!-- BEGIN:implementation-notes -->
Size: S | Complexity: medium

DEGRADED FIELD NOTE: this workspace's 'task' type schema defines only priority/size/status, so 'complexity' has no structured field and is recorded here as enum-validated prose (trivial|low|medium|high) per the Stage two-axis sizing contract. Size IS structured (size=S, size_source=agent, size_ruleset_version=autoharness-2h-v1). Both axes are independent: size=effort/volume, complexity=difficulty/uncertainty.
<!-- END:implementation-notes -->