---
artifact_type: task
created_at: 2026-09-11T17:22:50.2055487Z
custom_fields:
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 018.004-T
id: 018.005-T
labels:
    - sub-epic-a
    - test-harness
    - chore
parent_id: 018-F
priority: high
status: queued
title: Implement fixture-suite driver over stubbed detector
updated_at: 2026-09-12T02:45:00.0000000Z
---

Parent feature 018-F. Sub-epic A, plan section 5.5 of docs/plans/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-plan.md. Replace the H0 stub's mode dispatch in scripts/check-direct-push-language.sh with the real DRIVER: mode dispatch (default scan, --self-test), fixture/manifest loading, bijection and prefix-agreement assertions, house-style exit codes 0/1/2 and ::notice:: output matching the existing scripts/check-*.sh contract. The DETECTOR REMAINS STUBBED - narrowed from the H0 'not implemented' stub to one classifying every construct 'accept' - so the reject fixtures are observed failing. 018.006-T replaces it. This task owns the driver, so it also owns the EXECUTED non-vacuity proof of the bijection and prefix-agreement assertions (AC-A2.4), which 018.004-T cannot perform because it authors fixture data only.
SCOPE CHANGE (rev 4): this task no longer CREATES the harness. The Go harness (tests/integration/directpush_gate_test.go) and the not-implemented stub script are H0 deliverables produced by harness-architect at Ship Step 2 per plan section 5.0, because Step 2 runs once up front for the whole queue - a task whose content is 'create the harness' is structurally unreachable in Ship's flow.
HARNESS (P-002/P-004): harness function TestDirectPushGate_SelfTestDriverEnumeratesFixtures, which asserts the DURABLE driver contract (--self-test enumerates every manifest fixture by name, emits a per-fixture verdict, and runs fixtures before the real-tree scan) and NOT the transient all-accept-stub behaviour, so it stays green after 018.006-T installs the real detector (plan section 5.0.1). harness_cmd: go test ./tests/integration -run '^TestDirectPushGate_SelfTestDriverEnumeratesFixtures$' -gatetask=A2
ACCEPTANCE CRITERIA:
AC-A2.1 Every reject fixture is reported FAILING against the all-accept stub, by name, and --self-test exits NON-ZERO - the observed RED for the detector itself. REV-8 SCOPE CORRECTION: the non-zero requirement binds --self-test ONLY. Bare-scan exit is deliberately NOT constrained here: this task's stub classifies every construct 'accept', so a bare scan correctly reports zero findings and exits 0. Bare-mode red is 018.006-T's obligation (AC-A3.3), once a real detector exists. REV-10 CLARIFICATION: this criterion is verified from the SCRIPT'S OWN --self-test OUTPUT at this task's boundary, and is deliberately NOT encoded as a permanent assertion in TestDirectPushGate_SelfTestDriverEnumeratesFixtures - a Go assertion that the detector is still a stub would go RED the moment 018.006-T replaces it and would wedge go test ./... for the rest of the shipment (plan section 5.0.1). That harness function asserts the DURABLE DRIVER CONTRACT instead: --self-test ENUMERATES EVERY MANIFEST FIXTURE BY NAME, emits a per-fixture verdict, and runs fixtures before the real-tree scan. The criterion itself is unchanged and unweakened; only its carrier is named.
AC-A2.2 --self-test runs fixtures BEFORE any real-tree scan, so a broken checker reports 'self-test failed' rather than a false violation.
AC-A2.3 git diff --exit-code -- .github/workflows/ci.yml is clean (no CI change in this task).
AC-A2.4 Bijection and filename-prefix/manifest-verdict agreement assertions each FAIL when deliberately violated - non-vacuity proven by EXECUTION of this driver, not asserted. The proof is performed against a TEMPORARY FIXTURE COPY under t.TempDir(), NEVER by editing the committed fixture tree (same no-dirty-tree-edit rule as AC-C2.2); git status --porcelain is empty before and after.
AC-A2.5 TWO SEPARATE, EXACTLY-ANCHORED INVOCATIONS (rev 10). A single driver-scoped -run command cannot establish anything about a test it does not run, so the evidence is split: (1) go test ./tests/integration -run '^TestDirectPushGate_SelfTestDriverEnumeratesFixtures$' -gatetask=A2 exits 0; and (2) go test ./tests/integration -run '^TestDirectPushGate_SelfTestPasses$' -gatetask=A3 exits NON-ZERO and its output carries '--- FAIL: TestDirectPushGate_SelfTestPasses' together with the expected DETECTOR-STUB reason (this task's stub classifies every construct 'accept', so the reject fixtures' actual verdicts disagree with the manifest). An ARBITRARY failure does NOT satisfy (2): a compile error, an absent bash, a missing fixture corpus, and a mis-typed -gatetask value are harness defects, not the observed red, and each is rejected by name. Both regexes are ANCHORED. The '--- FAIL:' requirement is the NON-VACUITY GUARD and is load-bearing: go test -run with a pattern matching no test prints 'ok ... [no tests to run]' and exits 0, so 'exits non-zero' alone proves nothing. Bare-mode red remains 018.006-T's obligation (AC-A3.3) and is NOT asserted here; P-004 is not weakened in either direction - this criterion adds an observation and removes none.

<!-- BEGIN:implementation-notes -->
DEGRADATION NOTE: this workspace's backlogit type schema defines 'size' but NO 'complexity' field on any artifact type (verified via backlogit metadata types: task fields = priority, size, status), despite backlog-registry.yaml declaring features.sizing: true. Complexity is therefore recorded below as enum-validated prose per the Stage two-axis sizing contract. Size is stored structurally (size_source=agent, size_ruleset_version=2h-rule-v1).

Size: S | Complexity: medium

Rationale: Driver mirrors an existing scripts/check-*.sh scaffold, but the deliberate-stub arrangement, the self-test-before-scan ordering, and the inherited AC-A2.4 non-vacuity proof need care.

HARNESS BINDING (rev 4, plan section 5.0): this task does NOT carry a harness-ready label. harness-architect applies that label at Ship Step 2 after producing the Go harness and confirming go vet ./... exits 0 and go test ./... is red. Stage does not forge P-004's postcondition.

REV-10 RECONCILIATION (PR #54 second adversarial review, finding 4): AC-A2.5 previously asserted, from ONE driver-scoped -run invocation, that a DIFFERENT test was still red. That invocation does not run the other test, so the claim was unprovable by the command that was supposed to prove it. It is split into two exactly-anchored invocations with an explicit '--- FAIL:' non-vacuity guard, because a go test -run pattern that matches nothing exits 0 with '[no tests to run]'. Also (finding 5): harness_cmd gains the -gatetask=A2 activation selector introduced by plan section 5.0.1, which is what makes this task's red observable at claim time now that the default suite skips a task whose surface does not yet exist. LATENT DEFECT FOUND WHILE DESIGNING THAT MECHANISM, recorded rather than silently repaired: this task's harness function was specified to assert that --self-test reports every reject fixture FAILING AGAINST THE ALL-ACCEPT STUB with a non-zero exit - a property true ONLY while this task's stub is the current implementation and FALSE the moment 018.006-T installs the real detector. The function would have gone red at 018.006-T and stayed red, wedging go test ./... for the rest of the shipment. It now asserts the DURABLE driver contract; AC-A2.1 keeps the stub-specific observations, read from the script's own output at this task's boundary. AC-A2.1 IS NOT WEAKENED - it still demands the observed red for the detector, still binds --self-test only, and still reserves bare-mode red for AC-A3.3; only its carrier is named. Plan sections 5.0, 5.0.1 and 5.5 carry identical text from the same change, so the plan<->task same-contract invariant holds. Size and complexity unchanged - the driver's scope is untouched; only its completion evidence is made provable.
<!-- END:implementation-notes -->

<!-- BEGIN:acceptance-criteria -->
- **AC-A2.1** Every reject fixture is reported FAILING against the all-accept stub detector, by name, and **`--self-test` exits non-zero** — the observed RED for the detector itself. **Rev 8 scope correction**: the non-zero requirement binds **`--self-test` only**. Bare-scan exit is deliberately **not** constrained here, because this task's stub classifies every construct `accept`, so a bare scan correctly reports zero findings and exits **0** — demanding red there would make this criterion unsatisfiable by a correct A2 implementation, or satisfiable only by a stub that lies about the real tree. Bare-mode red is 018.006-T's obligation (**AC-A3.3**), and the 018.005-T -> 018.006-T dependency edge guarantees it is evaluated after this one. **Rev 10 clarification**: this criterion is verified from the **script's own --self-test output** at this task's boundary, and is deliberately **not** encoded as a permanent assertion in `TestDirectPushGate_SelfTestDriverEnumeratesFixtures` — a Go assertion that the detector is still a stub would go **red the moment 018.006-T replaces it** and would wedge `go test ./...` for the rest of the shipment (plan §5.0.1). That harness function asserts the **durable driver contract** instead: `--self-test` enumerates every manifest fixture **by name**, emits a per-fixture verdict, and runs fixtures before the real-tree scan. The criterion itself is unchanged and unweakened; only its carrier is named.
- **AC-A2.2** `--self-test` runs fixtures BEFORE any real-tree scan, so a broken checker reports `self-test failed` rather than a false violation.
- **AC-A2.3** `git diff --exit-code -- .github/workflows/ci.yml` is clean (no CI change in this task).
- **AC-A2.4** Bijection and filename-prefix/manifest-verdict agreement assertions each FAIL when deliberately violated — non-vacuity proven by EXECUTION of this driver, not asserted. The proof is performed against a **temporary fixture copy under `t.TempDir()`**, never by editing the committed fixture tree (same no-dirty-tree-edit rule as AC-C2.2); `git status --porcelain` is empty before and after. (Relocated from 018.004-T, which owns fixture data only and cannot execute an assertion; the 018.004-T -> 018.005-T dependency edge already guarantees the fixtures exist first.)
- **AC-A2.5** **Two separate, exactly-anchored invocations (rev 10).** A single driver-scoped `-run` command cannot establish anything about a test it does not run, so the evidence is split:
  1. `go test ./tests/integration -run '^TestDirectPushGate_SelfTestDriverEnumeratesFixtures$' -gatetask=A2` exits **0**.
  2. `go test ./tests/integration -run '^TestDirectPushGate_SelfTestPasses$' -gatetask=A3` exits **non-zero**, **and** its output carries `--- FAIL: TestDirectPushGate_SelfTestPasses` together with the expected **detector-stub** reason — this task's stub classifies every construct `accept`, so the reject fixtures' actual verdicts disagree with the manifest. An arbitrary failure does **not** satisfy this: a compile error, an absent `bash`, a missing fixture corpus, or a mis-typed `-gatetask` value is a harness defect, not the observed red, and each is rejected by name.

  Both regexes are **anchored** (`^…$`). The `--- FAIL:` requirement is the non-vacuity guard and is load-bearing: `go test -run` with a pattern matching no test prints `ok … [no tests to run]` and exits **0**, so "exits non-zero" alone could be satisfied — or silently unsatisfiable — by a pattern that selects nothing. **Bare-mode red stays 018.006-T's obligation** (AC-A3.3) and is not asserted here; P-004 is not weakened in either direction — this criterion adds an observation, it removes none.
<!-- END:acceptance-criteria -->