---
artifact_type: feature
created_at: 2026-09-12T19:22:10.5699766Z
custom_fields:
    harness_status: pending
id: 019-F
labels:
    - chore
    - policy
    - ci
    - governance
    - deferred
links:
    - link_type: related_to
      target_id: 018-F
priority: high
references:
    - docs/plans/2026-09-12-intercom-go-stage-persistence-enforcement-plan.md
    - docs/decisions/2026-09-12-intercom-go-stage-policy-gap-re-plan-decision.md
status: queued
title: Stage persistence route execution and mechanical policy enforcement
updated_at: 2026-09-12T19:37:00.863875Z
---

DEFERRED companion to 018-F. Carries the separable verification-platform and end-to-end persistence-route work removed from 018-F/017-S during the 2026-09-12 re-plan. 018-F is reduced to the PROHIBITION half of the Stage default-branch policy gap (Stage may not mutate the default branch; a dedicated artifact branch must exist before any tracked write). THIS feature carries the EXECUTION half plus the mechanical enforcement platform. Depends on 018-F: the corrected contract must exist before a regeneration-resistant detector can assert on it. NOT part of the current single-shipment execution scope.

<!-- BEGIN:goals -->
DEFERRED FEATURE - created by the 2026-09-12 re-plan of 018-F/017-S. Shipment: 018-S (queued, NOT claimed in the current run).

WHY THIS EXISTS: 018-F/017-S accumulated a verification platform (generalized direct-push detector, fixture corpus, harness-state manifest, terminal witness, selector flag, MP0-MP6 mutation proofs, regeneration-resistant CI wiring) plus the end-to-end persistence route, and failed adversarial plan review repeatedly at revs 10-15. The re-plan reduced 018-F to the PROHIBITION half of the policy gap (one task) and moved everything separable here.

SCOPE - TWO COHERENT STRANDS:
A. PERSISTENCE ROUTE EXECUTION (makes the corrected policy actually runnable end to end)
   - 019.007-T Stage handback record emission with concrete artifact paths
   - 019.008-T no-shipment terminal outcome made reachable
   - 019.009-T Stage artifact commit step with out-of-root path safety
   - 019.004-T Orchestrator Step 1.5 authorization, branch discovery and concrete post-merge verification
B. MECHANICAL ENFORCEMENT PLATFORM (makes regeneration unable to silently reopen the gap)
   - 019.001-T direct-push fixture corpus and manifest
   - 019.002-T fixture-suite driver over a stubbed detector
   - 019.003-T structure-aware direct-push detector
   - 019.005-T CI lint-job wiring, divergence ledger, CODEOWNERS
   - 019.006-T coupling verification with executed mutation proof

DEPENDENCY ON 018-F: 019.001-T, 019.007-T and 019.009-T carry blocking edges on 018.008-T. The corrected contract must EXIST before a detector can assert on it and before a handback can name the branch the Step 1.9 gate creates. 018-F does NOT depend on 019-F in any direction.

BLOCKING PREREQUISITE BEFORE THIS FEATURE IS IMPLEMENTATION-READY - HARNESS LIFECYCLE MUST BE SOLVED FIRST:
This feature has NINE tasks. P-004 requires EVERY generated test function to be red before implementation; Ship Step 4.3 requires the FULL "go test ./..." to be green after EVERY task. Those two rules are JOINTLY UNSATISFIABLE for any shipment containing more than one test-bearing task - which is the root cause that defeated revs 10-15, not a detail of those revisions. 018-S therefore MUST NOT be claimed as a single nine-task shipment.
Before execution, a reviewed plan for this feature must choose ONE of:
   (i) split 018-S into a sequence of SINGLE-TASK shipments, each with exactly one red-to-green transition (the approach proven by the reduced 017-S); or
   (ii) amend the installed harness contract (P-002/P-004, _ship.agent.md Step 2/4.3, harness-architect) so a multi-task upfront-red batch is legitimately supported - which was EXPRESSLY OUT OF SCOPE for 018-F and is a deliberate decision requiring its own deliberation; or
   (iii) an equivalent mechanism that does NOT reintroduce surface-predicate activation, whose self-disarming failure mode was confirmed by a four-model adversarial panel.
Option (i) is the recommended default: it is already proven and requires no contract amendment. Do NOT resurrect the rev-11 manifest/witness mechanism without explicitly re-deliberating it.

STATUS: scope and dependencies are defined; this feature is NOT implementation-ready and requires its own impl-plan and plan-review before 018-S is claimed.
<!-- END:goals -->

<!-- BEGIN:dod -->
DEFINITION OF DONE - 019-F (deferred; provisional until this feature receives its own impl-plan and plan-review).

- Every Stage artifact reaching the default branch does so through a dedicated branch plus a pull request, and that route is EXECUTABLE END TO END rather than merely authorized - including when NO shipment is formed.
- The Orchestrator receives an explicit staging handback record (stage_branch, stage_base_commit, stage_head_commit, stage_outcome, stage_artifact_paths, shipment_id when formed) and verifies a BRANCH-SPECIFIC commit range, never the default branch against its own remote-tracking ref.
- The no-shipment arm proves CONCRETE FILE paths across the whole session, not directories and not only the tip commit, and FAILURES STILL HALT - no-shipment is never a re-label for a P-003 violation, an errored harvest, or a harvest that could not form a required shipment.
- Stage commits a BOUNDED artifact set: mutations outside STAGE_ARTIFACT_ROOTS are refused, not silently committed.
- A structure-aware direct-push detector exists, is wired into the CI lint job, and FAILS rather than silently skips when its own inputs are missing or corrupt.
- Regeneration of the autoharness-generated contract files cannot silently reopen the gap; the divergence ledger and CODEOWNERS reflect the coupling.
- A mutation proof demonstrates the gate detects a reintroduced direct-push path.
- go vet ./... exits 0 and go test ./... is fully green with NO skipped or conditionally-activated assertions at the end of the feature.

GATING PRECONDITION: the harness-lifecycle decision recorded in this feature's goals (single-task shipments, contract amendment, or an equivalent non-surface-predicate mechanism) is made and reviewed BEFORE 018-S is claimed.
<!-- END:dod -->