---
artifact_type: task
created_at: 2026-09-11T17:23:36.9815296Z
custom_fields:
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 018.007-T
    - 018.008-T
    - 018.009-T
id: 018.010-T
labels:
    - sub-epic-c
    - ci
    - chore
parent_id: 018-F
priority: high
status: queued
title: Wire gate into CI lint job, ledger and CODEOWNERS
updated_at: 2026-09-12T02:45:00.0000000Z
---

Parent feature 018-F. Sub-epic C (CI coupling and verification), plan section 7.1 of docs/plans/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-plan.md. Wire the gate into CI as a BLOCKING check. Add one step to job 'lint' in .github/workflows/ci.yml invoking scripts/check-direct-push-language.sh (the step-name numeric suffix (0NN.00X-T) is a PLACEHOLDER to be resolved against the then-current ledger numbering). Add a LOCAL DIVERGENCE ledger entry for the new gate, widening the membership wording as needed to admit wildcard/range forms rather than the literal 011.0xx-T, and recording that removal of this step is CAUGHT (not accepted) by the non-generated Go harness running from the generated-baseline 'Test (race)' step - go test -race -mod=readonly ./... - in job 'expensive' (name: test). Update .github/CODEOWNERS to own the new gate script and the three currently unowned gate scripts, and generalize the fixture-data carve-out from scripts/testdata/gitignore/** to scripts/testdata/**. CI configuration only.
HARNESS (P-002/P-004): harness function TestDirectPushGate_CIWiringIsBlocking in tests/integration/directpush_gate_test.go, produced by harness-architect at Ship Step 2 per plan section 5.0. harness_cmd: go test ./tests/integration -run '^TestDirectPushGate_CIWiringIsBlocking$' -gatetask=C1
ACCEPTANCE CRITERIA (PLAIN-TEXT RENDERING ONLY - NOT AUTHORITATIVE. The authoritative text is the acceptance-criteria section below, which is plan section 7.1 VERBATIM. If this rendering and that section ever disagree, the section wins and this rendering is the defect):
AC-C1.1 The gate runs in job 'lint' and is blocking from its FIRST wiring - no advisory window, no toggle.
AC-C1.2 ci-gate still transitively depends on 'lint'.
AC-C1.3 The LOCAL DIVERGENCE ledger documents the new gate, its membership rule admits the new entry, and it records that removal of the step is CAUGHT by the Go harness running from the generated-baseline 'Test (race)' step (go test -race -mod=readonly ./...) in job 'expensive', rather than accepted.
AC-C1.4 CODEOWNERS coverage is asserted STRUCTURALLY, not by count: every script invoked by a step in .github/workflows/ci.yml has an owner line in .github/CODEOWNERS.
AC-C1.5 The script header states verbatim that this is an ANTI-ACCIDENT control, not an anti-adversary control (CI runs it from the PR head), and discloses that the job-lint wiring step sits INSIDE the autoharness render boundary AND that its removal is DETECTED - NOT ACCEPTED - by TestDirectPushGate_CIWiringIsBlocking (plan section 5.0).
AC-C1.6 RENDER-SURVIVAL ASSERTION - STRUCTURAL AND NON-VACUOUS (rev 5). Step presence is determined by PARSING ci.yml and walking jobs.lint.steps[], NEVER by a file-wide text search (which would self-match the ledger comment naming the same script). Non-vacuity proven by EXECUTION against a t.TempDir() copy with the step removed. No dirty-tree edit.

<!-- BEGIN:implementation-notes -->
DEGRADATION NOTE: this workspace's backlogit type schema defines 'size' but NO 'complexity' field on any artifact type (verified via backlogit metadata types: task fields = priority, size, status), despite backlog-registry.yaml declaring features.sizing: true. Complexity is therefore recorded below as enum-validated prose per the Stage two-axis sizing contract. Size is stored structurally (size_source=agent, size_ruleset_version=2h-rule-v1).

Size: S | Complexity: medium

Rationale: Three coupled CI-configuration surfaces (ci.yml step, divergence ledger, CODEOWNERS); blocking-from-first-wiring is only safe because sub-epic B lands first. Size/complexity UNCHANGED at rev 5: AC-C1.6 constrains the SHAPE of an assertion authored at H0 by harness-architect (structural parse instead of text grep, plus a t.TempDir() non-vacuity case), not the wiring work this task performs, so this task's own 2-hour envelope does not grow.

REV-5 RECONCILIATION (PR #54 thread PRRT_kwDOTPuhps6hnPkD, comment 3992775719, plan section 11 R5): rev 4's R5 accepted the residual that a render could wipe this step and silently disable the gate, contradicting plan section 1 and the 018-F DoD. Rev 5 does NOT narrow the objective. It names the tracked enforcement path that already survives the render boundary - the plan section 5.0 Go harness, which is non-generated and is invoked by the GENERATED-BASELINE 'Test (race)' step (go test -race -mod=readonly ./...) in job 'expensive' (a render re-emits that step rather than removing it), with the trigger link closed because any render that rewrites ci.yml is a .github/** change and therefore sets changes.code == 'true'. AC-C1.6 added (structural, non-vacuous step-presence assertion); AC-C1.5 reworded from 'accepted residual' to 'detected, not accepted'; AC-C1.3 extended to require the ledger to record the same. Paired with AC-C2.8 on 018.011-T. R5 reclassified accepted -> mitigated; the narrower VISIBLE residual is R12.

REV-8 RECONCILIATION (PR #54 adversarial review, P3 finding 6): the generated-baseline invocation was cited as "the go test step in job 'expensive'", which names no step in .github/workflows/ci.yml. The actual step is 'Test (race)' and its command is go test -race -mod=readonly ./..., now cited exactly in the description, AC-C1.3 and this note. The plan's separate constitutional requirement (AC-C2.7's gofmt / go vet / go test ./... / go build sequence) is UNCHANGED and remains a distinct obligation - the two are not interchangeable. This task's own harness_cmd stays narrow (go test ./tests/integration -run TestDirectPushGate_CIWiringIsBlocking). No scope, size, or criterion change.

HARNESS BINDING (rev 4, plan section 5.0): this task does NOT carry a harness-ready label. harness-architect applies that label at Ship Step 2 after producing the Go harness and confirming go vet ./... exits 0 and go test ./... is red. Stage does not forge P-004's postcondition.
<!-- END:implementation-notes -->

<!-- BEGIN:acceptance-criteria -->
- **AC-C1.1** The step is present in job `lint`, BLOCKING from its FIRST wiring (no `continue-on-error`, no toggle variable, no advisory window), and task-ID-suffixed.
- **AC-C1.2** `.github/workflows/ci.yml` remains valid YAML (`python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` passes) AND `ci-gate` still transitively depends on job `lint`, so the new step is genuinely merge-blocking.
- **AC-C1.3** The LOCAL DIVERGENCE ledger names the gate, uses the generalized membership rule (wildcard/range form rather than the literal `011.0xx-T`), carries no magic count, records the no-toggle decision and the D2 residual, and records that removal of this step is CAUGHT, NOT ACCEPTED - `tests/integration/directpush_gate_test.go` (`TestDirectPushGate_CIWiringIsBlocking`) is not autoharness-generated and runs from the generated-baseline `Test (race)` step (`go test -race -mod=readonly ./...`) in job `expensive` (`name: test`), so a render that drops this step turns CI RED rather than green (plan section 5.0). **Rev 8**: the CI step is cited by its exact name and command, because "the `go test` step in job `expensive`" names no step in this workflow.
- **AC-C1.4** CODEOWNERS coverage is asserted STRUCTURALLY, not by count: every script invoked by a step in `.github/workflows/ci.yml` has an owner line in `.github/CODEOWNERS`; the fixture-data carve-out is generalized from `scripts/testdata/gitignore/**` to `scripts/testdata/**`.
- **AC-C1.5** The script header states VERBATIM that this is an ANTI-ACCIDENT control, not an anti-adversary control (CI runs it from the PR's own head), and discloses that the job-`lint` wiring step sits INSIDE the autoharness render boundary (deliberation D6) AND that its removal is DETECTED - NOT ACCEPTED - by `TestDirectPushGate_CIWiringIsBlocking` (plan section 5.0), so the disclosure points a future reader at the surviving enforcement path rather than at a silent failure. Rev 5 replaced the "accepted residual" half of this disclosure per the R5 reclassification.
- **AC-C1.6** **Render-survival assertion - structural and non-vacuous (rev 5).** `TestDirectPushGate_CIWiringIsBlocking` determines step presence by PARSING `.github/workflows/ci.yml` and walking `jobs.lint.steps[]` for a step whose `run` invokes `scripts/check-direct-push-language.sh` - NEVER by a file-wide text search, which would self-match the LOCAL DIVERGENCE ledger comment naming the same script and pass vacuously after the step itself was deleted (plan section 5.0; prior art `docs/compound/2026-09-06-ci-self-matching-grep-and-actionlint-verification-gap.md`). Non-vacuity is proven by EXECUTION: the test writes a copy of the current `ci.yml` with that step removed into `t.TempDir()` and asserts the same check FAILS against the copy. NO edit is made to the real tree - `git status --porcelain` stays empty - the same no-dirty-tree-edit rule as AC-A2.4 and AC-C2.2.
<!-- END:acceptance-criteria -->