---
artifact_type: task
created_at: 2026-09-11T17:23:38.5643207Z
custom_fields:
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 018.010-T
id: 018.011-T
labels:
    - sub-epic-c
    - verification
    - chore
parent_id: 018-F
priority: high
status: queued
title: Verify coupling with executed mutation proof
updated_at: 2026-09-12T02:45:00.0000000Z
---

Parent feature 018-F. Sub-epic C, plan sections 7.2 and 8 of docs/plans/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-plan.md. Verify generated/installed coupling and record the proof. This task's acceptance criteria are plan section 7.2 VERBATIM - the plan and this task are ONE verification contract and MUST NOT diverge.
HARNESS (P-002/P-004): harness function TestDirectPushGate_FullCorpusClean in tests/integration/directpush_gate_test.go, produced by harness-architect at Ship Step 2 per plan section 5.0. harness_cmd: go test ./tests/integration -run '^TestDirectPushGate_FullCorpusClean$' -gatetask=C2
HARNESS LIFECYCLE (rev 11, plan section 5.0.1): activation is derived SOLELY from the tracked harness-state manifest tests/integration/testdata/directpush-gate/harness-state.json and the -gatetask selector, NEVER from any surface this harness asserts on. Rev 10 gave C2 LITERALLY C1's predicate (presence of the job-lint step), so a regeneration that dropped that step disarmed BOTH functions and MP2 - which lives inside this function - never executed; that is the P0 defect this revision closes. In H0's red phase ALL EIGHT functions activate and fail, each with its own task-specific marker - this task's marker is 'not implemented: C2 full-corpus coupling evidence'. TARGETED RED (unchanged per-task obligation): run the harness_cmd above BEFORE implementing; -gatetask=C2 activates unconditionally in every phase and regardless of current surface presence, so this function evaluates its REAL assertion and must fail for this task's expected reason. COMPLETION (TERMINAL): atomically with the substantive work, set completed to the EXACT eight-element ordered set ["A1","A2","A3","B1","B2","B3","C1","C2"], terminal to true, and phase to 'terminal' - the three fields move TOGETHER, and a partial write is an inconsistent triple that MP1 rejects. From that point the default suite activates all eight substantive assertions and NO ACTIVATION SKIP REMAINS ANYWHERE in the harness (zero '--- SKIP:' lines among the eight gate functions). MP2 - terminal completeness - is evaluated FROM MANIFEST STATE BEFORE any surface-dependent assertion in this function, so no surface failure can mask the completeness verdict; the real full-corpus assertions then run. Once C1 and C2 are in completed they are active PERMANENTLY, so a regeneration that removes the lint step leaves both ACTIVE AND FAILING LOUDLY rather than skipping. MISSING OR MALFORMED MANIFEST, DUPLICATE/UNKNOWN/OUT-OF-ORDER ID, UNMET PREREQUISITE, INCONSISTENT phase/terminal, OR AN UNKNOWN -gatetask VALUE ALL FAIL CLOSED - never skip.
REV 12 - THREE LIFECYCLE CORRECTIONS THAT BIND THIS TASK (plan sections 5.0.1 and 5.0.2). (1) MP5 IS RETIRED - REMOVED, NOT RENUMBERED, AND MUST NOT BE IMPLEMENTED. Its rev-11 rule (a task outside completed must have its surface ABSENT, else FAIL) was wrong: 018.011-T's probe was the job-lint step that 018.010-T ITSELF CREATES, so the entirely legitimate post-018.010-T / pre-018.011-T window failed the DEFAULT SUITE - the Ship Step 4.3 deadlock, reintroduced by the guard meant to prevent it. THE GENERAL RULE, NOW SYMMETRIC: NEITHER ACTIVATION NOR ANY GATE-LEVEL FAILURE MAY BE DERIVED FROM A SURFACE THIS HARNESS ASSERTS ON. Rollback of a COMPLETED task's surface is caught by THAT TASK'S OWN SUBSTANTIVE ASSERTION (active in default mode from its completion onward); an INCOMPLETE task legitimately may have prerequisites and even surface fragments present and MUST NOT fail anything. The ACTIVE checks are SIX: MP0, MP1, MP2, MP3, MP4, MP6. (2) TERMINAL COMPLETION WITNESS (MP6). A second tracked, non-generated file tests/integration/testdata/directpush-gate/terminal-witness.json, INDEPENDENT of the manifest, is written ONLY by 018.011-T's completion - WITNESS FIRST, THEN the manifest's ATOMIC replacement with the exact terminal state. IT IS ABSENT AT H0 AND THROUGHOUT BUILD, and that absence is VALID, not an error. gate() runs MP6 BEFORE ANY ACTIVATION DECISION: a present witness must be VALID (schema directpush-gate/terminal-witness, version 1, contract 017-S/018-F, completed == gateOrder, digest == SHA-256 of the eight IDs joined with newline) and REQUIRES a terminal manifest AND FORCES 018.011-T ACTIVE; a terminal manifest REQUIRES a valid witness; missing/malformed/mismatched WHEREVER TERMINAL OR WITNESS EVIDENCE EXISTS IS FAIL-CLOSED. So a manifest-only rollback and a witness-only rollback each FAIL LOUDLY. RECORDED RESIDUAL (R22): rolling back BOTH in one change resembles legitimate pre-018.011-T build state and is caught by DIFF AND REVIEW, NOT BY RUNTIME HISTORY - no claim is made that a stateless validator detects every coordinated rollback. (3) VALID STATES ARE EXHAUSTIVE - EXACTLY THREE: red => completed EMPTY and terminal false and witness absent; build => completed a NON-EMPTY, order-preserving, DEPENDENCY-CLOSED PROPER SUBSET of gateOrder and terminal false and witness absent; terminal => completed the EXACT eight-element ordered set and terminal true and a VALID WITNESS PRESENT. EVERY OTHER COMBINATION IS REJECTED, including red+terminal, red with a non-empty completed, build with an empty completed, build with all eight, build+terminal, terminal with a proper subset, terminal with terminal:false, terminal with the witness absent, a witness present alongside red or build, and any phase outside {red,build,terminal}. ZERO-SKIPS IS A DEFAULT-MODE CLAIM (red and terminal phases): with no -gatetask, all eight activate and none skips. TARGETED MODE LEGITIMATELY SKIPS THE SEVEN NON-SELECTED FUNCTIONS in every phase - those are SELECTOR skips, not activation skips - and its only obligation is that THE SELECTED TEST ACTUALLY EXECUTES. DEPENDENCY DIRECTION: backlogit stores edges as `dep add <item> <depends-on> --type blocks`, so a recorded edge reads DEPENDENT -> PREREQUISITE despite the 'blocks' label; gateDeps[X] is therefore X'S PREREQUISITES, and reading the label the other way inverts the graph. SCOPE: the one-line manifest edit is the SHARED HARNESS-STATE COMPLETION RECORD, not a new skill domain and not a new deliverable; this task's size and complexity are UNCHANGED and it remains inside the 2-hour rule.
ACCEPTANCE CRITERIA (PLAIN-TEXT RENDERING ONLY - NOT AUTHORITATIVE. The authoritative text is the acceptance-criteria section below, which is plan section 7.2 VERBATIM. If this rendering and that section ever disagree, the section wins and this rendering is the defect):
AC-C2.1 Full-corpus bare scan at HEAD exits 0 with ZERO findings; the resolved corpus file count and the findings count are recorded in the PR description as structured evidence.
AC-C2.2 MUTATION PROOF - FIXTURE-BACKED, NO DIRTY-TREE EDIT. Reintroduction-detection for both violation shapes is proven by --self-test against the committed fixture corpus: directpush-reject-attempt-first.md locks the literal 3e push construct and directpush-reject-commit-on-default.md locks the default-branch commit authorization construct, and the run reports every fixture BY NAME with actual == manifest verdict. NO temporary edit is made to the real tree at any point; git status --porcelain is empty immediately before and after the proof, and the evidence recorded is the --self-test output, not a narrated local edit. A reintroduction proof performed by mutating the committed tree is EXPLICITLY REJECTED: it is unrecorded, unreproducible in CI, and leaves a window in which the repository contains the very construct this gate exists to forbid. REV 11 EXTENDS THIS CRITERION TO THE HARNESS-STATE MANIFEST ITSELF (plan section 5.0.1 MP4): against a SCRATCH COPY under t.TempDir() - same no-dirty-tree-edit rule, never the committed file - gate() is proven to FAIL, not skip, for each of TEN mutations (SEVEN manifest cases plus the THREE rev-12 witness cases) of tests/integration/testdata/directpush-gate/harness-state.json and tests/integration/testdata/directpush-gate/terminal-witness.json: (i) deleted; (ii) malformed (invalid JSON); (iii) unknown phase value; (iv) ROLLBACK (completed truncated, or terminal/phase left inconsistent with it); (v) REORDERING (completed permuted out of gateOrder sequence); (vi) SKIPPED PREREQUISITE (a member whose gateDeps prerequisite is absent); (vii) duplicate or unknown ID. REV 12 ADDS THE THREE WITNESS CASES to the same scratch-copy proof: (viii) WITNESS-ONLY (a valid witness present while the manifest is red or build); (ix) TERMINAL-MANIFEST-ONLY (a terminal manifest with the witness ABSENT); (x) MALFORMED OR MISMATCHED WITNESS (invalid JSON, wrong schema/version/contract, completed != gateOrder, or a digest that does not match its own array). Each of the TEN cases records an observed failure. The state files are part of the mutation proof because they are now part of the control: an unguarded state file would be a silent off-switch for every assertion in the harness.
AC-C2.3 SELF-MATCH NEGATIVE PROOF, EXECUTED. In the same bare scan, the three real marker-free prohibition constructs in the corrected tree - the post-change P-010 Stage prohibition bullet, P-010's Ship MUST NOT bullet 'Commit or push directly to `main`' in workflow-policies.md, and _ship.agent.md's Role Boundary Git Forbidden cell - all score ACCEPT. Each is located by FILE plus QUOTED CONSTRUCT TEXT, never by line ordinal (rev 8), so the criterion survives any edit that shifts line numbers in those files - including 018.008-T's own Amendment Log append. Prior art: docs/compound/2026-09-06-ci-self-matching-grep-and-actionlint-verification-gap.md.
AC-C2.4 AUTHORIZATION-SURFACE AGREEMENT, AS THE SURFACES EXIST AT THE END STATE (corrected rev 12). The installed .github/** artifacts and the corrected policy AGREE - no surviving contradiction across the LIVE POST-CHANGE authorization surfaces. REV 12 CORRECTION: this criterion previously read 'the four SECTION 2 authorization surfaces'. Plan section 2 is the HISTORICAL ROOT-CAUSE enumeration, and one of its four entries is _orchestrator.agent.md Step 1.5 SUB-STEP 3e, which 018.007-T DELETES - so the criterion asserted agreement across a surface that does not exist at the end state. The live set is: (1) _stage.agent.md ROLE BOUNDARY GIT ROW; (2) workflow-policies.md P-010 STAGE bullets; (3) _orchestrator.agent.md Step 1.5 PREAMBLE in its reworded verification/postcondition form; (4) _orchestrator.agent.md Step 1.5 SUB-STEP 3a, which carries the branch-point instruction folded in from the deleted 3e. Plus an explicit 3e-ABSENCE clause: sub-step 3e MUST NOT be present at the end state (AC-B1.1), so it is not a surface that can agree or disagree. The plan's narrative quotation of the 3e construct is a HISTORICAL RECORD outside the scanned corpus and is deliberately retained. REV 6: the STAGING HANDBACK CONTRACT also agrees across its two surfaces - every field _orchestrator.agent.md Step 1 requires (AC-B1.5) is emitted by _stage.agent.md Step 6 (AC-B3.5), with no field required in one and absent from the other. REV 7: that agreement extends to the SHAPE of stage_artifact_paths - producer (AC-B3.5) and consumer (AC-B1.10) declare the same concrete-file contract and the same derivation command, and NEITHER surface retains a directory-prefix default. REV 8, two further agreements: (a) both surfaces carry stage_base_commit and the SAME AGGREGATE two-tree derivation, so neither retains a single-commit form; and (b) the no-shipment OUTCOME VALUE ITSELF is agreed - the consumer's arm is keyed on an outcome the producer can actually emit (AC-B3.6). REV 9, the PRODUCING-ACTION agreement: every handback field the consumer reads has a PRODUCING STEP in the producer surface, and those steps are ORDERED correctly - _stage.agent.md carries an operative branch gate before its first artifact write and an operative artifact commit before its summary (AC-B3.7) - so stage_branch, stage_base_commit, stage_head_commit and stage_artifact_paths name a branch that was created and commits that were made. Both surfaces also agree on the BRANCH-NAMING FORM: neither requires a shipment_id to name the branch (AC-B2.2, AC-B3.2). REV 10, two final agreements: (a) the OUTCOME/SHIPMENT_ID PAIR is agreed - the producer emits stage_outcome EXPLICITLY and consistently with shipment_id (AC-B3.5), and the consumer PRESERVES a reported outcome, derives only when ABSENT, and HALTS FAIL-CLOSED on an unknown value or an inconsistent pair before selecting an arm (AC-B1.12); NEITHER surface retains the unconditional derive-from-shipment_id rule that silently reclassified a reported shipment run as a successful no-shipment one. (b) The producer's PRE-GATE DEFERRAL and OUT-OF-ROOT DETECTION clauses are present and correctly ORDERED (AC-B3.7) - the deferral rule is CATEGORICAL over every tracked mutation and names its three classes while expressly EXCLUDING gitignored disposables, and the commit step's full-tree inspection is positioned BEFORE its staging clause - so the handback the consumer reads describes a commit that could not have silently omitted or silently widened its contents. A field agreeing in name but disagreeing in shape, an arm keyed on an outcome no producer can reach, a field that is declared, shaped and emitted but never PRODUCED, and a field whose value is agreed but whose PRODUCTION ORDER admits a write to the wrong branch are the same defect class one, two, three and four levels down: all four pass a name-only comparison, the last three also pass a shape-only one, and the last passes a producing-step comparison too.
AC-C2.5 markdownlint exits 0 on every changed markdown file (P-008).
AC-C2.6 Branch and PR merge-commit rules intact - P-009, P-011, P-016 textually unchanged.
AC-C2.7 Constitution Quality Gates run IN DECLARED ORDER, none skipped, as SEPARATE commands: gofmt -l . empty, then go vet ./..., then go test ./..., then go build ./... - each exits 0 and each result recorded independently. go test ./... is NO LONGER a pure regression check: it includes the tests/integration/directpush_gate_test.go harness, which must be green in full, so all eight per-task functions pass here. REV 11: at C2's boundary the manifest is in terminal phase, so 'green in full' means ALL EIGHT FUNCTIONS ACTIVATE AND PASS THEIR REAL ASSERTIONS - go test -v ./tests/integration -run TestDirectPushGate shows ZERO '--- SKIP:' lines for the eight gate functions. A skip at this point is a FAILURE of this criterion, not a pass. REV 12 QUALIFICATION: that zero-skip claim is scoped to a DEFAULT-MODE run (no -gatetask). A TARGETED run legitimately skips the seven non-selected functions in any phase - those are SELECTOR skips, not activation skips - and its only requirement is that THE SELECTED TEST EXECUTES. Evidence for this criterion MUST be taken from a default-mode invocation.
AC-C2.8 REGENERATION-RESISTANT ENFORCEMENT, RECORDED (rev 5; PROVENANCE CORRECTED rev 12). The evidence records that the AC-C2.1 full-corpus scan is executed by TestDirectPushGate_FullCorpusClean from job 'expensive' (name: test)'s test step - whose OBSERVED INSTALLED command is go test -race -mod=readonly ./... (rev 8 - cited precisely, because "the go test step" does not identify a step in this workflow) - and NOT SOLELY by the job-lint step added in C1. REV 12 CORRECTS WHAT MAY BE CLAIMED ABOUT THAT COMMAND'S PROVENANCE: it is the OBSERVED INSTALLED command, NOT the generated baseline. The template emits {{TEST_COMMAND}} and this workspace's recorded render input is go test ./..., so the property the evidence may assert is that a FULL-SUITE ./... INVOCATION exists in a job the render re-emits and the code filter cannot skip - which is what makes tests/integration run. The evidence MUST NOT assert that the -race or -mod=readonly flags are render-guaranteed, and MUST cite AC-C1.7's post-render verification as the mechanism that keeps the ./... property true across a regeneration. This is SEPARATE FROM, and does not replace, AC-C2.7's constitutional go test ./... requirement. Both halves of the plan section 5.0 invariant are green in the same run: (a) zero findings over installed surfaces, (b) job-lint step still present per AC-C1.6. A render that drops the C1 step fails (b) LOUDLY while (a) keeps executing. REV 11 SUPPLIES THE PROPERTY THIS CRITERION SILENTLY ASSUMED: both halves only execute if both functions are ACTIVE, and at rev 10 both were activated by the very step whose removal they were meant to detect, so a render dropped the step AND the two detectors together. Activation now comes from the harness-state manifest alone (AC-C2.9), so the evidence must record that C1 and C2 are in completed and that neither function's activation reads any surface it asserts on.
AC-C2.9 TERMINAL COMPLETENESS AND ACTIVATION INDEPENDENCE, FROM MANIFEST STATE (rev 11). C2's completion sets tests/integration/testdata/directpush-gate/harness-state.json to the EXACT eight-element ordered set ["A1","A2","A3","B1","B2","B3","C1","C2"] with terminal=true and phase=terminal, ATOMICALLY - the three fields move together, and a partial write is an inconsistent triple that MP1 rejects. Inside TestDirectPushGate_FullCorpusClean that completeness check is evaluated FROM MANIFEST STATE BEFORE ANY SURFACE-DEPENDENT ASSERTION in the function, so no surface failure can mask the completeness verdict; the real full-corpus assertions then run. In terminal phase the default suite activates ALL EIGHT substantive assertions and NO ACTIVATION SKIP REMAINS ANYWHERE in the harness - evidenced by zero '--- SKIP:' lines for the eight gate functions IN A DEFAULT-MODE RUN (rev 12: a targeted run skips the seven non-selected functions by design and is not evidence for or against this clause). Neither C1 nor C2 derives activation from the job-lint step or from any other surface either function asserts on: once their IDs are in completed they are active PERMANENTLY, so a regeneration that removes the lint step leaves both ACTIVE AND FAILING LOUDLY rather than skipping. Targeted -gatetask=C1 and -gatetask=C2 execute their real assertions REGARDLESS of current surface presence, phase, or manifest contents (manifest VALIDITY is still enforced - MP1 fails closed in every mode). C2's completion also performs the one-line insertion into completed described in plan section 5.0.1 - the shared harness-state completion record, not a second skill domain; C2's size band is unchanged.
AC-C2.10 TERMINAL COMPLETION WITNESS - INDEPENDENT, MINIMAL, FAIL-CLOSED (rev 12). C2's completion creates tests/integration/testdata/directpush-gate/terminal-witness.json, a tracked, non-generated file INDEPENDENT OF THE HARNESS-STATE MANIFEST, with EXACTLY these fields and no others: schema = "directpush-gate/terminal-witness", version = 1, contract = "017-S/018-F", completed = the exact eight-element ordered set, digest = lowercase hex SHA-256 of those eight IDs joined with newline (no trailing newline), computed with stdlib crypto/sha256. It carries NO TIMESTAMPS AND NO ENVIRONMENT-SPECIFIC DATA - no host, user, path, branch, commit or run identifier - so it is byte-reproducible from the contract alone. WRITE ORDER IS NORMATIVE: the WITNESS IS WRITTEN FIRST, then the manifest is ATOMICALLY REPLACED with the exact terminal state; the tests are green only when the two AGREE. gate() enforces MP6 BEFORE ANY ACTIVATION DECISION: a present witness must be valid and requires a terminal manifest AND FORCES C2 ACTIVE; a terminal manifest requires a present valid witness; a MISSING, MALFORMED OR MISMATCHED witness WHEREVER TERMINAL OR WITNESS EVIDENCE EXISTS is FAIL-CLOSED; and ABSENCE AT H0 AND THROUGHOUT BUILD IS VALID, not an error. Consequently a rollback of ONLY the manifest and a rollback of ONLY the witness each FAIL LOUDLY (proven by execution - AC-C2.2 cases viii-x). RECORDED RESIDUAL, NOT CLAIMED AS CLOSED: rolling back or deleting BOTH files in one change yields a state a stateless validator cannot distinguish from legitimate pre-C2 build, because that is what it looks like; it is caught by DIFF AND REVIEW of two tracked non-generated files, NOT BY RUNTIME HISTORY (R22). No claim is made that any stateless validator detects every coordinated rollback.
AC-C2.11 LIFECYCLE EVIDENCE LEDGER (rev 12). The PR evidence records all EIGHT plan section 5.0.2 observation points, each as a run command plus its observed output, never as narration: (1) H0 - 8 expected failures with their own markers, ZERO DEFAULT-MODE ACTIVATION SKIPS, WITNESS ABSENT; (2) POST-A2 - A2 targeted green, A3 targeted expected-red, DEFAULT SUITE GREEN; (3) LEGAL B1/B2/B3 ORDERS - each dependency-closed order-preserving subsequence ACCEPTED, no legal order rejected; (4) POST-C1 - DEFAULT SUITE GREEN, C2 targeted expected-red, WITNESS ABSENT (this is the state rev-11's MP5 deadlocked); (5) TERMINAL - exact eight-task set PLUS valid witness, all 8 substantive assertions green, ZERO DEFAULT-MODE SKIPS; (6) TERMINAL TREE WITH THE MANIFEST ROLLED BACK TO SEVEN TASKS WHILE THE WITNESS REMAINS - FAILURE; (7) TERMINAL MANIFEST WITH THE JOB-LINT STEP REMOVED - C1 FAILS and C2 EXECUTES; (8) WITNESS-ONLY, TERMINAL-MANIFEST-ONLY, AND MALFORMED WITNESS - FAILURE in each. Points 1, 2 and 4 are captured at the boundaries where they occur; points 3, 5, 6, 7 and 8 are executed or replayed at C2 under t.TempDir() per AC-C2.2's no-dirty-tree-edit rule. Every 'zero skips' figure is taken from a DEFAULT-MODE run.

<!-- BEGIN:implementation-notes -->
DEGRADATION NOTE: this workspace's backlogit type schema defines 'size' but NO 'complexity' field on any artifact type (verified via backlogit metadata types: task fields = priority, size, status), despite backlog-registry.yaml declaring features.sizing: true. Complexity is therefore recorded below as enum-validated prose per the Stage two-axis sizing contract. Size is stored structurally (size_source=agent, size_ruleset_version=2h-rule-v1).

Size: S | Complexity: medium

Rationale: Execution and evidence capture rather than authoring; the mutation proof must be genuinely run, including the negative self-match check.

REV-4 RECONCILIATION (PR #54 thread PRRT_kwDOTPuhps6hl8Nd): the previous AC-C2.3 mandated a dirty-tree reintroduction edit, contradicting plan section 7.2 C2.2's fixture-backed proof, and the C2 numbering diverged from the plan's. This task now carries plan section 7.2 VERBATIM under a single renumbered AC-C2.1..AC-C2.8 contract. The dirty-tree form is explicitly rejected inside AC-C2.2 so it cannot be reintroduced by paraphrase.

REV-5 RECONCILIATION (PR #54 thread PRRT_kwDOTPuhps6hnPkD): AC-C2.8 added as the (a)-half of the plan section 5.0 regeneration-resistant enforcement invariant, paired with AC-C1.6 on 018.010-T which carries the (b)-half. This task's C2 contract remains plan section 7.2 VERBATIM: the plan section was extended with the same AC-C2.8 text in the same change, so the single-verification-contract rule is preserved and the two did not diverge. Size/complexity UNCHANGED: AC-C2.8 is an evidence-recording obligation over a run this task already performs (AC-C2.7 already requires the full harness green), not additional execution work.

REV-6 RECONCILIATION (PR #54 thread PRRT_kwDOTPuhps6hqWDe): AC-C2.4 extended to cover the staging handback contract added by 018.007-T (AC-B1.5, consumer) and 018.009-T (AC-B3.5, producer), so the coupling check catches a field required in one surface and absent from the other - the same defect class AC-B3.2 guards for grants. Plan section 7.2 was extended with identical text in the same change, so this task remains plan section 7.2 VERBATIM. Size/complexity UNCHANGED: this is one additional agreement clause inside a cross-surface comparison this task already performs.

REV-7 RECONCILIATION (PR #54 thread PRRT_kwDOTPuhps6hrC0c): AC-C2.4 extended so the handback-contract agreement check compares field SHAPE, not just field NAME. Rev 6's name-only comparison would have passed a tree in which the producer emitted directory prefixes and the consumer demanded concrete files - or vice versa - because both surfaces name stage_artifact_paths. This task remains plan section 7.2 VERBATIM: the plan section was extended with identical text in the same change. Size/complexity UNCHANGED: one additional agreement clause inside a cross-surface comparison this task already performs.

REV-8 RECONCILIATION (PR #54 adversarial review, P1 blockers 1 and 2, P2 finding 5, P3 finding 6): three criteria corrected in place, none added or removed. (a) AC-C2.3's locator for the Ship mirror bullet was the LINE ORDINAL 'workflow-policies.md L238' - brittle in general and self-invalidating in particular, since 018.008-T appends an Amendment Log row to that very file and shifts the line. It is now located by file plus quoted construct text, the anchoring rule the deliberation's own section 8.5 promised and did not apply. (b) AC-C2.4 gains the two rev-8 agreements: stage_base_commit plus the AGGREGATE two-tree derivation on both surfaces, and agreement on the no-shipment OUTCOME VALUE itself - the review established that the consumer's arm was keyed on an outcome no producer could emit (see 018.009-T AC-B3.6), and an unreachable arm passes every name-only agreement test. (c) AC-C2.8 now cites the generated-baseline step by its exact name and command ('Test (race)', go test -race -mod=readonly ./... in job expensive) and states explicitly that AC-C2.7's constitutional go test ./... is a SEPARATE, still-required gate. This task remains plan section 7.2 VERBATIM: the plan section was corrected with identical text in the same change. Size/complexity UNCHANGED: all three are precision corrections to comparisons and evidence this task already performs.

HARNESS BINDING (rev 4, plan section 5.0): this task does NOT carry a harness-ready label. harness-architect applies that label at Ship Step 2 after producing the Go harness and confirming go vet ./... exits 0 and go test ./... is red. Stage does not forge P-004's postcondition.

REV-9 RECONCILIATION (PR #54 thread PRRT_kwDOTPuhps6hrbAk, comment 3994429008): AC-C2.4 gains the PRODUCING-ACTION agreement. Revisions 6-8 progressively strengthened this cross-surface comparison from field NAMES to field SHAPES to the reachability of the no-shipment OUTCOME VALUE; the review established a fourth level the previous three all pass over - a field can be declared by the consumer, shaped identically on both surfaces, and dutifully emitted by the producer's summary contract while NO STEP ANYWHERE PRODUCES IT. That was the state of stage_branch, stage_base_commit, stage_head_commit and stage_artifact_paths until 018.009-T AC-B3.7 added the operative branch gate and artifact commit. The extended criterion therefore checks that every consumed field has a producing step AND that the two steps are ORDERED - branch before the first artifact write, commit before the summary - because an out-of-order producing step yields values that describe a state that does not exist. It also checks the two surfaces agree on the BRANCH-NAMING FORM, since neither may require a shipment_id that does not exist when the branch is created. This task remains plan section 7.2 VERBATIM: the plan section was extended with identical text in the same change. Size/complexity UNCHANGED: one additional agreement clause inside a cross-surface comparison this task already performs.

REV-10 RECONCILIATION (PR #54 second adversarial review): AC-C2.4 gains the rev-10 agreements. (a) The OUTCOME/SHIPMENT PAIR: a coupling criterion that compares only field NAMES, SHAPES and PRODUCING STEPS still passes when the consumer's RESOLUTION RULE can overwrite a correct reported value with a derived default whose fallback is a SUCCESS terminal - which is exactly what thread PRRT_kwDOTPuhps6hsPRZ found. The agreement therefore now binds the resolution rule itself on both sides: explicit consistent emission by the producer (AC-B3.5), preserve-then-validate by the consumer (AC-B1.12). (b) The PRODUCTION ORDER: the producer's deferral rule must be categorical rather than an enumeration, and its out-of-root detection must precede its staging clause (AC-B3.7), because a handback produced by a step that could write to the wrong branch, or by a commit that could silently omit or widen its contents, describes a state the consumer cannot trust even when every field name, shape and producing step agrees. Also: harness_cmd gains the -gatetask=C2 selector from plan section 5.0.1, and this task's function additionally carries MP2 - the terminal-completeness check that requires all eight activation predicates to hold once C1's surface exists, which is what stops the rev-10 activation mechanism degenerating into a permanently skipped suite (plan section 5.0.1, R19). Plan section 7.2 carries identical text from the same change, so the plan<->task same-contract invariant holds. Size and complexity unchanged; NO new task, file, fixture, harness function, or dependency edge.

REV-12 RECONCILIATION (PR #54 post-remediation adversarial re-review; anchor GPT-5.6 Sol with GPT-5.4-mini, Claude Sonnet 5 and Claude Opus 5; 4/4 usable, no route degradation). Three P1 residuals bind this task. (a) MP5 RETIRED: its rule (a task outside completed must have its surface absent, else FAIL) failed the DEFAULT SUITE on the legitimate post-018.010-T / pre-018.011-T state, because THIS TASK'S probe was the job-lint step that 018.010-T CREATES - the Ship Step 4.3 deadlock reintroduced by the guard meant to prevent it. The active checks are now SIX (MP0, MP1, MP2, MP3, MP4, MP6); numbering is deliberately NOT compacted. (b) TERMINAL WITNESS (AC-C2.10, MP6): MP2 lives INSIDE this function and this function's activation was read from the SAME manifest MP2 validates, so a CONSISTENT rewind of phase/completed/terminal skipped this function and MP2 with it. A second independent tracked file plus a gate()-level cross-check run BEFORE activation closes the asymmetric cases; the symmetric both-files rollback is recorded honestly as R22 and is caught by diff/review, not runtime history. (c) PROVENANCE (AC-C2.8, AC-C1.7): go test -race -mod=readonly ./... is the OBSERVED INSTALLED command, NOT the generated baseline - the template emits {{TEST_COMMAND}} and the recorded render input is go test ./... - so the guarantee is scoped to a FULL-SUITE ./... INVOCATION and post-render verification is now mandatory. Also: AC-C2.2 extended from seven to TEN mutation cases; AC-C2.4 retargeted from the historical plan-section-2 enumeration (which counted the DELETED sub-step 3e) to the LIVE post-change preamble/3a pair plus the explicit 3e-absence clause and the Stage/P-010 surfaces; AC-C2.7 and AC-C2.9 zero-skip claims QUALIFIED as DEFAULT-MODE; AC-C2.11 added as the lifecycle evidence ledger. This task remains plan section 7.2 VERBATIM: the plan section was corrected with identical text in the same change. SIZE AND COMPLEXITY UNCHANGED - the witness is one additional write inside the completion transaction this task already performs, in a directory and a domain it already touches; the added evidence obligations are recordings of runs this task already makes. NO new task, file beyond the witness, fixture, harness function, shipment, or dependency edge.
<!-- END:implementation-notes -->

<!-- BEGIN:acceptance-criteria -->
- **AC-C2.1** Full-corpus bare scan at HEAD exits **0** with **zero findings**; the resolved corpus
  file count and the findings count are recorded in the PR description as structured evidence.
- **AC-C2.2** **Mutation proof — fixture-backed, NO dirty-tree edit.** Reintroduction-detection for
  both violation shapes is proven by `--self-test` against the committed fixture corpus:
  `directpush-reject-attempt-first.md` locks the literal 3e **push** construct and
  `directpush-reject-commit-on-default.md` locks the **default-branch commit authorization**
  construct, and the run reports every fixture **by name** with actual == manifest verdict. **No
  temporary edit is made to the real tree at any point**; `git status --porcelain` is empty
  immediately before and after the proof, and the evidence recorded is the `--self-test` output,
  not a narrated local edit. A reintroduction proof performed by mutating the committed tree is
  **explicitly rejected**: it is unrecorded, unreproducible in CI, and leaves a window in which the
  repository contains the very construct this gate exists to forbid. **Rev 11 extends this criterion
  to the harness-state manifest itself** (§5.0.1 MP4): against a **scratch copy** under
  `t.TempDir()` — same no-dirty-tree-edit rule, never the committed file — `gate()` is proven to
  **fail**, not skip, for each of **seven** mutations of
  `tests/integration/testdata/directpush-gate/harness-state.json`: (i) **deleted**; (ii) **malformed**
  (invalid JSON); (iii) **unknown `phase` value**; (iv) **rollback** (`completed` truncated, or
  `terminal`/`phase` left inconsistent with it); (v) **reordering** (`completed` permuted out of
  `gateOrder` sequence); (vi) **skipped prerequisite** (a member whose `gateDeps` prerequisite is
  absent); (vii) **duplicate or unknown ID**. **Rev 12 adds the three witness cases** to the same
  scratch-copy proof: (viii) **witness-only** — a valid witness present while the manifest is
  `red` or `build`; (ix) **terminal-manifest-only** — a terminal manifest with the witness
  **absent**; and (x) **malformed or mismatched witness** — invalid JSON, wrong `schema`,
  `version`, or `contract`, `completed` ≠ `gateOrder`, or a `digest` that does not match its own
  array. Each of the **ten** cases records an observed failure. The state files are
  part of the mutation proof because they are now part of the control: an unguarded state file would be
  a silent off-switch for every assertion in the harness.
- **AC-C2.3** **Self-match negative proof, executed.** In the same bare scan, the three real
  marker-free prohibition constructs in the corrected tree — the post-change P-010 **Stage**
  prohibition bullet, P-010's **Ship MUST NOT** bullet "Commit or push directly to `main`" in
  `workflow-policies.md`, and `_ship.agent.md`'s Role Boundary `| Git |` Forbidden cell — all score
  **accept**. Each is located by **file plus quoted construct text**, never by line ordinal
  (rev 8), so the criterion survives any edit that shifts line numbers in those files. Rev 1's
  AC3.3 (ci.yml step names) was vacuous because `.github/workflows/**` is never in the corpus.
  Prior art:
  `docs/compound/2026-09-06-ci-self-matching-grep-and-actionlint-verification-gap.md`.
- **AC-C2.4** **Authorization-surface agreement, as the surfaces exist at the end state (corrected
  rev 12).** The installed `.github/**` artifacts and the corrected policy agree — no surviving
  contradiction across the **live post-change** authorization surfaces. **Rev 12 correction**: this
  criterion previously read "the four **§2** authorization surfaces". §2 is the **historical
  root-cause enumeration**, and one of its four entries is `_orchestrator.agent.md` Step 1.5
  **sub-step 3e**, which **B1 deletes** — so the criterion asserted agreement across a surface that
  does not exist at the end state, the same defect AC-C2.4's own rev-11 DoD fix corrected one file
  over. The live set it now compares is: (1) `_stage.agent.md` **Role Boundary Git row** (the
  Stage surface); (2) `workflow-policies.md` **P-010 Stage** bullets (the P-010 surface);
  (3) `_orchestrator.agent.md` Step 1.5 **preamble**, in its reworded verification/postcondition
  form; and (4) `_orchestrator.agent.md` Step 1.5 **sub-step 3a**, which carries the branch-point
  instruction folded in from the deleted 3e. Plus an explicit **3e-absence** clause: sub-step 3e
  MUST NOT be present at the end state (AC-B1.1), so it is not a surface that can agree or
  disagree. §2's narrative quotation of the 3e construct is a **historical record** outside the
  scanned corpus (§5.2) and is deliberately retained. **Rev 6**: the **staging handback contract** also agrees across its two surfaces — every field
  `_orchestrator.agent.md` Step 1 requires (AC-B1.5) is emitted by `_stage.agent.md` Step 6
  (AC-B3.5), with no field required in one and absent from the other. **Rev 7**: that agreement
  extends to the **shape** of `stage_artifact_paths` — the producer (`_stage.agent.md` Step 6,
  AC-B3.5) and the consumer (`_orchestrator.agent.md` Step 1.5 step 4, AC-B1.10) declare the same
  concrete-file contract and the same derivation command, and **neither** surface retains a
  directory-prefix default. A field agreeing in name but disagreeing in shape is the same
  defect class one level down, and passes a name-only comparison. **Rev 8**, two further
  agreements: (a) both surfaces carry `stage_base_commit` and the **same aggregate** two-tree
  derivation, so neither retains a single-commit form; and (b) the `no-shipment` **outcome value
  itself** is agreed — the consumer's arm is keyed on an outcome the producer can actually emit
  (AC-B3.6). A verification arm keyed on an outcome no producer can reach is not a passing check
  but an **unreachable** one, and it passes every agreement test that compares only field names.
  **Rev 9**, the producing-action agreement: every handback field the consumer reads has a
  **producing step** in the producer surface, and those steps are **ordered** correctly —
  `_stage.agent.md` carries an operative branch gate before its first artifact write and an
  operative artifact commit before its summary (AC-B3.7), so `stage_branch`, `stage_base_commit`,
  `stage_head_commit`, and `stage_artifact_paths` name a branch that was created and commits that
  were made. Both surfaces also agree on the **branch-naming form**: neither requires a
  `shipment_id` to name the branch (AC-B2.2, AC-B3.2). A field that is declared, shaped, and
  emitted but never *produced* is the same defect class a third level down, and it passes both a
  name-only and a shape-only comparison. **Rev 10**, two final agreements: (a) the
  outcome/`shipment_id` **pair** is agreed — the producer emits `stage_outcome` explicitly and
  consistently with `shipment_id` (AC-B3.5), and the consumer **preserves** a reported outcome,
  derives only when absent, and **halts fail-closed** on an unknown value or an inconsistent pair
  before selecting an arm (AC-B1.12); neither surface retains the unconditional
  derive-from-`shipment_id` rule that silently reclassified a reported `shipment` run as a
  successful `no-shipment` one. (b) The producer's **pre-gate deferral** and **out-of-root
  detection** clauses are present and correctly ordered (AC-B3.7) — the deferral rule is
  categorical over every tracked mutation and names its three classes while expressly excluding
  gitignored disposables, and the commit step's full-tree inspection is positioned **before** its
  staging clause, so the handback the consumer reads describes a commit that could not have
  silently omitted or silently widened its contents. A field whose value is agreed but whose
  *production order* admits a write to the wrong branch is the same defect class a fourth level
  down, and it passes name, shape, and producing-step comparisons alike.
- **AC-C2.5** `markdownlint` exits **0** on every changed markdown file (P-008).
- **AC-C2.6** Branch and PR merge-commit rules intact — P-009, P-011, P-016 textually unchanged.
- **AC-C2.7** Constitution Quality Gates run **in declared order, none skipped, as separate
  commands** (never chained with `&&`, which would short-circuit and mask which gate failed):
  `gofmt -l .` empty, then `go vet ./...`, then `go test ./...`, then `go build ./...` — each
  exits 0 and each result is recorded independently. **Rev 4**: `go test ./...` is no longer a pure
  regression check — it now includes the `tests/integration/directpush_gate_test.go` harness (§5.0),
  which must be **green** in full, so all eight per-task functions pass here. **Rev 11**: at C2's
  boundary the manifest is in `terminal` phase, so "green in full" means **all eight functions
  activate and pass their real assertions** — `go test -v ./tests/integration -run TestDirectPushGate`
  shows **zero `--- SKIP:`** lines for the eight gate functions. A skip at this point is a failure of
  this criterion, not a pass. **Rev 12 qualification**: that zero-skip claim is scoped to a
  **default-mode** run (no `-gatetask`). A **targeted** run legitimately skips the seven
  non-selected functions in any phase — those are selector skips, not activation skips — and the
  only targeted-mode requirement is that the **selected** test actually executes. Evidence for this
  criterion MUST therefore be taken from a default-mode invocation.
- **AC-C2.8** **Regeneration-resistant enforcement, recorded (rev 5; provenance corrected rev 12).**
  The evidence records that the full-corpus scan of AC-C2.1 is executed by
  `TestDirectPushGate_FullCorpusClean` from job `expensive` (`name: test`)'s test step — whose
  **observed installed** command is `go test -race -mod=readonly ./...` (rev 8 — cited precisely,
  because "the `go test` step" does not identify a step in this workflow) — and **not solely** by
  the job-`lint` step added in C1. **Rev 12 corrects what may be claimed about that command's
  provenance**: it is the **observed installed** command, **not** the generated baseline. The
  template emits `{{TEST_COMMAND}}` and this workspace's recorded render input is `go test ./...`
  (§5.0 "Invocation provenance"), so the property the evidence may assert is that a **full-suite
  `./...` invocation** exists in a job the render re-emits and the `code` filter cannot skip —
  which is exactly what makes `tests/integration` run. The evidence MUST NOT assert that the
  `-race` or `-mod=readonly` flags are render-guaranteed, and MUST cite AC-C1.7's post-render
  verification as the mechanism that keeps the `./...` property true across a regeneration.
  This is **separate from**, and does not replace, AC-C2.7's constitutional `go test ./...`
  requirement: that gate is run locally in declared order, this one names the CI step that survives
  a render. Both halves of the §5.0 invariant are asserted green in the same run: **(a)** zero
  findings over the installed surfaces, and **(b)** the job-`lint` step still present, structurally
  and non-vacuously per AC-C1.6. A future render that drops the C1 step therefore fails (b)
  **loudly** while (a) continues to execute — which is what makes §1's "cannot silently reopen the
  gap" true as written rather than aspirational. **Rev 11 supplies the property this criterion
  silently assumed**: both halves only execute if both functions are **active**, and at rev 10 both
  were activated by the very step whose removal they were meant to detect, so a render dropped the
  step *and* the two detectors together. Activation now comes from the harness-state manifest alone
  (AC-C2.9), so the evidence must record that C1 and C2 are in `completed` and that neither
  function's activation reads any surface it asserts on.
- **AC-C2.9** **Terminal completeness and activation independence, from manifest state (rev 11).**
  C2's completion sets `tests/integration/testdata/directpush-gate/harness-state.json` to the
  **exact eight-element ordered set** `["A1","A2","A3","B1","B2","B3","C1","C2"]` with
  `terminal: true` and `phase: terminal`, **atomically** — the three fields move together, and a
  partial write is an inconsistent triple that MP1 rejects. Inside
  `TestDirectPushGate_FullCorpusClean`, that completeness check is evaluated **from manifest state
  before any surface-dependent assertion in the function**, so no surface failure can mask the
  completeness verdict; the real full-corpus assertions then run. In `terminal` phase the default
  suite activates **all eight** substantive assertions and **no activation skip remains anywhere in
  the harness** — evidenced by zero `--- SKIP:` lines for the eight gate functions **in a
  default-mode run** (rev 12: a targeted run skips the seven non-selected functions by design and is
  not evidence for or against this clause). Neither C1 nor
  C2 derives activation from the job-`lint` step or from any other surface either function asserts
  on: once their IDs are in `completed` they are active **permanently**, so a regeneration that
  removes the lint step leaves both **active and failing loudly** rather than skipping. Targeted
  `-gatetask=C1` and `-gatetask=C2` execute their real assertions **regardless** of current surface
  presence, phase, or manifest contents (manifest **validity** is still enforced — MP1 fails closed
  in every mode). C2's completion also performs the one-line insertion into `completed` described in
  §5.0.1 — the shared harness-state completion record, not a second skill domain; C2's size band is
  unchanged.
- **AC-C2.10** **Terminal completion witness — independent, minimal, fail-closed (rev 12).** C2's
  completion creates `tests/integration/testdata/directpush-gate/terminal-witness.json`, a tracked,
  non-generated file **independent of the harness-state manifest**, with **exactly** these fields
  and no others: `schema` = `"directpush-gate/terminal-witness"`, `version` = `1`, `contract` =
  `"017-S/018-F"`, `completed` = the exact eight-element ordered set, and `digest` = the lowercase
  hex SHA-256 of those eight IDs joined with `\n` (no trailing newline), computed with stdlib
  `crypto/sha256`. It carries **no timestamps and no environment-specific data** — no host, user,
  path, branch, commit, or run identifier — so it is byte-reproducible from the contract alone.
  **Write order is normative**: the **witness is written first**, then the manifest is **atomically
  replaced** with the exact terminal state; the tests are green only when the two **agree**.
  `gate()` enforces MP6 **before any activation decision**: a present witness must be valid and
  requires a terminal manifest **and forces `C2` active**; a terminal manifest requires a present
  valid witness; a **missing, malformed, or mismatched** witness **wherever terminal or witness
  evidence exists** is **fail-closed**; and **absence at H0 and throughout `build` is valid**, not
  an error. Consequently a rollback of **only** the manifest and a rollback of **only** the witness
  each **fail loudly** (proven by execution — AC-C2.2 cases viii–x). **Recorded residual, not
  claimed as closed**: rolling back or deleting **both** files in one change yields a state that a
  stateless validator cannot distinguish from legitimate pre-C2 `build`, because that is what it
  looks like; it is caught by **diff and review** of two tracked non-generated files, **not** by
  runtime history (**R22**). No claim is made here that any stateless validator detects every
  coordinated rollback.
- **AC-C2.11** **Lifecycle evidence ledger (rev 12).** The PR evidence records all **eight**
  §5.0.2 observation points, each as a run command plus its observed output, never as narration:
  (1) **H0** — 8 expected failures with their own markers, **zero default-mode activation skips**,
  **witness absent**; (2) **post-A2** — A2 targeted green, A3 targeted expected-red, **default suite
  green**; (3) **legal B1/B2/B3 orders** — each dependency-closed order-preserving subsequence is
  **accepted**, no legal order rejected; (4) **post-C1** — **default suite green**, C2 targeted
  expected-red, **witness absent** (this is the state rev-11's MP5 deadlocked); (5) **terminal** —
  exact eight-task set **plus** valid witness, all 8 substantive assertions green, **zero
  default-mode skips**; (6) **terminal tree with the manifest rolled back to seven tasks while the
  witness remains** — **failure**; (7) **terminal manifest with the job-`lint` step removed** — C1
  **fails** and C2 **executes**; (8) **witness-only, terminal-manifest-only, and malformed witness**
  — **failure** in each. Points 1, 2 and 4 are captured at the boundaries where they occur; points
  3, 5, 6, 7 and 8 are executed or replayed at C2 under `t.TempDir()` per AC-C2.2's
  no-dirty-tree-edit rule. Every "zero skips" figure is taken from a **default-mode** run.
<!-- END:acceptance-criteria -->
