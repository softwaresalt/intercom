---
artifact_type: task
created_at: 2026-09-11T17:23:38.5643207Z
custom_fields:
    origin_feature: 018-F
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 020.004-T
id: 020.005-T
labels:
    - sub-epic-c
    - verification
    - chore
parent_id: 020-F
priority: high
status: blocked
title: Verify coupling with executed mutation proof
updated_at: 2026-09-12T20:38:05.2923567Z
---

DEFERRED / BLOCKED / READINESS-LOCKED. Parent feature 020-F (Strand B), shipment 019-S. 019-S is status blocked AND carries a blocks edge to readiness-gate shipment 020-S, so it is mechanically ineligible for Orchestrator Step 2 selection and for Ship claim. Do not execute. Stage unlocks only after 021.001-T records a reviewed harness-execution-model decision.

SCOPE. Prove the coupling between the corrected contract and the CI gate by EXECUTING a mutation, not by asserting that the wiring exists.
Over a COPY of the workspace - the real checkout is never mutated - revert one named property of the corrected contract, run the wired CI step, and require it to FAIL and to name the reverted property. Then restore the property and require it to PASS. The proof is the observed pair of outcomes; a passing wiring test that never observes a failure proves nothing.
The observation must be driven by the gate's own machine-readable output (for a Go step, the "go test -json" event stream parsed as events, never scraped text); a malformed or truncated stream FAILS the proof rather than reading as inconclusive. The real working tree must be byte-identically unchanged before and after, verified by comparing "git status --porcelain=v1 -z --untracked-files=all" snapshots.

DEPENDS ON 020.004-T: there must be a wired blocking gate to mutate against.

NON-GOVERNING PROVENANCE. This task body was fully rewritten on 2026-09-12 (rev 17 remediation). Its previous body carried the retired rev-11 to rev-15 harness-state manifest, terminal witness, -gatetask selector and MP0-MP6 mutation-proof machinery as ACTIVE acceptance criteria, together with a stale parent (018-F), stale predecessor IDs and a stale 017-S composition. All of that is WITHDRAWN and NON-GOVERNING. It is preserved, unedited, in git at: git show 14d64e4:.backlogit/queue/019.006-T.md and in the historical appendix of docs/plans/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-plan.md. Do not treat any of it as a requirement.

<!-- BEGIN:acceptance-criteria -->
- AC-1: With one named corrected-contract property reverted in a COPY of the workspace, the wired CI step FAILS with a non-zero exit and its output names the reverted property.
- AC-2: With that property restored, the same step PASSES. Both outcomes are OBSERVED in the same proof run; neither is assumed.
- AC-3: The observation parses the gate's machine-readable event stream; a malformed or truncated stream FAILS the proof and is never treated as inconclusive.
- AC-4: The real working tree is unchanged: "git status --porcelain=v1 -z --untracked-files=all" snapshots taken before and after the proof are byte-identical.
- AC-5: The proof is bounded - the child invocation runs under an explicit timeout and a deadline-exceeded or killed child FAILS the proof.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
SIZING: Size S (structured). Complexity medium (enum-validated prose; see 018.008-T degradation note).
2-HOUR RULE: one verification test exercising an existing wired gate over a workspace copy; single skill domain (tests).
HARNESS: DEFERRED. 020-F is NOT implementation-ready. This feature has five test-bearing tasks and the multi-task Step 4.3 deadlock is unresolved - see 021.001-T. Harness design MUST follow the option chosen there. Do NOT assume or resurrect the retired rev-11 manifest/witness/selector mechanism.
TRACEABILITY: adopted from the former 019.006-T (origin_feature 019-F) into 020-F on 2026-09-12.
<!-- END:implementation-notes -->