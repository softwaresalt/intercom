---
artifact_type: feature
created_at: 2026-09-11T17:18:45.0231601Z
custom_fields:
    harness_status: pending
id: 018-F
labels:
    - chore
    - policy
    - ci
    - governance
priority: high
references:
    - docs/plans/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-plan.md
    - docs/decisions/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-deliberation.md
    - docs/decisions/2026-09-12-intercom-go-stage-policy-gap-re-plan-decision.md
status: queued
title: Stage artifact branch/PR policy gap correction
updated_at: 2026-09-13T02:56:00.5148464Z
---

Close the PROHIBITION half of the pipeline policy gap that allowed Stage planning artifacts to reach the default branch without a pull request (historical evidence: commit fdff9e4). After this release unit Stage is forbidden to commit or push on the default branch, is granted a dedicated chore/stage-{scope-slug} artifact branch, and must pass a NON-NEGOTIABLE Step 1.9 branch gate before ANY tracked Stage artifact mutation. PERSISTENCE EXECUTION (push, PR, no-shipment terminal) AND MECHANICAL CI ENFORCEMENT ARE NOT DELIVERED HERE: they are deferred FEATURE STRANDS 019-F and 020-F, which belong to NO live shipment. Source deliberation: docs/decisions/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-deliberation.md. Governing contract: section R16 of docs/plans/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-plan.md. Stash origin: 638A410B. Classification: chore (technical-debt / contract correction); backlogit exposes no chore type, so the chore classification is carried on the 'chore' label.

<!-- BEGIN:goals -->
REDUCED CURRENT RELEASE UNIT (re-planned 2026-09-12, remediated 2026-09-12 rev 18). This feature was reduced from 8 tasks to 1. All rev-11 through rev-15 harness-activation instructions previously carried here are SUPERSEDED AND WITHDRAWN; audit history is preserved in docs/plans/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-plan.md (historical appendix) and in git, not here.

CURRENT CONTRACT - THE WHOLE OF IT. After this release unit, and ONLY this:
1. Stage is forbidden, in operative policy (P-010) and in its own agent contract, from committing or pushing artifacts on the default branch.
2. Stage is granted, in the same two surfaces, authority to create and check out a dedicated artifact branch chore/stage-{scope-slug}.
3. A NON-NEGOTIABLE Step 1.9 gate in _stage.agent.md is registered in the mandatory Step Sequence checklist and FAILS CLOSED before every tracked Stage artifact mutation of the session.
4. The OPERATOR - never Stage, and never the Orchestrator under any authority this unit creates - pushes the Stage artifact branch and opens/approves the merge-commit staging PR. Stage's PR prohibition is UNWEAKENED.

EXECUTABLE SET: exactly ONE task, 018.008-T (size M, complexity medium). Nothing else in this feature is executable.

SHIPMENT 017-S MANIFEST AND P-015 CLOSURE (repaired again 2026-09-12 rev 18; the rev-17 13-entry shape is WITHDRAWN). The manifest is the CLOSURE MEMBERSHIP RECORD, never the executable set. It now contains 12 entries and is TASK-ONLY: 018.008-T (the sole live task) plus the 11 pre-archived legacy descendants 018.001-T, 018.002-T, 018.003-T and their 8 subtasks. 018-F IS DELIBERATELY NOT A MEMBER.
WHY THE REV-17 13-ENTRY SHAPE WAS WRONG. Including 018-F made the manifest P-015-CASCADE-qualified, but it also made 018-F a manifest ITEM, and Ship Step 6 item 1.a invokes shipment-reconcile mode: pre with expected_status: done. Pre-mode step 3 checks .backlogit/queue/ FIRST and, when the record is found there, compares its declared status to expected_status with NO artifact-type exemption. At Step 6, 018-F is live in .backlogit/queue/ with status active (backlogit's claim moves the covering feature queued -> active) and NO Ship step ever moves a covering FEATURE to done - Step 4.5 moves TASKS only. 018-F would therefore classify status-mismatch, pre-mode would return HALT - operator reconcile required, and closure would never begin. Verified by execution against backlogit 1.10.1 in an isolated probe workspace: after a task-only claim and a task completion, the covering feature remained status: active and was never auto-completed.
WHY THE 12-ENTRY TASK-ONLY SHAPE IS CORRECT. Ship Step 0.5 item 2 expressly accepts task-only manifests and resolves the covering feature through each task's parent_id (the 097-S precedent). With 018-F excluded: pre-mode evaluates ONLY the 12 members - 018.008-T (relocated to .backlogit/archive/ with declared status done by Step 4.5's move --status done, hence classified pre-archived, which pre-mode accepts without a status check) and the 11 legacy members (already archived, classified pre-archived). Nothing is missing, nothing is status-mismatched, there are no orphans (no queue record declares shipment_id: 017-S), the record's own status is active hence record-consistent, and pre-mode returns PROCEED. 018-F is never evaluated, and stays queued/active and PROTECTED.
SAFE-CLOSE CLASSIFICATION AND PROTECTED SET. Step 0(c) returns SAFE_CLOSE, deterministically: the P-015 exception is quantified over every FEATURE MEMBER of the manifest, and precondition 4 requires the manifest to contain nothing beyond the qualifying root feature(s) and their descendants. With ZERO feature members there are zero qualifying roots, so every task member is "a task whose ancestry does not lead back to one of the qualifying root features" and the whole manifest falls back to safe-close. Safe-close step 2 then derives the covering feature 018-F from the manifest hierarchy, finds it ABSENT from the manifest, and classifies 017-S a partial-feature shipment: PROTECTED SET = {018-F} EXACTLY. Every other descendant of 018-F at every depth - the 3 legacy tasks and the 8 subtasks - IS a manifest member and therefore contributes nothing to the protected set. Step 3's baseline integrity gate requires every protected-set member to be present in .backlogit/queue/: 018-F is, so the gate PASSES. This is precisely the gate that HALTED under the earlier 2-entry manifest, where the 11 archived non-members were protected and the protected set has NO pre-archived exemption.
ARCHIVE AND POST-CLOSE STATE. Step 4 loops the manifest only: all 12 members are already in .backlogit/archive/ at that point, so every one classifies pre-archived and is SKIPPED - safe-close performs ZERO archival mutations on backlog items. The verify-after-each invariant and the final re-check both confirm {018-F} still present in .backlogit/queue/. Verified by execution: neither backlogit shipment claim nor a single-artifact archive mutates an archived manifest member (byte-identical file hashes before and after) and neither touches a covering feature that is not an explicit manifest member.
RESIDUAL CLOSURE BLOCKER - HONEST, PRE-EXISTING, NOT INTRODUCED HERE. Safe-close step 8 closes the shipment record by "backlogit move {shipment_id} --status shipped". backlogit 1.10.1 REFUSES that command for shipment artifacts with exit code 9 ("shipment must be shipped via ShipShipment, not a direct status update") - verified by execution in this cycle, and already recorded in this workspace by the 009-S safe-close report and by docs/compound/2026-05-07-backlogit-shipment-status-constraints.md. A SAFE_CLOSE-classified shipment therefore halts fail-closed at step 8 with RECONCILE_FAIL_SHIPMENT_RECORD_LIVE_STATUS. This is a WORKSPACE-WIDE tool/contract conflict affecting EVERY partial-feature shipment; NO manifest shape can avoid it, because the only shape that reaches the permitted cascade path is one containing 018-F, which fails pre-mode first. It is NOT remediable inside this release unit: resolving it requires either a backlogit change or a separately deliberated, reviewed amendment to shipment-reconcile step 8 / P-015, both explicitly out of scope. Recorded as a stash entry for Stage intake. Until it is resolved, 017-S executes, reviews and MERGES normally, and then PAUSES at Ship Step 6 closure for operator disposition.

LEGACY MEMBERS ARE NOT EXECUTABLE. 018.001-T, 018.002-T and 018.003-T are ARCHIVED provisional sub-epic containers from the original Stage session. They are manifest members for CLOSURE ONLY. Ship Step 3 item 1 filters the manifest to TASK ARTIFACTS - the AUTHORITATIVE predicate is the record's frontmatter artifact_type == "task", NEVER an ID suffix (a suffix test is wrong: subtask IDs end "-ST", whose last character is also "T", so it would admit all eight subtasks) - and then applies its positive status rule: KEEP queued/active, SKIP-AND-REPORT archived as pre_archived_skipped, REPORT done as already_done, FAIL CLOSED on anything else. Derived executable set is exactly [018.008-T]; the three archived tasks are pre_archived_skipped; the eight subtasks are excluded at the artifact_type filter BEFORE any status is read and never enter the derivation. They MUST NOT be unarchived, claimed, moved to active, or executed.

INTERIM PERSISTENCE - OPERATOR-ONLY AFTER STAGE HANDBACK. NO AUTOMATION IS CLAIMED AND NO ORCHESTRATOR AUTHORITY IS ASSERTED. This unit delivers the branch GATE only. It does NOT deliver a Stage commit step (deferred to 019.009-T), a Stage handback record (019.007-T), no-shipment terminal reachability (019.008-T), or Orchestrator branch discovery (019.004-T). Therefore:
(a) STAGE BRANCH GATE: Step 1.9 prevents Stage from performing ANY tracked artifact mutation on the default branch. All bounded Stage mutations happen on chore/stage-{scope-slug}.
(b) STAGE TERMINUS: Stage ENDS ITS SESSION AT A NAMED FAIL-CLOSED HANDBACK, STAGE_ARTIFACTS_UNCOMMITTED, with its bounded artifacts UNCOMMITTED on that branch under the four Stage artifact roots (.backlogit/, docs/plans/, docs/decisions/, docs/memory/). The artifacts are uncommitted BECAUSE commit and handback automation is DEFERRED (019.009-T / 019.007-T), not because leaving them uncommitted is desirable.
(c) THE ORCHESTRATOR MUST HALT HERE. The Orchestrator MUST NOT execute Step 1.5 item 3 (its uncommitted/unpushed arm) for this interim route. That arm commits Stage artifacts, and its sub-item 3(e) instructs a DIRECT PUSH TO main to be ATTEMPTED FIRST. Both actions contradict the Orchestrator's own installed Role statement ("You do NOT triage stash entries yourself. You do NOT write code or create PRs yourself"), and a direct-main attempt is exactly the behaviour this release unit exists to prohibit. The rev-17 claim that Step 1.5 item 3 is "naturally fail-closed" or supplies pre-existing Orchestrator authority to commit Stage artifacts is FACTUALLY WRONG and is WITHDRAWN. This release unit creates NO Orchestrator authority.
(d) OPERATOR-ONLY COMPLETION SEQUENCE. ONLY THE OPERATOR may: (1) verify the branch, worktree topology and the uncommitted artifact set; (2) commit ONLY the four allowed Stage artifact roots on that branch; (3) push the side branch; (4) open and approve a MERGE-COMMIT staging PR (P-009: no squash, no rebase); (5) update the default branch; (6) re-verify that the shipment manifest is present on origin/main. No agent performs any of these steps under this contract.
(e) CURRENT DARK RUN PAUSES AT THAT OPERATOR CHECKPOINT. Because the operator is AFK, current automation MAY NOT PROCEED PAST IT until the staging-automation work in 019-F ships. This means the dark pipeline CANNOT FINISH AUTONOMOUSLY after this release unit is implemented. That is stated plainly rather than papered over; a reviewer must not read any part of this feature as claiming otherwise.

DEFERRED FEATURE STRANDS - NO LIVE SHIPMENT, NOT CLAIMABLE, NOT IN THIS RELEASE UNIT. Strand A -> feature 019-F: handback record emission, stage_artifact_paths derivation, no-shipment terminal reachability, the Stage artifact commit step, out-of-root path safety, Orchestrator Step 1.5 branch-discovery and post-merge verification. Strand B -> feature 020-F: the structure-aware direct-push detector, fixture corpus, go test -json event parsing, regeneration-resistant CI wiring, CODEOWNERS and divergence-ledger updates. BOTH FEATURES AND ALL THEIR TASKS ARE status: blocked AND BELONG TO NO SHIPMENT. The rev-17 shipment candidates 018-S and 019-S, and the lock-token shipment 020-S, were ARCHIVED (backlogit archive) in rev 18 because backlogit's shipment status enum is exactly queued|active|shipped|abandoned - "blocked" is NOT a valid shipment status, and a queued shipment would be claimable. Stage will create FRESH shipment(s) only after P-006 plan review selects a valid harness execution model (021.001-T). A reviewer MUST NOT fail this unit for the absence of any deferred capability.

SINGLE-TASK LIFECYCLE - LOAD-BEARING. P-004 requires EVERY generated test function red before implementation, with the expected failure marker; Ship Step 4.3 requires the FULL "go test ./..." green after EVERY task. With two or more test-bearing tasks in one shipment those are jointly unsatisfiable. ONE queued task under 018-F means ONE generated function, ONE expected marker, a literal 1-of-1 red H0, one red-to-green transition, and a green full suite immediately. NO activation manifest, terminal witness, selector flag, surface predicate, or MP-series check is required or permitted. Adding a SECOND queued task under 018-F re-creates the deadlock and is FORBIDDEN.

SIZING NOTE (verified 2026-09-12): size is stored structurally. This workspace's .backlogit/header-def.yaml defines NO complexity field on the task type, so "backlogit update <id> --complexity <v>" fails validation even though the CLI exposes the flag and the backlog registry declares the param. Complexity is therefore carried as enum-validated prose in each task's implementation-notes. Registry/schema mismatch, reporting defect only, no execution effect.
<!-- END:goals -->

<!-- BEGIN:dod -->
DEFINITION OF DONE - REDUCED CURRENT RELEASE UNIT (re-planned 2026-09-12, remediated rev 18). All prior rev-6 through rev-15 DoD clauses are SUPERSEDED AND WITHDRAWN; history is preserved in the plan appendix and in git.

CONTRACT SURFACE 1 - .github/policies/workflow-policies.md (P-010):
- The Stage MAY commit bullet grants artifact commit ONLY on a dedicated chore/stage-* branch. No default-branch commit grant remains anywhere in P-010.
- P-010 carries an ACTOR-NAMED Stage MUST NOT bullet forbidding direct commit or push to the default branch, mirroring the existing Ship bullet whose exact Markdown text is: - Commit or push directly to `main` (backticked `main`; located by that quoted text, NEVER by line ordinal).
- P-010 carries an explicit Stage MAY grant to create and check out chore/stage-{scope-slug}, plus the clarifier that a Stage artifact branch/PR is neither an implementation branch under P-016 nor a release unit under P-001.
- P-016's "Allowed exception (Stage spike/research only)" paragraph is PRESERVED VERBATIM, byte-for-byte. The word "only" in the new commit grant constrains WHICH BRANCH Stage may commit on; it does not narrow, qualify, or delete the P-016 spike/research worktree exception.
- P-010's Amendment Log carries entry 1.25.0.

CONTRACT SURFACE 2 - .github/agents/_stage.agent.md Role Boundary:
- The Git row Allowed cell permits commits ONLY on the dedicated Stage artifact branch and includes the create/checkout grant for chore/stage-{scope-slug}.
- The Git row Allowed cell still carries the existing spike/research worktree clause unchanged, and the Git row Forbidden cell is unchanged.
- The PR row carries the actor note (the OPERATOR, never Stage, and never the Orchestrator under any authority created here) and its prohibition is UNWEAKENED.

CONTRACT SURFACE 3 - .github/agents/_stage.agent.md Step 1.9, an OPERATIVE MANDATORY FAIL-CLOSED GATE (not prose positioned before Step 2):
- A section titled "Step 1.9: Stage Artifact Branch Gate (NON-NEGOTIABLE)" exists strictly after Step 1.8 and strictly before Step 2.
- Step 1.9 is REGISTERED as its own line in the mandatory Step Sequence Contract checklist, inserted between the byte-exact anchors "[ ] Step 1.8 — Learnings retrieval" and "[ ] Step 2   — Deliberation" (EM DASH U+2014; three ASCII spaces after "Step 2"), located by exact text or by the documented normalized locator, with a STAGE_BRANCH_GATE_ANCHOR_FAIL halt on a zero-match or multi-match and NO ordinal insertion.
- The gate runs the P-016 worktree topology precheck FIRST ("git worktree list --porcelain", three-way classification, spike/research class PASSES and is not narrowed, prohibited/ambiguous HALTS) before any branch create, select or resume and before any tracked mutation.
- The gate specifies a DETERMINISTIC {scope-slug} derivation naming the EXACT source field for each intake shape, a five-step normalization in fixed order, an explicit 48-byte maximum, and the literal "stage-session" empty fallback. A shipment ID MAY be used when already known but is NEVER required, because Step 5.5 creates the shipment as the session's LAST mutation.
- The gate resolves base_ref from origin/HEAD (never assuming "main") and halts if it cannot.
- The gate specifies CREATE-OR-SELECT/RESUME plus an OWNERSHIP / BASE-IDENTITY / COLLISION DISCRIMINATOR: resume is permitted ONLY when the pre-existing branch shares a non-empty merge-base with origin/{base_ref} AND its two-dot-three diff touches only the four Stage artifact roots; any other same-name branch is a COLLISION that HALTS.
- The gate requires symbolic HEAD (git symbolic-ref --short HEAD) to EQUAL the expected branch, and requires the expected branch to DIFFER from the resolved base_ref; a detached HEAD fails the gate.
- The gate states a NAMED FAIL-CLOSED HALT (STAGE_BRANCH_GATE_FAIL) that precedes EVERY categorically enumerated tracked-mutation site of the session, with the categorical deferral rule that any such mutation otherwise scheduled earlier is DEFERRED until after the gate. The enumeration is CATEGORICAL and the named examples are ILLUSTRATIVE, not a closed list.

VERIFICATION - P-004 RED PHASE IS UNCONDITIONAL:
- At H0: go vet ./... exits 0; the harness run exits NON-ZERO; the output carries the literal expected marker "not implemented: stage branch gate contract" attributed to TestStageBranchGate_ContractCorrected; the H0 red count over this unit's generated functions is LITERALLY 1 of 1.
- After the sole task: the marker is gone, go vet ./... exits 0 and the FULL go test ./... exits 0.
- Exactly ONE test function was generated for this release unit, TestStageBranchGate_ContractCorrected, and it transitioned red to green exactly once.
- SCOPED ZERO-SKIP CLAIM: TestStageBranchGate_ContractCorrected and every assertion inside it execute UNCONDITIONALLY - no t.Skip, no t.Skipf, no build tag, no environment-variable or platform gate, no activation manifest, no selector flag, no surface predicate. This claim is scoped to the generated current-release-unit harness assertions ONLY. Pre-existing environment-conditional skips elsewhere in the repository (pwsh-availability guards in tests/integration/build_script_test.go and tests/integration/start_script_test.go; symlink and junction privilege guards in tests/integration/output_path_guard_test.go and internal/pathsafe/*) are OUT OF SCOPE, are neither introduced nor modified by this unit, and MUST NOT be counted against it. The earlier claim of "no skipped, pending, or conditionally-activated assertions anywhere in the suite" was factually unsatisfiable - 14 existing test files already call t.Skip - and is WITHDRAWN.

EXPLICITLY NOT REQUIRED BY THIS RELEASE UNIT (deferred to 019-F / 020-F, neither of which has a shipment): any direct-push detector, fixture corpus, go test -json event parser, harness-state manifest or terminal witness, CI job wiring, CODEOWNERS or divergence-ledger change, mutation-proof series, regeneration proof, the Stage handback record, the Stage artifact commit step, the no-shipment terminal, and the Orchestrator branch-discovery/verification arms. A reviewer MUST NOT fail this unit for their absence.

EXPLICITLY OUT OF SCOPE AND NOT A DEFECT OF THIS UNIT: the safe-close step 8 shipment-record closure conflict (backlogit refuses "move {shipment_id} --status shipped" with exit 9). It is pre-existing, workspace-wide, shape-independent, and requires either a backlogit change or a separately reviewed shipment-reconcile / P-015 amendment. It does not block implementation, review, CI or merge of this unit; it pauses Ship Step 6 closure for operator disposition.
<!-- END:dod -->

ACTOR RECONCILIATION (added 2026-09-12, Stage rev-5 remediation; does NOT weaken any prohibition above). The operator-only completion sequence stated above is CONFIRMED BY EXECUTION, not merely asserted. It was challenged on the ground that Ship's Role Boundary explicitly classifies Git branch/commit/push and PR create/update/merge as ALLOWED, which would make a "Ship in PR-lifecycle-only mode" handoff legitimate and would put the earlier "No agent performs any of these steps" phrasing in conflict with Ship's own contract. Stage TESTED that claim instead of reasoning about it (Probe 17: docs/plans/evidence/2026-09-12-task-only-shipment-finalization/probe17-ship-pr-lifecycle-gate.txt). RESULT: Ship's Role Boundary is NECESSARY BUT NOT SUFFICIENT. Ship Step 5 items 1a/5a mandate "autoharness gate pipeline-topology --mode agent --shipment {shipment_id} --phase lifecycle --json" under the rule "exit 1/2 halts immediately with the reported token/message (never inferred, never fail-open)". That gate is installed and live here (.autoharness/gates/pipeline-topology-force-audit.log records real operator --force use on 2026-09-05). All four routes FAIL CLOSED for an unclaimed session: no --shipment -> exit 2 "agent mode requires --shipment <shipment_id>"; --shipment 021-S while queued -> exit 1 "LIFECYCLE_NO_ACTIVE_SHIPMENT: expected exactly one active shipment"; --mode manual without shipment -> exit 2 "--phase lifecycle requires --shipment <shipment_id> in any mode"; --phase ambient -> exit 2. The gate passes ONLY with exactly one ACTIVE shipment, which requires a CLAIM - precisely what a PR-lifecycle-only session must not do, and what the 021-S sequencing gate forbids before PR #54 merges. "--force" is documented "Operator-only ... Audited. Never reachable from an agent surface." CONCLUSION: the OPERATOR remains the authorized PR actor and the operator-only language above STANDS UNWEAKENED - now evidence-backed rather than assumed. The distinction that matters: this is a SEQUENCE limitation, not a Role Boundary limitation, so nothing here contradicts Ship's Role Boundary table. SEPARATELY AND INDEPENDENTLY: no non-Stage actor may modify planning content in any mode - Ship's own Role Boundary lists "Create or modify deliberation, spike, plan, or review artifacts" as FORBIDDEN - so any review comment requiring a planning or contract change PAUSES and ROUTES BACK TO STAGE, whoever is carrying the PR. See section 11 of docs/plans/2026-09-12-intercom-go-task-only-shipment-finalization-plan.md.