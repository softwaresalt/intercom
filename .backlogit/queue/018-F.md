---
artifact_type: feature
created_at: 2026-09-11T17:18:45.0231601Z
custom_fields:
    harness_status: pending
id: 018-F
labels:
    - chore
    - policy
    - ci
    - governance
priority: high
references:
    - docs/plans/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-plan.md
    - docs/decisions/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-deliberation.md
    - docs/decisions/2026-09-12-intercom-go-stage-policy-gap-re-plan-decision.md
status: queued
title: Stage artifact branch/PR policy gap correction
updated_at: 2026-09-12T20:30:53.4855225Z
---

Close the PROHIBITION half of the pipeline policy gap that allowed Stage planning artifacts to reach the default branch without a pull request (historical evidence: commit fdff9e4). After this release unit Stage is forbidden to commit or push on the default branch, is granted a dedicated chore/stage-{scope-slug} artifact branch, and must pass a NON-NEGOTIABLE Step 1.9 branch gate before ANY tracked Stage artifact mutation. PERSISTENCE EXECUTION (push, PR, no-shipment terminal) AND MECHANICAL CI ENFORCEMENT ARE NOT DELIVERED HERE: they are deferred to 019-F (persistence route, shipment 018-S) and 020-F (mechanical enforcement platform, shipment 019-S), both readiness-locked behind 020-S. Source deliberation: docs/decisions/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-deliberation.md. Governing contract: section R16 of docs/plans/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-plan.md. Stash origin: 638A410B. Classification: chore (technical-debt / contract correction); backlogit exposes no chore type, so the chore classification is carried on the 'chore' label.

<!-- BEGIN:goals -->
REDUCED CURRENT RELEASE UNIT (re-planned 2026-09-12, remediated 2026-09-12 rev 17). This feature was reduced from 8 tasks to 1. All rev-11 through rev-15 harness-activation instructions previously carried here are SUPERSEDED AND WITHDRAWN; audit history is preserved in docs/plans/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-plan.md (historical appendix) and in git, not here.

CURRENT CONTRACT - THE WHOLE OF IT. After this release unit, and ONLY this:
1. Stage is forbidden, in operative policy (P-010) and in its own agent contract, from committing or pushing artifacts on the default branch.
2. Stage is granted, in the same two surfaces, authority to create and check out a dedicated artifact branch chore/stage-{scope-slug}.
3. A NON-NEGOTIABLE Step 1.9 gate in _stage.agent.md is registered in the mandatory Step Sequence checklist and FAILS CLOSED before every tracked Stage artifact mutation of the session.
4. The Orchestrator or the operator - never Stage - pushes the branch and opens/merges the staging PR. That prohibition is UNWEAKENED.

EXECUTABLE SET: exactly ONE task, 018.008-T (size M, complexity medium). Nothing else in this feature is executable.

SHIPMENT 017-S MANIFEST AND P-015 CLOSURE (repaired 2026-09-12). The manifest is the CLOSURE MEMBERSHIP RECORD, never the executable set. It now contains 13 entries: 018-F plus its COMPLETE descendant closure at every depth - 018.008-T (queued) and the 11 pre-archived legacy descendants 018.001-T, 018.002-T, 018.003-T and their 8 subtasks. This is REQUIRED, not cosmetic. With the earlier 2-entry manifest, P-015 / shipment-reconcile classified 017-S as SAFE_CLOSE (018-F was not fully covered), then computed the 11 archived non-manifest descendants into the PROTECTED SET, and its baseline integrity gate - which requires every protected-set member to be present in .backlogit/queue/ and grants NO pre-archived exemption to the protected set - would have halted closure with "HALT - cascade detected, revert required". 017-S was mechanically UNCLOSABLE. With full descendant coverage the classifier now returns CASCADE (018-F is a root, fully covered at every depth, manifest contains nothing else, no torn records), and a CASCADE manifest HAS NO PROTECTED SET BY CONSTRUCTION. P-015 exception item 7 expressly tolerates pre-archived manifest members: they have no transition to report and are correctly absent from archived_ids. required_ids for the two-set gate is exactly {017-S, 018-F, 018.008-T}. 018-F carries no custom_fields.source_deliberation_id and no ID matching the engine deliberation matcher, so the cascade archives no linked deliberation. The members were added with the SUPPORTED operation "backlogit shipment add" (MCP backlogit_add_to_shipment), which did not mutate a single archived record: every archived file is byte-identical and still declares status: archived / archived_status: queued.

LEGACY MEMBERS ARE NOT EXECUTABLE. 018.001-T, 018.002-T and 018.003-T are ARCHIVED provisional sub-epic containers from the original Stage session. They are manifest members for CLOSURE ONLY. Ship Step 3 item 1 filters the manifest to task artifacts, then applies its positive status rule: KEEP queued/active, SKIP-AND-REPORT archived as pre_archived_skipped. Verified mechanically: Ship's derived executable set is exactly [018.008-T]; the three archived tasks are pre_archived_skipped; the 8 subtasks are not task artifacts and never enter the derivation. They MUST NOT be unarchived, claimed, moved to active, or executed.

INTERIM PERSISTENCE - WHAT ACTUALLY HAPPENS. NO AUTOMATION IS CLAIMED. This unit delivers the branch GATE only. It does NOT deliver a Stage commit step (deferred to 019.009-T), a Stage handback record (019.007-T), no-shipment terminal reachability (019.008-T), or Orchestrator branch discovery (019.004-T). Therefore:
(a) STAGE TERMINUS: Stage creates or selects chore/stage-{scope-slug} at Step 1.9, performs its bounded tracked mutations under the Stage artifact roots (.backlogit/, docs/plans/, docs/decisions/, docs/memory/), and ENDS THE SESSION WITH THOSE ARTIFACTS UNCOMMITTED on that branch. Stage commit production is explicitly OUT OF SCOPE of 018.008-T.
(b) The installed Orchestrator Step 1.5 is MAIN-SCOPED: its step 2 evaluates "git log origin/main..main", which is empty whenever Stage worked on a side branch. It can neither discover chore/stage-{scope-slug} nor guarantee PR-only persistence on its own.
(c) FAIL-CLOSED TERMINAL: absent a manual sequence, Orchestrator Step 1.5 step 4 ("git show origin/main:.backlogit/queue/{shipment_id}.md") FAILS and halts with STAGING_GATE_FAIL. That halt is the INTENDED interim behaviour - the pipeline stops rather than silently persisting to the default branch.
(d) THE ONLY REACHABLE INTERIM SEQUENCE: 1. Stage hands back the branch name and the uncommitted artifact set. 2. The ORCHESTRATOR (pipeline invocation, acting under its OWN installed Step 1.5 item 3 authority) or the OPERATOR (direct invocation) verifies branch and worktree state. 3. Commits the Stage artifact roots on that branch. 4. Pushes the branch. 5. Opens and merges the staging PR into the default branch. 6. Pulls the default branch. 7. Re-runs Step 1.5 step 4 and confirms the shipment manifest is present on origin/main. Only then may Ship be routed.
AUTHORITY NOTE - NO NEW GRANT IS CREATED: the Orchestrator's authority to commit, push and open the staging PR is the one already written in ITS OWN installed agent contract at Step 1.5 item 3 ("Staging Artifact Merge Gate (NON-NEGOTIABLE)"). P-010 enumerates no Orchestrator MAY/MUST NOT bullets beyond "must not perform Stage or Ship work directly". This release unit grants the Orchestrator NOTHING NEW and does not amend P-010's Orchestrator statement. If an operator judges Step 1.5's commit arm to exceed the Orchestrator role boundary, the OPERATOR performs steps 2-7 directly; that substitution is always available under installed permissions and needs no contract change.
WITHDRAWN OVERCLAIM: the previous assertion that this is "exactly the interim behaviour already in use" is FALSE and is WITHDRAWN. The behaviour in use before this unit permitted Stage to commit on the default branch - the very defect being closed.

DEFERRED, READINESS-LOCKED, NOT IN THIS RELEASE UNIT. Strand A -> 019-F / shipment 018-S: handback record emission, stage_artifact_paths derivation, no-shipment terminal reachability, the Stage artifact commit step, out-of-root path safety, Orchestrator Step 1.5 branch-discovery and post-merge verification. Strand B -> 020-F / shipment 019-S: the structure-aware direct-push detector, fixture corpus, go test -json event parsing, regeneration-resistant CI wiring, CODEOWNERS and divergence-ledger updates. Both shipments are status blocked AND carry a blocks edge to the readiness-gate shipment 020-S. A reviewer MUST NOT fail this unit for the absence of any of it.

SINGLE-TASK LIFECYCLE - LOAD-BEARING. P-004 requires EVERY generated test function red before implementation; Ship Step 4.3 requires the FULL "go test ./..." green after EVERY task. With two or more test-bearing tasks in one shipment those are jointly unsatisfiable. ONE queued task under 018-F means ONE red function, one red-to-green transition, and a green full suite immediately. NO activation manifest, terminal witness, selector flag, surface predicate, or MP-series check is required or permitted. Adding a SECOND queued task under 018-F re-creates the deadlock and is FORBIDDEN.

SIZING NOTE (verified 2026-09-12): size is stored structurally. This workspace's .backlogit/header-def.yaml defines NO complexity field on the task type, so "backlogit update <id> --complexity <v>" fails validation even though the CLI exposes the flag and the backlog registry declares the param. Complexity is therefore carried as enum-validated prose in each task's implementation-notes. Registry/schema mismatch, reporting defect only, no execution effect.
<!-- END:goals -->

<!-- BEGIN:dod -->
DEFINITION OF DONE - REDUCED CURRENT RELEASE UNIT (re-planned 2026-09-12, remediated rev 17). All prior rev-6 through rev-15 DoD clauses are SUPERSEDED AND WITHDRAWN; history is preserved in the plan appendix and in git.

CONTRACT SURFACE 1 - .github/policies/workflow-policies.md (P-010):
- The Stage MAY commit bullet grants artifact commit ONLY on a dedicated chore/stage-* branch. No default-branch commit grant remains anywhere in P-010.
- P-010 carries an ACTOR-NAMED Stage MUST NOT bullet forbidding direct commit or push to the default branch, mirroring the existing Ship bullet whose exact Markdown text is: - Commit or push directly to `main` (backticked `main`; located by that quoted text, NEVER by line ordinal).
- P-010 carries an explicit Stage MAY grant to create and check out chore/stage-{scope-slug}, plus the clarifier that a Stage artifact branch/PR is neither an implementation branch under P-016 nor a release unit under P-001.
- P-016's "Allowed exception (Stage spike/research only)" paragraph is PRESERVED VERBATIM, byte-for-byte. The word "only" in the new commit grant constrains WHICH BRANCH Stage may commit on; it does not narrow, qualify, or delete the P-016 spike/research worktree exception.
- P-010's Amendment Log carries entry 1.25.0.

CONTRACT SURFACE 2 - .github/agents/_stage.agent.md Role Boundary:
- The Git row Allowed cell permits commits ONLY on the dedicated Stage artifact branch and includes the create/checkout grant for chore/stage-{scope-slug}.
- The Git row Allowed cell still carries the existing spike/research worktree clause unchanged, and the Git row Forbidden cell is unchanged.
- The PR row carries the actor note (Orchestrator or operator, never Stage) and its prohibition is UNWEAKENED.

CONTRACT SURFACE 3 - .github/agents/_stage.agent.md Step 1.9, an OPERATIVE MANDATORY FAIL-CLOSED GATE (not prose positioned before Step 2):
- A section titled "Step 1.9: Stage Artifact Branch Gate (NON-NEGOTIABLE)" exists strictly after Step 1.8 and strictly before Step 2.
- Step 1.9 is REGISTERED as its own line in the mandatory Step Sequence Contract checklist, between "[ ] Step 1.8 - Learnings retrieval" and "[ ] Step 2   - Deliberation", so the existing rule that the summary is blocked until every applicable step is complete covers it.
- The gate specifies a DETERMINISTIC {scope-slug} derivation that resolves BEFORE the first tracked mutation for BOTH intake shapes (feature-shaped: Step 1 classification; task-shaped: Step 1.5 grouping selection), and states that a shipment ID MAY be used when already known but is NEVER required, because Step 5.5 creates the shipment as the session's LAST mutation.
- The gate specifies CREATE-OR-SELECT/RESUME behaviour for chore/stage-{scope-slug}: create when absent, reuse when already checked out, and defined resumption behaviour on collision with an existing branch of that name.
- The gate requires symbolic HEAD (git symbolic-ref --short HEAD) to EQUAL the expected branch, and requires the expected branch to DIFFER from the configured default branch; a detached HEAD fails the gate.
- The gate states a NAMED FAIL-CLOSED HALT that precedes EVERY categorically enumerated tracked-mutation site of the session, with the categorical deferral rule that any such mutation otherwise scheduled earlier is DEFERRED until after the gate. The enumeration is CATEGORICAL and the named examples are ILLUSTRATIVE, not a closed list.

VERIFICATION:
- go vet ./... exits 0 and go test ./... exits 0.
- Exactly ONE test function was generated for this release unit, TestStageBranchGate_ContractCorrected, and it transitioned red to green exactly once.
- SCOPED ZERO-SKIP CLAIM: TestStageBranchGate_ContractCorrected and every assertion inside it execute UNCONDITIONALLY - no t.Skip, no t.Skipf, no build tag, no environment-variable or platform gate, no activation manifest, no selector flag, no surface predicate. This claim is scoped to the generated current-release-unit harness assertions ONLY. Pre-existing environment-conditional skips elsewhere in the repository (pwsh-availability guards in tests/integration/build_script_test.go and tests/integration/start_script_test.go; symlink and junction privilege guards in tests/integration/output_path_guard_test.go and internal/pathsafe/*) are OUT OF SCOPE, are neither introduced nor modified by this unit, and MUST NOT be counted against it. The earlier claim of "no skipped, pending, or conditionally-activated assertions anywhere in the suite" was factually unsatisfiable - 14 existing test files already call t.Skip - and is WITHDRAWN.

EXPLICITLY NOT REQUIRED BY THIS RELEASE UNIT (deferred to 019-F / 020-F): any direct-push detector, fixture corpus, go test -json event parser, harness-state manifest or terminal witness, CI job wiring, CODEOWNERS or divergence-ledger change, mutation-proof series, regeneration proof, the Stage handback record, the Stage artifact commit step, the no-shipment terminal, and the Orchestrator branch-discovery/verification arms. A reviewer MUST NOT fail this unit for their absence.
<!-- END:dod -->