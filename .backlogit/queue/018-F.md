---
artifact_type: feature
created_at: 2026-09-11T17:18:45.0231601Z
custom_fields:
    harness_status: pending
id: 018-F
labels:
    - chore
    - policy
    - ci
    - governance
priority: high
references:
    - docs/plans/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-plan.md
    - docs/decisions/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-deliberation.md
status: queued
title: Stage artifact branch/PR policy gap correction
updated_at: 2026-09-12T02:45:00.0000000Z
---

Close the pipeline policy gap that allowed Stage planning artifacts to reach the default branch without a pull request (historical evidence: commit fdff9e4). Makes a dedicated Stage/admin branch plus PR the only path by which any Stage artifact reaches the default branch - including when no shipment is formed - and enforces it mechanically with a deterministic CI regression gate. Source deliberation: docs/decisions/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-deliberation.md. Implementation plan: docs/plans/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-plan.md. Stash origin: 638A410B. Classification: chore (technical-debt / contract correction); backlogit exposes no chore type, so the chore classification is carried on the 'chore' label.

<!-- BEGIN:goals -->
Make a dedicated Stage/admin branch plus a pull request the ONLY path by which a Stage artifact reaches the default branch - including when no shipment is formed - and enforce it mechanically so regeneration of the autoharness-generated contract files cannot silently reopen the gap.

EXECUTION ORDER (test-first, enforced by 'blocks' dependency edges):
0. H0 Ship Step 2 harness-architect (NOT a task, no dependency edge) -> produces tests/integration/directpush_gate_test.go (8 per-task functions plus the rev-11 gate() helper, gateOrder and gateDeps) plus the not-implemented scripts/check-direct-push-language.sh stub plus the harness-state manifest tests/integration/testdata/directpush-gate/harness-state.json initialized to {"phase":"red","completed":[],"terminal":false}; go vet ./... exits 0, go test ./... RED with ALL EIGHT functions failing on their own task-specific 'not implemented:' markers (the literal 8/8), then harness-ready is applied. Plan sections 5.0 and 5.0.1. REV 13 - H0 ELIGIBILITY, RESOLVED AGAINST THE INSTALLED CONTRACT WITHOUT CHANGING IT: _ship.agent.md STEP 2 selects the shipment's FULL QUEUED TASK SET for harness generation (it runs ONCE UP FRONT, NOT IN A LOOP; lists QUEUED tasks; partitions SOLELY on the harness-ready label; and HALTS unless EVERY queued task carries it afterwards), while STEP 3 SEPARATELY builds the dependency-ordered execution queue. IMPLEMENTATION DEPENDENCIES GATE CLAIM AND EXECUTION, NEVER UPFRONT HARNESS GENERATION. harness-architect Step 1 excludes 'blocked' as a BACKLOG STATUS VALUE, not a derived dependency state, and ALL EIGHT 017-S TASKS ARE QUEUED. H0 therefore batches ALL EIGHT explicitly selected queued shipment tasks even though seven have unmet implementation dependencies, and verifies LITERAL 8/8 RED before labelling any harness-ready. MECHANICAL CHECK: the H0 selected set must EQUAL the eight task IDs 018.004-T, 018.005-T, 018.006-T, 018.007-T, 018.008-T, 018.009-T, 018.010-T, 018.011-T, with 018-F resolved via parent_id and NEVER executed; no task may be omitted for dependency state and a proper subset is an H0 FAILURE. If a future contract is read to say otherwise, REPORT THE CONTRADICTION rather than relaxing the 8/8 requirement.
1. 018.004-T fixture corpus + manifest
2. 018.005-T fixture-suite driver over stubbed detector -> REGRESSION HARNESS OBSERVED RED
3. 018.006-T structure-aware detector           -> fixtures green, real-tree scan RED
4. 018.007-T / 018.008-T / 018.009-T contract correction (4 surfaces + rev-6 discovery/handback + rev-7 concrete-path verification + rev-8 aggregate base-to-head verification and reachable no-shipment terminal + rev-9 operative ordered branch gate and artifact commit + rev-10 fail-closed outcome pair, categorical pre-gate deferral and implementable out-of-root detection) -> real-tree scan GREEN
5. 018.010-T CI wiring, divergence ledger, CODEOWNERS
6. 018.011-T coupling verification + fixture-backed mutation proof

HARNESS NOTE FOR SHIP (rev 4, activation added rev 10, activation MECHANISM REPLACED rev 11): NO task in this shipment carries a harness-ready label, and none should be labelled by prose. harness_status stays 'pending' until harness-architect runs at Step 2 and records Compilation: PASS / Red Phase: CONFIRMED - which at rev 11 requires the LITERAL 8/8 expected-red evidence (eight '--- FAIL:' lines, each carrying its own task-specific 'not implemented:' marker). Each task records its harness function and harness_cmd so build-feature has a one-task-wide boundary with exactly one observable red->green transition. Plan section 10.1 declares NO P-002/P-004 deviation: the existing Go harness path is satisfied literally by a real Go test whose subject is the bash gate, matching the existing tests/integration/build_script_test.go precedent. Do NOT amend P-002, P-004, _ship.agent.md Step 2, the harness-architect skill, or _ship.agent.md Step 4.3's full-suite scope - ALL explicitly out of scope (plan section 11). The rev-11 harness-state manifest is what makes amending any of them unnecessary: it is an H0 deliverable inside the harness's own tests/ tree, strictly smaller than the non-Go stub script harness-architect already emits.
PER-TASK ACTIVATION - READ THIS BEFORE STEP 4.3 (rev 10 mechanism REPLACED at rev 11; plan section 5.0.1): the rev 4-9 harness contract required all EIGHT per-task functions to be red after H0 and to stay red until their task landed, while Ship's Step 4.3 runs the FULL test suite (go test ./...) after EVERY task. The moment 018.004-T went green the seven future functions were still intentionally red, Step 4.3 failed, and NO TASK COULD EVER COMPLETE. Rev 10 fixed that with an eight-row table of SURFACE PREDICATES (a function activated iff its own deliverable was present), and that fix was ITSELF DEFECTIVE ON TWO COUNTS - both confirmed UNANIMOUSLY by a four-model adversarial panel as P0 blockers. (1) SELF-DISARMING: 018.010-T's predicate was the presence of the job-lint step - the exact proposition 018.010-T exists to assert - and 018.011-T reused that predicate verbatim, so a regeneration dropping the step made BOTH functions t.Skip instead of fail, MP2 (inside 018.011-T) never ran, and go test ./... stayed GREEN. (2) CONTRACT BREACH: the installed P-002/P-004/harness-architect contract requires go test ./... RED WITH ALL GENERATED TESTS FAILING THEIR EXPECTED NOT-IMPLEMENTED MARKERS before any task is labelled harness-ready, and one failing anchor plus seven skips is not that.
REV-11 MECHANISM (authoritative - supersedes the rev-10 table entirely): the harness carries ONE selector flag (-gatetask, dot-free so it parses identically under bash, pwsh and cmd) and ONE tracked, non-generated, fail-closed HARNESS-STATE MANIFEST at tests/integration/testdata/directpush-gate/harness-state.json. THERE IS NO SURFACE PREDICATE IN THE ACTIVATION PATH AT ALL - that edge is DELETED, not exempted, which is what closes the class rather than the two instances found. Activation derives ONLY from the manifest and the selector. THREE PHASES. RED (H0): all eight activate, NONE skips, all eight fail with their own task-specific 'not implemented: <ID> <surface>' marker - the LITERAL 8/8, and ONLY THEN may harness-ready be applied. BUILD: 018.004-T's completion performs the ONE-WAY red -> build transition; default mode then runs the completed set and skips only not-yet-started tasks, so STEP 4.3 IS GREEN AFTER EVERY TASK. TERMINAL: 018.011-T's completion sets completed to the exact eight-element ordered set with terminal=true and phase=terminal ATOMICALLY, after which all eight substantive assertions run and NO ACTIVATION SKIP REMAINS ANYWHERE. TARGETED MODE (-gatetask={ID}) forces exactly that task's REAL assertion in EVERY phase and regardless of surface state - every harness_cmd carries it, which is how each task keeps its observed red at claim time. A TEST IS NEVER GREEN MERELY BECAUSE ITS ID WAS WRITTEN: the manifest controls ACTIVATION only, never the verdict. REV 13 - SELECTOR PRECEDENCE, MADE STRUCTURAL: gate() runs in TWO EXPLICITLY ORDERED STAGES. STAGE 1 (INTEGRITY) IS MODE-INDEPENDENT - MP1 manifest validity and MP6 witness/manifest cross-agreement both FAIL CLOSED on EVERY invocation, targeted or default, before any activation decision. STAGE 2 (ACTIVATION) IS MODE-DEPENDENT, and A VALID -gatetask SELECTOR HAS ACTIVATION PRECEDENCE OVER EVERY DEFAULT-MODE RULE INCLUDING MP6's FORCING CLAUSE, so a targeted run executes EXACTLY the selected test and SKIPS THE OTHER SEVEN in EVERY phase - INCLUDING terminal, REGARDLESS OF WITNESS PRESENCE. The rev-12 table listed the witness row BEFORE the -gatetask rows unqualified by mode, which read sequentially made a targeted terminal-state run activate all eight bodies, contradicting MP3 and AC-C2.7/AC-C2.9/AC-C2.11. WITNESS INTEGRITY MAY STILL FAIL A TARGETED INVOCATION; WITNESS PRESENCE MAY NEVER ACTIVATE A TEST BODY THE OPERATOR DID NOT SELECT. This subtracts NO detection, because the rewind MP6 exists to catch is caught by the MODE-INDEPENDENT integrity half. REV 13 - CONTROL-STATE INPUTS ARE EXPRESSLY EXEMPT, NARROWLY: the independence invariant is scoped to SUBSTANTIVE SURFACES (agent/policy/workflow/script behaviour a test asserts on); the harness's DEDICATED CONTROL-STATE INPUTS - the validated manifest and the terminal witness - are ALLOWED AND REQUIRED inputs to MP1 and MP6, are covered by their own separate mutation/integrity assertions, and are NOT substantive surfaces whose absence can self-disable their own check (a missing manifest FAILS ALL EIGHT, whereas a missing lint step under rev 10 SILENTLY SKIPPED its own detector - opposite failure directions, which is the whole test). NOTHING ELSE MAY BE ADDED TO THE EXEMPT SET.
COMPLETION OBLIGATION FOR EVERY TASK: atomically with its substantive work, each task inserts its own ID into the manifest's completed array at its gateOrder position. completed must remain an ORDER-PRESERVING SUBSEQUENCE of [A1,A2,A3,B1,B2,B3,C1,C2] that is DEPENDENCY-CLOSED. Because 018.007-T/018.008-T/018.009-T are PARALLEL (each blocks only on 018.006-T; 018.010-T requires all three), the rule is subsequence-plus-closure and NOT a strict prefix, so any of the three may land first. SCOPE: this one-line edit to a shared state file H0 authored is the HARNESS-STATE COMPLETION RECORD - NOT a second skill domain, NOT a new deliverable; no task's size or complexity changes and every task stays inside the 2-hour rule.
SIX ACTIVE MUTATION-PROOF CHECKS (MP0, MP1, MP2, MP3, MP4, MP6) stop this degenerating into a permanently skipped suite - MP5 IS RETIRED AT REV 12, REMOVED AND NOT RENUMBERED, AND MUST NOT BE IMPLEMENTED: MP0 gateOrder/gateDeps integrity (eight IDs; each gateDeps row lists that task's PREREQUISITES - backlogit stores edges as dep add <item> <depends-on> --type blocks, so a recorded edge reads DEPENDENT -> PREREQUISITE despite the 'blocks' label, and reading it the other way inverts the graph); MP1 manifest validity enforced INSIDE gate() ON EVERY CALL, failing closed on a missing, malformed, unknown-phase, duplicate, unknown-ID, non-subsequence, dependency-open, or inconsistent phase/terminal state - so a corrupt state file fails ALL EIGHT functions rather than silently disabling them; MP2 terminal completeness in 018.011-T, evaluated FROM MANIFEST STATE BEFORE any surface-dependent assertion; MP3 visible t.Skip reasons plus a STRICT selector that FAILS on an unknown ID, with skips structurally impossible in red and terminal phase; MP4 the state files are THEMSELVES part of the mutation proof - TEN cases (deletion, malformed JSON, unknown phase, rollback, reordering, skipped prerequisite, duplicate/unknown ID, plus the rev-12 witness cases: witness-only, terminal-manifest-only, malformed/mismatched witness) proven to FAIL rather than skip against a t.TempDir() scratch copy (AC-C2.2); MP6 (rev 12) WITNESS/MANIFEST CROSS-AGREEMENT, run inside gate() BEFORE ANY ACTIVATION DECISION. MP5 IS RETIRED AT REV 12: its rev-11 rule (a task outside completed must have its surface ABSENT, else FAIL) was WRONG - 018.011-T's probe was the job-lint step that 018.010-T ITSELF CREATES, so the entirely legitimate post-018.010-T / pre-018.011-T window failed the DEFAULT SUITE, reintroducing the exact Ship Step 4.3 deadlock the mechanism exists to prevent. THE RULE IS NOW SYMMETRIC: NEITHER ACTIVATION NOR ANY GATE-LEVEL FAILURE MAY BE DERIVED FROM A SURFACE THIS HARNESS ASSERTS ON. Nothing is lost: a COMPLETED task's surface rollback is caught by THAT TASK'S OWN SUBSTANTIVE ASSERTION (active in default mode from its completion onward), manifest/witness rollback by MP1 and MP6, and an INCOMPLETE task legitimately may have prerequisites and surface fragments present. The stale 'MP1 probe non-vacuity' attribution on 018.004-T is WITHDRAWN with it - MP1 is the manifest validator and has no probes. HARNESS-READY SEMANTICS ARE UNCHANGED AND NOW STRONGER: the label still means compilation clean plus default suite red, which is exactly what harness-architect verifies, and at rev 11 that red IS the literal 8/8, so the label no longer implies more than the evidence shows. The per-task red remains a per-task obligation discharged by Ship at claim time. The manifest is IMPLEMENTATION/HARNESS STATE, NEVER BACKLOG STATUS - it is never derived from or reconciled against .backlogit/ item statuses, so no status edit can activate or deactivate an assertion.

MANIFEST NOTE FOR SHIP: the authoritative membership of shipment 017-S is its explicit 'items' array - exactly 9 IDs (018-F plus 018.004-T through 018.011-T). The derived size_composition roll-up additionally reports 3 'unsized' members (018.001-T, 018.002-T, 018.003-T). Those three are ARCHIVED provisional sub-epic containers from this same Stage session, retired once it was verified that this workspace's backlogit schema stores size/complexity only on the 'task' type. They are NOT in scope, NOT in the shipment items array, and must NOT be executed.

SIZING NOTE: 'size' is stored structurally. This workspace's schema defines NO 'complexity' field on any artifact type, so complexity is carried as enum-validated prose in each task's implementation-notes section per the Stage two-axis sizing contract.
<!-- END:goals -->

<!-- BEGIN:dod -->
- No 'git push origin main' or equivalent direct-push path exists for Stage anywhere in the contract corpus.
- All Stage artifact persistence uses a dedicated branch plus PR, explicitly including the case where NO shipment is formed.
- THE NO-SHIPMENT ROUTE IS EXECUTABLE END TO END, NOT ONLY AUTHORIZED (rev 6). Orchestrator Step 1.5 receives the Stage artifact branch EXPLICITLY as a staging handback record (stage_branch, stage_base_commit, stage_head_commit, stage_outcome, stage_artifact_paths) and inspects a BRANCH-SPECIFIC range (origin/main..HEAD, equivalently origin/main..$STAGE_BRANCH, proven equal by a HEAD assertion) rather than the default branch against its own remote-tracking ref, which is empty by construction once Stage commits on chore/stage-*. The branch-resolution block sits ABOVE step 1's dirty/clean split, so its rev-parse --verify and HEAD-equality guards run on the dirty path too - the path that actually mutates the repo. Step 1's dirtiness pathspec is the FIXED STAGE_ARTIFACT_ROOTS constant (.backlogit/ docs/plans/ docs/decisions/ docs/memory/), not .backlogit/ alone and NOT the handback field (rev 7), so neither a docs-only run nor a narrow handback can be reported clean. Step 4 carries a SECOND VERIFICATION ARM for stage_outcome: no-shipment - a concrete post-merge target that names NO shipment_id. No self-deadlock and NO WEDGE: Stage creates/checks out/commits its artifact branch and returns base/head/branch/outcome; the Orchestrator pushes, opens the PR, waits for merge, and verifies on origin/main. EVERY handback field has a deterministic default so the HANDBACK RESOLUTION never halts; when the HEAD resolution yields the default branch, stage_branch stays UNRESOLVED and the run routes to step 3a's create-arm - exactly the pre-change behaviour - and the default branch is NEVER substituted for the artifact branch.
- THE NO-SHIPMENT ARM PROVES CONCRETE FILES ACROSS THE WHOLE SESSION, NOT DIRECTORIES AND NOT ONLY THE TIP COMMIT (rev 7, aggregate at rev 8). The Orchestrator CAPTURES AND RETAINS stage_base_commit (git rev-parse HEAD) IMMEDIATELY BEFORE INVOKING STAGE; that retained value is AUTHORITATIVE over any Stage-reported one. The arm runs SIX ORDERED CHECKS, any failure halting with STAGING_GATE_FAIL: rev-parse --verify of stage_head_commit; rev-parse --verify of stage_base_commit, rejecting an UNRESOLVED base; merge-base --is-ancestor {stage_base_commit} {stage_head_commit} (RANGE VALIDITY); merge-base --is-ancestor {stage_head_commit} origin/main (COMMIT ANCESTRY); derivation of AGGREGATE = git diff --name-only -z --diff-filter=d {stage_base_commit} {stage_head_commit} -- {STAGE_ARTIFACT_ROOTS}, halting when EMPTY; and VERIFY_SET resolution followed by git cat-file -t "origin/main:{path}" for EVERY path, requiring exit 0 AND output exactly 'blob' (READABILITY AND FILE-NESS). stage_artifact_paths is a NON-EMPTY set of CONCRETE repository-relative FILE paths changed between the two commits and is NEVER defaulted to a directory list; when the handback supplies it, the set must contain only file paths (no trailing slash, no bare root, no glob) under STAGE_ARTIFACT_ROOTS and must be EQUAL to AGGREGATE - equality, not subset, because a subset check accepts both an invented path and a silently shrunken set. DIRECTORIES, EMPTY SETS, OUT-OF-ROOT PATHS, AND CHANGED-FILE-SET MISMATCH ARE ALL REJECTED. THREE ALGORITHMS ARE EXPLICITLY FORBIDDEN AND MUST NOT REAPPEAR: git show origin/main:{path} (it EXITS 0 ON A TREE - verified - and those four directories exist on origin/main in every repository, so the rev-6 loop was a guaranteed pass that read no file the Stage run wrote); a DIRECTORY-PREFIX default for the path set; and a SINGLE-COMMIT git diff-tree derivation over stage_head_commit alone, which verified only the tip commit's files and left every artifact written in an earlier commit of the same session covered by ancestry alone - the very property the per-file check exists because ancestry does not provide. The RANGE form origin/main..{stage_head_commit} is equally refused: EMPTY BY CONSTRUCTION post-merge. The TWO-TREE diff is chosen because it reads IDENTICALLY BEFORE AND AFTER THE MERGE. Step 3a RE-RECORDS stage_head_commit as git rev-parse HEAD after committing, PRESERVES stage_base_commit unchanged (or sets it to the HEAD captured BEFORE that commit when it was UNRESOLVED), and discards any reported paths, so the recomputed set is the aggregate over the preserved base. All new halts are in VERIFICATION, not in handback defaulting, so the never-halts invariant above is intact.
- THE NO-SHIPMENT OUTCOME IS REACHABLE, NOT ONLY SPECIFIED (rev 8). As installed, _stage.agent.md Step 5.5 declares shipment assembly MANDATORY whenever the registry advertises features.shipments: true, and Step 6's pre-summary gate HALTS when no shipment_id exists - so stage_outcome: no-shipment could never be emitted and the entire verification arm above was dead prescription. 018.009-T scopes Step 5.5's mandatory rule to a harvest that PRODUCED ITEMS (preserving its empty-harvest / unresolved-P-003 guardrail VERBATIM as the discriminator), requires shipment_id in Step 6's gate ONLY when stage_outcome is shipment, emits the COMPLETE handback record plus an explicit terminal statement for a no-shipment run, and does NOT route the operator to Ship. FAILURES STAY FAILURES: P-003 lineage violations, harvest failures, and a missing required shipment after a NON-EMPTY harvest continue to HALT and are NEVER recorded as no-shipment. no-shipment is a SUCCESS terminal for a run that had nothing to ship, never a re-label for one that failed.
- STAGE ACTUALLY BRANCHES AND COMMITS - THE PERMISSION IS BACKED BY AN OPERATIVE, ORDERED STEP (rev 9). Revisions 1-8 gave Stage the PERMISSION to create and commit on the artifact branch and the OBLIGATION to REPORT what it did, but no instruction that DOES either: the installed _stage.agent.md has NO branch operation and NO commit operation at any step, so an executor satisfying the Role Boundary and handback criteria to the letter could finish a session ON THE DEFAULT BRANCH with every artifact written there - the fdff9e4 shape - while emitting a handback naming a branch that was never created and commits that were never made. 018.009-T AC-B3.7 adds TWO operative steps to that file, each registered in its Step Sequence Contract checklist and each backed by a step section. STEP 1.9 - PRE-MUTATION BRANCH GATE, positioned AFTER learnings retrieval and BEFORE deliberation - a FIXED position, with every Stage mutation that would otherwise precede it (the Step 1 deferred-expansion duplicate archival, the session's first memory checkpoint) explicitly DEFERRED UNTIL AFTER it, so no artifact write precedes the gate: it derives a {scope-slug} WITHOUT requiring a shipment_id, records stage_base_commit (ECHOED from the Orchestrator in a pipeline invocation, CAPTURED from HEAD BEFORE branch creation in a direct invocation), VERIFIES AND USES an Orchestrator-supplied branch or CREATES AND CHECKS OUT chore/stage-{scope-slug} when none was supplied, REFUSES to write any artifact while HEAD is the default branch (STAGE_BRANCH_GATE_FAIL, P-010 recorded), and keeps P-016 SINGLE-WORKTREE by switching the one existing worktree rather than adding one. STEP 5.7 - POST-MUTATION ARTIFACT COMMIT, positioned AFTER consumed-stash archival and BEFORE the summary: it asserts HEAD is the Stage artifact branch and not the default branch, stages ONLY the four STAGE_ARTIFACT_ROOTS, commits in Conventional Commits form, sets stage_head_commit from the RESULTING HEAD, derives stage_artifact_paths by the aggregate two-tree diff over the PRESERVED base, and STOPS THERE - Stage neither pushes nor creates, updates, or merges a pull request, which the Orchestrator or the operator owns. Step 6's pre-summary gate requires both steps complete. ORDERING IS THE CONTRACT, NOT PRESENCE: a gate placed after harvest leaves the artifacts on the default branch and a commit placed after the summary emits a head that does not yet exist, so both positions are asserted STRUCTURALLY by checklist-index and heading-position comparison, never by text search (the AC-C1.6 self-match hazard one file over). BRANCH NAMING NO LONGER DEPENDS ON A SHIPMENT ID: chore/stage-{scope-slug}, with a shipment ID permitted as the slug but never required, because Step 5.5 creates the shipment as the session's LAST mutation while the branch must exist before its FIRST write.
- THE HANDBACK OUTCOME PAIR FAILS CLOSED, NOT OPEN (rev 10). stage_outcome is PRESERVED when Stage reports it and derived from whether a shipment_id was returned ONLY WHEN THE FIELD IS ABSENT; the derivation NEVER overwrites a reported value. Before Orchestrator Step 1.5 step 4 selects an arm, the resolved pair is VALIDATED and any failure halts STAGING_GATE_FAIL: an outcome that is neither 'shipment' nor 'no-shipment' (including an unresolvable empty value) halts as unknown; 'shipment' with an empty or missing shipment_id halts; 'no-shipment' with a shipment_id present halts. A REPORTED 'shipment' OUTCOME IS NEVER RECLASSIFIED AS A SUCCESSFUL 'no-shipment' RUN. The rev 6-9 rule derived the field UNCONDITIONALLY with no 'absent' branch and a SUCCESS terminal as its 'otherwise' clause, so a run reporting stage_outcome: shipment whose shipment_id did not reach the Orchestrator was silently rewritten to no-shipment: the shipment arm was SKIPPED, the no-shipment arm PASSED (it verifies FILES, not INTENT, and the artifacts really were on origin/main), the gate reported SUCCESS, and SHIP WAS NEVER ROUTED - a formed shipment silently abandoned, on precisely the input class most likely to be degraded. The PRODUCER emits stage_outcome EXPLICITLY and consistently with shipment_id (018.009-T AC-B3.5); the CONSUMER preserves, derives only when absent, and validates (018.007-T AC-B1.12). The validation lives in step 4 VERIFICATION, never in field defaulting, so the never-halts handback-resolution invariant above is intact.
- NOTHING TRACKED WRITES BEFORE THE BRANCH GATE - THE DEFERRAL RULE IS CATEGORICAL, NOT AN ENUMERATION (rev 10). Step 1.9 precedes EVERY tracked Stage artifact mutation of the session, WITHOUT EXCEPTION. Rev 9 named exactly two deferred mutations, while the installed Stage contract also schedules, BEFORE that ordinal, a STASH-CLASSIFICATION checkpoint, a CONTEXTUAL-GROUPING / OPERATOR-SELECTION checkpoint, and the WRITE HALF of the MANDATORY LATE-IDENTIFIER RECONCILIATION - each tracked, each therefore still legal on the default branch under the rev-9 wording, which is the fdff9e4 shape the gate exists to prevent. The rule is now categorical, with three classes enumerated as ILLUSTRATION rather than as a closed list: (i) the write half of late-identifier reconciliation (the analysis is READ-ONLY and completes in Step 1; only the in-place entry update moves), (ii) the UNCONDITIONAL duplicate-entry archival (detection is read-only; only the archive call moves), and (iii) EVERY tracked docs/memory/ checkpoint whose triggering milestone precedes the gate, naming the stash-classification and contextual-grouping/operator-selection checkpoints explicitly. READ-ONLY CLASSIFICATION AND GROUPING STILL RUN FIRST AND MUST - they are what make the scope slug derivable, so deferring them would make the gate's own position underivable; what is deferred is the WRITE, never the analysis or the decision. GITIGNORED DISPOSABLES ARE EXPRESSLY OUT OF SCOPE AND ARE NOT OVERCLAIMED: index synchronization (.backlogit/backlogit.db*), structured backlogit checkpoints (.backlogit/checkpoints/), hook-event polling and acknowledgement (.backlogit/hooks_queue.jsonl), and .backlogit/runtime/ state never appear in git status, cannot be committed, and therefore cannot reach the default branch.
- THE OUT-OF-ROOT HALT HAS AN ACTUAL DETECTOR (rev 10). Rev 9 required Step 5.7 to halt on a change outside STAGE_ARTIFACT_ROOTS but prescribed only a root-scoped git add, which IGNORES such a change rather than reporting it - the halt could never fire. Step 5.7 now runs an explicit NUL-safe full-tree inspection BEFORE staging anything: git status --porcelain=v1 -z --untracked-files=all. BOTH flags are load-bearing - the default 'normal' untracked mode collapses an untracked subtree to a DIRECTORY entry no per-file containment test can evaluate, and without -z paths with spaces are quoted and unusual bytes backslash-escaped, so line splitting is unsafe. Records are parsed NUL-WISE, never by line: each record is XY + space + PATH, and when either status column is R or C the NEXT record is the ORIG_PATH with no XY prefix, so BOTH ENDPOINTS of a rename or copy are checked. Any affected path outside the roots halts with STAGE_COMMIT_GATE_FAIL and a recorded P-010 signal, naming every offender. PRE-EXISTING UNRELATED DIRT IS HANDLED FAIL-CLOSED BY HALTING AND LEAVING IT ALONE: Stage MUST NOT checkout, restore, stash, revert, or delete it - it may be a human operator's in-flight work, and discarding it would be a destructive act without approval (Constitution VII) and a data-loss risk. Only after the inspection passes does Stage stage the four roots; the pathspec is a BOUND, not a detector. Detection-before-staging is asserted as an INTRA-SECTION ORDERING fact, by index comparison, not by presence alone.
- THE FOUR AUTHORIZATION SURFACES AGREE, AS THEY EXIST AT THE END STATE (corrected rev 11). The generated/installed Orchestrator and policy surfaces agree - no surviving contradiction across the four authorization surfaces, NAMED AS THE CORRECTED CONTRACT LEAVES THEM: (1) _stage.agent.md Role Boundary Git row; (2) workflow-policies.md P-010 Stage MAY bullet; (3) _orchestrator.agent.md Step 1.5 PREAMBLE, in its reworded verification/postcondition form ('have reached the default branch via a merged staging PR and are present on the remote'), with its scope clause binding WHETHER OR NOT A SHIPMENT WAS FORMED; (4) _orchestrator.agent.md Step 1.5 SUB-STEP 3a, which carries the branch-point instruction ('from the current commit') folded in from the deleted 3e and reuses the Stage-reported branch when one exists. EXPLICIT 3e ABSENCE: sub-step 3e is DELETED by 018.007-T and MUST NOT be present at the end state - AC-B1.1 asserts that NO direct-push-to-default-branch instruction remains anywhere in Step 1.5, so 3e is not a surface that can agree or disagree, it is a surface that must NOT EXIST. This bullet previously named 3e as one of the four live surfaces (rev 1-10), which asserted agreement across a surface the release unit deletes; the live pair is the preamble plus 3a. 3b-3d and step 4's verification are preserved unchanged. The plan's narrative quotations of the 3e construct (plan sections 2, 3, 6.1.1) are HISTORICAL RECORDS of the defect, sit OUTSIDE the scanned corpus (plan section 5.2), and are deliberately retained.
- scripts/check-direct-push-language.sh fails on reintroduction of either violation shape, proven by a FIXTURE-BACKED mutation proof with NO dirty-tree edit (plan section 7.2 AC-C2.2), and does not self-match the corpus's own prohibition text.
- The gate is blocking in CI job 'lint' from its first wiring; ci-gate still transitively depends on 'lint'.
- REGENERATION CANNOT SILENTLY DISABLE THE CONTROL (rev 5, plan section 5.0). The job-lint step is a LOCAL DIVERGENCE inside the autoharness render boundary, so enforcement does not rest on it alone. Both halves of the invariant are carried by the non-generated Go harness tests/integration/directpush_gate_test.go, invoked by job 'expensive' (name: test)'s FULL-SUITE ./... test step, a step a render RE-EMITS rather than removes, and any render that rewrites ci.yml is a .github/** change, so changes.code == 'true' and that job cannot skip. REV-12 PROVENANCE CORRECTION: 'go test -race -mod=readonly ./...' is the OBSERVED INSTALLED command (verified in .github/workflows/ci.yml), NOT the generated baseline - the generation template emits {{TEST_COMMAND}} and this workspace's recorded render input (.autoharness/harness-manifest.yaml, .autoharness/workspace-profile.yaml) is 'go test ./...'. THE GUARANTEE REQUIRES A FULL-SUITE INVOCATION INCLUDING ./... THAT RUNS THE INTEGRATION HARNESS, NOT A PERMANENT PROMISE THAT THE EXACT RACE/-MOD FLAGS SURVIVE A RENDER. AC-C1.7 therefore requires POST-RENDER VERIFICATION OF THE RENDERED FULL-SUITE COMMAND after any regeneration of ci.yml; if a render narrows the package pattern the harness stops running and R5 reverts to an OPEN residual (R23):
  (a) DETECTOR STILL RUNS: TestDirectPushGate_FullCorpusClean re-runs the full-corpus scan over installed surfaces and must report zero findings, even with the job-lint step gone (AC-C2.8).
  (b) INVOCATION STILL EXISTS: TestDirectPushGate_CIWiringIsBlocking asserts the job-lint step is still present, determined by PARSING ci.yml and walking jobs.lint.steps[] (never a text search, which would self-match the ledger comment), with non-vacuity proven by execution against a t.TempDir() copy that has the step removed (AC-C1.6).
  (c) BOTH DETECTORS REMAIN ACTIVE WHEN THE STEP IS GONE (rev 11 - the P0 correction). Halves (a) and (b) only execute if both functions ACTIVATE. Rev 10 derived C1's default-mode activation from the PRESENCE OF THE JOB-LINT STEP - the exact proposition C1 exists to assert - and gave C2 LITERALLY C1's predicate, so a render that dropped the step disarmed BOTH functions, MP2 (inside C2) never ran, and go test ./... stayed GREEN: the control disarmed itself at precisely the moment it was needed. Activation is now derived SOLELY from the tracked harness-state manifest tests/integration/testdata/directpush-gate/harness-state.json and the -gatetask selector, NEVER from any surface the harness asserts on. Once C1 and C2 are in that manifest's completed set they are ACTIVE PERMANENTLY, so a regeneration that removes the lint step produces a LOUD '--- FAIL: TestDirectPushGate_CIWiringIsBlocking' rather than a silent skip (AC-C2.9). The manifest lives under tests/**, which NO autoharness template covers, so no render can reach it either. REV 13 - THE EVIDENCE FOR THIS HALF MUST BE TAKEN UNSELECTED. The rev-12 observation used -gatetask=C1, which activates that function BY CONSTRUCTION and would have passed identically under the rev-10 self-disarming design, and it never exercised 018.011-T at all. The authoritative observation (plan section 5.0.2 point 7) is now a DEFAULT-MODE run against a COPIED TERMINAL-STATE REPOSITORY FIXTURE under t.TempDir() - real tree NEVER mutated, manifest and witness VALID AND IN AGREEMENT, only the job-lint step removed - showing BY TEST-EVENT NAME that TestDirectPushGate_CIWiringIsBlocking FAILS FOR THE MISSING LINT STEP and that TestDirectPushGate_FullCorpusClean EXECUTES (never --- SKIP:). The targeted command is RETAINED SEPARATELY as point 7a, SELECTOR EVIDENCE ONLY.
  A render that drops the gate step therefore turns CI RED rather than leaving it green. SCOPE OF THE GUARANTEE (not overstated): this excludes SILENT regeneration disablement only. Deleting the harness itself remains possible via a reviewable pull-request diff to a non-generated tracked file - recorded as risk R12, NOT claimed as closed. The same applies to the harness-state manifest, recorded as risk R21. CODEOWNERS is explicitly NOT part of this guarantee: it is advisory-only in this repo until code-owner review is required on main, which is deliberately not enabled.
- THE HARNESS RED PHASE IS LITERAL 8/8, AND THE BUILD PHASE STILL LEAVES THE SUITE GREEN (rev 11). The installed P-002/P-004/harness-architect contract requires go test ./... RED WITH ALL GENERATED TESTS FAILING THEIR EXPECTED NOT-IMPLEMENTED MARKERS before ANY task may be labelled harness-ready. Rev 10 delivered ONE failing function plus SEVEN SKIPS, which is not that postcondition; reverting to rev 9's simultaneous all-eight-red would satisfy it but restore the Ship Step 4.3 deadlock (Step 4.3 runs the FULL go test ./... after EVERY task). The two are only irreconcilable while activation is a STATIC PREDICATE SET; they reconcile once it is a PHASE. H0 writes tests/integration/testdata/directpush-gate/harness-state.json as {"phase":"red","completed":[],"terminal":false}. RED: all eight functions activate, none skips, and all eight fail with their OWN task-specific 'not implemented: <ID> <surface>' marker - the literal 8/8, and ONLY THEN may the tasks be labelled harness-ready. BUILD: A1's completion performs the ONE-WAY red -> build transition, after which the default suite runs the COMPLETED SET and skips only not-yet-started tasks, so Step 4.3 is GREEN AFTER EVERY TASK. TERMINAL: C2's completion sets completed to the exact eight-element ordered set with terminal=true and phase=terminal ATOMICALLY, after which all eight substantive assertions run and NO ACTIVATION SKIP REMAINS. TARGETED MODE (-gatetask={ID}) always executes the real assertion in every phase, so every task keeps its observed red->green boundary. A test is NEVER green merely because its ID was written: the manifest controls ACTIVATION only, never the verdict.
- THE HARNESS-STATE MANIFEST IS FAIL-CLOSED AND IS ITSELF PART OF THE MUTATION PROOF (rev 11). Missing or malformed manifest, unknown phase value, duplicate/unknown/out-of-order ID, a completed set that is not an ORDER-PRESERVING SUBSEQUENCE of [A1,A2,A3,B1,B2,B3,C1,C2], a set that is not DEPENDENCY-CLOSED (a skipped prerequisite), an inconsistent phase/terminal triple, an impossible reverse transition, or an unknown -gatetask value ALL FAIL CLOSED - never skip. The validator runs inside gate() on EVERY call, so a corrupt state file fails ALL EIGHT functions rather than silently disabling them. Because B1/B2/B3 are PARALLEL in the dependency graph (each depends only on A3; C1 requires all three), the rule is subsequence-plus-closure and NOT a strict prefix, so any of the three may land first. AC-C2.2 proves SEVEN manifest mutations - deletion, malformed JSON, unknown phase, rollback, reordering, skipped prerequisite, duplicate/unknown ID - FAIL rather than skip, executed against a t.TempDir() scratch copy with NO dirty-tree edit. The manifest is IMPLEMENTATION/HARNESS STATE, NOT BACKLOG STATUS: it is never derived from, compared against, or synchronized with .backlogit/ item statuses, so no status edit can activate or deactivate an assertion.
- THE TERMINAL CLAIM IS CARRIED BY TWO INDEPENDENT FILES THAT MUST AGREE (rev 12). MP2 - terminal completeness - lives INSIDE 018.011-T, and that function's default-mode activation was read from the SAME manifest MP2 validates. A CONSISTENT rewind of phase, completed and terminal TOGETHER therefore left a valid-looking build state in which MP1 passed, 018.011-T SKIPPED as a not-yet-started task, and MP2 NEVER RAN - the terminal claim withdrawn with nothing firing. A second small, tracked, NON-GENERATED TERMINAL COMPLETION WITNESS at tests/integration/testdata/directpush-gate/terminal-witness.json, INDEPENDENT OF THE MAIN MANIFEST, closes the asymmetric cases. IT IS MINIMAL BY DESIGN: fixed schema ('directpush-gate/terminal-witness'), fixed version (1), contract identifier ('017-S/018-F'), the exact terminal task set, and a digest (lowercase hex SHA-256 of the eight IDs joined with newline) - NO TIMESTAMPS AND NO ENVIRONMENT-SPECIFIC DATA, so it is byte-reproducible from the contract alone. H0 REQUIRES IT ABSENT, AND EVERY LEGITIMATE PRE-018.011-T BUILD STATE REQUIRES IT ABSENT. 018.011-T's completion writes THE WITNESS FIRST and then ATOMICALLY REPLACES the manifest with the exact terminal state; the tests are GREEN ONLY WHEN BOTH AGREE. MP6 runs inside gate() BEFORE ANY ACTIVATION DECISION - which is what stops 018.011-T being skipped past MP2: a PRESENT WITNESS FORCES 018.011-T ACTIVE and REQUIRES a terminal manifest; a TERMINAL MANIFEST REQUIRES a valid witness; MISSING, MALFORMED OR MISMATCHED evidence is FAIL-CLOSED WHEREVER TERMINAL OR WITNESS EVIDENCE EXISTS, while H0/build ABSENCE IS VALID. So ROLLING BACK ONLY THE MANIFEST FAILS LOUDLY and ROLLING BACK ONLY THE WITNESS FAILS LOUDLY. HONEST RESIDUAL (R22), RECORDED NOT HIDDEN: rolling back or deleting BOTH IN ONE REVIEWED DIFF resembles legitimate pre-018.011-T build state and is caught by DIFF AND REVIEW of two tracked non-generated files, NOT BY RUNTIME HISTORY. NO CLAIM IS MADE THAT A STATELESS VALIDATOR DETECTS EVERY COORDINATED ROLLBACK.
- VALID MANIFEST STATES ARE DEFINED EXHAUSTIVELY - EXACTLY THREE (rev 12). red => completed EMPTY, terminal FALSE, witness ABSENT. build => completed a NON-EMPTY, ORDER-PRESERVING, DEPENDENCY-CLOSED PROPER SUBSET of [A1,A2,A3,B1,B2,B3,C1,C2], terminal FALSE, witness ABSENT. terminal => completed the EXACT EIGHT-ELEMENT ORDERED SET, terminal TRUE, and a VALID WITNESS PRESENT. EVERY OTHER COMBINATION IS REJECTED, including red+terminal, red with a non-empty completed, build with an empty completed, build with all eight, build+terminal, terminal with a proper subset, terminal with terminal:false, terminal with the witness absent, a witness present alongside red or build, and any phase outside {red,build,terminal}. The rule is SUBSEQUENCE-PLUS-CLOSURE, NEVER A PREFIX, and in build phase specifically a PROPER SUBSET.
- ZERO-SKIPS IS A DEFAULT-MODE CLAIM (rev 12). 'No activation skip remains anywhere' describes DEFAULT MODE in red and terminal phase: with no -gatetask, all eight gate functions activate and none skips. It is NOT a claim about TARGETED mode, which by design runs exactly the selected function's real assertion and SKIPS THE OTHER SEVEN in every phase - those are SELECTOR skips and they are correct. What targeted mode may NEVER do is skip THE TEST IT SELECTED. Evidence citing 'zero --- SKIP: lines' MUST therefore be taken from a DEFAULT-MODE run. REV 13 makes this structural rather than conventional: the selector holds ACTIVATION PRECEDENCE in gate()'s Stage 2, so a valid terminal witness cannot activate the seven unselected bodies in a targeted run, and NO TARGETED RUN MAY BE OFFERED AS EVIDENCE OF A DEFAULT-MODE ACTIVATION CLAIM.
- LIFECYCLE EVIDENCE IS REQUIRED, NOT NARRATED (rev 12; plan section 5.0.2, AC-C2.11). Eight observation points, each a recorded command plus its observed output: (1) H0 - 8 EXPECTED FAILURES each with its own marker, ZERO DEFAULT-MODE ACTIVATION SKIPS, WITNESS ABSENT; (2) POST-018.005-T - 018.005-T targeted GREEN, 018.006-T targeted EXPECTED-RED, DEFAULT SUITE GREEN; (3) LEGAL 018.007-T/018.008-T/018.009-T ORDERS - every dependency-closed subsequence ACCEPTED, no legal order rejected; (4) POST-018.010-T - DEFAULT SUITE GREEN, 018.011-T targeted EXPECTED-RED, WITNESS ABSENT (this is precisely the state the retired MP5 deadlocked); (5) TERMINAL - exact eight-task set PLUS witness, all 8 SUBSTANTIVE ASSERTIONS GREEN, ZERO DEFAULT-MODE SKIPS; (6) TERMINAL TREE WITH A SEVEN-TASK MANIFEST ROLLBACK WHILE THE WITNESS REMAINS - FAILURE; (7) TERMINAL MANIFEST WITH THE LINT STEP REMOVED - 018.010-T FAILS AND 018.011-T EXECUTES; (8) WITNESS-ONLY, TERMINAL-MANIFEST-ONLY, AND MALFORMED WITNESS - FAILURE in each. REV 13: POINT 1 additionally asserts SET EQUALITY between the harnessed task set and the eight 017-S task IDs, none omitted for dependency state. POINT 6 fails via MP6's MODE-INDEPENDENT integrity half. POINT 7 IS REWRITTEN AS A DEFAULT-MODE OBSERVATION against a COPIED TERMINAL-STATE FIXTURE under t.TempDir() (manifest and witness valid and in agreement, real tree never mutated), asserting BY TEST-EVENT NAME that 018.010-T's function FAILS FOR THE MISSING LINT STEP and 018.011-T's function EXECUTES rather than skips; POINT 7a records the targeted form SEPARATELY as SELECTOR EVIDENCE ONLY. The count stays EIGHT NUMBERED POINTS - 7a is a companion to point 7, not a ninth point.
- FOUNDATIONAL CONTRACTS UNCHANGED (rev 11). This release unit plans NO modification to .github/skills/harness-architect/SKILL.md, to P-002 or P-004 in workflow-policies.md, or to _ship.agent.md Step 2 or Step 4.3. harness-architect already emits a NON-GO H0 deliverable (the structural check-direct-push-language.sh stub), so a write-once testdata/ JSON constant is strictly SMALLER than what the skill already produces; P-004's red phase is met as the literal 8/8; Step 2's harness-ready label partition is untouched and Stage applies no label; and Step 4.3 keeps its FULL go test ./... scope, with the rev-10 proposal to narrow it to a scoped -run still REJECTED and still out of scope.
- Existing branch rules and PR merge-commit rules remain intact: Step 1.5 stays NON-NEGOTIABLE and Orchestrator-owned; 3b-3d, step 4's git show origin/main verification, and the STAGING_GATE_FAIL halt are preserved. Rev 6 narrows AC-B1.4 from whole-block byte-identity to SHIPMENT-ARM verbatim preservation (lead sentence, git show command, both bullets, halt string, semantics), because whole-block identity is unsatisfiable once step 4 must carry the no-shipment arm - and an unmodified block is exactly what left that path unverified. Clean-tree and single-worktree gates are preserved and P-009/P-011/P-016 stay textually unchanged; every command revisions 6-8 add is a read form (status, rev-parse, fetch, log, show, merge-base, cat-file, diff) or a checkout, so no direct default-branch push is introduced.
- SELF-MATCH DISCIPLINE (rev 6): the corrected Step 1.5 states its discovery prohibition DESCRIPTIVELY and must NOT quote the literal forbidden commit range. _orchestrator.agent.md is in the scanned corpus, so quoting it would self-match the AC-B1.6 absence assertion and vacate the mechanical rejection - the failure class recorded in docs/compound/2026-09-06-ci-self-matching-grep-and-actionlint-verification-gap.md. The detector's closed construct set and fixture corpus are DELIBERATELY NOT widened for this defect (it is neither violation construct); rejection is carried by the existing per-surface harness functions instead, and directpush-accept-read-and-pr-forms.md keeps its read-form accept case unchanged.
- Role isolation preserved: Orchestrator owns staging branch/PR orchestration, Stage owns planning artifacts, Ship owns implementation delivery.
- Constitution Quality Gates pass in declared order: gofmt, go vet, go test ./..., go build. go test ./... now includes the section 5.0 direct-push gate harness and must be green in full. REV 11: at the end state the harness-state manifest is in TERMINAL phase, so 'green in full' means ALL EIGHT FUNCTIONS ACTIVATE AND PASS THEIR REAL ASSERTIONS - zero '--- SKIP:' lines among the eight gate functions IN A DEFAULT-MODE RUN (AC-C2.7, AC-C2.9; qualified at rev 12 - a targeted run skips the seven non-selected functions by design). A skip at that point is a FAILURE, not a pass. This local constitutional sequence is SEPARATE from, and not replaced by, the CI full-suite step named above.
<!-- END:dod -->