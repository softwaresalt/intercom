---
artifact_type: task
created_at: 2026-09-11T17:23:36.9815296Z
custom_fields:
    origin_feature: 018-F
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 019.004-T
    - 018.008-T
    - 019.007-T
id: 019.005-T
labels:
    - sub-epic-c
    - ci
    - chore
parent_id: 019-F
priority: high
status: queued
title: Wire gate into CI lint job, ledger and CODEOWNERS
updated_at: 2026-09-12T19:23:09.5035773Z
---

Parent feature 018-F. Sub-epic C (CI coupling and verification), plan section 7.1 of docs/plans/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-plan.md. Wire the gate into CI as a BLOCKING check. Add one step to job 'lint' in .github/workflows/ci.yml invoking scripts/check-direct-push-language.sh (the step-name numeric suffix (0NN.00X-T) is a PLACEHOLDER to be resolved against the then-current ledger numbering). Add a LOCAL DIVERGENCE ledger entry for the new gate, widening the membership wording as needed to admit wildcard/range forms rather than the literal 011.0xx-T, and recording that removal of this step is CAUGHT (not accepted) by the non-generated Go harness running from job 'expensive' (name: test)'s FULL-SUITE ./... test step. REV 12 PROVENANCE CORRECTION: 'go test -race -mod=readonly ./...' is the OBSERVED INSTALLED command, NOT the generated baseline - the template emits {{TEST_COMMAND}} and this workspace's recorded render input is 'go test ./...'. The ledger MUST NOT claim the installed race command is template baseline; what a render re-emits is the STEP and its {{TEST_COMMAND}} substitution, so the load-bearing property is a FULL-SUITE ./... PACKAGE PATTERN, never the race/-mod flags. AC-C1.7 adds the post-render verification of the rendered full-suite command. Update .github/CODEOWNERS to own the new gate script and the three currently unowned gate scripts, and generalize the fixture-data carve-out from scripts/testdata/gitignore/** to scripts/testdata/**. CI configuration only.
HARNESS (P-002/P-004): harness function TestDirectPushGate_CIWiringIsBlocking in tests/integration/directpush_gate_test.go, produced by harness-architect at Ship Step 2 per plan section 5.0. harness_cmd: go test ./tests/integration -run '^TestDirectPushGate_CIWiringIsBlocking$' -gatetask=C1
HARNESS LIFECYCLE (rev 11, plan section 5.0.1): activation is derived SOLELY from the tracked harness-state manifest tests/integration/testdata/directpush-gate/harness-state.json and the -gatetask selector, NEVER from any surface this harness asserts on (the rev-10 self-disarming defect). In H0's red phase ALL EIGHT functions activate and fail, each with its own task-specific marker - this task's marker is 'not implemented: C1 CI wiring and ownership' - which is the literal 8/8 red the installed P-002/P-004/harness-architect contract requires before ANY task may be labelled harness-ready. TARGETED RED (unchanged per-task obligation): run the harness_cmd above BEFORE implementing; -gatetask activates unconditionally in every phase, so this function evaluates its REAL assertion and must fail for this task's expected reason. COMPLETION: atomically with the substantive work, insert "C1" into the manifest's completed array at its gateOrder position; phase stays 'build'. completed must remain an ORDER-PRESERVING SUBSEQUENCE of [A1,A2,A3,B1,B2,B3,C1,C2] that is DEPENDENCY-CLOSED (B1/B2/B3 are parallel - each depends only on A3 - so any of the three may land first; C1 requires all three). Writing the ID does NOT make the test pass: targeted mode ignores the manifest for activation and default mode still evaluates the real assertion, so a manifest advanced ahead of the work fails on the assertion. REV 14 - THE RULE STATED PRECISELY, WITH ITS ONE DELIBERATE EXCEPTION (plan section 5.0.1 lifecycle item 4): for 018.004-T THROUGH 018.010-T the rule is ABSOLUTE - completion state is BOOKKEEPING ONLY, is never itself a subject of the task's substantive assertion, and can NEVER ON ITS OWN turn that assertion green. 018.011-T IS THE DELIBERATE EXCEPTION, AND IT IS AN EXCEPTION OF SUBJECT, NOT OF RIGOUR: finalizing the terminal state - writing a valid terminal-witness.json and atomically replacing the manifest with the exact eight-element terminal triple - IS ITSELF PART OF THAT TASK'S SUBSTANTIVE VERIFICATION CONTRACT (MP2, AC-C2.9, AC-C2.10), because the property it verifies INCLUDES that the lifecycle is closed and the closure is carried by two agreeing tracked files. WHAT DOES NOT FOLLOW, AND IS EXPRESSLY FORBIDDEN: 018.011-T CANNOT PASS FROM BOOKKEEPING ALONE - it must still EXECUTE AND PASS its full corpus, self-test, mutation-proof, marker-free-construct and regeneration assertions, with MP2 evaluated from manifest state BEFORE any surface-dependent assertion. In build phase the default suite runs the completed set and skips only not-yet-started tasks, which is what keeps Ship Step 4.3's go test ./... green after this task. MISSING OR MALFORMED MANIFEST, DUPLICATE/UNKNOWN/OUT-OF-ORDER ID, UNMET PREREQUISITE, INCONSISTENT phase/terminal, OR AN UNKNOWN -gatetask VALUE ALL FAIL CLOSED - never skip.
REV 12 - THREE LIFECYCLE CORRECTIONS THAT BIND THIS TASK (plan sections 5.0.1 and 5.0.2). (1) MP5 IS RETIRED - REMOVED, NOT RENUMBERED, AND MUST NOT BE IMPLEMENTED. Its rev-11 rule (a task outside completed must have its surface ABSENT, else FAIL) was wrong: 018.011-T's probe was the job-lint step that 018.010-T ITSELF CREATES, so the entirely legitimate post-018.010-T / pre-018.011-T window failed the DEFAULT SUITE - the Ship Step 4.3 deadlock, reintroduced by the guard meant to prevent it. THE GENERAL RULE, NOW SYMMETRIC: NEITHER ACTIVATION NOR ANY GATE-LEVEL FAILURE MAY BE DERIVED FROM A SURFACE THIS HARNESS ASSERTS ON. Rollback of a COMPLETED task's surface is caught by THAT TASK'S OWN SUBSTANTIVE ASSERTION (active in default mode from its completion onward); an INCOMPLETE task legitimately may have prerequisites and even surface fragments present and MUST NOT fail anything. The ACTIVE checks are SIX: MP0, MP1, MP2, MP3, MP4, MP6. (2) TERMINAL COMPLETION WITNESS (MP6). A second tracked, non-generated file tests/integration/testdata/directpush-gate/terminal-witness.json, INDEPENDENT of the manifest, is written ONLY by 018.011-T's completion - WITNESS FIRST, THEN the manifest's ATOMIC replacement with the exact terminal state. IT IS ABSENT AT H0 AND THROUGHOUT BUILD, and that absence is VALID, not an error. gate() runs MP6 BEFORE ANY ACTIVATION DECISION: a present witness must be VALID (schema directpush-gate/terminal-witness, version 1, contract 017-S/018-F, completed == gateOrder, digest == SHA-256 of the eight IDs joined with newline) and REQUIRES a terminal manifest AND FORCES 018.011-T ACTIVE; a terminal manifest REQUIRES a valid witness; missing/malformed/mismatched WHEREVER TERMINAL OR WITNESS EVIDENCE EXISTS IS FAIL-CLOSED. So a manifest-only rollback and a witness-only rollback each FAIL LOUDLY. RECORDED RESIDUAL (R22): rolling back BOTH in one change resembles legitimate pre-018.011-T build state and is caught by DIFF AND REVIEW, NOT BY RUNTIME HISTORY - no claim is made that a stateless validator detects every coordinated rollback. (3) VALID STATES ARE EXHAUSTIVE - EXACTLY THREE: red => completed EMPTY and terminal false and witness absent; build => completed a NON-EMPTY, order-preserving, DEPENDENCY-CLOSED PROPER SUBSET of gateOrder and terminal false and witness absent; terminal => completed the EXACT eight-element ordered set and terminal true and a VALID WITNESS PRESENT. EVERY OTHER COMBINATION IS REJECTED, including red+terminal, red with a non-empty completed, build with an empty completed, build with all eight, build+terminal, terminal with a proper subset, terminal with terminal:false, terminal with the witness absent, a witness present alongside red or build, and any phase outside {red,build,terminal}. ZERO-SKIPS IS A DEFAULT-MODE CLAIM (red and terminal phases): with no -gatetask, all eight activate and none skips. TARGETED MODE LEGITIMATELY SKIPS THE SEVEN NON-SELECTED FUNCTIONS in every phase - those are SELECTOR skips, not activation skips - and its only obligation is that THE SELECTED TEST ACTUALLY EXECUTES. REV 13 - SELECTOR PRECEDENCE, MADE STRUCTURAL (plan section 5.0.1). gate() now runs in TWO EXPLICITLY ORDERED STAGES. STAGE 1 - INTEGRITY - IS MODE-INDEPENDENT and runs on EVERY invocation before any activation decision: MP1 manifest validity and MP6 witness/manifest cross-agreement both FAIL CLOSED here, in targeted mode exactly as in default mode. STAGE 2 - ACTIVATION - IS MODE-DEPENDENT, and A VALID -gatetask SELECTOR HAS ACTIVATION PRECEDENCE OVER EVERY DEFAULT-MODE RULE, INCLUDING MP6's FORCING CLAUSE. So a targeted run executes EXACTLY the selected test and SKIPS THE OTHER SEVEN in EVERY phase - INCLUDING terminal, and REGARDLESS OF WITNESS PRESENCE. The rev-12 table listed the witness row BEFORE the -gatetask rows and said a valid witness 'forces 018.011-T active; all eight activate' with no mode qualifier, which read sequentially made a targeted terminal-state run activate all eight bodies - contradicting MP3 and AC-C2.7/AC-C2.9/AC-C2.11. WITNESS INTEGRITY MAY STILL FAIL A TARGETED INVOCATION; WITNESS PRESENCE MAY NEVER ACTIVATE A TEST BODY THE OPERATOR DID NOT SELECT. Scoping the forcing clause to default mode subtracts NO detection, because the rewind MP6 exists to catch is caught by the MODE-INDEPENDENT integrity half. DEFAULT MODE IS UNCHANGED: red and terminal activate all eight; build activates the completed set. REV 13 - CONTROL-STATE INPUTS ARE EXPRESSLY EXEMPT, NARROWLY. The independence invariant is scoped to SUBSTANTIVE SURFACES - agent, policy, workflow and script behaviour that a test asserts on. The harness's DEDICATED CONTROL-STATE INPUTS - the validated harness-state manifest and the terminal witness - are ALLOWED AND REQUIRED inputs to MP1 and MP6, and activation and fail-closed behaviour are SUPPOSED to derive from them. They are covered by their own separate mutation/integrity assertions (MP0/MP1/MP4/MP6, AC-C2.2's ten cases) and are NOT substantive product or governance surfaces whose absence can self-disable their own check: a missing or malformed manifest FAILS ALL EIGHT functions, whereas a missing job-lint step under the rev-10 design SILENTLY SKIPPED the function meant to notice it. The failure directions are opposite, which is the whole test. NOTHING ELSE MAY BE ADDED TO THE EXEMPT COLUMN. REV 13 - EVIDENCE FOR TERMINAL DEFAULT-MODE ACTIVATION MUST BE TAKEN UNSELECTED (plan section 5.0.2 point 7, rewritten). The rev-12 lint-removal evidence used -gatetask=C1, which activates that function BY CONSTRUCTION and would have passed identically under the rev-10 self-disarming design, and it never exercised 018.011-T at all. The authoritative observation is now a DEFAULT-MODE run against a COPIED TERMINAL-STATE REPOSITORY FIXTURE under t.TempDir() - the real tree is NEVER mutated and git status --porcelain stays empty - whose manifest and witness remain VALID AND IN AGREEMENT, with only the job-lint step removed. It must show BY TEST-EVENT NAME that TestDirectPushGate_CIWiringIsBlocking FAILS FOR THE MISSING LINT STEP and that TestDirectPushGate_FullCorpusClean EXECUTES (a run/pass event, never --- SKIP:). The targeted command is RETAINED SEPARATELY as point 7a and is SELECTOR EVIDENCE ONLY. REV 14 - THAT OBSERVATION NOW HAS AN EXECUTABLE MECHANISM, BECAUSE THE REV-13 FORM COULD NOT PRODUCE IT: repoRoot(t) walks up to go.mod from the process working directory and therefore resolves the REAL CHECKOUT, and the observation lives INSIDE TestDirectPushGate_FullCorpusClean and would RECURSE. The mechanism is normative and is implemented as UNEXPORTED HELPERS IN THE EXISTING tests/integration/directpush_gate_test.go - NO NEW HARNESS FUNCTION AND NO NEW TEST: copyTrackedRepoFixture(t) makes a deterministic `git ls-files -z` TRACKED-FILE copy into t.TempDir() (real tree never written; git status --porcelain empty before and after); ONLY the job-lint step is removed from the COPIED ci.yml via the structural jobs.lint.steps[] walk, with the copied manifest and witness left VALID AND IN AGREEMENT; runFixtureChildGoTest(t, fixtureRoot) launches a CHILD go test PROCESS whose exec.Cmd.Dir is the COPIED MODULE ROOT - which is what makes repoRoot(t) resolve the copy - running `go test -json ./tests/integration -run '^TestDirectPushGate_(CIWiringIsBlocking|FullCorpusClean)$' -count=1` with NO -gatetask so activation is DEFAULT MODE, while -run bounds PROCESS SCOPE ONLY and is never an activation input; the parent sets the NON-RECURSIVE SENTINEL DIRECTPUSH_GATE_FIXTURE_CHILD=1, which disables the PARENT-ONLY SPAWN BLOCK AND NOTHING ELSE - IT MUST NOT SKIP TestDirectPushGate_FullCorpusClean AND MUST NOT BYPASS MP1/MP6, AND IT HAS NO ACTIVATION EFFECT AND NO ROW IN EITHER gate() TABLE; and the parent decodes the -json EVENT STREAM (never scraped text) requiring a terminal fail event for TestDirectPushGate_CIWiringIsBlocking citing the MISSING STEP, a terminal pass-or-fail event for TestDirectPushGate_FullCorpusClean (a skip, or NO terminal event for that name, FAILS), a NON-ZERO CHILD EXIT, NO recursion or timeout marker, and a WELL-FORMED stream - MALFORMED OR TRUNCATED FAILS rather than reading as inconclusive. The child is bounded by an explicit context.WithTimeout and invoked through exec.CommandContext on the Go binary with NO SHELL; its stdout/stderr appear ONLY in bounded failure output. Residual risk R24 records this mechanism's own failure modes. The sentinel is set by the PARENT PROCESS through exec.Cmd.Env, never typed into a shell, so the -gatetask flag's shell-portability rationale is untouched. REV 13 - H0 HARNESS ELIGIBILITY, RESOLVED AGAINST THE INSTALLED CONTRACT WITHOUT CHANGING IT (plan section 5.0 'H0 eligibility'). _ship.agent.md STEP 2 selects the shipment's FULL QUEUED TASK SET for harness generation - it runs ONCE UP FRONT, NOT IN A LOOP, lists tasks in QUEUED status, partitions SOLELY on the harness-ready label, and HALTS unless EVERY queued task carries that label afterwards - and STEP 3 SEPARATELY builds the dependency-ordered execution queue. IMPLEMENTATION DEPENDENCIES THEREFORE GATE CLAIM AND EXECUTION, NEVER UPFRONT HARNESS GENERATION. REV 14 - CITATION CORRECTED, FAITHFULLY: Step 2's closing paragraph DOES direct Ship, when dependency operations are supported, to VERIFY THE DEPENDENCY GRAPH BEFORE PROCEEDING, so the rev-13 phrasing 'no step of this sequence consults the dependency graph' is WITHDRAWN AS WRONG. What Step 2 verifies is the graph's VALIDITY; what it does NOT do is filter the batch by dependency READINESS. Harness generation remains an UPFRONT BATCH OVER THE EXPLICITLY SELECTED FULL SHIPMENT TASK SET, and dependency READINESS gates execution ORDER (Step 3) and CLAIM (Step 4). REV 14 - SHIP MUST SUPPLY THE EXPLICIT EIGHT-ID INPUT: harness-architect's ${input:tasks} is declared OPTIONAL and its omission falls back to 'all ready tasks under the feature', a selection this plan must not delegate, so Ship passes the EXACT EIGHT 017-S TASK IDS as ${input:tasks}. SET EQUALITY WITH THE SHIPMENT MANIFEST (items minus the covering feature 018-F, resolved via parent_id and never executed) IS CHECKED BEFORE INVOCATION, and Step 2 item 4's ALL-QUEUED POSTCONDITION HALTS ON OMISSION AFTER IT - so an under-selected batch is caught on BOTH SIDES OF THE CALL. 'blocked' is a LIFECYCLE STATUS VALUE, not merely an unmet dependency, and all eight 017-S tasks are QUEUED. harness-architect Step 1 excludes 'blocked' as a BACKLOG STATUS VALUE, not a derived dependency state, and all eight 017-S tasks are QUEUED. H0 accordingly batches ALL EIGHT explicitly selected queued shipment tasks even though seven have unmet implementation dependencies, and verifies LITERAL 8/8 RED before labelling any of them harness-ready. MECHANICAL CHECK (AC-C2.11 evidence point 1): the H0 selected set must EQUAL the eight task IDs of shipment 017-S - 018.004-T, 018.005-T, 018.006-T, 018.007-T, 018.008-T, 018.009-T, 018.010-T, 018.011-T - with 018-F resolved through parent_id and NEVER executed; NO task may be omitted for dependency state, and a proper subset is an H0 FAILURE, not a partial success. NO PERMISSION IS INVENTED: if a future contract is read to exclude dependency-unready tasks, REPORT THE CONTRADICTION with Step 2's all-queued-tasks halt rather than relaxing the 8/8 requirement. REV 13 SCOPE: all of the above are CLARIFICATIONS of assertion mode and evidence form. NO new task, harness function, fixture, file, shipment or dependency edge; NO new acceptance criterion; THIS TASK'S SIZE AND COMPLEXITY ARE UNCHANGED and it remains inside the 2-hour rule. DEPENDENCY DIRECTION: backlogit stores edges as `dep add <item> <depends-on> --type blocks`, so a recorded edge reads DEPENDENT -> PREREQUISITE despite the 'blocks' label; gateDeps[X] is therefore X'S PREREQUISITES, and reading the label the other way inverts the graph. SCOPE: the one-line manifest edit is the SHARED HARNESS-STATE COMPLETION RECORD, not a new skill domain and not a new deliverable; this task's size and complexity are UNCHANGED and it remains inside the 2-hour rule.
ACCEPTANCE CRITERIA (PLAIN-TEXT RENDERING ONLY - NOT AUTHORITATIVE. The authoritative text is the acceptance-criteria section below, which is plan section 7.1 VERBATIM. If this rendering and that section ever disagree, the section wins and this rendering is the defect):
AC-C1.1 The gate runs in job 'lint' and is blocking from its FIRST wiring - no advisory window, no toggle.
AC-C1.2 ci.yml stays valid YAML and ci-gate still transitively depends on 'lint', so the step is genuinely merge-blocking.
AC-C1.3 The LOCAL DIVERGENCE ledger documents the new gate, its membership rule admits the new entry (wildcard/range form, no magic count), it records the no-toggle decision and the D2 residual, and it records that removal of the step is CAUGHT, NOT ACCEPTED, by the non-generated Go harness running from job 'expensive''s FULL-SUITE ./... test step. REV 12: the ledger MUST NOT assert that the observed installed command go test -race -mod=readonly ./... is the GENERATED BASELINE (the template emits {{TEST_COMMAND}}; the recorded render input is go test ./...). It may cite the installed command AS OBSERVED, and its survival claim is scoped to the full-suite ./... package pattern.
AC-C1.4 CODEOWNERS coverage is asserted STRUCTURALLY, not by count: every script invoked by a step in .github/workflows/ci.yml has an owner line in .github/CODEOWNERS, and the fixture-data carve-out is generalized to scripts/testdata/**.
AC-C1.5 The script header states verbatim that this is an ANTI-ACCIDENT control, not an anti-adversary control (CI runs it from the PR head), and discloses that the job-lint wiring step sits INSIDE the autoharness render boundary AND that its removal is DETECTED - NOT ACCEPTED - by TestDirectPushGate_CIWiringIsBlocking (plan section 5.0).
AC-C1.6 RENDER-SURVIVAL ASSERTION - STRUCTURAL AND NON-VACUOUS (rev 5). Step presence is determined by PARSING ci.yml and walking jobs.lint.steps[], NEVER by a file-wide text search (which would self-match the ledger comment naming the same script). Non-vacuity proven by EXECUTION against a t.TempDir() copy with the step removed. No dirty-tree edit. REV 11 - ACTIVATION INDEPENDENCE (the P0 correction): this function's DEFAULT-MODE ACTIVATION is derived SOLELY from the harness-state manifest (C1 present in completed), and NEVER from the presence of the job-lint step it asserts on; the criterion is satisfied ONLY IF, with C1 in completed and the job-lint step ABSENT, the function RUNS AND FAILS - a skip is an EXPLICIT FAILURE. REV 12: 'runs and fails' is a DEFAULT-MODE requirement, and a targeted -gatetask=C1 run must likewise execute the real assertion and never skip; C1 carries NO MP5 obligation (MP5 is RETIRED). REV 13: THE EVIDENCE MUST BE TAKEN UNSELECTED - a targeted run activates this function BY CONSTRUCTION and would have passed under the rev-10 self-disarming design, so the authoritative observation is the DEFAULT-MODE plan section 5.0.2 point-7 run against a COPIED TERMINAL-STATE FIXTURE under t.TempDir() (manifest and witness valid and in agreement), showing BY TEST-EVENT NAME that TestDirectPushGate_CIWiringIsBlocking FAILS for the missing lint step and TestDirectPushGate_FullCorpusClean EXECUTES rather than skips. The targeted invocation is retained separately as point 7a, SELECTOR EVIDENCE ONLY. REV 14 - THE MECHANISM THAT MAKES THAT EVIDENCE OBTAINABLE IS NAMED, NOT LEFT TO THE IMPLEMENTER. Rev 13 specified the observation but no way to run it: repoRoot(t) resolves the REAL CHECKOUT by walking up to go.mod from the process working directory, so an in-process run can never see the copy, and the observation lives inside TestDirectPushGate_FullCorpusClean, so an unguarded re-run RECURSES. The criterion is satisfied ONLY by the plan section 5.0.2 point-7 PROCESS CONTRACT, implemented as unexported helpers in the EXISTING tests/integration/directpush_gate_test.go (NO NEW HARNESS FUNCTION, NO NEW TEST): (i) copyTrackedRepoFixture(t) builds the fixture from `git ls-files -s -z` TRACKED FILES ONLY into t.TempDir() - REV 15: -s reads INDEX MODES, 100644/100755 are copied (executable bit preserved) and 120000 symlinks, 160000 gitlinks, every other mode and every unmerged stage FAIL CLOSED (verified: all 629 entries of this repository are 100644, and references/ is NOT in the index at all, so no skip-list is written); the GENERATED terminal-witness.json is copied EXPLICITLY from the working tree, REQUIRED TO EXIST, parsed and VALIDATED against the copied manifest; and the copy is made a SELF-CONTAINED GIT REPOSITORY via git init + local non-secret identity + git add + a deterministic local commit (NO real .git copy, NO submodule recursion, NO network) with git -C {copy} rev-parse --show-toplevel asserted EQUAL to the canonical copy root so no nested or sibling repository can be selected; the real tree is left unwritten, proven by BYTE-IDENTICAL git status --porcelain=v1 -z --untracked-files=all snapshots before and after rather than by an empty-tree assertion, which is unsatisfiable exactly when the evidence is required; (ii) ONLY the job-lint step is removed from the COPIED ci.yml by the same jobs.lint.steps[] structural walk this criterion already mandates, AFTER the copied manifest and witness are proven VALID AND IN AGREEMENT (rev 15 ordering, so the child's failure stays ATTRIBUTABLE to the removal); (iii) runFixtureChildGoTest(t, fixtureRoot) launches a CHILD go test PROCESS whose exec.Cmd.Dir is the COPIED MODULE ROOT - which is precisely what makes repoRoot(t) resolve the copy - invoking `go test -json ./tests/integration -run '^TestDirectPushGate_(CIWiringIsBlocking|FullCorpusClean)$' -count=1` with NO -gatetask, so activation is DEFAULT MODE while -run bounds only the child's PROCESS SCOPE; (iv) the parent sets the non-recursive sentinel DIRECTPUSH_GATE_FIXTURE_CHILD=1 on the child, which disables the PARENT-ONLY SPAWN BLOCK and nothing else - it MUST NOT skip TestDirectPushGate_FullCorpusClean and MUST NOT bypass MP1/MP6 (plan section 5.0.1 'The fixture-child sentinel'); and (v) the parent decodes the child's -json EVENT STREAM and requires a terminal fail event for TestDirectPushGate_CIWiringIsBlocking citing the missing step, a terminal pass-or-fail event for TestDirectPushGate_FullCorpusClean (a skip or an ABSENT terminal event FAILS this criterion), a NON-ZERO child exit, NO context-deadline or killed-child termination (REV 15: the rev-14 'recursion marker' is WITHDRAWN - the guard means a sentinel-set process never REACHES the spawn block, so that condition is unsatisfiable; recursion is PREVENTED parent-side and BOUNDED by the child timeout), and it FAILS ON A MALFORMED EVENT STREAM rather than reading it as inconclusive. The child runs under an explicit context.WithTimeout, and its stdout/stderr are surfaced ONLY in bounded failure output. Invocation is exec.CommandContext on the Go binary - NO SHELL, so the mechanism is identical on every supported platform.
AC-C1.7 POST-RENDER VERIFICATION OF THE RENDERED FULL-SUITE COMMAND (rev 12). The ledger carries a standing obligation: after ANY autoharness install/tune regeneration of .github/workflows/ci.yml, VERIFY BY READING THE RENDERED STEP that job 'expensive''s test command still invokes the FULL SUITE OVER ./... (which is what pulls tests/integration, and therefore this harness, into the run), and re-apply the C1 divergence step. Satisfied by observing a rendered package pattern of ./...; NOT satisfied by the flags matching. If a render narrows the pattern the harness stops running and R5 reverts to an OPEN residual - recorded as R23.

<!-- BEGIN:implementation-notes -->
DEGRADATION NOTE: this workspace's backlogit type schema defines 'size' but NO 'complexity' field on any artifact type (verified via backlogit metadata types: task fields = priority, size, status), despite backlog-registry.yaml declaring features.sizing: true. Complexity is therefore recorded below as enum-validated prose per the Stage two-axis sizing contract. Size is stored structurally (size_source=agent, size_ruleset_version=2h-rule-v1).

Size: S | Complexity: medium

Rationale: Three coupled CI-configuration surfaces (ci.yml step, divergence ledger, CODEOWNERS); blocking-from-first-wiring is only safe because sub-epic B lands first. Size/complexity UNCHANGED at rev 5: AC-C1.6 constrains the SHAPE of an assertion authored at H0 by harness-architect (structural parse instead of text grep, plus a t.TempDir() non-vacuity case), not the wiring work this task performs, so this task's own 2-hour envelope does not grow.

REV-5 RECONCILIATION (PR #54 thread PRRT_kwDOTPuhps6hnPkD, comment 3992775719, plan section 11 R5): rev 4's R5 accepted the residual that a render could wipe this step and silently disable the gate, contradicting plan section 1 and the 018-F DoD. Rev 5 does NOT narrow the objective. It names the tracked enforcement path that already survives the render boundary - the plan section 5.0 Go harness, which is non-generated and is invoked by job 'expensive''s FULL-SUITE ./... test step (a render re-emits that step rather than removing it; REV-12 PROVENANCE CORRECTION - 'go test -race -mod=readonly ./...' is the OBSERVED INSTALLED command, NOT the generated baseline, because the template emits {{TEST_COMMAND}} and the recorded render input is 'go test ./...', so what survives is the FULL-SUITE ./... PACKAGE PATTERN and NOT the flags - see AC-C1.7 and R23), with the trigger link closed because any render that rewrites ci.yml is a .github/** change and therefore sets changes.code == 'true'. AC-C1.6 added (structural, non-vacuous step-presence assertion); AC-C1.5 reworded from 'accepted residual' to 'detected, not accepted'; AC-C1.3 extended to require the ledger to record the same. Paired with AC-C2.8 on 018.011-T. R5 reclassified accepted -> mitigated; the narrower VISIBLE residual is R12.

REV-8 RECONCILIATION (PR #54 adversarial review, P3 finding 6): the generated-baseline invocation was cited as "the go test step in job 'expensive'", which names no step in .github/workflows/ci.yml. The actual step is 'Test (race)' and its command is go test -race -mod=readonly ./..., now cited exactly in the description, AC-C1.3 and this note. The plan's separate constitutional requirement (AC-C2.7's gofmt / go vet / go test ./... / go build sequence) is UNCHANGED and remains a distinct obligation - the two are not interchangeable. This task's own harness_cmd stays narrow (go test ./tests/integration -run TestDirectPushGate_CIWiringIsBlocking). No scope, size, or criterion change.

HARNESS BINDING (rev 4, plan section 5.0): this task does NOT carry a harness-ready label. harness-architect applies that label at Ship Step 2 after producing the Go harness and confirming go vet ./... exits 0 and go test ./... is red. Stage does not forge P-004's postcondition.
<!-- END:implementation-notes -->

<!-- BEGIN:acceptance-criteria -->
- **AC-C1.1** The step is present in job `lint`, **BLOCKING from its FIRST wiring** (no
  `continue-on-error`, no toggle variable, no advisory window), and task-ID-suffixed.
- **AC-C1.2** `.github/workflows/ci.yml` remains valid YAML
  (`python -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml'))"` passes) **and**
  `ci-gate` still transitively depends on job `lint`, so the new step is genuinely merge-blocking.
- **AC-C1.3** The LOCAL DIVERGENCE ledger names the gate, uses the generalized membership rule
  (wildcard/range form rather than the literal `011.0xx-T`), carries no magic count, records the
  no-toggle decision and the D2 residual, and records that removal of this step is **CAUGHT, NOT
  ACCEPTED** — `tests/integration/directpush_gate_test.go`
  (`TestDirectPushGate_CIWiringIsBlocking`) is not autoharness-generated and runs from job
  `expensive` (`name: test`)'s **full-suite `./...`** test step, so a render that drops this step
  turns CI **red** rather than green (§5.0). **Rev 8**: the CI step is cited by its exact name and
  command, because "the `go test` step in job `expensive`" names no step in this workflow.
  **Rev 12 — provenance correction**: the ledger MUST NOT assert that the observed installed command
  `go test -race -mod=readonly ./...` is the **generated baseline**. The template emits
  `{{TEST_COMMAND}}` and this workspace's recorded render input is `go test ./...` (§5.0 "Invocation
  provenance"), so the flags are local to the installed artifact. The installed command may still be
  cited **as the observed command** — that is what makes the evidence name a real step — but the
  ledger's survival claim is scoped to the **full-suite `./...` package pattern**, which is what a
  render actually re-emits.
- **AC-C1.4** CODEOWNERS coverage is asserted **structurally, not by count** (rev 3): every script
  invoked by a step in `.github/workflows/ci.yml` has an owner line in `.github/CODEOWNERS`; the
  header's fixture-data carve-out is generalized from `scripts/testdata/gitignore/**` to
  `scripts/testdata/**` so the new fixture tree is covered by the same stated rationale.
- **AC-C1.5** The script header states **VERBATIM** that this is an **ANTI-ACCIDENT control, not an
  anti-adversary control** (CI runs it from the PR's own head), and discloses that the job-`lint`
  wiring step sits **INSIDE** the autoharness render boundary (deliberation D6) **and** that its
  removal is **DETECTED — NOT ACCEPTED — by** `TestDirectPushGate_CIWiringIsBlocking` (§5.0), so the
  disclosure points a future reader at the surviving enforcement path rather than at a silent
  failure. Added at rev 4 — review round 3 — to close a plan↔task drift: `018.010-T` already carried
  this criterion while the plan did not state it. **Rev 5** replaced the "accepted residual" half of
  the disclosure per the R5 reclassification.
- **AC-C1.6** **Render-survival assertion — structural and non-vacuous (rev 5).**
  `TestDirectPushGate_CIWiringIsBlocking` determines step presence by **PARSING**
  `.github/workflows/ci.yml` and walking `jobs.lint.steps[]` for a step whose `run` invokes
  `scripts/check-direct-push-language.sh` — **NEVER** by a file-wide text search, which would
  self-match the LOCAL DIVERGENCE ledger comment naming the same script and pass vacuously after the
  step itself was deleted (§5.0; prior art
  `docs/compound/2026-09-06-ci-self-matching-grep-and-actionlint-verification-gap.md`). Non-vacuity
  is proven by **EXECUTION**: the test writes a copy of the current `ci.yml` with that step removed
  into `t.TempDir()` and asserts the same check **FAILS** against the copy. **No edit is made to the
  real tree** — proven by **byte-identical** `git status --porcelain=v1 -z --untracked-files=all`
  snapshots taken immediately before and after (rev 15: snapshot **equality**, not an empty tree,
  because C2's own manifest and witness writes and any locally-excluded path make "empty"
  unsatisfiable exactly when the evidence is needed) — the same no-dirty-tree-edit rule as AC-A2.4
  and AC-C2.2. **Rev 11 — activation independence (the P0 correction).** This function's
  default-mode activation is derived **solely** from the harness-state manifest (`C1` present in
  `completed`), and **never** from the presence of the job-`lint` step it asserts on. Rev 10 keyed
  activation on that step, so a regeneration that dropped it made this function `t.Skip` instead of
  fail — the assertion disarmed itself at precisely the moment it was meant to fire. The criterion
  is therefore satisfied only if, with `C1` in `completed` and the job-`lint` step **absent**, the
  function **runs and fails**; a skip is an explicit failure of this criterion. C1's completion also
  inserts `"C1"` into `completed` at its `gateOrder` position (§5.0.1) — one line in
  `tests/integration/testdata/directpush-gate/harness-state.json`, the shared harness-state
  completion record, not a second skill domain; C1's size band is unchanged. **Rev 12**: "runs and
  fails" is a **default-mode** requirement; a targeted `-gatetask=C1` run must likewise execute the
  real assertion and never skip. C1 carries **no** MP5 obligation — MP5 is **RETIRED** (§5.0.1), and
  its retirement does not weaken this criterion, because C1's activation was already manifest-only.
  **Rev 13 — the evidence must be taken UNSELECTED.** A targeted `-gatetask=C1` run activates this
  function **by construction**, so it cannot demonstrate that manifest-derived activation survives
  removal of the lint step, and it would have passed identically under the rev-10 self-disarming
  design. The criterion's evidence is therefore the **default-mode** §5.0.2 point-7 observation
  against a **copied terminal-state fixture** under `t.TempDir()` whose manifest and witness remain
  valid and in agreement: it must show, **by test-event name**, `--- FAIL:
  TestDirectPushGate_CIWiringIsBlocking` citing the **missing lint step**, and
  `TestDirectPushGate_FullCorpusClean` **executing** rather than skipping. The targeted invocation is
  retained **separately** as §5.0.2 point 7a and is **selector evidence only**.
  **Rev 14 — the mechanism that makes that evidence obtainable is NAMED, not left to the
  implementer.** Rev 13 specified the observation but no way to run it: `repoRoot(t)` resolves the
  **real checkout** by walking up to `go.mod` from the process working directory, so an in-process
  run can never see the copy, and the observation lives inside C2, so an unguarded re-run recurses.
  The criterion is therefore satisfied only by the §5.0.2 point-7 **process contract**, implemented
  as unexported helpers in the existing `tests/integration/directpush_gate_test.go` (**no new
  harness function, no new test**): (i) `copyTrackedRepoFixture(t)` builds the fixture from
  `git ls-files -s -z` **tracked files only** into `t.TempDir()` — **rev 15**: `-s` so index
  **modes** are read, `100644`/`100755` copied (executable bit preserved) and **every** other mode,
  including `120000` symlinks and `160000` gitlinks, and every unmerged stage, **failing closed**;
  the generated `terminal-witness.json` copied **explicitly** from the working tree and validated
  against the copied manifest; the copy made a **self-contained Git repository** (`git init`,
  local non-secret identity, `git add`, deterministic local commit, **no** real `.git` copy, **no**
  submodule recursion, **no** network) with
  `git -C {copy} rev-parse --show-toplevel` asserted **equal to the copy root**; and the real tree
  left unwritten, proven by **byte-identical** `git status --porcelain=v1 -z --untracked-files=all`
  snapshots before and after; (ii) only the job-`lint` step is removed from the
  **copied** `ci.yml`, by the same `jobs.lint.steps[]` structural walk this criterion already
  mandates, **after** the copied manifest and witness are proven **valid and in agreement** (rev 15
  ordering, so the child's failure stays attributable to the removal); (iii)
  `runFixtureChildGoTest(t, fixtureRoot)` launches a **child `go test` process** whose
  `exec.Cmd.Dir` is the **copied module root** — which is precisely what makes `repoRoot(t)` resolve
  the copy — invoking
  `go test -json ./tests/integration -run '^TestDirectPushGate_(CIWiringIsBlocking|FullCorpusClean)$' -count=1`
  with **no `-gatetask`**, so activation is **default mode** while `-run` bounds only the child's
  **process scope**; (iv) the parent sets the non-recursive sentinel
  `DIRECTPUSH_GATE_FIXTURE_CHILD=1` on the child, which disables the **parent-only spawn block** and
  nothing else — it MUST NOT skip C2 and MUST NOT bypass MP1/MP6 (§5.0.1 "The fixture-child
  sentinel"); and (v) the parent decodes the child's `-json` **event stream** and requires a
  terminal `"Action":"fail"` event for `TestDirectPushGate_CIWiringIsBlocking` citing the missing
  step, a terminal `pass`-or-`fail` event for `TestDirectPushGate_FullCorpusClean` (a `skip` or an
  **absent** terminal event fails this criterion), a **non-zero** child exit, **no** recursion or
  timeout marker, and it **fails on a malformed event stream** rather than reading it as
  inconclusive. The child runs under an explicit `context.WithTimeout`, and its stdout/stderr are
  surfaced **only** in bounded failure output. Invocation is `exec.CommandContext` on the Go binary —
  **no shell**, so the mechanism is identical on every supported platform.
- **AC-C1.7** **Post-render verification of the rendered full-suite command (rev 12).** The
  regeneration-resistant guarantee (§5.0) depends on job `expensive`'s test step invoking the **full
  suite over `./...`**, because `./...` is what pulls `tests/integration` — and therefore this
  harness — into the run. That step is rendered from `{{TEST_COMMAND}}`, so the **package pattern is
  a render output, not a fixed constant**, and **no claim is made** that the observed `-race` /
  `-mod=readonly` flags survive a render. C1 therefore records, in the LOCAL DIVERGENCE ledger, a
  standing obligation: **after any `autoharness install` / `tune` regeneration of
  `.github/workflows/ci.yml`, verify — by reading the rendered step, not by assuming it — that the
  rendered expensive-job test command still invokes the full suite over `./...`**, and re-apply the
  C1 divergence step. The verification is satisfied by observing a rendered command whose package
  pattern is `./...`; it is **not** satisfied by the flags matching. If a render narrows the pattern,
  the harness stops running and **R5 reverts to an open residual** — which is why this obligation is
  written into the ledger rather than left to memory. Residual: **R23**. Size/complexity
  **UNCHANGED**: one additional sentence in a ledger this task already authors.
<!-- END:acceptance-criteria -->
