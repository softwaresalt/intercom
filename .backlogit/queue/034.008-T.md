---
artifact_type: task
created_at: 2026-09-19T02:31:07.0785119Z
custom_fields:
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 034.005-T
    - 034.007-T
id: 034.008-T
parent_id: 034-F
priority: medium
references:
    - docs/plans/2026-09-18-intercom-go-gate-reliability-plan.md
status: queued
title: Add self-test fixtures for every vector plus positive control
updated_at: 2026-09-19T02:31:07.0785119Z
---

U4-T6. Fixtures covering named-alias write, syscall write, os.OpenRoot construction, an os.Root TYPE-USE fixture, and the POSITIVE CONTROL (a writing syscall.CreateFile inside an allowance-scoped file must still be REJECTED). The os.Root fixture is DISTINCT from and NOT satisfied by the os.OpenRoot construction fixture: an os.OpenRoot(...) call site can pass while containing no os.Root type selector, so without a separate fixture that exercises the os.Root type selector directly (e.g. an os.Root-typed declaration, parameter, field or return), a broken or omitted os.Root selector from 034.004-T stays undetected. All four selectors added by 034.004-T (syscall.CreateFile, syscall.Write, os.OpenRoot, os.Root) MUST each have a fixture. All fixtures MUST be gofmt and goimports clean, because the ci.yml lint job formats the whole tree including testdata and would otherwise fail on the new fixtures. Posture: test-first. Size: M | Complexity: medium. ACCEPTANCE CRITERIA: AC-4.1 --self-test catches every listed vector and each new fixture FAILS before the corresponding change; AC-4.9 all new fixtures are gofmt and goimports clean; AC-4.5 no TOCTOU or hard-link mitigation is added and no write primitive is introduced anywhere in this feature -- introducing a write primitive is a named stop condition.