---
artifact_type: task
created_at: 2026-09-07T18:20:56.111443Z
custom_fields:
    size: S
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
id: 012.002-T
labels:
    - tests
    - security
    - pathsafe
    - red-phase
    - windows
parent_id: 012-F
priority: high
references:
    - docs/plans/2026-09-07-intercom-go-pathsafe-containment-correctness-plan.md
status: queued
title: 'RED: privilege-free Windows junction containment coverage'
updated_at: 2026-09-07T18:23:39.9852532Z
---

Resolves the P0 review finding for ECE3DAB7. Create a NEW file internal/pathsafe/junction_windows_test.go. Go build constraints are FILE-SCOPED, so this test MUST NOT be added to symlink_test.go - doing so would exclude every existing cross-platform symlink test from the required Linux gate. The _windows filename suffix supplies the constraint. Directory junctions do NOT require SeCreateSymbolicLinkPrivilege, so this test must NOT be gated behind requireSymlinkOrFailClosed: it must run unconditionally on Windows. This matters because N1/N2 skip on unprivileged Windows runners, leaving the platform with the widest attacker capability with zero test evidence. FIXTURE SEQUENCE (the junction must end up DANGLING): (1) create the outside target directory; (2) create the junction pointing at it; (3) REMOVE the target directory leaving the junction dangling; (4) call Resolve('junction/new-file.txt'). Junction-creation APIs generally require the target to exist at creation time, so a naive point-at-nonexistent-target fixture may silently fail or produce a NON-dangling junction, which would make the test green before the fix and destroy its red phase. Reuse the junction-creation approach proven in tests/integration/output_path_guard_test.go createDirectoryJunction. Domain: tests only.

<!-- BEGIN:acceptance-criteria -->
Test lives in a NEW file internal/pathsafe/junction_windows_test.go and does NOT modify symlink_test.go. Test RUNS (does not skip) on a stock unprivileged Windows runner. The junction is verified DANGLING before Resolve is called. Test is observed FAILING against the current checkSymlinkEscape. Asserts rejection with symlinkEscapeMsg.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
Size: S | Complexity: medium. NOTE: complexity is recorded here as labeled prose because this workspace's 'task' WIT type does not define a structured complexity field (backlogit rejects --complexity for artifact_type=task). Size IS structured (custom_fields.size, size_source=agent). Both axes are independently assigned per the 2-hour rule: size = effort/volume, complexity = difficulty/uncertainty. Test-first required for all code units (Constitution VIII).
<!-- END:implementation-notes -->