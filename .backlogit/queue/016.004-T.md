---
artifact_type: task
created_at: 2026-09-10T22:38:03.0898504Z
custom_fields:
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 016.001-T
id: 016.004-T
labels:
    - sub-epic-B
    - E4C5413F
    - windows
    - code
parent_id: 016-F
priority: high
status: queued
title: 'U5: long-path prefixing before CreateFile in canonicalizeReparse'
updated_at: 2026-09-10T22:39:58.8766996Z
---

HARD PREREQUISITE of the NewRoot change: once NewRoot routes through canonicalizeReparse it loses the long-path handling filepath.EvalSymlinks gave it via os.Lstat, so shipping the NewRoot change without this introduces a NEW fail-closed regression for long workspace roots. AC1 RED first: a deeply-nested in-root ancestor over MAX_PATH resolves instead of being spuriously rejected. AC2 the prefixing predicate is PINNED in code and comment: apply only when absolute, not already \\?\-prefixed, not a \\.\ device path, not a bare UNC path; document the MAX_PATH threshold; boundary test included. AC3 short paths unaffected; prefix/strip round-trip with U6 verified. AC4 negative coverage: an already-extended volume-GUID input is NOT double-prefixed and a device path is NOT prefixed. AC5 the UNC long-path form is an explicitly recorded risk-register residual, deliberately not implemented.

<!-- BEGIN:implementation-notes -->
Size: M | Complexity: high (size_source: agent; ruleset: 2h-rule-v1). Complexity is recorded here as labeled prose because this workspace's 'task' type defines no structured complexity field; size IS structured in frontmatter custom_fields.
<!-- END:implementation-notes -->