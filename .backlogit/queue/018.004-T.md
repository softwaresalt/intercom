---
artifact_type: task
created_at: 2026-09-11T17:22:48.3929035Z
custom_fields:
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
id: 018.004-T
labels:
    - sub-epic-a
    - test-data
    - chore
parent_id: 018-F
priority: high
status: queued
title: Create direct-push fixture corpus and manifest
updated_at: 2026-09-12T02:45:00.0000000Z
---

Parent feature 018-F. Sub-epic A (failing regression harness), plan section 5.4 of docs/plans/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-plan.md. Create the 14-fixture corpus scripts/testdata/directpush/*.md - 9 reject shapes (literal 3e construct; git push origin main; default-branch prose; {{DEFAULT_BRANCH}} placeholder; commit-shaped permission; trailing-marker; 'rather than'; lazy-continuation wrapped; mixed block) and 5 accept shapes (bold lead-in prohibition; Forbidden column-header table cell; marker-precedes; read/checkout/PR forms; verification/postcondition form) - plus scripts/testdata/directpush-manifest.json with sorted keys. Test data only, no executable logic: this task authors and statically checks fixture data; the EXECUTED non-vacuity proof of the bijection and prefix-agreement assertions belongs to 018.005-T (AC-A2.4), which owns the fixture-suite driver.
HARNESS (P-002/P-004): harness function TestDirectPushGate_FixtureCorpusIsComplete in tests/integration/directpush_gate_test.go, produced by harness-architect at Ship Step 2 per plan section 5.0. harness_cmd: go test ./tests/integration -run '^TestDirectPushGate_FixtureCorpusIsComplete$' -gatetask=A1
HARNESS LIFECYCLE (rev 11, plan section 5.0.1): activation is derived SOLELY from the tracked harness-state manifest tests/integration/testdata/directpush-gate/harness-state.json and the -gatetask selector, NEVER from any surface this harness asserts on (the rev-10 self-disarming defect). H0 writes that manifest as {"phase":"red","completed":[],"terminal":false}; in red phase ALL EIGHT functions activate and fail, each with its own task-specific marker - this task's marker is 'not implemented: A1 direct-push fixture corpus' - which is the literal 8/8 red the installed P-002/P-004/harness-architect contract requires before ANY task may be labelled harness-ready. TARGETED RED (unchanged per-task obligation): run the harness_cmd above BEFORE implementing; -gatetask activates unconditionally in every phase, so this function evaluates its REAL assertion and must fail for this task's expected reason. COMPLETION: atomically with the fixture work, insert "A1" into the manifest's completed array and perform the ONE-WAY phase transition red -> build (there is NO reverse transition; build -> red is an impossible state and fails closed). completed must remain an ORDER-PRESERVING SUBSEQUENCE of [A1,A2,A3,B1,B2,B3,C1,C2] that is DEPENDENCY-CLOSED (B1/B2/B3 are parallel - each depends only on A3 - so any of the three may land first; C1 requires all three). Writing the ID does NOT make the test pass: targeted mode ignores the manifest for activation and default mode still evaluates the real assertion, so a manifest advanced ahead of the work fails on the assertion. MISSING OR MALFORMED MANIFEST, DUPLICATE/UNKNOWN/OUT-OF-ORDER ID, UNMET PREREQUISITE, INCONSISTENT phase/terminal, OR AN UNKNOWN -gatetask VALUE ALL FAIL CLOSED - never skip. A1 additionally carries MP0 (gateOrder/gateDeps integrity), MP1 probe non-vacuity, and MP5 (live no-rollback anchor). SCOPE: the one-line manifest edit is the SHARED HARNESS-STATE COMPLETION RECORD, not a new skill domain and not a new deliverable; this task's size and complexity are UNCHANGED and it remains inside the 2-hour rule.
ACCEPTANCE CRITERIA:
AC-A1.1 14 fixtures exist; manifest keys are sorted and in bijection with the directory.
AC-A1.2 Every manifest verdict agrees with its fixture's filename prefix (directpush-reject-* / directpush-accept-* - the actual on-disk names), and both classes are non-empty: 9 reject, 5 accept. Static data check only.
AC-A1.3 markdownlint 'scripts/testdata/directpush/**' exits 0 (single leading H1 per fixture for MD041/MD025).
AC-A1.4 HARNESS-STATE LIFECYCLE OPENED (rev 11). H0's tests/integration/testdata/directpush-gate/harness-state.json is present and was initialized to exactly {"phase":"red","completed":[],"terminal":false}, and the recorded H0 evidence is the LITERAL 8/8 red - eight '--- FAIL:' lines, each carrying its own task-specific 'not implemented:' marker from the plan section 5.0.1 marker table. On this task's completion, and ATOMICALLY with the fixture work above, the manifest performs the ONE-WAY red -> build transition and completed becomes exactly ["A1"]. The transition is one-way: build -> red is an impossible state and fails closed (MP1). The insertion does NOT by itself make this test pass - -gatetask=A1 evaluates AC-A1.1 through AC-A1.3 regardless of manifest state, so a manifest advanced ahead of the fixtures fails on the real assertion. A1 additionally carries MP0 (gateOrder has exactly the eight task IDs in declared sequence and gateDeps matches the recorded blocks edges), MP1 probe non-vacuity (each rollback probe returns false against an empty t.TempDir() root), and MP5 (live no-rollback anchor: no task outside completed may have its surface present, and phase=red implies A1's own surface is absent).

<!-- BEGIN:implementation-notes -->
DEGRADATION NOTE: this workspace's backlogit type schema defines 'size' but NO 'complexity' field on any artifact type (verified via backlogit metadata types: task fields = priority, size, status), despite backlog-registry.yaml declaring features.sizing: true. Complexity is therefore recorded below as enum-validated prose per the Stage two-axis sizing contract. Size is stored structurally (size_source=agent, size_ruleset_version=2h-rule-v1).

Size: S | Complexity: low

Rationale: Fixture authoring is mechanical once the shape list is fixed; volume is 15 small files.

HARNESS BINDING (rev 4, plan section 5.0): this task does NOT carry a harness-ready label. harness-architect applies that label at Ship Step 2 after producing the Go harness and confirming go vet ./... exits 0 and go test ./... is red. Stage does not forge P-004's postcondition.
<!-- END:implementation-notes -->

<!-- BEGIN:acceptance-criteria -->
- **AC-A1.1** 14 fixtures exist under `scripts/testdata/directpush/`; `directpush-manifest.json` keys are sorted and in exact bijection with the directory listing.
- **AC-A1.2** Every manifest verdict agrees with its fixture's filename prefix (`directpush-reject-*` -> `reject`, `directpush-accept-*` -> `accept` — the actual on-disk names, corrected at rev 8 from the truncated `reject-*` / `accept-*` form), and both classes are non-empty: 9 reject shapes and 5 accept shapes. Static data check; the executed non-vacuity proof for these assertions belongs to 018.005-T (AC-A2.4), which owns the fixture-suite driver.
- **AC-A1.3** `markdownlint "scripts/testdata/directpush/**"` exits 0 (single leading H1 per fixture for MD041/MD025).
- **AC-A1.4** **Harness-state lifecycle opened (rev 11).** H0's `tests/integration/testdata/directpush-gate/harness-state.json` is present and was initialized to exactly `{"phase":"red","completed":[],"terminal":false}`, and the recorded H0 evidence is the **literal 8/8** red — eight `--- FAIL:` lines, each carrying its own task-specific `not implemented:` marker from the §5.0.1 marker table. On this task's completion, and **atomically with** the fixture work above, the manifest performs the **one-way** `red`→`build` transition and `completed` becomes exactly `["A1"]`. The transition is one-way: `build`→`red` is an impossible state and fails closed (MP1). The insertion does **not** by itself make this test pass — `-gatetask=A1` evaluates AC-A1.1–AC-A1.3 regardless of manifest state, so a manifest advanced ahead of the fixtures fails on the real assertion. A1 additionally carries MP0 (order and graph integrity), MP1 probe non-vacuity, and MP5 (live no-rollback anchor: no task outside `completed` may have its surface present, and `phase: red` implies A1's own surface is absent). **Scope**: the one-line manifest edit is the shared harness-state completion record, not a second skill domain; this task's size and complexity are unchanged.
<!-- END:acceptance-criteria -->