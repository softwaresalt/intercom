---
artifact_type: task
created_at: 2026-09-11T17:22:48.3929035Z
custom_fields:
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
id: 018.004-T
labels:
    - sub-epic-a
    - test-data
    - chore
parent_id: 018-F
priority: high
status: queued
title: Create direct-push fixture corpus and manifest
updated_at: 2026-09-12T02:45:00.0000000Z
---

Parent feature 018-F. Sub-epic A (failing regression harness), plan section 5.4 of docs/plans/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-plan.md. Create the 14-fixture corpus scripts/testdata/directpush/*.md - 9 reject shapes (literal 3e construct; git push origin main; default-branch prose; {{DEFAULT_BRANCH}} placeholder; commit-shaped permission; trailing-marker; 'rather than'; lazy-continuation wrapped; mixed block) and 5 accept shapes (bold lead-in prohibition; Forbidden column-header table cell; marker-precedes; read/checkout/PR forms; verification/postcondition form) - plus scripts/testdata/directpush-manifest.json with sorted keys. Test data only, no executable logic: this task authors and statically checks fixture data; the EXECUTED non-vacuity proof of the bijection and prefix-agreement assertions belongs to 018.005-T (AC-A2.4), which owns the fixture-suite driver.
HARNESS (P-002/P-004): harness function TestDirectPushGate_FixtureCorpusIsComplete in tests/integration/directpush_gate_test.go, produced by harness-architect at Ship Step 2 per plan section 5.0. harness_cmd: go test ./tests/integration -run '^TestDirectPushGate_FixtureCorpusIsComplete$' -gatetask=A1
HARNESS LIFECYCLE (rev 11, plan section 5.0.1): activation is derived SOLELY from the tracked harness-state manifest tests/integration/testdata/directpush-gate/harness-state.json and the -gatetask selector, NEVER from any surface this harness asserts on (the rev-10 self-disarming defect). H0 writes that manifest as {"phase":"red","completed":[],"terminal":false}; in red phase ALL EIGHT functions activate and fail, each with its own task-specific marker - this task's marker is 'not implemented: A1 direct-push fixture corpus' - which is the literal 8/8 red the installed P-002/P-004/harness-architect contract requires before ANY task may be labelled harness-ready. TARGETED RED (unchanged per-task obligation): run the harness_cmd above BEFORE implementing; -gatetask activates unconditionally in every phase, so this function evaluates its REAL assertion and must fail for this task's expected reason. COMPLETION: atomically with the fixture work, insert "A1" into the manifest's completed array and perform the ONE-WAY phase transition red -> build (there is NO reverse transition; build -> red is an impossible state and fails closed). completed must remain an ORDER-PRESERVING SUBSEQUENCE of [A1,A2,A3,B1,B2,B3,C1,C2] that is DEPENDENCY-CLOSED (B1/B2/B3 are parallel - each depends only on A3 - so any of the three may land first; C1 requires all three). Writing the ID does NOT make the test pass: targeted mode ignores the manifest for activation and default mode still evaluates the real assertion, so a manifest advanced ahead of the work fails on the assertion. MISSING OR MALFORMED MANIFEST, DUPLICATE/UNKNOWN/OUT-OF-ORDER ID, UNMET PREREQUISITE, INCONSISTENT phase/terminal, OR AN UNKNOWN -gatetask VALUE ALL FAIL CLOSED - never skip.
REV 12 - THREE LIFECYCLE CORRECTIONS THAT BIND THIS TASK (plan sections 5.0.1 and 5.0.2). (1) MP5 IS RETIRED - REMOVED, NOT RENUMBERED, AND MUST NOT BE IMPLEMENTED. Its rev-11 rule (a task outside completed must have its surface ABSENT, else FAIL) was wrong: 018.011-T's probe was the job-lint step that 018.010-T ITSELF CREATES, so the entirely legitimate post-018.010-T / pre-018.011-T window failed the DEFAULT SUITE - the Ship Step 4.3 deadlock, reintroduced by the guard meant to prevent it. THE GENERAL RULE, NOW SYMMETRIC: NEITHER ACTIVATION NOR ANY GATE-LEVEL FAILURE MAY BE DERIVED FROM A SURFACE THIS HARNESS ASSERTS ON. Rollback of a COMPLETED task's surface is caught by THAT TASK'S OWN SUBSTANTIVE ASSERTION (active in default mode from its completion onward); an INCOMPLETE task legitimately may have prerequisites and even surface fragments present and MUST NOT fail anything. The ACTIVE checks are SIX: MP0, MP1, MP2, MP3, MP4, MP6. (2) TERMINAL COMPLETION WITNESS (MP6). A second tracked, non-generated file tests/integration/testdata/directpush-gate/terminal-witness.json, INDEPENDENT of the manifest, is written ONLY by 018.011-T's completion - WITNESS FIRST, THEN the manifest's ATOMIC replacement with the exact terminal state. IT IS ABSENT AT H0 AND THROUGHOUT BUILD, and that absence is VALID, not an error. gate() runs MP6 BEFORE ANY ACTIVATION DECISION: a present witness must be VALID (schema directpush-gate/terminal-witness, version 1, contract 017-S/018-F, completed == gateOrder, digest == SHA-256 of the eight IDs joined with newline) and REQUIRES a terminal manifest AND FORCES 018.011-T ACTIVE; a terminal manifest REQUIRES a valid witness; missing/malformed/mismatched WHEREVER TERMINAL OR WITNESS EVIDENCE EXISTS IS FAIL-CLOSED. So a manifest-only rollback and a witness-only rollback each FAIL LOUDLY. RECORDED RESIDUAL (R22): rolling back BOTH in one change resembles legitimate pre-018.011-T build state and is caught by DIFF AND REVIEW, NOT BY RUNTIME HISTORY - no claim is made that a stateless validator detects every coordinated rollback. (3) VALID STATES ARE EXHAUSTIVE - EXACTLY THREE: red => completed EMPTY and terminal false and witness absent; build => completed a NON-EMPTY, order-preserving, DEPENDENCY-CLOSED PROPER SUBSET of gateOrder and terminal false and witness absent; terminal => completed the EXACT eight-element ordered set and terminal true and a VALID WITNESS PRESENT. EVERY OTHER COMBINATION IS REJECTED, including red+terminal, red with a non-empty completed, build with an empty completed, build with all eight, build+terminal, terminal with a proper subset, terminal with terminal:false, terminal with the witness absent, a witness present alongside red or build, and any phase outside {red,build,terminal}. ZERO-SKIPS IS A DEFAULT-MODE CLAIM (red and terminal phases): with no -gatetask, all eight activate and none skips. TARGETED MODE LEGITIMATELY SKIPS THE SEVEN NON-SELECTED FUNCTIONS in every phase - those are SELECTOR skips, not activation skips - and its only obligation is that THE SELECTED TEST ACTUALLY EXECUTES. DEPENDENCY DIRECTION: backlogit stores edges as `dep add <item> <depends-on> --type blocks`, so a recorded edge reads DEPENDENT -> PREREQUISITE despite the 'blocks' label; gateDeps[X] is therefore X'S PREREQUISITES, and reading the label the other way inverts the graph. A1 additionally carries MP0 (gateOrder/gateDeps integrity: exactly the eight IDs in declared sequence, and each gateDeps row listing that task's PREREQUISITES). REV 12: A1 carries NO MP5 obligation (MP5 is RETIRED) and NO 'MP1 probe non-vacuity' obligation (MP1 is the manifest validator and HAS NO PROBES - that phrase was a stale attribution of MP5's retired probe check). SCOPE: the one-line manifest edit is the SHARED HARNESS-STATE COMPLETION RECORD, not a new skill domain and not a new deliverable; this task's size and complexity are UNCHANGED and it remains inside the 2-hour rule.
ACCEPTANCE CRITERIA:
AC-A1.1 14 fixtures exist; manifest keys are sorted and in bijection with the directory.
AC-A1.2 Every manifest verdict agrees with its fixture's filename prefix (directpush-reject-* / directpush-accept-* - the actual on-disk names), and both classes are non-empty: 9 reject, 5 accept. Static data check only.
AC-A1.3 markdownlint 'scripts/testdata/directpush/**' exits 0 (single leading H1 per fixture for MD041/MD025).
AC-A1.4 HARNESS-STATE LIFECYCLE OPENED (rev 11). H0's tests/integration/testdata/directpush-gate/harness-state.json is present and was initialized to exactly {"phase":"red","completed":[],"terminal":false}, and the recorded H0 evidence is the LITERAL 8/8 red - eight '--- FAIL:' lines, each carrying its own task-specific 'not implemented:' marker from the plan section 5.0.1 marker table - with ZERO DEFAULT-MODE ACTIVATION SKIPS and the TERMINAL WITNESS ABSENT (rev 12). On this task's completion, and ATOMICALLY with the fixture work above, the manifest performs the ONE-WAY red -> build transition and completed becomes exactly ["A1"]. The transition is one-way: build -> red is an impossible state and fails closed (MP1). The insertion does NOT by itself make this test pass - -gatetask=A1 evaluates AC-A1.1 through AC-A1.3 regardless of manifest state, so a manifest advanced ahead of the fixtures fails on the real assertion. A1 additionally carries MP0 (gateOrder has exactly the eight task IDs in declared sequence, and each gateDeps row lists that task's PREREQUISITES - the depends-on targets of the recorded edges). REV 12: A1 carries NO MP5 obligation - MP5 is RETIRED because its rule failed the DEFAULT SUITE on the legitimate post-018.010-T / pre-018.011-T state - and NO 'MP1 probe non-vacuity' obligation, because MP1 is the manifest validator and has no probes; that phrase was a stale attribution of MP5's retired check. The TERMINAL COMPLETION WITNESS is ABSENT at H0 and stays absent throughout build; a witness present at H0 is a FAIL-CLOSED error (MP6).

<!-- BEGIN:implementation-notes -->
DEGRADATION NOTE: this workspace's backlogit type schema defines 'size' but NO 'complexity' field on any artifact type (verified via backlogit metadata types: task fields = priority, size, status), despite backlog-registry.yaml declaring features.sizing: true. Complexity is therefore recorded below as enum-validated prose per the Stage two-axis sizing contract. Size is stored structurally (size_source=agent, size_ruleset_version=2h-rule-v1).

Size: S | Complexity: low

Rationale: Fixture authoring is mechanical once the shape list is fixed; volume is 15 small files.

HARNESS BINDING (rev 4, plan section 5.0): this task does NOT carry a harness-ready label. harness-architect applies that label at Ship Step 2 after producing the Go harness and confirming go vet ./... exits 0 and go test ./... is red. Stage does not forge P-004's postcondition.
<!-- END:implementation-notes -->

<!-- BEGIN:acceptance-criteria -->
- **AC-A1.1** 14 fixtures exist under `scripts/testdata/directpush/`; `directpush-manifest.json` keys are sorted and in exact bijection with the directory listing.
- **AC-A1.2** Every manifest verdict agrees with its fixture's filename prefix (`directpush-reject-*` -> `reject`, `directpush-accept-*` -> `accept` — the actual on-disk names, corrected at rev 8 from the truncated `reject-*` / `accept-*` form), and both classes are non-empty: 9 reject shapes and 5 accept shapes. Static data check; the executed non-vacuity proof for these assertions belongs to 018.005-T (AC-A2.4), which owns the fixture-suite driver.
- **AC-A1.3** `markdownlint "scripts/testdata/directpush/**"` exits 0 (single leading H1 per fixture for MD041/MD025).
- **AC-A1.4** **Harness-state lifecycle opened (rev 11).** H0's `tests/integration/testdata/directpush-gate/harness-state.json` is present and was initialized to exactly `{"phase":"red","completed":[],"terminal":false}`, and the recorded H0 evidence is the **literal 8/8** red — eight `--- FAIL:` lines, each carrying its own task-specific `not implemented:` marker from the §5.0.1 marker table, with **zero default-mode activation skips** and the **terminal witness absent** (rev 12). On this task's completion, and **atomically with** the fixture work above, the manifest performs the **one-way** `red`→`build` transition and `completed` becomes exactly `["A1"]`. The transition is one-way: `build`→`red` is an impossible state and fails closed (MP1). The insertion does **not** by itself make this test pass — `-gatetask=A1` evaluates AC-A1.1–AC-A1.3 regardless of manifest state, so a manifest advanced ahead of the fixtures fails on the real assertion. A1 additionally carries **MP0** (order and graph integrity: `gateOrder` is exactly the eight IDs in declared sequence, and each `gateDeps` row lists that task's **prerequisites** — the `depends-on` targets of the recorded edges). **Rev 12**: A1 carries **no** MP5 obligation (MP5 is **RETIRED** — it failed the default suite on the legitimate post-C1/pre-C2 state) and **no** "MP1 probe non-vacuity" obligation (MP1 is the manifest validator and has no probes; the phrase was a stale attribution of MP5's retired check). The **terminal completion witness** is **absent** at H0 and throughout `build` — a witness present at H0 is a fail-closed error (MP6). **Scope**: the one-line manifest edit is the shared harness-state completion record, not a second skill domain; this task's size and complexity are unchanged.
<!-- END:acceptance-criteria -->