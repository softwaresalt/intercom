---
artifact_type: task
created_at: 2026-09-11T17:23:16.5517109Z
custom_fields:
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 018.006-T
id: 018.007-T
labels:
    - sub-epic-b
    - governance
    - chore
parent_id: 018-F
priority: high
status: queued
title: Correct Orchestrator Step 1.5 authorization and branch discovery
updated_at: 2026-09-11T17:46:38.3923435Z
---

Parent feature 018-F. Sub-epic B (contract correction), plan sections 6.1, 6.1.1 and 6.1.2 of docs/plans/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-plan.md. Correct .github/agents/_orchestrator.agent.md on TWO axes. AXIS 1 - AUTHORIZATION (plan 6.1.1): (1) Reword the Step 1.5 PREAMBLE from 'are committed to the default branch and present on the remote' to the verification/postcondition form 'have reached the default branch via a merged staging PR and are present on the remote', and widen its scope clause to bind WHETHER OR NOT A SHIPMENT WAS FORMED. (2) Delete sub-step 3e entirely and fold its branch-point instruction ('from the current commit') into 3a, which also reuses the Stage-reported branch when one exists. AXIS 2 - DISCOVERY AND VERIFICATION (plan 6.1.2, rev 6): (3) Step 1 (Route to Stage) declares Stage's output as the STAGING HANDBACK RECORD - stage_branch, stage_head_commit, stage_outcome (shipment|no-shipment), stage_artifact_paths, plus shipment_id when formed - and step 4 records it, applying a TOTAL set of defaults so the degraded path NEVER HALTS: branch/commit resolve from git rev-parse --abbrev-ref HEAD / git rev-parse HEAD; stage_outcome derives from whether a shipment_id was returned; stage_artifact_paths defaults to the step-1 path set; STAGING_HANDBACK_DEGRADED recorded once and surfaced. If the HEAD resolution yields the default branch, leave stage_branch UNRESOLVED - NEVER substitute the default branch - and route to step 3a's create-arm, which is exactly the pre-change behaviour. (4) Add a RESOLUTION BLOCK immediately after the Step 1.5 preamble, ABOVE step 1's dirty/clean split: set STAGE_BRANCH from the handback; when resolved, git rev-parse --verify it (halt STAGING_GATE_FAIL: staging branch {stage_branch} does not exist) and assert git rev-parse --abbrev-ref HEAD equals it (halt STAGING_GATE_FAIL: worktree is on {head} but Stage reported {stage_branch}; this is also the P-016 single-worktree gate); when UNRESOLVED, record that and apply no branch assertion, leaving steps 1 and 2 to run unchanged so any work they find routes to step 3a's create-arm. It MUST sit above step 1 because step 1 routes a dirty tree straight to step 3, so guards placed in step 2 are skipped on the very path that mutates the repo. (5) Widen step 1's git status pathspec from .backlogit/ alone to stage_artifact_paths (default: .backlogit/ docs/plans/ docs/decisions/ docs/memory/). (6) Replace step 2's default-branch-only unpushed-commit range with git fetch origin main then git log origin/main..HEAD --oneline against the already-resolved staging branch. (7) Give step 4 a SECOND ARM keyed on stage_outcome: no-shipment that verifies git merge-base --is-ancestor {stage_head_commit} origin/main plus git show origin/main:{path} over a NON-EMPTY resolved path set (empty set = STAGING_GATE_FAIL, never a pass - an empty loop plus a trivially-true ancestry check would otherwise report success having verified nothing), halts with STAGING_GATE_FAIL otherwise, and terminates the gate without routing to Ship. Step 4's shipment arm keeps its pre-change lead sentence VERBATIM under an added arm-label line; no outer lead sentence may replace or reword it. CRITICAL - SELF-MATCH HAZARD (plan 6.1.2): state the step-2 prohibition DESCRIPTIVELY ('the default branch against its own remote-tracking ref'). Do NOT quote the literal forbidden range anywhere in Step 1.5 - this file IS in the scanned corpus, and a prohibition quoting it would self-match the AC-B1.6 absence assertion and vacate it (same failure class as docs/compound/2026-09-06-ci-self-matching-grep-and-actionlint-verification-gap.md). Preserve 3b-3d, the shipment arm of step 4 verbatim, the verified-on-origin broadcast, and Step 1.5's NON-NEGOTIABLE Orchestrator-owned status. Introduce NO direct default-branch push: every command added is a read form (status, rev-parse, fetch, log, show, merge-base, diff-tree, cat-file) or a checkout.
AXIS 2 ADDENDUM - CONCRETE ARTIFACT VERIFICATION (plan 6.1.2 corrections 4 and 5, rev 7): (8) stage_artifact_paths is REDEFINED as a NON-EMPTY set of CONCRETE repository-relative FILE paths carried by stage_head_commit. It is NEVER defaulted to a directory list; the step-4 default is to leave it empty and DERIVE it. Introduce STAGE_ARTIFACT_ROOTS as a FIXED CONSTANT of the gate (.backlogit/ docs/plans/ docs/decisions/ docs/memory/) serving exactly two roles - step 1's dirtiness pathspec and the containment allow-list for concrete paths - and pin step 1's pathspec to that CONSTANT rather than to the handback field, so a handback reporting one narrow path cannot shrink the dirtiness scan. (9) REWRITE the no-shipment arm of step 4 as FIVE ORDERED CHECKS, any failure halting: (i) git rev-parse --verify {stage_head_commit}^{commit}; (ii) git merge-base --is-ancestor {stage_head_commit} origin/main; (iii) derive DERIVED = git diff-tree --no-commit-id --name-only -r -z --diff-filter=d {stage_head_commit} filtered to STAGE_ARTIFACT_ROOTS, halting when EMPTY; (iv) resolve VERIFY_SET - a reported non-empty stage_artifact_paths must contain only file paths (no trailing slash, no bare root, no glob) under STAGE_ARTIFACT_ROOTS and must be EQUAL to DERIVED, else halt; otherwise VERIFY_SET := DERIVED; (v) for EVERY path in VERIFY_SET, git cat-file -t "origin/main:{path}" must exit 0 AND print exactly 'blob'. REMOVE git show origin/main:{path} from this arm entirely - git show EXITS 0 ON A TREE, so it cannot reject the directory prefixes rev 6 defaulted to, and those four directories already exist on origin/main in every repository, making the rev-6 loop a guaranteed pass that read no file the Stage run wrote. (10) In 3a, AFTER committing, RE-RECORD stage_head_commit as git rev-parse HEAD and DISCARD any reported stage_artifact_paths, because 3a creates the commit that carries the artifacts while the handback value was resolved before it existed; a no-op when step 3 was entered for unpushed commits alone. Preserve AC-B1.3's 'from the current commit' branch-point text verbatim. The handback-resolution NEVER-HALTS invariant is UNCHANGED: all new halts are in step 4 VERIFICATION, not in field defaulting.
HARNESS (P-002/P-004): harness function TestDirectPushGate_OrchestratorSurfaceClean in tests/integration/directpush_gate_test.go, produced by harness-architect at Ship Step 2 per plan section 5.0. harness_cmd: go test ./tests/integration -run TestDirectPushGate_OrchestratorSurfaceClean
ACCEPTANCE CRITERIA:
AC-B1.1 No direct-push-to-default-branch instruction remains anywhere in Step 1.5.
AC-B1.2 The preamble states the postcondition as reaching the default branch VIA A MERGED STAGING PR, and its scope clause binds whether or not a shipment was formed.
AC-B1.3 3a textually carries the branch point for the already-committed-but-unpushed path (textual, not structural - the a-e sub-steps are lazy paragraph continuations, not a real nested list).
AC-B1.4 Step 4's SHIPMENT ARM preserves verbatim the pre-change lead sentence (capitalization included), git show command, both outcome bullets, and STAGING_GATE_FAIL halt string, semantics unchanged; the step may gain arm-label lines and the no-shipment arm, and nothing else - no outer lead sentence replaces or rewords the preserved one.
AC-B1.5 Step 1 declares and step 4 records the staging handback record with a TOTAL set of defaults, so the degraded path NEVER halts; stage_artifact_paths defaults to EMPTY and is derived in step 4 from stage_head_commit's changed-file set, NEVER to a directory list (rev 7); a HEAD resolution yielding the default branch leaves stage_branch UNRESOLVED and routes to step 3a's create-arm, and the default branch is never substituted.
AC-B1.6 Step 1.5's unpushed-commit check is branch-specific: origin/main..HEAD - equivalently origin/main..$STAGE_BRANCH whenever a branch is resolved, and degrading to exactly the pre-change comparison when UNRESOLVED so neither population is skipped; the default-branch self-range literal is absent from all of Step 1.5, including the prohibition sentence, which is phrased descriptively.
AC-B1.7 Step 1's git status pathspec is the FIXED STAGE_ARTIFACT_ROOTS constant, not .backlogit/ alone and not the handback field (rev 7), so a narrow handback cannot shrink the dirtiness scan. Directory prefixes are correct here (step 1 runs before the artifact commit exists) and are rejected in step 4.
AC-B1.8 Step 4's no-shipment arm runs five ordered checks - rev-parse --verify of the commit, merge-base --is-ancestor against origin/main, derivation of a NON-EMPTY changed-file set, VERIFY_SET resolution per AC-B1.10, and git cat-file -t "origin/main:{path}" requiring exit 0 AND output exactly 'blob' for EVERY path - halting on any failure, terminating the gate without routing to Ship, and naming no shipment_id. git show origin/main:{path} no longer appears in this arm (rev 7): it exits 0 on a tree.
AC-B1.9 The resolution block sits ABOVE step 1's dirty/clean split so its verify and HEAD-equality guards run on the dirty path too; clean-tree and single-worktree gates preserved; P-009, P-011, P-016 textually unchanged.
AC-B1.10 CONCRETE-PATH CONTRACT (rev 7). stage_artifact_paths is a NON-EMPTY set of concrete repository-relative FILE paths carried by stage_head_commit. Step 4 halts on: a trailing-slash path, a bare STAGE_ARTIFACT_ROOTS entry, or a glob (not a file path); a path outside STAGE_ARTIFACT_ROOTS; an EMPTY resolved set; a reported set not EQUAL to the commit's derived changed-file set (equality, not subset - a subset check accepts both an invented path and a silently shrunken set); and any path resolving on origin/main to an object type other than blob. No directory prefix can satisfy completion, by construction: diff-tree -r emits blob paths only and cat-file -t must print exactly blob.
AC-B1.11 HANDBACK COMMIT RE-RECORDED (rev 7). 3a sets stage_head_commit to git rev-parse HEAD after committing and discards any reported stage_artifact_paths, so step 4 derives from the commit that actually carries the artifacts rather than the pre-commit HEAD; a no-op when step 3 was entered for unpushed commits alone. AC-B1.3's 'from the current commit' text is preserved verbatim.

<!-- BEGIN:implementation-notes -->
DEGRADATION NOTE: this workspace's backlogit type schema defines 'size' but NO 'complexity' field on any artifact type (verified via backlogit metadata types: task fields = priority, size, status), despite backlog-registry.yaml declaring features.sizing: true. Complexity is therefore recorded below as enum-validated prose per the Stage two-axis sizing contract. Size is stored structurally (size_source=agent, size_ruleset_version=2h-rule-v1).

Size: M | Complexity: medium

Rationale: Six edits in ONE file, single domain (contract prose), with the exact replacement text fully specified in plan sections 6.1.1 and 6.1.2 - so volume grew (S->M) but uncertainty did not. Well inside the 2-hour rule. The load-bearing details are that step 4's shipment arm stays verbatim, and that the step-2 prohibition must NOT quote the literal forbidden range.

REV-6 RECONCILIATION (PR #54 thread PRRT_kwDOTPuhps6hqWDe, comment 3993998429): revisions 1-5 corrected what Step 1.5 AUTHORIZES but never its DISCOVERY logic, which inspects only .backlogit/ dirtiness and a default-branch-only commit range. Once 018.008-T/018.009-T put Stage's commit on chore/stage-{chore-slug} and leave the worktree there, both inputs read 'synchronized', the gate skips its commit/push/PR step, and then fails the origin/main manifest check - and a shipment-less run has no manifest to check at all, so the documented no-shipment route could not complete. This task gains corrections 3-6 and AC-B1.5..AC-B1.9; AC-B1.2 is extended and AC-B1.4 is NARROWED from whole-block byte-identity to shipment-arm verbatim preservation, because whole-block identity is unsatisfiable once step 4 must carry the no-shipment arm. Plan section 6.1 was extended with the same text in the same change, so the plan<->task same-contract invariant holds. NO new task, file, fixture, harness function, or dependency edge; shipment 017-S membership and order unchanged.

REV-7 RECONCILIATION (PR #54 thread PRRT_kwDOTPuhps6hrC0c, comment 3994280334, plan L626): rev 6's no-shipment arm iterated git show origin/main:{path} over a path set whose documented default was the DIRECTORY LIST .backlogit/ docs/plans/ docs/decisions/ docs/memory/. git show EXITS 0 ON A TREE (verified: git show origin/main:docs/plans/ prints 'tree origin/main:docs/plans/' with status 0), and those four directories exist on origin/main in every repository the gate will run in - so the loop passed having read NO file the Stage run wrote. Paired with the degraded stage_head_commit default (git rev-parse HEAD, routinely already an ancestor of origin/main) the ancestry check was trivially true too, and the arm reported a FALSE PASS. Rev 6 removed the zero-iteration loop and left the always-satisfied one. This task gains rev-7 corrections (8)-(10) and AC-B1.10/AC-B1.11; AC-B1.5, AC-B1.7 and AC-B1.8 are STRENGTHENED, never relaxed. Plan section 6.1 was extended with identical text in the same change, so the plan<->task same-contract invariant holds. NO new task, file, fixture, harness function, or dependency edge; shipment 017-S membership and order unchanged. SIZE UNCHANGED (M): still six-plus edits in ONE file, single domain (contract prose), with replacement text fully specified in plan section 6.1.2; volume grew inside the existing boundary and uncertainty did not.

HARNESS BINDING (rev 4, plan section 5.0): this task does NOT carry a harness-ready label. harness-architect applies that label at Ship Step 2 after producing the Go harness and confirming go vet ./... exits 0 and go test ./... is red. Stage does not forge P-004's postcondition.
<!-- END:implementation-notes -->

<!-- BEGIN:acceptance-criteria -->
- **AC-B1.1** No direct-push-to-default-branch instruction remains anywhere in Step 1.5.
- **AC-B1.2** The preamble states the postcondition as artifacts having **reached** the default branch **via a merged staging PR** (rev 3's fourth authorization surface), and its scope clause binds **whether or not a shipment was formed** (rev 6).
- **AC-B1.3** 3a textually carries the branch point (`from the current commit`) for the already-committed-but-unpushed path — textual, not structural, because the a–e sub-steps are lazy paragraph continuations, not a real nested list.
- **AC-B1.4** Step 4's **shipment arm** preserves, verbatim, the pre-change lead sentence (`Verify the shipment manifest exists on the remote default branch:`, capitalization included), the `git show origin/main:.backlogit/queue/{shipment_id}.md` command, both outcome bullets, and the `STAGING_GATE_FAIL: shipment manifest {shipment_id} not found on origin/main` halt string, with pass/fail semantics unchanged. **Rev 6 scope refinement**: this criterion previously required the whole step-4 block to be byte-identical, which is no longer satisfiable because step 4 must gain the `no-shipment` arm (AC-B1.8) — and an unmodified block is precisely what leaves that path unverified. It is narrowed to what it always protected: the authoritative post-merge gate is preserved and unrelaxed. The step may gain **outcome-arm label lines** and the second arm, **and nothing else** — no outer lead sentence replaces or rewords the preserved one.
- **AC-B1.5** **Staging handback recorded, with total defaults.** Step 1 declares and step 4 records `stage_branch`, `stage_head_commit`, `stage_outcome`, and `stage_artifact_paths`. **Every** field has a deterministic default, so the degraded path **never halts**: branch/commit resolve from `HEAD`; `stage_outcome` derives from whether a `shipment_id` was returned; `stage_artifact_paths` is left **empty** and derived in step 4 from `stage_head_commit`'s own changed-file set (rev 7 — it is **never** defaulted to a directory list); `STAGING_HANDBACK_DEGRADED` is recorded once and surfaced. When the `HEAD` resolution yields the default branch, `stage_branch` is left **UNRESOLVED** and the run routes to step 3a's create-arm. The default branch is **never** substituted for the Stage artifact branch.
- **AC-B1.6** **Branch-specific discovery.** Step 1.5's unpushed-commit check compares `origin/main..HEAD` — equivalently `origin/main..$STAGE_BRANCH` whenever a branch is resolved, proven equal by the `git rev-parse --abbrev-ref HEAD` assertion in the resolution block, and degrading to exactly the pre-change comparison when it is UNRESOLVED, so neither population is skipped. The literal default-branch self-range is **absent from the whole of Step 1.5**, including from the prohibition sentence, which is phrased descriptively so the absence assertion cannot self-match.
- **AC-B1.7** **Dirtiness pathspec covers Stage artifacts.** Step 1's `git status` pathspec is the **fixed** `STAGE_ARTIFACT_ROOTS` constant — `.backlogit/ docs/plans/ docs/decisions/ docs/memory/`, not `.backlogit/` alone — so a no-shipment run that writes only `docs/**` is not reported clean. **Rev 7**: the pathspec is that constant and **not** the handback's `stage_artifact_paths`, so a handback reporting one narrow path cannot shrink the dirtiness scan and hide uncommitted artifacts. Directory prefixes are correct *here* — step 1 runs before the artifact commit exists, so there is no commit to derive files from — and are rejected in step 4, where a commit does exist.
- **AC-B1.8** **No-shipment verification arm.** Step 4 carries a second arm keyed on `stage_outcome: no-shipment` that runs five ordered checks and halts with `STAGING_GATE_FAIL` on any failure: (1) `git rev-parse --verify {stage_head_commit}^{commit}`; (2) `git merge-base --is-ancestor {stage_head_commit} origin/main`; (3) derivation of `DERIVED` from `git diff-tree --no-commit-id --name-only -r -z --diff-filter=d {stage_head_commit}` filtered to `STAGE_ARTIFACT_ROOTS`, halting when it is **empty**; (4) resolution of `VERIFY_SET` per AC-B1.10; (5) `git cat-file -t "origin/main:{path}"` for **every** path in `VERIFY_SET`, requiring exit 0 **and** output exactly `blob`. It terminates the gate without routing to Ship and names no `shipment_id`. **Rev 7**: `git show origin/main:{path}` no longer appears in this arm — it exits 0 on a tree, so it could not reject the directory prefixes rev 6 defaulted to.
- **AC-B1.9** **Clean-tree and single-worktree gates preserved on every path.** The branch-resolution block sits **above** step 1's dirty/clean split, so `git rev-parse --verify` and the HEAD-equals-`$STAGE_BRANCH` assertion execute on the dirty path too — 3a never checks out an unverified branch name. Step 1.5 halts when HEAD is not the resolved staging branch; no new branch or worktree is created when the handback reports one; and P-009, P-011, and P-016 remain textually unchanged.
- **AC-B1.10** **Concrete-path contract (rev 7).** `stage_artifact_paths` is defined as a **non-empty set of concrete repository-relative file paths carried by `stage_head_commit`**. Step 4 rejects, by halting: a path with a trailing `/`, a bare `STAGE_ARTIFACT_ROOTS` entry, or any glob/wildcard form (not a file path); a path not under `STAGE_ARTIFACT_ROOTS` (outside the allowed Stage artifact roots); an **empty** resolved set; any reported set that is not **equal** to the commit's derived changed-file set (equality, not subset — a subset check would accept both an invented path and a silently shrunken set); and any path that resolves on `origin/main` to an object type other than `blob`. No directory prefix can satisfy completion on this arm, by construction: `diff-tree -r` emits blob paths only, and `cat-file -t` must print exactly `blob`.
- **AC-B1.11** **Handback commit re-recorded after step 3 commits (rev 7).** 3a sets `stage_head_commit` to `git rev-parse HEAD` after committing and discards any reported `stage_artifact_paths`, so step 4 derives its file set from the commit that actually carries the artifacts rather than from the pre-commit `HEAD` the handback resolved. The re-record is a no-op when step 3 was entered for unpushed commits alone. AC-B1.3's `from the current commit` branch-point text is preserved verbatim.
<!-- END:acceptance-criteria -->