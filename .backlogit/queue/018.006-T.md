---
artifact_type: task
created_at: 2026-09-11T17:22:52.0346224Z
custom_fields:
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 018.005-T
id: 018.006-T
labels:
    - sub-epic-a
    - test-harness
    - chore
parent_id: 018-F
priority: high
status: queued
title: Implement structure-aware direct-push detector
updated_at: 2026-09-11T17:46:17.9631319Z
---

Parent feature 018-F. Sub-epic A, plan sections 5.6 and 5.3 of docs/plans/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-plan.md. Replace the stub with the real detector: block normalization covering LAZY PARAGRAPH CONTINUATIONS (the shape Step 1.5 actually uses); PER-CONSTRUCT verdicts, never block-level; two violation shapes (target-bound push; permissive commit-on-default authorization, excluding verificational/postcondition phrasing); default-branch token alternation including the literal {{DEFAULT_BRANCH}}; CELL-scoped table matching; structural prohibition resolution via bold lead-in line, Forbidden/MUST NOT column header, ATX heading, or a marker that PRECEDES the construct ('rather than'/'instead of' excluded); read/checkout verb exclusions including pull and checkout; and GUARDED default-branch resolution that cannot abort under set -euo pipefail.
ACCEPTANCE CRITERIA:
AC-A3.1 --self-test reports every fixture by name with actual verdict == manifest verdict.
AC-A3.2 Guarded default-branch resolution is asserted for BOTH the resolution-failure fallback to main AND a non-main configured default branch.
AC-A3.3 Bare invocation exits 1 and names all four authorization surfaces, identified by file and matched construct TEXT, never by line ordinal.
AC-A3.4 The first full-corpus scan output (resolved corpus file count plus the complete enumerated finding list) is recorded on this task BEFORE sub-epic B begins - risk R9, an undiscovered fifth surface must surface here, not at C2.
AC-A3.5 A ::notice:: names the resolved mode on every invocation.

<!-- BEGIN:implementation-notes -->
DEGRADATION NOTE: this workspace's backlogit type schema defines 'size' but NO 'complexity' field on any artifact type (verified via backlogit metadata types: task fields = priority, size, status), despite backlog-registry.yaml declaring features.sizing: true. Complexity is therefore recorded below as enum-validated prose per the Stage two-axis sizing contract. Size is stored structurally (size_source=agent, size_ruleset_version=2h-rule-v1).

Size: M | Complexity: high

Rationale: HIGHEST-RISK TASK. Per-construct verdicts, lazy-continuation normalization, cell-scoped table matching, structural prohibition resolution, and the set -euo pipefail resolution guard are each independently subtle, and the self-matching-grep failure mode is the specific prior art this task must not repeat. De-risked by the A1 fixture corpus landing first and by A2 proving the harness red.
<!-- END:implementation-notes -->

<!-- BEGIN:acceptance-criteria -->
- **AC-A3.1** `--self-test` reports every fixture by name with actual verdict == manifest verdict.
- **AC-A3.2** Guarded default-branch resolution is asserted for BOTH the resolution-failure fallback to `main` AND a non-`main` configured default branch.
- **AC-A3.3** Bare invocation exits 1 and names all FOUR authorization surfaces, identified by file and matched construct TEXT, never by line ordinal.
- **AC-A3.4** The first full-corpus scan output (resolved corpus file count plus the complete enumerated finding list) is recorded on this task BEFORE sub-epic B begins — risk R9: an undiscovered fifth surface must surface here, not at C2.
- **AC-A3.5** A `::notice::` names the resolved mode on every invocation.
<!-- END:acceptance-criteria -->