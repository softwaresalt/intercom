---
artifact_type: subtask
created_at: 2026-09-03T17:38:17.0157111Z
dependencies:
    - 001.001.002-ST
    - 001.001.003-ST
id: 001.001.004-ST
labels:
    - go
    - tooling
parent_id: 001.001-T
priority: high
references:
    - docs/plans/2026-09-03-intercom-go-foundation-plan.md
status: queued
title: Add canonical cross-compile build script
updated_at: 2026-09-03T17:39:30.3383225Z
---

Size: M | Complexity: medium (size_source: agent; ruleset: 2h-rule-v1)

Create scripts/build.ps1 and scripts/targets.json. targets.json is the SINGLE SOURCE OF TRUTH for the target matrix (linux-amd64, windows-amd64, darwin-amd64, darwin-arm64) and is read by both this script and the CI matrix job, so local and CI builds cannot drift. Build both binaries with CGO_ENABLED=0 into dist/. Files (2): scripts/build.ps1, scripts/targets.json. Posture: test-first. CONSTRAINT (invariant I5): set \Continue='Stop'; resolve dist/ from the script's own location; REFUSE to operate if the resolved output path is not a descendant of the repo root; create/overwrite only - NO RECURSIVE DELETE (route any cleanup through 'go clean' scoped to the module). Acceptance: (1) produces 8 artifacts (2 binaries x 4 targets); (2) 'go version -m' on a produced artifact reports CGO_ENABLED=0; (3) NEGATIVE TEST - the script exits non-zero when the resolved output path is forced outside the repo root.