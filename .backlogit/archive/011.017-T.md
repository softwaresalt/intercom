---
artifact_type: task
commit: e267848
created_at: 2026-09-06T23:59:56.664373Z
custom_fields:
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 011.018-T
id: 011.017-T
parent_id: 011-F
priority: high
status: done
title: 'F: Restore start.ps1 secret and exit-code handling with no-echo enforced'
updated_at: 2026-09-07T01:06:15.7882406Z
---

Partially resolves 9F9A3CB2 (include-list item 2). STOWAWAY - droppable. Restores GITHUB_PERSONAL_ACCESS_TOKEN resolution, the trailing exit propagating the Copilot exit code, and widens the .env.local key regex from [A-Z_][A-Z0-9_]* back to [A-Za-z_][A-Za-z0-9_]*. CREDENTIAL CONTROLS REQUIRED: this unit broadens what secret material is loaded into the process environment IN THE SAME CYCLE in which EF9352FB documents a live plaintext Tavily key in .env.local as an OPEN, TIME-SENSITIVE exposure. Approach: test-first. Width: PowerShell. AC1 NO LOADED VALUE IS EVER ECHOED, LOGGED, OR WRITTEN TO A TRANSCRIPT - asserted, covering the restored GITHUB_PERSONAL_ACCESS_TOKEN path. AC2 the regex widening is JUSTIFIED AGAINST A CONCRETE KEY SET; if no lowercase/mixed-case key is actually required, the narrower regex is RETAINED. AC3 exit-code propagation asserted concretely (a non-zero Copilot exit produces a non-zero launcher exit). AC4 verification runs against a FIXTURE SCRIPT ROOT or with .env.local absent; the step must not capture or echo the process environment. AC5 .env.local is verified ignored and unstaged. NO SECRET VALUE IS READ, PRINTED, OR COMMITTED. AC6 the restored token is passed BY ENVIRONMENT ONLY and never as a child-process argument (argv is readable by same-user processes and captured by PowerShell transcription). AC7 the existing NO-OVERWRITE precedence is pinned - start.ps1 sets a variable only when unset, so a restoration that overwrites an already-present ambient token would silently escalate token scope for the whole session. AC8 drop-protocol control (Constitution VII): backup verified plus operator approval before any drop-and-return.

<!-- BEGIN:description -->
Partially resolves 9F9A3CB2 (include-list item 2). STOWAWAY - droppable. Restores GITHUB_PERSONAL_ACCESS_TOKEN resolution, the trailing exit propagating the Copilot exit code, and widens the .env.local key regex from [A-Z_][A-Z0-9_]* back to [A-Za-z_][A-Za-z0-9_]*. CREDENTIAL CONTROLS REQUIRED: this unit broadens what secret material is loaded into the process environment IN THE SAME CYCLE in which EF9352FB documents a live plaintext Tavily key in .env.local as an OPEN, TIME-SENSITIVE exposure. Approach: test-first. Width: PowerShell. AC1 NO LOADED VALUE IS EVER ECHOED, LOGGED, OR WRITTEN TO A TRANSCRIPT - asserted, covering the restored GITHUB_PERSONAL_ACCESS_TOKEN path. AC2 the regex widening is JUSTIFIED AGAINST A CONCRETE KEY SET; if no lowercase/mixed-case key is actually required, the narrower regex is RETAINED. AC3 exit-code propagation asserted concretely (a non-zero Copilot exit produces a non-zero launcher exit). AC4 verification runs against a FIXTURE SCRIPT ROOT or with .env.local absent; the step must not capture or echo the process environment. AC5 .env.local is verified ignored and unstaged. NO SECRET VALUE IS READ, PRINTED, OR COMMITTED. AC6 the restored token is passed BY ENVIRONMENT ONLY and never as a child-process argument (argv is readable by same-user processes and captured by PowerShell transcription). AC7 the existing NO-OVERWRITE precedence is pinned - start.ps1 sets a variable only when unset, so a restoration that overwrites an already-present ambient token would silently escalate token scope for the whole session. AC8 drop-protocol control (Constitution VII): backup verified plus operator approval before any drop-and-return.

SIZING (two independent axes): Size: M | Complexity: medium. NOTE: 'size' is stored as a structured field; 'complexity' is recorded as prose because this workspace's WIT does not define a complexity field for artifact type 'task' (backlogit validation error) - a documented, flagged degradation, not an omission.
<!-- END:description -->