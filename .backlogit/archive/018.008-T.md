---
artifact_type: task
created_at: 2026-09-11T17:23:18.6085075Z
custom_fields:
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
id: 018.008-T
labels:
    - policy
    - chore
    - governance
    - reduced-scope
    - harness-ready
links:
    - target_id: 019.007-T
      link_type: related_to
parent_id: 018-F
priority: high
status: done
title: Gate Stage artifact mutation behind a dedicated branch
updated_at: 2026-09-18T02:41:29.0541718Z
---

Parent feature 018-F. SINGLE executable task of the reduced release unit (2026-09-12 re-plan, remediated rev 18). Closes the PROHIBITION half of the Stage default-branch policy gap: Stage must not commit or push on the default branch, and a dedicated Stage artifact branch must exist and be verified before any tracked artifact write. Absorbs the current-scope items of the retired sub-epic-B split: the P-010 correction formerly scoped here, plus items (1)(2)(3) and (7a) of the retired 018.009-T.

SURFACE 1 - .github/policies/workflow-policies.md (P-010). Replace the Stage MAY bullet phrase "on the default branch or a dedicated chore/admin branch" with a dedicated-branch-only grant. Add an ACTOR-NAMED Stage MUST NOT bullet forbidding direct commit or push to the default branch. LOCATOR (EXACT, NOT ORDINAL): the Ship mirror bullet to match is the Markdown line whose exact text is
- Commit or push directly to `main`
with `main` in Markdown backticks. Locate it by that quoted text. NEVER by line ordinal. If an exact match is not found, the executor MUST fall back to the documented normalized locator - the unique Markdown list item under the "**Ship MUST NOT**:" heading whose text, after stripping Markdown backticks and collapsing internal whitespace, equals "Commit or push directly to main" - and MUST HALT if that normalized match is also absent or non-unique rather than guessing a position.
Add an explicit Stage MAY grant to create and check out chore/stage-{scope-slug}. Add the clarifier that a Stage artifact branch/PR carries no source, test or config change and is therefore neither an implementation branch under P-016 nor a release unit under P-001. DO NOT add any clarifier that grants, implies or restates Orchestrator authority to commit Stage artifacts or push the default branch: the installed Orchestrator Step 1.5 item 3 contains a direct-main push attempt (3e) and a commit action (3a) that contradict the Orchestrator's own orchestration-only Role statement, and repairing that contradiction is deferred to 019.004-T. P-010's Orchestrator statement is left UNCHANGED. Append Amendment Log entry 1.25.0.
P-016 PRESERVATION (NON-NEGOTIABLE): the P-016 paragraph beginning "**Allowed exception (Stage spike/research only)**" MUST remain byte-for-byte identical. The word "only" introduced by the new commit grant constrains WHICH BRANCH Stage may commit on; it MUST NOT be drafted in any way that narrows, qualifies, supersedes or deletes that exception. Any edit that touches the exception paragraph fails this task.

SURFACE 2 - .github/agents/_stage.agent.md Role Boundary. Narrow the Git row Allowed cell from "Commit backlog/planning artifacts on default or admin branch" to the dedicated Stage artifact branch only, and in the SAME cell add the create/checkout grant for chore/stage-{scope-slug}. The existing spike/research worktree clause in that same cell is PRESERVED. The Git row Forbidden cell is unchanged. Add an actor note on the PR row recording that the OPERATOR - never Stage - pushes the Stage artifact branch and opens/approves the merge-commit staging PR, and that no Orchestrator authority to do so is granted or assumed by this release unit (see 019.004-T). The PR row prohibition itself is UNWEAKENED.

SURFACE 3 - .github/agents/_stage.agent.md Step 1.9. This is an OPERATIVE MANDATORY FAIL-CLOSED GATE, not prose that merely sits before Step 2. It has TWO parts and BOTH are required:

(3a) REGISTRATION - EXACT ANCHORS, NEVER LINE ORDINALS. Insert one new checklist line into the single fenced "text" block that follows the heading "## Step Sequence Contract (NON-NEGOTIABLE)". The insertion point is located by TWO EXACT ANCHOR LINES, quoted here byte-for-byte as they exist in the installed file (both separators are EM DASH U+2014, bytes E2 80 94; note the THREE ASCII spaces after "Step 2"):
  ANCHOR-A: [ ] Step 1.8 — Learnings retrieval
  ANCHOR-B: [ ] Step 2   — Deliberation
The new line is inserted strictly between them and reads (one ASCII space after "1.9", EM DASH U+2014, one ASCII space either side of it):
  [ ] Step 1.9 — Stage artifact branch gate (fail-closed)
NORMALIZED FALLBACK LOCATOR. If an exact byte match for either anchor is absent, fall back to the normalized locator: inside that same fenced block, the unique line whose text, after (1) stripping a leading "[ ] " or "[x] " marker, (2) replacing every run of EM DASH (U+2014), EN DASH (U+2013) or ASCII hyphen with a single ASCII "-", and (3) collapsing internal whitespace runs to one ASCII space, equals exactly "Step 1.8 - Learnings retrieval" (for ANCHOR-A) or "Step 2 - Deliberation" (for ANCHOR-B).
FAIL ON ZERO OR MULTIPLE. If EITHER locator (exact or normalized) yields ZERO matches or MORE THAN ONE match, the executor MUST HALT with STAGE_BRANCH_GATE_ANCHOR_FAIL and record a P-005 event. It MUST NOT insert by line ordinal, MUST NOT guess a position, and MUST NOT insert into a second fenced block. Without this registration the gate is not covered by the existing rule that the summary is blocked until every applicable step is complete, and a skipped gate would not be a P-005 violation.

(3b) SECTION. Insert a section titled "Step 1.9: Stage Artifact Branch Gate (NON-NEGOTIABLE)" strictly after Step 1.8 (Learnings Retrieval) and strictly before Step 2 (Deliberation). That fixed position is the first point at which a stable scope slug is derivable for both intake shapes. The section MUST specify all of (i)-(vii), in this order:

  (i) WORKTREE TOPOLOGY PRECHECK (P-016) - RUNS FIRST, BEFORE ANY BRANCH CREATE, SELECT OR RESUME. Run "git worktree list --porcelain" and classify EVERY attached worktree into EXACTLY ONE of three classes: (1) the CURRENT worktree; (2) an EXPLICIT, TIME-BOXED STAGE SPIKE/RESEARCH worktree whose spike context is recorded for this session, per the P-016 paragraph beginning "**Allowed exception (Stage spike/research only)**"; (3) PROHIBITED/AMBIGUOUS. Class (2) is PERMITTED and PASSES the gate - this precheck MUST NOT narrow, qualify or supersede the P-016 exception in any way. Any worktree falling in class (3), and any worktree that cannot be positively classified into (1) or (2), HALTS the gate with STAGE_BRANCH_GATE_FAIL and records a P-016/P-005 violation, BEFORE any branch operation and BEFORE any tracked mutation.

  (ii) DETERMINISTIC SLUG DERIVATION - EXACT SOURCE FIELDS. The slug MUST resolve BEFORE the first tracked mutation, from exactly one of these named sources:
    - FEATURE-SHAPED intake: the "title" field of the Step 1 classified feature/epic/chore backlog artifact; when no backlog artifact exists yet, the verbatim stash-entry title text that served as the Step 1 classification subject.
    - TASK-SHAPED intake: the "Proposed covering feature title" string of the Step 1.5 grouping proposal the operator SELECTED.
    - SHIPMENT-SHAPED (OPTIONAL, NEVER REQUIRED): when a shipment ID is already known at Step 1.9 - resumption only - the "title" field of that shipment record. A shipment ID MAY be used; it is NEVER required, because Step 5.5 creates the shipment as the session's LAST mutation.
  NORMALIZATION - APPLIED IN THIS EXACT ORDER: (1) NFC-normalize the source string; (2) lowercase by ASCII case folding only (A-Z to a-z; no locale-dependent folding); (3) replace every maximal run of characters outside [a-z0-9] with a single ASCII "-"; (4) trim leading and trailing "-"; (5) TRUNCATE to at most 48 bytes, then trim any trailing "-" produced by the truncation. The 48-byte bound is chosen so that "chore/stage-" (12 bytes) plus the slug never exceeds the workspace's configured max_slug_length of 60 (.backlogit/config.yaml).
  EMPTY FALLBACK: if the normalized result is the EMPTY STRING (empty source, or a source containing no [a-z0-9] character after folding), use the literal slug "stage-session". The gate MUST NOT halt on an empty slug and MUST NOT invent a slug from any other source.
  The expected branch name is therefore exactly "chore/stage-{scope-slug}".

  (iii) BASE REF RESOLUTION - NEVER ASSUMED. Resolve the configured default branch as base_ref: run "git symbolic-ref --short refs/remotes/origin/HEAD" and strip the leading "origin/"; if that fails, run "git rev-parse --abbrev-ref origin/HEAD" and strip the leading "origin/". If BOTH fail, HALT with STAGE_BRANCH_GATE_FAIL. The gate MUST NOT assume the literal name "main".

  (iv) CREATE-OR-SELECT/RESUME, WITH AN OWNERSHIP AND BASE-IDENTITY DISCRIMINATOR. Evaluate exactly one case:
    (a) The branch does NOT exist ("git rev-parse --verify --quiet refs/heads/{expected_branch}" exits non-zero): require a clean worktree ("git status --porcelain=v1" produces NO output), then CREATE and check it out from "refs/remotes/origin/{base_ref}". This is the ownership-establishing case; no discriminator applies.
    (b) The branch IS already the checked-out branch: SELECT it and proceed, but ONLY after the discriminator below passes.
    (c) The branch EXISTS but is NOT checked out: require a clean worktree, check it out, and RESUME, but ONLY after the discriminator below passes.
  OWNERSHIP / BASE-IDENTITY / COLLISION DISCRIMINATOR (cases (b) and (c) ONLY). A pre-existing branch of the expected name is a RESUMPTION only when it is provably THIS pipeline's Stage artifact branch. BOTH conditions MUST hold:
    1. BASE IDENTITY: "git merge-base refs/heads/{expected_branch} refs/remotes/origin/{base_ref}" MUST succeed and return a non-empty commit SHA. An unrelated-histories failure (or an empty result) means the branch does not share ancestry with the configured default branch.
    2. CONTENT OWNERSHIP: "git diff --name-only refs/remotes/origin/{base_ref}...refs/heads/{expected_branch}" MUST be EMPTY or list ONLY paths under the four Stage artifact roots ".backlogit/", "docs/plans/", "docs/decisions/", "docs/memory/". A SINGLE path outside those roots proves the branch is not a Stage artifact branch.
  If EITHER condition fails, the name match is a COLLISION, NOT a resumption: HALT with STAGE_BRANCH_GATE_FAIL, record a P-005 event, perform NO tracked mutation, and do NOT create, rename, delete, force-update or otherwise reuse the colliding branch. Branch resume is permitted ONLY when identity matches; otherwise the gate halts.

  (v) SYMBOLIC HEAD EQUALITY. After (iv) completes, "git symbolic-ref --short HEAD" MUST equal the expected "chore/stage-{scope-slug}" exactly. A detached HEAD (non-zero exit from that command) FAILS the gate.

  (vi) DEFAULT-BRANCH INEQUALITY. The expected branch MUST differ from base_ref as resolved in (iii); equality FAILS the gate. This assertion is retained even though the "chore/stage-" prefix makes equality structurally unreachable today, so that a future prefix change cannot silently defeat the gate.

  (vii) NAMED FAIL-CLOSED HALT AND CATEGORICAL DEFERRAL RULE. On any failure of (i)-(vi) the gate halts with the named token STAGE_BRANCH_GATE_FAIL (or STAGE_BRANCH_GATE_ANCHOR_FAIL for the (3a) locator failure), records a P-005 policy-violation event, and performs NO tracked mutation. The gate precedes EVERY tracked Stage artifact mutation of the session WITHOUT EXCEPTION, and any such mutation otherwise scheduled earlier is DEFERRED until after the gate passes. The rule is CATEGORICAL; the following are ILLUSTRATIVE, not a closed list: (1) the WRITE half of the Step 1 late-identifier reconciliation (its search/analysis half is read-only and stays at Step 1), and (2) the ARCHIVAL half of the Step 1 unconditional duplicate-detection scan (its detection half is read-only and stays at Step 1).

WHY SURFACE 3 IS LOAD-BEARING: surfaces 1-2 establish what Stage MAY do; neither is an instruction to DO it. The installed _stage.agent.md contains NO branch operation and NO commit operation at any step, so an executor satisfying only surfaces 1-2 can finish an entire session on the default branch. Surface 3 is what makes the grant operative, and (3a) is what makes it enforced.

SCOPE LIMIT - STAGE COMMIT PRODUCTION IS NOT IN THIS TASK. This task adds NO commit step. Stage ends its session with bounded tracked artifacts UNCOMMITTED on the Stage branch, and the pipeline then PAUSES at an OPERATOR-ONLY checkpoint. The commit step is 019.009-T, the handback record is 019.007-T, the no-shipment terminal is 019.008-T, and Orchestrator branch discovery is 019.004-T - all deferred, all outside every live shipment. The interim terminal and the OPERATOR-ONLY completion sequence are recorded in 018-F goals and in section R16.5 of the governing plan. This task MUST NOT be drafted so as to assert, imply or create any Orchestrator authority to commit Stage artifacts or to push the default branch.

OUT OF SCOPE - DEFERRED: everything in 019-F and 020-F. This task carries NONE of that machinery.

<!-- BEGIN:acceptance-criteria -->
- AC-1: P-010's Stage MAY commit bullet grants artifact commit ONLY on a dedicated chore/stage-* branch; no default-branch commit grant remains anywhere in P-010.
- AC-2: P-010 carries an actor-named Stage MUST NOT bullet forbidding direct commit or push to the default branch, placed symmetrically with the Ship bullet located by its exact Markdown text "- Commit or push directly to `main`" (or, if absent, by the documented normalized locator, halting if that is also absent or non-unique); and P-010 carries an explicit Stage MAY grant to create and check out chore/stage-{scope-slug}.
- AC-3: P-010 carries the P-016 / P-001 clarifying sentence for the Stage artifact branch/PR, adds NO clarifier granting or restating Orchestrator authority to commit Stage artifacts or push the default branch, leaves P-010's Orchestrator statement unchanged, and the Amendment Log contains a 1.25.0 entry describing this change.
- AC-4: P-016's paragraph beginning "**Allowed exception (Stage spike/research only)**" is byte-for-byte unchanged by this task, verified by diff; no new "only" wording narrows, qualifies or deletes it.
- AC-5: _stage.agent.md Role Boundary Git row Allowed cell names ONLY the dedicated Stage artifact branch, includes the create/checkout grant, and retains the existing spike/research worktree clause; the Git row Forbidden cell is unchanged; the PR row actor note names the OPERATOR (never Stage) as the actor who pushes the Stage artifact branch and opens/approves the merge-commit staging PR, states that no Orchestrator authority is granted or assumed by this release unit, and the PR row prohibition is unweakened.
- AC-6: The "Step Sequence Contract (NON-NEGOTIABLE)" fenced checklist contains the line "[ ] Step 1.9 — Stage artifact branch gate (fail-closed)" (EM DASH U+2014) positioned strictly between the byte-exact anchor "[ ] Step 1.8 — Learnings retrieval" and the byte-exact anchor "[ ] Step 2   — Deliberation"; the anchors were located by exact text or the documented normalized locator, and a zero-match or multi-match on either anchor halted the task with STAGE_BRANCH_GATE_ANCHOR_FAIL rather than an ordinal insertion.
- AC-7: _stage.agent.md contains a "Step 1.9: Stage Artifact Branch Gate (NON-NEGOTIABLE)" section positioned strictly after Step 1.8 and strictly before Step 2.
- AC-8: That section specifies the P-016 worktree topology precheck FIRST: "git worktree list --porcelain", classification of EVERY attached worktree into current / explicit Stage spike-research / prohibited-ambiguous, a PASS for the spike/research class that neither narrows nor qualifies the P-016 exception, and a STAGE_BRANCH_GATE_FAIL halt on any prohibited or unclassifiable worktree before any branch operation or tracked mutation.
- AC-9: That section names the EXACT slug source field per intake shape (feature-shaped: the classified artifact's "title", or the verbatim stash-entry title when no artifact exists; task-shaped: the selected Step 1.5 grouping's "Proposed covering feature title"; shipment-shaped: the shipment record's "title", optional and never required), states the five-step normalization in order, states the explicit numeric maximum of 48 bytes with its max_slug_length rationale, and states the literal "stage-session" empty fallback.
- AC-10: That section specifies base_ref resolution from "git symbolic-ref --short refs/remotes/origin/HEAD" with the documented fallback and a halt when both fail (never assuming "main"); specifies the three create-or-select/resume cases with the clean-worktree precondition; and specifies the ownership / base-identity / collision discriminator (non-empty merge-base with origin/{base_ref} AND a two-dot-three diff touching only .backlogit/, docs/plans/, docs/decisions/, docs/memory/) such that resume is permitted ONLY on identity match and any other collision HALTS.
- AC-11: That section requires "git symbolic-ref --short HEAD" to equal the expected branch (detached HEAD fails) and requires the expected branch to differ from the resolved base_ref.
- AC-12: That section names the fail-closed halt tokens STAGE_BRANCH_GATE_FAIL and STAGE_BRANCH_GATE_ANCHOR_FAIL, requires a P-005 record on failure, requires that no tracked mutation occur on failure, and states the categorical deferral rule covering every tracked Stage artifact mutation site of the session, naming the two illustrative deferred write halves and marking the enumeration as illustrative rather than closed.
- AC-13: P-004 RED-PHASE EVIDENCE (unconditional). At H0, before any implementation: "go vet ./..." exits 0; "go test ./tests/integration/ -run TestStageBranchGate_ContractCorrected" exits non-zero; the captured output contains the literal marker string "not implemented: stage branch gate contract" attributed to TestStageBranchGate_ContractCorrected; and the H0 red count over this release unit's generated functions is literally 1 of 1. After the sole task completes, the marker is gone, TestStageBranchGate_ContractCorrected passes, "go vet ./..." exits 0 and the FULL "go test ./..." exits 0.
- AC-14: The generated function and every assertion inside it execute unconditionally - no t.Skip/t.Skipf, no build tag, no env-var or platform gate, no activation manifest and no selector flag. This zero-skip claim is scoped to this release unit's generated harness assertions only; pre-existing environment-conditional skips elsewhere in the repository are out of scope and are neither introduced nor modified.
<!-- END:acceptance-criteria -->

<!-- BEGIN:implementation-notes -->
SIZING (two independent axes): Size M (structured field). Complexity medium (enum-validated PROSE).
DEGRADATION NOTE - VERIFIED 2026-09-12, NOT ASSUMED: backlogit 1.10.1 exposes a --complexity flag and the backlog registry declares a complexity param, but this workspace's .backlogit/header-def.yaml defines NO complexity field on the task artifact type, so "backlogit update <id> --complexity medium" FAILS with: artifact type "task" does not define a complexity field. Complexity is therefore carried as enum-validated prose. Adding the field would mutate .backlogit/header-def.yaml, a tooling CONFIG file outside Stage's role boundary, so it is NOT done here. Registry/schema mismatch recorded as an operator finding; reporting defect only.

HARNESS FOOTPRINT - P-004 COMPLIANT UNCONDITIONALLY (rev 18). The production contract surfaces of this task are TWO MARKDOWN FILES: .github/policies/workflow-policies.md and .github/agents/_stage.agent.md. There is NO Go production code implementing this contract, because the contract is prompt/policy text. The harness is therefore a CHARACTERIZATION harness that READS those two installed files directly (os.ReadFile from the module root) and asserts the corrected properties. NO companion Go production stub is created, and NO new Go package, file or exported function is added outside the single test file: a test that only reads files compiles with no production stub at all, so harness-architect Step 4 item 3's "matching production stub" instruction (which exists so the MODULE COMPILES while a test calls not-yet-existing production code) has no object here.
THE EXPECTED MARKER IS MANDATORY, NOT CONDITIONAL. P-004 requires "go test ./... exits non-zero with expected failure markers in the output for EVERY test function", and harness-architect Step 5.2 requires ALL harness tests to fail with the expected failure marker panic("not implemented: <reason>"). Both are UNCONDITIONAL; neither is contingent on any reviewer's or skill's insistence. The previous "FALLBACK, IF AND ONLY IF harness-architect insists" wording is WITHDRAWN as P-004-noncompliant. The harness MUST therefore contain, at H0, EXACTLY ONE expected marker: an UNEXPORTED HELPER declared INSIDE tests/integration/stage_branch_gate_test.go whose body is panic("not implemented: stage branch gate contract"), invoked UNCONDITIONALLY by TestStageBranchGate_ContractCorrected. It MUST NOT be a new file, a new package, or anything under internal/ or cmd/. Its FINAL STATE after this task is an ordinary test helper carrying the contract assertions, with no panic.
H0 EVIDENCE - EXPLICIT AND CAPTURED. Before any implementation: (1) "go vet ./..." exits 0; (2) "go test ./tests/integration/ -run TestStageBranchGate_ContractCorrected" exits NON-ZERO; (3) the captured output contains the literal string "not implemented: stage branch gate contract" attributed to TestStageBranchGate_ContractCorrected; (4) the H0 red count over this release unit's generated functions is LITERALLY 1 of 1 - one generated test function, one expected marker, all red. After the SOLE task completes: the marker is gone, the function passes, and the FULL "go test ./..." exits 0 (Ship Step 4.3 is green immediately because 018-F has exactly one queued task).
FILE COUNT: 3 files total - 2 edited contract files (.github/policies/workflow-policies.md, .github/agents/_stage.agent.md) + 1 generated test file (tests/integration/stage_branch_gate_test.go). 0 new production files.
FUNCTION COUNT: 1 generated test function (TestStageBranchGate_ContractCorrected) plus at most 2 unexported helpers inside the same test file, one of which carries the H0 panic marker.
2-HOUR RULE: 3 files, 1 test function, pre-specified textual contract corrections, one test scenario group, single skill domain (policy/contract text - no code, schema, or CI domain mixed in). WITHIN the 2-hour rule; the task stays ATOMIC and is NOT split. Evidence for atomicity: every edit is a named, quoted, pre-located textual insertion or replacement in two files; no design work, no discovery, no build-system or schema change.
harness_cmd: go test ./tests/integration/ -run TestStageBranchGate_ContractCorrected
POSTURE: characterization-first over the installed contract corpus.

SINGLE-TASK LIFECYCLE - LOAD-BEARING, DO NOT WIDEN: feature 018-F has EXACTLY ONE queued task, so Ship Step 2 generates EXACTLY ONE red function, this task's completion turns it green, and Ship Step 4.3's full "go test ./..." is green immediately afterwards. This is the ONLY configuration that satisfies P-004 and Ship Step 4.3 simultaneously. NO activation manifest, terminal witness, selector flag, surface predicate, or MP-series check is required or permitted. Adding a SECOND queued task under 018-F re-creates the Step 4.3 deadlock that blocked revs 10-15 and is FORBIDDEN.
EXECUTABLE FILTERING IS BY artifact_type, NEVER BY ID SUFFIX. Ship Step 3 item 1 and the shipment-reconcile record-status classification both filter the manifest to TASK ARTIFACTS. The AUTHORITATIVE predicate is the record's frontmatter field artifact_type == "task". An ID-suffix heuristic is WRONG and MUST NOT be used: subtask IDs end in "-ST", whose final character is also "T", so a suffix test admits every subtask. 017-S's manifest contains eight subtasks precisely so the protected set stays empty of them, and they are excluded by artifact_type == "subtask" BEFORE any status is read.

TRACEABILITY: supersedes the current-scope half (items 1, 2, 3 and 7a) of the retired L-sized 018.009-T. That task was adopted to 019.007-T and then split THREE ways; its three named successors are 019.007-T (handback emission, items 4 and 5), 019.008-T (no-shipment terminal reachability, item 6) and 019.009-T (Stage artifact commit step with out-of-root path safety, item 7b). All three are deferred to feature 019-F and belong to NO live shipment.
CURRENT ROUTE AND CLOSURE SHAPE (rev 19, 2026-09-13; reconciled with the A-only simplification). Governing decision: docs/decisions/2026-09-13-intercom-go-a-only-simplification-decision.md (SIMPLIFICATION_VALID). Shipment 017-S is a 13-ENTRY FULLY-COVERED ROOT (root 018-F + all 12 descendant members) and depends on 021-S ONLY. It closes by the EXISTING P-015 CASCADE exception once shipment A lands Step 6.1(a1): Step 4.5 moves THIS TASK to done, Step 6.1(a1) moves 018-F active -> done as a PRESENT feature member (case ii), pre-mode returns PROCEED over all 13 members, and classification returns CASCADE. NOTHING IN THIS TASK'S SCOPE, SURFACE, ACCEPTANCE CRITERIA, SIZE OR HARNESS CHANGES as a result - the manifest is the closure membership record, not the executable set. The executable set derived by Ship Step 3 item 1 remains EXACTLY [018.008-T]: 018-F is excluded by the artifact_type filter (it is a feature, not a task) BEFORE any status is read, so adding it to the manifest does not widen the single-task lifecycle above and does not create a second queued test-bearing task. The 11 pre-archived legacy descendants remain pre_archived_skipped. TASK_ONLY_FINALIZE is NOT used and is NOT a prerequisite: 023-F/024-F and shipments 022-S/023-S are DEFERRED generalized platform work (blocked and archived respectively). The rev-18 SAFE_CLOSE step-8 exit-9 pause at Ship Step 6 is OFF-ROUTE for this unit.

<!-- END:implementation-notes -->

ACTOR RECONCILIATION (added 2026-09-12, Stage rev-5 remediation; does NOT weaken any prohibition above). The operator-only completion sequence stated above is CONFIRMED BY EXECUTION, not merely asserted. It was challenged on the ground that Ship's Role Boundary explicitly classifies Git branch/commit/push and PR create/update/merge as ALLOWED, which would make a "Ship in PR-lifecycle-only mode" handoff legitimate and would put the earlier "No agent performs any of these steps" phrasing in conflict with Ship's own contract. Stage TESTED that claim instead of reasoning about it (Probe 17: docs/plans/evidence/2026-09-12-task-only-shipment-finalization/probe17-ship-pr-lifecycle-gate.txt). RESULT: Ship's Role Boundary is NECESSARY BUT NOT SUFFICIENT. Ship Step 5 items 1a/5a mandate "autoharness gate pipeline-topology --mode agent --shipment {shipment_id} --phase lifecycle --json" under the rule "exit 1/2 halts immediately with the reported token/message (never inferred, never fail-open)". That gate is installed and live here (.autoharness/gates/pipeline-topology-force-audit.log records real operator --force use on 2026-09-05). All four routes FAIL CLOSED for an unclaimed session: no --shipment -> exit 2 "agent mode requires --shipment <shipment_id>"; --shipment 021-S while queued -> exit 1 "LIFECYCLE_NO_ACTIVE_SHIPMENT: expected exactly one active shipment"; --mode manual without shipment -> exit 2 "--phase lifecycle requires --shipment <shipment_id> in any mode"; --phase ambient -> exit 2. The gate passes ONLY with exactly one ACTIVE shipment, which requires a CLAIM - precisely what a PR-lifecycle-only session must not do, and what the 021-S sequencing gate forbids before PR #54 merges. "--force" is documented "Operator-only ... Audited. Never reachable from an agent surface." CONCLUSION: the OPERATOR remains the authorized PR actor and the operator-only language above STANDS UNWEAKENED - now evidence-backed rather than assumed. The distinction that matters: this is a SEQUENCE limitation, not a Role Boundary limitation, so nothing here contradicts Ship's Role Boundary table. SEPARATELY AND INDEPENDENTLY: no non-Stage actor may modify planning content in any mode - Ship's own Role Boundary lists "Create or modify deliberation, spike, plan, or review artifacts" as FORBIDDEN - so any review comment requiring a planning or contract change PAUSES and ROUTES BACK TO STAGE, whoever is carrying the PR. See section 11 of docs/plans/2026-09-12-intercom-go-task-only-shipment-finalization-plan.md.

=== REV 20 RECONCILIATION (2026-09-13) ===

GOVERNING CONTRACT CORRECTED - SEE R16 REV 20. The governing plan docs/plans/2026-09-11-intercom-go-stage-artifact-branch-pr-policy-gap-plan.md is now REV 20, which resolves an internal contradiction inside section R16 itself: R16.2 declared the 13-entry fully-covered root current while R16.3/R16.3.1/R16.3.1a/R16.3.2/R16.3.3 still operatively asserted a 12-entry task-only manifest and an "unreachable by construction" CASCADE shape. R16.3 is now the SOLE OPERATIVE manifest contract; the contradicting subsections are relabelled HISTORICAL - NON-GOVERNING. THIS TASK'S SCOPE IS UNCHANGED by rev 20: the executable set is still exactly [018.008-T], the three surfaces are still (1) P-010 in .github/policies/workflow-policies.md, (2) the .github/agents/_stage.agent.md Role Boundary Git and PR rows, and (3) the Step 1.9 gate, and the harness is still ONE function with ONE expected marker. Ship Step 3 filters the manifest by artifact_type, so 018-F's membership never widens the executable set.

ORCHESTRATOR STEP 1.5 - THE DEFERRAL NOW CARRIES AN EXPLICIT RUNTIME PROHIBITION (rev 20, R16.5). This task's existing instruction stands UNCHANGED: do NOT add any clarifier granting, implying or restating Orchestrator authority to commit Stage artifacts or push the default branch, leave P-010's Orchestrator statement UNCHANGED, and leave the repair of the Orchestrator's own contradiction (item 3(a) commit, item 3(e) direct-main push attempt) deferred to 019.004-T. Rev 20 ADDS, in the plan rather than in any agent file, the fail-closed runtime rule that governs the interim: Step 1.5 item 3 MUST NOT be executed for Stage artifacts; the Orchestrator MUST HALT with STAGE_ARTIFACTS_UNCOMMITTED and hand off to the OPERATOR; the direct-main push MUST NOT be attempted even as an "attempt first, fall back on rejection" probe, because the prohibition is on the action and not on its outcome; and item 4's read-only origin/main manifest verification REMAINS PERMITTED AND REQUIRED. NOTHING IN THIS TASK MODIFIES .github/agents/_orchestrator.agent.md.

P-016 PRESERVATION AND THE OPERATOR-ONLY PR BOUNDARY BOTH STAND UNWEAKENED, exactly as stated above.

NOT YET EXECUTABLE. 017-S is blocked behind 021-S (017-S.dependencies = [021-S] exactly), and Plan A for 021-S is rev 4 with a FORMAL plan-review verdict of decision: FAIL on attempt 1. Ship must not claim 017-S or 021-S.