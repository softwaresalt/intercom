---
artifact_type: subtask
created_at: 2026-09-03T23:10:45.6825973Z
dependencies:
    - 002.002.003-ST
id: 002.002.004-ST
labels:
    - go
    - path-safety
    - security
parent_id: 002.002-T
priority: high
status: done
title: Detect symlink escapes for existing paths
updated_at: 2026-09-03T23:39:09.9845436Z
---

Size: M | Complexity: high (two independent axes; recorded as prose because this backend's 'subtask' template defines no structured size/complexity field).

Unit B4. Files: internal/pathsafe/pathsafe.go (extend), internal/pathsafe/symlink_test.go. Add checkSymlinkEscape() implementing oracle steps 6-7. If the resolved path EXISTS, run filepath.EvalSymlinks + stripUNCPrefix and re-assert containment via hasPathPrefix; on failure emit 'symlink target escapes workspace'. Use os.Stat (NOT os.Lstat) for the existence probe, matching the oracle's Path::exists() which follows symlinks - a broken symlink is therefore treated as non-existent and takes the lexical-only branch (finding GO-14, documented not silently inherited). Non-existent paths accepted after lexical checks only. Package doc comment MUST record two oracle-parity limitations: the TOCTOU window between validation and use, and that EvalSymlinks does NOT resolve HARDLINKS so a pre-existing in-workspace hardlink to an external file on the same volume passes validation (finding SEC-5). Tests gate on a RUNTIME os.Symlink privilege probe in t.TempDir(), not runtime.GOOS, so elevated Windows environments are still covered; on failure t.Skip with an explicit reason - never pass vacuously (finding GO-12). ACCEPTANCE: (1) symlink inside root whose target is outside root rejected with 'symlink target escapes workspace'; (2) symlink inside root targeting another in-root location resolves successfully; (3) non-existent relative path under root resolves successfully. Test-first.