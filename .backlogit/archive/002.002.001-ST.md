---
artifact_type: subtask
created_at: 2026-09-03T23:10:44.7908629Z
dependencies:
    - 002.001.001-ST
id: 002.002.001-ST
labels:
    - go
    - path-safety
    - security
parent_id: 002.002-T
priority: high
status: done
title: Add validated Root canonicalization type
updated_at: 2026-09-03T23:35:02.0805279Z
---

Size: S | Complexity: medium (two independent axes; recorded as prose because this backend's 'subtask' template defines no structured size/complexity field).

Unit B1. Files: internal/pathsafe/root.go, internal/pathsafe/root_test.go. Define 'type Root struct { path string }' and NewRoot(dir) canonicalizing ONCE: filepath.Abs then filepath.EvalSymlinks then stripUNCPrefix. Amortizes an EvalSymlinks syscall walk that would otherwise run per Resolve call - P10 validates 10-100 paths per patch (finding GO-4). Failure returns apperr.New(apperr.KindPathViolation, 'workspace root invalid: ...'). PROVENANCE: stripUNCPrefix mirrors oracle src/config.rs:24 (applied to default_workspace_root at src/config.rs:546); it is NOT called from src/diff/path_safety.rs, which relies on Rust's component-aware Path::starts_with. It is therefore a Go-specific adaptation, not oracle parity (findings SEC-4/ARCH-4). ACCEPTANCE: (1) NewRoot on an existing dir returns an absolute symlink-resolved path with no backslash-backslash-question-backslash prefix; (2) NewRoot on a non-existent dir returns PathViolation whose message begins 'path violation: workspace root invalid'; (3) NewRoot is idempotent - NewRoot(r.Path()) yields the same path. Test-first.