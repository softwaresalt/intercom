---
artifact_type: task
created_at: 2026-09-08T08:06:19.8633315Z
custom_fields:
    size: XS
    size_ruleset_version: 1.0.0
    size_source: agent
dependencies:
    - 013.001-T
id: 013.005-T
parent_id: 013-F
priority: high
references:
    - docs/plans/2026-09-08-intercom-go-gate-scope-realignment-plan.md
status: done
title: Route config.toml.example to the TOML engine (P0)
updated_at: 2026-09-08T18:44:59.6778947Z
---

Size: XS | Complexity: low (complexity recorded as prose: this workspace's task template defines no complexity field). U5. P0 FIX. scan_path() dispatches on Path.suffix, but Path('config.toml.example').suffix is '.example' NOT '.toml'. The file is selected by should_scan_repo_path and by the ls-files pathspec, then falls through to 'return []' - so scan_toml() is NEVER invoked on the real tree and the entire TOML engine is dead code in repo mode. config.toml.example is one of the three elements of D6's normative gate scope and is the file the governing deliberation records as having shipped host_cli_args contamination. This task owns BOTH halves of its own red/green pair. RED - NAMED OBSERVABLE (required): asserting 'the file produces findings' is UNSATISFIABLE because the file is clean, so scan_path returns [] both before AND after the fix, leaving the assertion permanently red and inviting an executor to weaken it. Instead extract a pure helper engine_for_path(path) -> 'go'|'toml'|None which scan_path() consumes, and assert engine_for_path('config.toml.example') == 'toml'. GREEN: dispatch on path.name == 'config.toml.example' or path.suffix == '.toml'. Also correct the header's '...as a Go identifier or TOML key' claim (false until this lands) and its stale 'for the corrected C1 config surface' characterization. VERIFIED PRECONDITIONS - both required, token-freeness alone is NOT sufficient: (1) config.toml.example contains no retired token (verified); (2) config.toml.example is valid TOML (verified) - scan_toml_with_tomllib fails CLOSED on any parse exception, so an unparseable example file would redden the gate at this commit boundary and breach invariant I-E. AC: the dispatch assertion reports PASS; gate still exits 0.