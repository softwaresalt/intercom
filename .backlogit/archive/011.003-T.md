---
archived_from: .backlogit/queue/011.003-T.md
archived_status: done
artifact_type: task
commit: 797fc1ed9e4833374552d8bfaa3ddd41e46fec26
created_at: 2026-09-06T23:57:46.7096402Z
custom_fields:
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
id: 011.003-T
parent_id: 011-F
priority: high
status: archived
title: 'B: Record bounded expiring Constitution exception for database.path'
updated_at: 2026-09-07T03:31:29.881995Z
---

Resolves 8D953C4B via branch (c) per deliberation Addendum D-I REVISED. Branch (a) is INFEASIBLE: pathsafe.normalize rejects EVERY absolute candidate and NewRoot requires an already-existing directory, while database.path is a not-yet-existing file whose rule 7 deliberately permits absolute paths as a visible operator privilege; implementing (a) would reject legitimate in-root absolute paths and force a new pathsafe capability, colliding with J2. Branch (b) alone is unsafe (unbounded exception could outlive its basis). Principle IV is NOT breached today - measured, nothing opens or writes database.path; the divergence is LATENT. Approach: documentation. Width: docs. AC1 a dated Constitution Check exception records the principle (III), the divergence, THE REJECTED SIMPLER ALTERNATIVE (branch a) and why it is infeasible, and its bound. AC2 the expiry trigger is THE SAME trigger as the write-path gate task, so it cannot outlive the arrival of a real write path. AC3 the D7/C4 persistence work carries a precondition that database.path gains containment BEFORE any code opens it. AC4 no production code changes; internal/config behaviour untouched.

<!-- BEGIN:description -->
Resolves 8D953C4B via branch (c) per deliberation Addendum D-I REVISED. Branch (a) is INFEASIBLE: pathsafe.normalize rejects EVERY absolute candidate and NewRoot requires an already-existing directory, while database.path is a not-yet-existing file whose rule 7 deliberately permits absolute paths as a visible operator privilege; implementing (a) would reject legitimate in-root absolute paths and force a new pathsafe capability, colliding with J2. Branch (b) alone is unsafe (unbounded exception could outlive its basis). Principle IV is NOT breached today - measured, nothing opens or writes database.path; the divergence is LATENT. Approach: documentation. Width: docs. AC1 a dated Constitution Check exception records the principle (III), the divergence, THE REJECTED SIMPLER ALTERNATIVE (branch a) and why it is infeasible, and its bound. AC2 the expiry trigger is THE SAME trigger as the write-path gate task, so it cannot outlive the arrival of a real write path. AC3 the D7/C4 persistence work carries a precondition that database.path gains containment BEFORE any code opens it. AC4 no production code changes; internal/config behaviour untouched.

SIZING (two independent axes): Size: S | Complexity: low. NOTE: 'size' is stored as a structured field; 'complexity' is recorded as prose because this workspace's WIT does not define a complexity field for artifact type 'task' (backlogit validation error) - a documented, flagged degradation, not an omission.
<!-- END:description -->