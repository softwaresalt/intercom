---
archived_from: .backlogit/queue/011.009-T.md
archived_status: done
artifact_type: task
commit: 750e8e8
created_at: 2026-09-06T23:58:18.8545278Z
custom_fields:
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 011.010-T
id: 011.009-T
parent_id: 011-F
priority: high
status: archived
title: 'C: Add advisory windows-latest CI job'
updated_at: 2026-09-07T03:31:33.6017765Z
---

Jointly resolves E89E5D00 and DA945722 - one root cause: all CI jobs run ubuntu-latest, so isRooted leading-backslash rejection, hasPathPrefix/pathEqual case-folding, stripUNCPrefix, and the junction-escape test NEVER execute. RISK FIX: revision 1 promoted this to a blocking gate in the same unit that created it while rating first-run failure High, putting a high-probability red gate in front of all 19 other units. This repo already has the right precedent - the PIPELINE_TOPOLOGY_GATE_REQUIRED advisory-to-required toggle at ci.yml:340-388. Approach: configuration. Width: CI config. AC1 new windows-latest job runs 'go test ./...'; -race is OMITTED with an inline comment citing docs/compound/build-errors/go-race-requires-cgo-fails-closed-2026-09-03.md (windows-latest has no cgo; -race fails closed with exit 2). AC2 all added actions pinned to FULL 40-hex SHAs; persist-credentials: false on checkout and job-level permissions: contents: read, matching every existing job. AC3 job is in ci-gate's needs: AND its results expression from day one (J8), with its verdict wrapped in continue-on-error using vars.WINDOWS_GATE_REQUIRED != 'true' - present and wired but ADVISORY until proven green. AC4 gated on needs.changes.outputs.code == 'true' like every other expensive job. AC5 the live-SDK opt-in task is a HARD PRECONDITION - 'go test ./...' on Windows would otherwise execute the live probe tests. AC6 GENERATED-FILE DRIFT: ci.yml is autoharness-generated; use the template-sanctioned OS-matrix escape hatch documented at ci.yml:17-20 or add an explicit 'LOCAL DIVERGENCE - re-apply after regeneration' marker.

<!-- BEGIN:description -->
Jointly resolves E89E5D00 and DA945722 - one root cause: all CI jobs run ubuntu-latest, so isRooted leading-backslash rejection, hasPathPrefix/pathEqual case-folding, stripUNCPrefix, and the junction-escape test NEVER execute. RISK FIX: revision 1 promoted this to a blocking gate in the same unit that created it while rating first-run failure High, putting a high-probability red gate in front of all 19 other units. This repo already has the right precedent - the PIPELINE_TOPOLOGY_GATE_REQUIRED advisory-to-required toggle at ci.yml:340-388. Approach: configuration. Width: CI config. AC1 new windows-latest job runs 'go test ./...'; -race is OMITTED with an inline comment citing docs/compound/build-errors/go-race-requires-cgo-fails-closed-2026-09-03.md (windows-latest has no cgo; -race fails closed with exit 2). AC2 all added actions pinned to FULL 40-hex SHAs; persist-credentials: false on checkout and job-level permissions: contents: read, matching every existing job. AC3 job is in ci-gate's needs: AND its results expression from day one (J8), with its verdict wrapped in continue-on-error using vars.WINDOWS_GATE_REQUIRED != 'true' - present and wired but ADVISORY until proven green. AC4 gated on needs.changes.outputs.code == 'true' like every other expensive job. AC5 the live-SDK opt-in task is a HARD PRECONDITION - 'go test ./...' on Windows would otherwise execute the live probe tests. AC6 GENERATED-FILE DRIFT: ci.yml is autoharness-generated; use the template-sanctioned OS-matrix escape hatch documented at ci.yml:17-20 or add an explicit 'LOCAL DIVERGENCE - re-apply after regeneration' marker.

SIZING (two independent axes): Size: M | Complexity: medium. NOTE: 'size' is stored as a structured field; 'complexity' is recorded as prose because this workspace's WIT does not define a complexity field for artifact type 'task' (backlogit validation error) - a documented, flagged degradation, not an omission.
<!-- END:description -->