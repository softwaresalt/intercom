---
archived_from: .backlogit/queue/013.002-T.md
archived_status: done
artifact_type: task
commit: 9d8937ae2bc721cdeb15b74963acb92221843f06
created_at: 2026-09-08T08:05:40.4497538Z
custom_fields:
    size: S
    size_ruleset_version: 1.0.0
    size_source: agent
dependencies:
    - 013.001-T
id: 013.002-T
parent_id: 013-F
priority: high
references:
    - docs/plans/2026-09-08-intercom-go-gate-scope-realignment-plan.md
status: archived
title: Add Go fixture harness with per-kind engine dispatch
updated_at: 2026-09-08T19:46:44.245909Z
---

Size: S | Complexity: medium (complexity recorded as prose: this workspace's task template defines no complexity field). U2. The existing harness CANNOT accept Go fixtures and adding them naively yields a FALSE GREEN in a merge-blocking control: fixture_glob is 'retired-*.toml' globbed non-recursively over scripts/testdata (never sees a subdir); manifest keys are bare path.name (Go entries fail the extra_manifest reconciliation); and the engines list is TOML-only and applied to EVERY fixture, so a .go fixture raises in tomllib and a REJECT fixture passes FOR THE WRONG REASON. Add a second fixture-suite descriptor (glob, manifest_path, engines) for scripts/testdata/retiredgo/, per-suite discovered-vs-manifest reconciliation keeping the bidirectional drift guard, and suffix-based dispatch. SINGLE DISPATCH SOURCE: route through the SAME engine_for_path() helper used by scan_path() (task 005) - never a second parallel dispatch table, which would drift and would mean fixtures do not exercise the real dispatch path. CREATE scripts/testdata/retiredgo-manifest.json here (sorted keys, Principle IX); do NOT modify retired-manifest.json. Update the script header Usage block, which documents the self-test as retired-*.toml only and would otherwise ship stale. AC: a Go REJECT fixture's failure text names the retired token and Go identifier (scan_go message format), NOT a TOML parse error (invariant I-F).