---
archived_from: .backlogit/queue/011.015-T.md
archived_status: done
artifact_type: task
commit: ee23297
created_at: 2026-09-06T23:59:22.2834644Z
custom_fields:
    size: S
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 011.019-T
id: 011.015-T
parent_id: 011-F
priority: medium
status: archived
title: 'F: Land and validate the full .gitignore stowaway hunk'
updated_at: 2026-09-07T03:31:37.4236282Z
---

Partially resolves 9F9A3CB2 (include-list item 1). STOWAWAY - droppable per the operator-authorized disposition. HEAD's .gitignore contains ZERO negations; the stowaway adds 52 LINES, six of them the first negations ever introduced into this repo's ignore policy. SCOPE CORRECTED: revisions 1-4 scoped this to the six negation lines only, leaving the other ~46 stowaway lines (bin/, logs/, .vscode/, .copilot/, .mcp.json, .env + !.env.example, backlogit.db, .backlogit/*, .autoharness/staging|backups/, .*.lock, .engram/, .graphtor/, prompt.md, diff.txt) with NO OWNING UNIT - 9F9A3CB2 item 1 would have been recorded as covered while ~46 operator-preserved lines were silently dropped. The negations are also INERT OR MEANINGLESS unless their preceding ignore lines land in the same commit (!plugin/.mcp.json needs .mcp.json; the four .engram/* negations need .engram/). DEPENDS ON the un-ignore differential task (the hardened checker is the validation instrument). Approach: configuration + verification. Width: ignore policy. AC1 the COMPLETE 52-line stowaway hunk lands as one commit, strictly append-only, AFTER the preserve-only gap-closure task's four lines so non-droppable content is never entangled with droppable content. AC2 all six negations are evaluated against the differential and the verdict RECORDED PER LINE. AC3 EFFECT ASSERTED NOT ASSUMED - four negations (!.engram/.version, !.engram/.workspace-id, !.engram/config.toml, !.engram/registry.yaml) are INERT under git semantics because the preceding .engram/ excludes the directory and git cannot re-include a file whose parent directory is excluded; assert their actual state via 'git check-ignore -v' and record it rather than shipping a silently-wrong ignore policy as validated. AC4 !plugin/.mcp.json CREDENTIAL CHECK - MCP configs conventionally embed tokens in env blocks; record that if ever tracked it must reference credentials by dollar-brace indirection only, and run gitleaks explicitly over the newly un-ignored paths. AC5 check-gitignore-append-only.sh passes including the new un-ignore rule, and the denylist still passes - GUARANTEED BY CONSTRUCTION since the denylist references no pattern this hunk introduces. AC6 committed as a SEPARATE commit appended LAST in file order so a drop is a clean git revert / tail truncation that trivially preserves I6, never a manual line-level edit. AC7 BEFORE ANY STASH RETURN (Constitution VII): the working-tree content is backed up and the backup VERIFIED, OPERATOR APPROVAL obtained, then the return is recorded.

<!-- BEGIN:description -->
Partially resolves 9F9A3CB2 (include-list item 1). STOWAWAY - droppable per the operator-authorized disposition. HEAD's .gitignore contains ZERO negations; the stowaway adds 52 LINES, six of them the first negations ever introduced into this repo's ignore policy. SCOPE CORRECTED: revisions 1-4 scoped this to the six negation lines only, leaving the other ~46 stowaway lines (bin/, logs/, .vscode/, .copilot/, .mcp.json, .env + !.env.example, backlogit.db, .backlogit/*, .autoharness/staging|backups/, .*.lock, .engram/, .graphtor/, prompt.md, diff.txt) with NO OWNING UNIT - 9F9A3CB2 item 1 would have been recorded as covered while ~46 operator-preserved lines were silently dropped. The negations are also INERT OR MEANINGLESS unless their preceding ignore lines land in the same commit (!plugin/.mcp.json needs .mcp.json; the four .engram/* negations need .engram/). DEPENDS ON the un-ignore differential task (the hardened checker is the validation instrument). Approach: configuration + verification. Width: ignore policy. AC1 the COMPLETE 52-line stowaway hunk lands as one commit, strictly append-only, AFTER the preserve-only gap-closure task's four lines so non-droppable content is never entangled with droppable content. AC2 all six negations are evaluated against the differential and the verdict RECORDED PER LINE. AC3 EFFECT ASSERTED NOT ASSUMED - four negations (!.engram/.version, !.engram/.workspace-id, !.engram/config.toml, !.engram/registry.yaml) are INERT under git semantics because the preceding .engram/ excludes the directory and git cannot re-include a file whose parent directory is excluded; assert their actual state via 'git check-ignore -v' and record it rather than shipping a silently-wrong ignore policy as validated. AC4 !plugin/.mcp.json CREDENTIAL CHECK - MCP configs conventionally embed tokens in env blocks; record that if ever tracked it must reference credentials by dollar-brace indirection only, and run gitleaks explicitly over the newly un-ignored paths. AC5 check-gitignore-append-only.sh passes including the new un-ignore rule, and the denylist still passes - GUARANTEED BY CONSTRUCTION since the denylist references no pattern this hunk introduces. AC6 committed as a SEPARATE commit appended LAST in file order so a drop is a clean git revert / tail truncation that trivially preserves I6, never a manual line-level edit. AC7 BEFORE ANY STASH RETURN (Constitution VII): the working-tree content is backed up and the backup VERIFIED, OPERATOR APPROVAL obtained, then the return is recorded.

SIZING (two independent axes): Size: S | Complexity: low. NOTE: 'size' is stored as a structured field; 'complexity' is recorded as prose because this workspace's WIT does not define a complexity field for artifact type 'task' (backlogit validation error) - a documented, flagged degradation, not an omission.
<!-- END:description -->