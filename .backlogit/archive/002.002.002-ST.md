---
artifact_type: subtask
created_at: 2026-09-03T23:10:45.217015Z
dependencies:
    - 002.002.001-ST
id: 002.002.002-ST
labels:
    - go
    - path-safety
    - security
parent_id: 002.002-T
priority: high
status: done
title: Implement lexical rejection and component normalization
updated_at: 2026-09-03T23:37:02.6354151Z
---

Size: M | Complexity: medium (two independent axes; recorded as prose because this backend's 'subtask' template defines no structured size/complexity field).

Unit B2. Files: internal/pathsafe/pathsafe.go, internal/pathsafe/lexical_test.go. Implement normalize(). Go has no Path::components(), so mechanism is fixed (finding GO-5): (1) BEFORE cleaning reject if filepath.VolumeName(candidate) != '' - catches Windows drive-relative 'C:foo' which filepath.IsAbs reports FALSE and which on NTFS is an alternate-data-stream reference; oracle rejects via Component::Prefix (finding SEC-1); (2) reject if filepath.IsAbs; both emit 'absolute paths are not allowed; use workspace-relative paths'; (3) filepath.Clean then split on separator; (4) walk: '..' pops the stack and errors 'path attempts to escape workspace' if already empty, '.' and '' skipped, else push. Clean collapses interior a/b/../c and redundant/trailing separators exactly as Rust's Normal-only iteration elides them, so the post-Clean walk yields the same final stack (finding CORR-3). ACCEPTANCE: (1) rejected with 'path attempts to escape workspace': ../secret.txt, src/../../secret.txt, subdir/../../escape.txt; (2) rejected with the absolute-path message: /etc/passwd, and on Windows C:\Windows\system32 and C:foo; (3) safe traversal preserved: a/b/../c.txt normalizes to a/c.txt and ./src/main.go to src/main.go. Test-first.