---
artifact_type: task
created_at: 2026-09-11T02:13:06.6511706Z
custom_fields:
    size: S
    size_ruleset_version: stage-2h-rule-v1
    size_source: agent
id: 017.001-T
labels:
    - pathsafe
    - windows
    - security
    - error-handling
parent_id: 017-F
priority: high
references:
    - docs/plans/2026-09-10-intercom-go-pathsafe-error-surface-parity-plan.md
status: done
title: Normalize checkSymlinkEscape reparse error via wrapPathError
updated_at: 2026-09-11T02:45:48.7801539Z
---

Route checkSymlinkEscape's canonicalizeReparse error branch (internal/pathsafe/pathsafe.go ~line 359) through the EXISTING wrapPathError helper so errors.As(err, &*fs.PathError) matches on Windows exactly as it already does for NewRoot's failure branch.

Change (single call site):
    real, err := canonicalizeReparse(ancestor)
    if err != nil {
        return "", apperr.Wrapf(apperr.KindPathViolation,
            wrapPathError("open", ancestor, err), symlinkUnverifiableMsg)
    }

Rationale: on Windows canonicalizeReparse returns a raw syscall.Errno; NewRoot re-forms it via wrapPathError but checkSymlinkEscape does not, so the two functionally-equivalent failures have divergent error surfaces. Rated P1 IN ISOLATION by the Architecture Strategist during 015-S adversarial review; deferred then on P-021 C1 SCOPE grounds only, not severity.

Do NOT relocate normalization into canonicalizeReparse (decision D-3): that would change a Windows-only internal contract relied on by two callers and enlarge blast radius on a NON-NEGOTIABLE control.

Verified preconditions: wrapPathError lives in root.go which carries NO build tag, so it is callable from platform-agnostic pathsafe.go. apperr.Wrapf retains its cause and *apperr.Error implements Unwrap(), so errors.As traverses the apperr layer.

Size: S | Complexity: medium
(complexity carried as prose: backlogit task WIT exposes 'size' only, no structured 'complexity' field)
Posture: test-first
Source stash: 7ADAC481 (DEFERRED SCOPE EXPANSION; PR ref reconciled N/A -> #48 per P-021 C5)
Safety modes: freeze-scope, investigate-first