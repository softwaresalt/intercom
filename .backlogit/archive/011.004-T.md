---
artifact_type: task
commit: 272b186
created_at: 2026-09-06T23:57:47.2511508Z
custom_fields:
    size: M
    size_ruleset_version: 2h-rule-v1
    size_source: agent
dependencies:
    - 011.003-T
id: 011.004-T
parent_id: 011-F
priority: high
status: done
title: 'B: Add mechanical write-path precondition gate and risk register'
updated_at: 2026-09-07T00:29:10.0647477Z
---

PARTIALLY resolves BF5DE670 as rescoped by D-B (mitigation itself deferred to the C4-C6 trigger). Add a NEW DEDICATED script (e.g. scripts/check-write-path-precondition.sh) wired into CI. It must NOT be bolted onto scripts/check-retired-architecture.sh - that script's narrowing to internal/config/** is the measured, still-UNMET completion condition (b) of retained tracker 4989A42D, and broadening it is an explicit anti-goal. Approach: test-first with its own fixture/self-test harness. Width: script + fixtures. AC1 THE CONSOLIDATED RISK REGISTER IS PRODUCED (BF5DE670 primary ask) unifying GO-14 write-through-dangling-symlink, the Resolve-to-use TOCTOU window, and SEC-5 EvalSymlinks-ignores-hardlinks, each naming current mitigation status and the condition forcing mitigation. AC2 the three acceptances STAY in internal/pathsafe godoc and cross-reference the register; detail is NOT relocated out of godoc. AC3 detector uses QUALIFIED selectors (os.WriteFile, os.Create, os.OpenFile, os.Remove, os.RemoveAll, os.Rename, os.Mkdir, os.MkdirAll, os.Symlink, os.Chmod, os.Truncate, io.Copy) plus store constructors (sql.Open, bbolt.Open) - a bare alternation would match CreateSession. AC4 scope is all non-test Go files in the module: internal/** AND cmd/** (cmd/intercom imports internal/config and is the likeliest persistence-wiring site). AC5 a fixture containing a write primitive FAILS; the current tree PASSES; both via --self-test. AC6 the failure message names the register, the Constitution exception, and the RETIREMENT PROCEDURE for the legitimate arrival of a write path at C4-C6. AC7 zero behavioural change to internal/pathsafe. ANTI-GOAL: no TOCTOU/hardlink mitigation mechanism is added.

<!-- BEGIN:description -->
PARTIALLY resolves BF5DE670 as rescoped by D-B (mitigation itself deferred to the C4-C6 trigger). Add a NEW DEDICATED script (e.g. scripts/check-write-path-precondition.sh) wired into CI. It must NOT be bolted onto scripts/check-retired-architecture.sh - that script's narrowing to internal/config/** is the measured, still-UNMET completion condition (b) of retained tracker 4989A42D, and broadening it is an explicit anti-goal. Approach: test-first with its own fixture/self-test harness. Width: script + fixtures. AC1 THE CONSOLIDATED RISK REGISTER IS PRODUCED (BF5DE670 primary ask) unifying GO-14 write-through-dangling-symlink, the Resolve-to-use TOCTOU window, and SEC-5 EvalSymlinks-ignores-hardlinks, each naming current mitigation status and the condition forcing mitigation. AC2 the three acceptances STAY in internal/pathsafe godoc and cross-reference the register; detail is NOT relocated out of godoc. AC3 detector uses QUALIFIED selectors (os.WriteFile, os.Create, os.OpenFile, os.Remove, os.RemoveAll, os.Rename, os.Mkdir, os.MkdirAll, os.Symlink, os.Chmod, os.Truncate, io.Copy) plus store constructors (sql.Open, bbolt.Open) - a bare alternation would match CreateSession. AC4 scope is all non-test Go files in the module: internal/** AND cmd/** (cmd/intercom imports internal/config and is the likeliest persistence-wiring site). AC5 a fixture containing a write primitive FAILS; the current tree PASSES; both via --self-test. AC6 the failure message names the register, the Constitution exception, and the RETIREMENT PROCEDURE for the legitimate arrival of a write path at C4-C6. AC7 zero behavioural change to internal/pathsafe. ANTI-GOAL: no TOCTOU/hardlink mitigation mechanism is added.

SIZING (two independent axes): Size: M | Complexity: medium. NOTE: 'size' is stored as a structured field; 'complexity' is recorded as prose because this workspace's WIT does not define a complexity field for artifact type 'task' (backlogit validation error) - a documented, flagged degradation, not an omission.
<!-- END:description -->