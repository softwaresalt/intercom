# intercom

## Configuration

`intercom` reads its configuration from a TOML file, `config.toml` by
default (override with `--config <path>`). See `config.toml.example` for a
complete, loadable example covering every configuration key, and
`docs/config-reference.md` for the full key reference, default values, and
validation rules enforced at load time.
